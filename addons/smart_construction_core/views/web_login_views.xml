<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="sc_web_login_inherit" inherit_id="web.login">
        <xpath expr="//form" position="before">
            <t t-if="request.env['ir.config_parameter'].sudo().get_param('sc.login.custom_enabled', '1') in ('1','true','True')">
                <t t-set="sc_login_env"
                   t-value="request.env['ir.config_parameter'].sudo().get_param('sc.login.env', 'prod')"/>
                <div class="sc-login-branding">
                    <div class="sc-login-logo">
                        <img src="/smart_construction_core/static/src/img/brand/logo.svg" alt="Smart Construction"/>
                    </div>
                    <h2>智能工程项目管理系统</h2>
                    <div class="sc-subtitle">工程全链路协同平台 · 项目 · 合同 · 结算 · 支付</div>
                </div>
                <t t-if="sc_login_env == 'demo'">
                    <div class="sc-login-env-badge is-demo">DEMO 环境</div>
                </t>
            </t>
        </xpath>
        <xpath expr="//form//div[.//input[@name='db']]" position="attributes">
            <attribute name="t-if">
                not (
                request.env['ir.config_parameter'].sudo().get_param('sc.login.custom_enabled', '1') in ('1','true','True')
                and request.env['ir.config_parameter'].sudo().get_param('sc.login.env', 'prod') in ('demo','prod')
                )
            </attribute>
        </xpath>

        <xpath expr="//form//input[@name='redirect']" position="attributes">
            <attribute name="t-att-value">
                request.params.get('redirect')
                or (
                request.env['ir.config_parameter'].sudo().get_param('sc.login.custom_enabled', '1') in ('1','true','True')
                and '/web#menu_id=%s&amp;action=%s' % (
                    request.env.ref('smart_construction_core.menu_sc_root').id,
                    request.env.ref('smart_construction_core.action_sc_project_workbench').id
                )
                )
                or ''
            </attribute>
        </xpath>
    </template>

    <template id="sc_web_layout_branding" inherit_id="web.layout">
        <xpath expr="//head/title" position="replace">
            <title>智能工程项目管理系统</title>
        </xpath>
        <xpath expr="//head" position="inside">
            <link rel="icon" type="image/svg+xml" href="/smart_construction_core/static/src/img/brand/favicon.svg"/>
        </xpath>
    </template>

    <template id="sc_web_login_layout_inherit" inherit_id="web.login_layout">
        <xpath expr="//t[@t-call='web.frontend_layout']" position="inside">
            <t t-set="disable_footer" t-value="True"/>
            <t t-set="disable_database_manager" t-value="True"/>
        </xpath>
        <xpath expr="//img[contains(@t-attf-src, '/web/binary/company_logo')]" position="replace">
            <img class="img-fluid" src="/smart_construction_core/static/src/img/brand/logo.svg" alt="Smart Construction"/>
        </xpath>
        <xpath expr="." position="inside">
            <t t-if="request.env['ir.config_parameter'].sudo().get_param('sc.login.custom_enabled', '1') in ('1','true','True')">
                <style>
                    .sc-login-branding h2{
                        margin: 0 0 8px 0;
                        font-weight: 700;
                        font-size: 22px;
                        line-height: 1.2;
                        white-space: nowrap;
                        overflow: hidden;
                        text-overflow: ellipsis;
                    }
                    .sc-login-logo{
                        margin-bottom: 10px;
                    }
                    .sc-login-logo img{
                        max-width: 220px;
                        height: auto;
                        display: block;
                    }
                    .sc-login-branding .sc-subtitle{
                        color: #666;
                        font-size: 13px;
                        margin-bottom: 8px;
                    }
                    .sc-login-env-badge{
                        display: inline-block;
                        padding: 2px 8px;
                        border-radius: 999px;
                        font-size: 12px;
                        font-weight: 600;
                        background: #eef6ff;
                        color: #1d4ed8;
                        margin-bottom: 10px;
                    }
                    .sc-login-env-badge.is-demo{ background: #ecfdf5; color: #047857; }
                    .sc-login-env-badge.is-prod{ background: #fff7ed; color: #9a3412; }
                    .o_login_footer,
                    .o_login_logo,
                    .o_login_image{
                        display: none !important;
                    }
                    .oe_login_buttons button[name='redirect']{ display: none !important; }
                </style>
            </t>
        </xpath>
        <xpath expr="//a[@href='/web/database/manager']" position="attributes">
            <attribute name="t-if">
                not (
                request.env['ir.config_parameter'].sudo().get_param('sc.login.custom_enabled', '1') in ('1','true','True')
                and request.env['ir.config_parameter'].sudo().get_param('sc.login.env', 'prod') in ('demo','prod')
                )
            </attribute>
        </xpath>
    </template>
</odoo>
