<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <!-- 列表 -->
        <record id="view_project_settlement_tree" model="ir.ui.view">
            <field name="name">project.settlement.tree</field>
            <field name="model">project.settlement</field>
            <field name="arch" type="xml">
                <tree string="项目结算">
                    <field name="name"/>
                    <field name="project_id"/>
                    <field name="type"/>
                    <field name="contract_id"/>
                    <field name="partner_id"/>
                    <field name="amount"/>
                    <field name="state"/>
                    <field name="date_settle"/>
                </tree>
            </field>
        </record>

        <!-- 表单 -->
        <record id="view_project_settlement_form" model="ir.ui.view">
            <field name="name">project.settlement.form</field>
            <field name="model">project.settlement</field>
            <field name="arch" type="xml">
                <form string="项目结算">
                    <header>
                        <button name="action_confirm"
                                type="object"
                                string="确认"
                                class="btn-primary"
                                invisible="state != 'draft'"/>
                        <button name="action_done"
                                type="object"
                                string="完成"
                                class="btn-secondary"
                                invisible="state not in ['draft', 'confirmed']"/>
                        <button name="action_cancel"
                                type="object"
                                string="取消"
                                class="btn-secondary"
                                invisible="state in ['done', 'cancel']"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,done,cancel"/>
                    </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="name" readonly="1"/>
                                <field name="project_id" options="{'no_create': True}"/>
                                <field name="type"/>
                                <field name="contract_id" domain="[('project_id', '=', project_id)]"/>
                            </group>
                            <group>
                                <field name="partner_id" options="{'no_create_edit': True}"/>
                                <field name="amount"/>
                                <field name="currency_id"/>
                                <field name="date_settle"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="结算明细">
                                <field name="line_ids">
                                    <tree editable="bottom">
                                        <field name="sequence" widget="handle"/>
                                        <field name="name"/>
                                        <field name="amount"/>
                                        <field name="currency_id" invisible="1"/>
                                    </tree>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- 动作 -->
        <record id="action_project_settlement" model="ir.actions.act_window">
            <field name="name">结算中心</field>
            <field name="res_model">project.settlement</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{'search_default_group_by_project_id': 1}</field>
            <field name="groups_id" eval="[(4, ref('smart_construction_core.group_sc_cap_finance_read'))]"/>
        </record>

        <!-- 菜单 -->
        <menuitem id="menu_sc_settlement_center"
                  name="结算中心"
                  parent="smart_construction_core.menu_sc_finance_center"
                  sequence="30"
                  groups="smart_construction_core.group_sc_cap_finance_read"/>

        <menuitem id="menu_project_settlement"
                  name="结算单"
                  parent="menu_sc_settlement_center"
                  action="action_project_settlement"
                  sequence="10"
                  groups="smart_construction_core.group_sc_cap_finance_read"/>

    </data>
</odoo>
