<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">

        <record id="server_action_construction_contract_on_approved" model="ir.actions.server">
            <field name="name">项目合同审批通过回调</field>
            <field name="model_id" ref="model_construction_contract"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "construction.contract" and env.context.get("active_id"):
    target_records = env["construction.contract"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
        </record>

        <record id="server_action_construction_contract_on_rejected" model="ir.actions.server">
            <field name="name">项目合同审批驳回回调</field>
            <field name="model_id" ref="model_construction_contract"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "construction.contract" and env.context.get("active_id"):
    target_records = env["construction.contract"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
        </record>

        <record id="server_action_general_contract_on_approved" model="ir.actions.server">
            <field name="name">一般合同审批通过回调</field>
            <field name="model_id" ref="model_sc_general_contract"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.general.contract" and env.context.get("active_id"):
    target_records = env["sc.general.contract"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
        </record>

        <record id="server_action_general_contract_on_rejected" model="ir.actions.server">
            <field name="name">一般合同审批驳回回调</field>
            <field name="model_id" ref="model_sc_general_contract"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.general.contract" and env.context.get("active_id"):
    target_records = env["sc.general.contract"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
        </record>

        <record id="server_action_legacy_purchase_contract_on_approved" model="ir.actions.server">
            <field name="name">采购/一般合同审批通过回调</field>
            <field name="model_id" ref="model_sc_legacy_purchase_contract_fact"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.legacy.purchase.contract.fact" and env.context.get("active_id"):
    target_records = env["sc.legacy.purchase.contract.fact"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
        </record>

        <record id="server_action_legacy_purchase_contract_on_rejected" model="ir.actions.server">
            <field name="name">采购/一般合同审批驳回回调</field>
            <field name="model_id" ref="model_sc_legacy_purchase_contract_fact"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.legacy.purchase.contract.fact" and env.context.get("active_id"):
    target_records = env["sc.legacy.purchase.contract.fact"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
        </record>

    </data>
</odoo>
