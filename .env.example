# =====================================
# Compose Environment Template (SOT)
# =====================================
#
# Copy to .env (single env) or .env.<env> (multi env)
# Examples: .env.dev .env.test .env.prod

# Pin compose project name (avoid cross-project collisions)
ENV=dev
ENV_FILE=.env.dev
COMPOSE_PROJECT_NAME=sc-backend-odoo-dev

# Database
DB_USER=odoo
DB_PASSWORD=odoo
DB_NAME=sc_demo

# Odoo master password
ADMIN_PASSWD=admin

# JWT secret (dev placeholder; production must be long random)
JWT_SECRET=dev-secret-change-me

# Required by odoo image entrypoint (renders odoo.conf from template)
# Lock Odoo to specific DB(s) to avoid DB listing & cross-db accidents.
# Single DB:
ODOO_DBFILTER=^sc_demo$
# Multi DB example:
# ODOO_DBFILTER=^sc_(odoo|demo|test)$

# Runtime data (named volumes, auto-prefixed by project)
DB_DATA=db_data
REDIS_DATA=redis_data
ODOO_DATA=odoo_data

# Host ports
NGINX_PORT=18081
ODOO_PORT=8070
