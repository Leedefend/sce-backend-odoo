task_id: ITER-2026-04-09-1447
title: remove file_upload required-group overreach and verify denial-source shift
status: active
type: backend-implementation
priority: high
mode: implement
batch: 1/1

goal: >
  消除 file.upload 解释层组门禁越界，确保该写链路由模型/记录 ACL 与允许模型策略裁决，
  不再因缺少 REQUIRED_GROUPS 被拒绝。

user_visible_outcome:
  - "file.upload 拒绝原因从 required-groups 转为事实权限/模型策略"

scope:
  allowed_paths:
    - addons/smart_core/handlers/file_upload.py
    - agent_ops/tasks/ITER-2026-04-09-1447.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1447.md
    - agent_ops/state/task_results/ITER-2026-04-09-1447.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
    - artifacts/codex/**
  forbidden_paths:
    - frontend/**
    - security/**
    - record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py

limits:
  max_files: 6
  max_output_lines: 200

change_rules:
  - bounded single-file implementation
  - no acl file or record rule changes

context:
  branch: codex/next-round
  baseline_sha: 9109f99
  explicit_user_authorization: true

architecture:
  layer_target: Backend permission-boundary recovery
  module: file.upload intent handler
  module_owner: smart_core backend contract stack
  kernel_or_scenario: scenario
  backend_sub_layer_target: business_fact
  backend_sub_layer_reason: >
    写权限应由模型/记录 ACL 与 allowed_models 事实裁决，不应由解释层硬编码组门禁改写。
  reason: >
    1446 显示 PM 在 file.upload 被 REQUIRED_GROUPS 拒绝，属于解释层越界，需要定点移除。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1447.yaml"
    - "python3 -m py_compile addons/smart_core/handlers/file_upload.py"
    - "make restart"
    - "make verify.portal.edit_tx_smoke.container DB_NAME=sc_demo E2E_LOGIN=sc_fx_pm E2E_PASSWORD=prod_like"
    - "make verify.portal.execute_button_smoke.container DB_NAME=sc_demo E2E_LOGIN=sc_fx_pm E2E_PASSWORD=prod_like"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_command_failed
  - forbidden_path_touched
  - denial_source_still_required_groups

deliverables:
  - file.upload boundary fix
  - denial-source probe evidence

depends_on:
  - ITER-2026-04-09-1446

rollback:
  - "git restore addons/smart_core/handlers/file_upload.py"
  - "git restore agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1447.md"
  - "git restore agent_ops/state/task_results/ITER-2026-04-09-1447.json"
  - "git restore docs/ops/iterations/delivery_context_switch_log_v1.md"
