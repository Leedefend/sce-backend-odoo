task_id: ITER-2026-03-29-241
title: Restore project form settings and system page fields
type: implementation_task
status: pending
priority: high

goal: Include the missing settings and sc_system page fields in the governed project.project user form contract so notebook pages survive pruning.
user_visible_outcome:
  - project detail user contract retains the Settings and 协作/系统 tabs
  - frontend can render the native notebook/page structure without guessing missing fields

scope:
  allowed_paths:
    - addons/smart_core/utils/**
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/__manifest__.py

change_rules:
  - additive_or_minimal_fix_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend governance projection
  module: smart_core project form user field picker
  reason: The governed user form is missing eight page-scoped fields from settings and sc_system, causing notebook/page nodes to be pruned out of the live contract.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-241.yaml
    - python3 -m py_compile addons/smart_core/utils/contract_governance.py agent_ops/scripts/project_form_layout_field_gap_audit.py
    - make verify.smart_core
    - make restart
    - bash -lc 'export E2E_BASE_URL=http://127.0.0.1:8070; python3 agent_ops/scripts/project_form_layout_field_gap_audit.py --db sc_demo --login sc_fx_pm --password prod_like --container sc-backend-odoo-dev-odoo-1 --action-id 449 --record-id 3'

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - settings_and_system_fields_still_missing

deliverables:
  - governance_fix
  - audit_report
  - task_result

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-241.yaml addons/smart_core/utils/contract_governance.py agent_ops/reports/2026-03-29/report.ITER-2026-03-29-241.md agent_ops/state/task_results/ITER-2026-03-29-241.json docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-29-240

notes: "Implementation batch: patch only the governed project form field picker for missing page fields."
