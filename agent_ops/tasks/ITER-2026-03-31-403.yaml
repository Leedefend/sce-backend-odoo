task_id: ITER-2026-03-31-403
title: Normalize confirmed customer structure into department role draft
type: audit
status: completed
priority: high

goal: >
  Convert the confirmed customer rules into a structured draft for departments,
  enterprise-specific posts, system roles, and project-department treatment so
  later system mapping can proceed without re-interpreting the workbook.

user_visible_outcome:
  - "形成可确认的企业组织结构草案"
  - "明确部门、岗位、系统角色的边界"
  - "明确项目部在系统中的特殊处理口径"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no implementation changes
  - use the user-confirmed rules from the current thread
  - produce a structured draft suitable for later system mapping

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Governance Audit
  module: customer structure normalization
  module_owner: smart_construction_custom onboarding
  kernel_or_scenario: scenario
  reason: >
    The owner confirmed the interpretation rules for departments, posts, system
    roles, and project department treatment. The next low-risk step is to
    freeze those rules into a structured draft.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-403.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - confirmed_structure_still_ambiguous

deliverables:
  - structured customer draft for departments, posts, system roles, and special project-department treatment
  - report and task result artifacts for ITER-2026-03-31-403
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-403.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-403.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-403.json

depends_on:
  - ITER-2026-03-31-402
