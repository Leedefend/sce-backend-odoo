task_id: ITER-2026-03-29-258
title: Audit and tighten generic kanban baseline from contract facts
type: implementation_task
status: completed
priority: high

goal: Resume the product-usability line by using project.project as the sample model to audit and tighten the generic kanban consumer so the frontend can render backend kanban contracts in a more native and generally reusable way.
user_visible_outcome:
  - project kanban becomes a clearer baseline sample for contract-driven kanban rendering
  - frontend kanban behavior moves closer to native contract facts without model-specific branching
  - the usability line continues on the generic page-rendering path

scope:
  allowed_paths:
    - frontend/apps/web/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**/package.json

change_rules:
  - generic_consumer_only
  - no_model_specific_hardcode
  - no_backend_contract_change
  - no_schema_change
  - no_acl_change

architecture:
  layer_target: frontend layer
  module: generic contract-driven kanban consumer
  reason: After stabilizing backend contract boundaries, the next product-facing priority is to continue the generic page usability line and make kanban the next baseline page type after list and detail.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-258.yaml
    - bash -lc 'source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20 >/dev/null && pnpm -C frontend/apps/web typecheck:strict'

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - model_specific_branch_required
  - typecheck_failed
  - contract_consumer_uncertain

deliverables:
  - generic_kanban_consumer_update
  - iteration_report

rollback:
  - git restore frontend/apps/web
  - git restore agent_ops/tasks/ITER-2026-03-29-258.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - 7468e72

notes: "Use project.project only as the audit sample. Keep the implementation generic."
