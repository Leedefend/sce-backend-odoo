task_id: ITER-2026-04-01-584
title: Verify repaired recordview HUD smoke and reopen continuity gate
type: frontend
status: active
priority: high
mode: verify

goal: >
  Verify that the repaired recordview HUD smoke now passes and reopens the
  continuity gate for subsequent record-view usability work.

user_visible_outcome:
  - "确认 HUD smoke 验证链已恢复"
  - "record-view continuity 线重新具备专用门禁"
  - "为后续 continuity 任务恢复可信状态"

scope:
  allowed_paths:
    - Makefile
    - scripts/verify/fe_recordview_hud_smoke.js
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - verify stage only
  - do not make new product edits in this batch
  - run only declared validation commands
  - keep scope on the dedicated HUD smoke

context:
  branch: codex/next-round
  baseline_sha: e430a7e
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-583
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: verification_recovery

architecture:
  layer_target: Frontend Layer
  module: recordview HUD smoke verification
  module_owner: frontend/apps/web verify tooling
  kernel_or_scenario: kernel
  reason: >
    The dedicated recordview HUD smoke is the blocking gate for the continuity
    line. This batch verifies whether the login-token compatibility repair is
    sufficient to restore that gate.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-584.yaml
    - make verify.portal.recordview_hud_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - dedicated_hud_smoke_still_fails

deliverables:
  - verification_result_for_repaired_recordview_hud_smoke
  - governance/report artifacts for ITER-2026-04-01-584

rollback:
  - git restore Makefile
  - git restore scripts/verify/fe_recordview_hud_smoke.js
  - git restore agent_ops/tasks/ITER-2026-04-01-584.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-584.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-584.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-583
