task_id: ITER-2026-04-05-1163
title: Promote non-native verify bundle into broader architecture gate
type: implementation
status: active
priority: high
mode: execute

goal: >
  Promote non-native capability drift verify bundle into a broader architecture
  governance gate for stable release-time adoption.

user_visible_outcome:
  - "新增 capability projection governance gate"
  - "non-native verify bundle 纳入更高层架构门禁"

scope:
  allowed_paths:
    - Makefile
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - scripts/**
    - security/**
    - record_rules/**
    - addons/**/__manifest__.py

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 320
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - makefile_governance_wiring_only
  - no_runtime_code_change

context:
  branch: codex/next-round
  baseline_sha: da08944
  explicit_user_authorization: true
  input_from: approved_after_1162
  priority_lane: governance_promotion

architecture:
  layer_target: Governance Monitoring
  module: Makefile gate orchestration
  module_owner: platform
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: gate promotion controls orchestration-level capability drift risk.
  reason: ensure non-native verify controls are applied in broader architecture governance flow.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-1163.yaml
    - make verify.architecture.capability_projection_governance_gate
    - make verify.architecture.platformization_boundary_closure_bundle
    - make verify.controller.boundary.guard

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - promoted architecture governance gate wiring

rollback:
  - git restore Makefile
  - git restore agent_ops/tasks/ITER-2026-04-05-1163.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-1163.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-1163.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-1162

