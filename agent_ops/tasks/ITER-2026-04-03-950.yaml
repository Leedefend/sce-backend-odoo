task_id: ITER-2026-04-03-950
title: Recover sc_demo runtime seed users for release verification strictness
type: backend
status: active
priority: high
mode: implement

goal: >
  Restore missing demo runtime users in sc_demo so release verification can run
  on seeded lanes instead of environment skip branches.

user_visible_outcome:
  - "release 主链验证恢复到可用 demo 用户语义"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - runtime_seed_recovery_commands_only
  - no_source_code_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-949
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: runtime demo seed recovery
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch executes runtime recovery and verification orchestration only.
  reason: >
    Current chain passes with SKIP_ENV due missing demo users; recover runtime
    seed lane to resume strict user-path checks.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-950.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim make db.demo.reset
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim make verify.demo.release.seed DB_NAME=sc_demo
    - ARTIFACTS_DIR=artifacts HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost E2E_LOGIN=admin E2E_PASSWORD=admin make verify.release.operator_surface.v1 DB_NAME=sc_demo

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - runtime_seed_recovered_for_release_lane

rollback:
  - "Re-run: ENV=test ENV_FILE=.env.prod.sim ... make db.demo.reset"
  - git restore agent_ops/tasks/ITER-2026-04-03-950.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-950.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-950.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-949
