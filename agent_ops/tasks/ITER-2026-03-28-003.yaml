task_id: ITER-2026-03-28-003
title: Validate queue stop on fail
type: governance_task
status: pending
priority: high

goal: Prove that the queue stops and records evidence when an iteration fails verification.
user_visible_outcome:
  - queue stop-on-fail branch is exercised
  - iteration report captures verify_failed as a stop condition
  - dedicated fail queue state records the fail stop event

scope:
  allowed_paths:
    - agent_ops/**
    - Makefile
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Governance/Tooling
  module: agent_ops queue + reports + docs/ops/releases/archive/temp
  reason: Validate the stop-on-fail control path without touching any business module.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-003.yaml
    - python3 -c "import sys; sys.exit(1)"

risk:
  level: low
  manual_approval_required: false
  threshold_overrides:
    max_changed_files: 200
    max_added_lines: 5000
    max_removed_lines: 1000

stop_conditions:
  - verify_failed

deliverables:
  - code
  - report

rollback:
  - git restore agent_ops Makefile docs/ops/iterations/delivery_context_switch_log_v1.md docs/ops/releases/archive/temp

depends_on:
  - ITER-2026-03-28-001
notes: "Intentional FAIL sample task for queue stop validation."
