task_id: ITER-2026-03-29-126
title: Make scene merge resolver preserve searchpanel semantics
type: governance_task
status: pending
priority: high

goal: Make scene policy/provider merge treat searchpanel as a first-class search surface field instead of only merging filters, group_by, and fields.
user_visible_outcome:
  - released scene contracts now preserve parser-derived searchpanel semantics across policy and provider merge steps

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core scene_merge_resolver
  reason: Scene merge resolver still treats searchpanel as a second-class field, so parser-derived faceted search semantics can be dropped during policy/provider merge. This iteration promotes searchpanel to a first-class merged search surface field.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-126.yaml
    - python3 -m py_compile addons/smart_core/core/scene_merge_resolver.py addons/smart_core/tests/test_scene_merge_resolver_searchpanel_semantics.py
    - python3 addons/smart_core/tests/test_scene_merge_resolver_searchpanel_semantics.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-126.yaml addons/smart_core/core/scene_merge_resolver.py addons/smart_core/tests/test_scene_merge_resolver_searchpanel_semantics.py

depends_on:
  - ITER-2026-03-29-125

notes: "Batch-3 continuation: faceted search semantics must survive scene policy/provider merge."
