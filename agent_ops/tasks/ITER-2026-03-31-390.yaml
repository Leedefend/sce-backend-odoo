task_id: ITER-2026-03-31-390
title: Audit capability matrix consistency after scene target repair
type: audit
status: in_progress
priority: high

goal: >
  Verify that the explicit `能力矩阵` consistency drift recorded in
  ITER-2026-03-31-388 has been closed after ITER-2026-03-31-389.

user_visible_outcome:
  - "明确能力矩阵是否已经与 scene facts 和自定义前端落点一致"
  - "把剩余一致性问题收缩到非能力矩阵项"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no code changes
  - focus only on capability-matrix consistency closure in this batch

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: capability matrix post-repair consistency
  module_owner: smart_construction_scene
  kernel_or_scenario: scenario
  reason: >
    After the scene target repair, the next low-risk step is to confirm whether
    the capability-matrix consistency issue is now fully closed.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-390.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - post_repair_scope_unclear

deliverables:
  - explicit closure decision for capability-matrix consistency
  - report and task result artifacts for ITER-2026-03-31-390
  - delivery log checkpoint with the next active line

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-390.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-390.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-390.json

depends_on:
  - ITER-2026-03-31-389
