task_id: ITER-2026-03-30-377
title: Audit remaining governance bootstrap data ownership after extension migration
type: audit
status: completed
priority: medium

goal: >
  Audit the two remaining non-demo bootstrap/governance data files in
  `smart_construction_core/data` and decide whether they should stay in core or
  move to another owner after the extension bootstrap migration.

user_visible_outcome:
  - remaining core bootstrap/governance files have explicit ownership decisions
  - no unnecessary migration is attempted without a clear target

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; do not migrate files in this batch
  - base conclusions on model, menu, action, ACL, and owner references
  - keep the result limited to explicit ownership decisions and next-step advice

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: remaining bootstrap and governance data ownership
  module_owner: smart_construction_core
  kernel_or_scenario: kernel
  reason: >
    After the extension bootstrap files moved to enterprise runtime ownership,
    the remaining ownership question is limited to `sc_subscription_default.xml`
    and `sc_cap_config_admin_user.xml`.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-377.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - audit_scope_becomes_unclear

deliverables:
  - explicit ownership recommendation for sc_subscription_default.xml
  - explicit ownership recommendation for sc_cap_config_admin_user.xml
  - report and task result artifacts for ITER-2026-03-30-377
  - delivery log checkpoint for the next cleanup line

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-377.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-377.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-377.json

depends_on:
  - ITER-2026-03-30-376
