task_id: ITER-2026-04-05-1003
title: Recover prod-sim runtime connectivity and rerun frontend api smoke
type: backend
status: active
priority: high
mode: verify

goal: >
  Recover runtime connectivity for prod-sim stack and rerun
  `verify.frontend_api` after P0 route migration.

user_visible_outcome:
  - "确认 prod-sim 恢复可达并给出 smoke 验证结论"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 180
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify_only
  - runtime_recovery_and_validation_only
  - no_source_code_change

context:
  branch: codex/next-round
  baseline_sha: f4102e4
  explicit_user_authorization: true
  input_from: ITER-2026-04-05-1002
  priority_lane: smart_construction_core_boundary_remediation_p0_verify_recovery

architecture:
  layer_target: Runtime Verification
  module: prod-sim api chain connectivity
  module_owner: operations_runtime
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Runtime restart and smoke check validate orchestration entry availability.
  reason: >
    Previous verify batch failed due to unreachable runtime; user requested continue execution.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-1003.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim make restart
    - FRONTEND_API_BASE_URL=http://127.0.0.1:18069 ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim make verify.frontend_api

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_failed

deliverables:
  - frontend_api_smoke_recovery_verdict

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-05-1003.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-1003.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-1003.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-1002
