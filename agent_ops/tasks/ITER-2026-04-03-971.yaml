task_id: ITER-2026-04-03-971
title: Align release navigation browser smoke with unified menu strategy
type: backend
status: active
priority: high
mode: implement

goal: >
  Update release navigation browser smoke expectations from legacy release-label
  rendering to unified system-menu semantic checks.

user_visible_outcome:
  - "发布门禁中的导航浏览器烟测不再因旧标签预期误报"

scope:
  allowed_paths:
    - scripts/verify/release_navigation_browser_smoke.mjs
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - browser_smoke_semantics_alignment_only
  - no_business_fact_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-970
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: release navigation browser smoke gate
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This change aligns verification semantics for runtime navigation consumption
    under unified menu projection, without changing business facts.
  reason: >
    Current smoke still waits for legacy FR label set and times out after menu
    strategy unification.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-971.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim make restart
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost DB_NAME=sc_prod_sim E2E_LOGIN=svc_e2e_smoke E2E_PASSWORD=demo make verify.portal.release_navigation_browser_smoke.host
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost API_BASE_URL=http://127.0.0.1:18069 DB_NAME=sc_prod_sim E2E_LOGIN=svc_e2e_smoke E2E_PASSWORD=demo make verify.release.delivery_engine.v1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - release_navigation_browser_smoke_aligned

rollback:
  - git restore scripts/verify/release_navigation_browser_smoke.mjs
  - git restore agent_ops/tasks/ITER-2026-04-03-971.yaml

depends_on:
  - ITER-2026-04-03-970
