task_id: ITER-2026-03-29-131
title: Preserve searchpanel in ui base search facts
type: governance_task
status: pending
priority: high

goal: Make ui base contract adapter preserve searchpanel semantics in canonical search facts instead of only exposing default_sort, filters, group_by, and fields.
user_visible_outcome:
  - orchestrator_input.search_fact now includes searchpanel semantics for faceted search contracts

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core ui_base_contract_adapter
  reason: Ui base contract adapter still emits a legacy search_fact subset that drops searchpanel semantics, so faceted search is not fully represented at the canonical adapter entry.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-131.yaml
    - python3 -m py_compile addons/smart_core/core/ui_base_contract_adapter.py addons/smart_core/tests/test_ui_base_contract_adapter.py
    - python3 addons/smart_core/tests/test_ui_base_contract_adapter.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-131.yaml addons/smart_core/core/ui_base_contract_adapter.py addons/smart_core/tests/test_ui_base_contract_adapter.py

depends_on:
  - ITER-2026-03-29-130

notes: "Batch-3 continuation: ui base canonical search facts should preserve searchpanel semantics."
