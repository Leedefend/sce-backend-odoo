task_id: ITER-2026-03-31-415
title: Freeze customer admin and common role mapping boundary
type: audit
status: active
priority: high

goal: >
  Freeze the repository-backed boundary for workbook roles `管理员角色` and
  `通用角色` by checking whether current role-profile and role-group mappings
  respect the newly confirmed customer rule: business-system highest authority
  must not imply platform-level settings authority.

user_visible_outcome:
  - "明确现有角色承载是否满足客户定义"
  - "明确是否可以直接复用现有 role profile"
  - "明确下一步是否必须进入受控高风险权限治理批次"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only
  - no security xml or model edits
  - use repository facts only

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Governance Audit
  module: smart_construction_custom customer bootstrap
  module_owner: customer-specific delivery customization
  kernel_or_scenario: scenario
  reason: >
    The customer has now defined `管理员角色` as the highest business-system
    authority that must still stop short of platform-level settings. The next
    low-risk step is to determine whether existing repository role carriers can
    satisfy that rule without security refactor.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-415.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - existing_role_profile_implies_platform_admin
  - workbook_role_mapping_requires_security_refactor

deliverables:
  - report and task result artifacts for ITER-2026-03-31-415
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-415.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-415.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-415.json

depends_on:
  - ITER-2026-03-31-414
