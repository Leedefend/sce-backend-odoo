task_id: ITER-2026-04-01-521
title: Reclassify the workflow family on the config-admin governance line
type: permission-governance
status: active
priority: medium

goal: >
  Continue the config-admin governance objective after quota-import closure by
  formally classifying the workflow family as a governance-side family rather
  than a delivered-role surface.

user_visible_outcome:
  - "继续扩展 config_admin / 平台治理面 覆盖"
  - "正式分类 workflow 家族"
  - "确认 workflow 是真实治理面还是仍有 delivered-role residual"

scope:
  allowed_paths:
    - addons/smart_construction_core/**
    - addons/smart_construction_custom/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit-first governance batch
  - implementation changes are out of scope unless a later narrow batch is opened
  - treat prior delivered-role false-positive findings as inputs, not as final governance classification

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom
  explicit_user_authorization: true

architecture:
  layer_target: Platform Governance Acceptance
  module: workflow family audit
  module_owner: platform governance surface
  kernel_or_scenario: scenario
  reason: >
    Earlier delivered-role screening isolated workflow as a config-admin
    platform surface. The next safe step is to classify it formally inside the
    config-admin governance objective.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-521.yaml
    - "repository audit confirms workflow menu and action ownership"
    - "runtime audit classifies workflow entrypoints without high-risk side effects"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - workflow_requires_acl_or_manifest_changes
  - runtime_cannot_classify_workflow_with_confidence
  - workflow_boundary_crosses_into_financial_domain

deliverables:
  - workflow family classification
  - governance/report artifacts for ITER-2026-04-01-521

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-521.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-521.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-521.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-520
