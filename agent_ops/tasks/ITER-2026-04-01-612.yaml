task_id: ITER-2026-04-01-612
title: Screen architecture-correction slice for lifecycle usability without frontend model branching
type: frontend
status: active
priority: high
mode: screen

goal: >
  Select a bounded correction slice that removes frontend model-specific
  branching and keeps lifecycle usability improvements generic.

user_visible_outcome:
  - "撤销前端按模型分支实现"
  - "保持生命周期可用性优化方向"
  - "转为消费后端通用语义"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 3
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - screen stage only
  - architecture guard first
  - do not reopen repo-wide scan
  - do not implement in this batch

context:
  branch: codex/next-round
  baseline_sha: 4ba974e
  explicit_user_authorization: true
  input_from: user_architecture_boundary_feedback
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_project_lifecycle_usability

architecture:
  layer_target: Frontend Layer
  module: lifecycle usability architecture correction screen
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    User explicitly rejected frontend model branching. Next slice must preserve
    lifecycle usability while consuming only generic contract semantics.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-612.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - no_bounded_candidate_remains

deliverables:
  - selected_architecture_correction_slice_for_lifecycle_usability
  - governance/report artifacts for ITER-2026-04-01-612

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-612.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-612.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-612.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-608
