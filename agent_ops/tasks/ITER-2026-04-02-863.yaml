task_id: ITER-2026-04-02-863
title: Fix cost tracking record create 500 due to missing accounting currency context
type: backend
status: active
priority: high
mode: implement

goal: >
  Repair cost.tracking.record.create by enforcing deterministic company/currency
  context on account.move and move lines, removing null-currency insert failures.

user_visible_outcome:
  - "成本记录创建恢复可用"
  - "成本链路闭环门禁恢复通过"

scope:
  allowed_paths:
    - addons/smart_construction_core/services/cost_tracking_entry_service.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive_only
  - backend_semantics_fix_only
  - no_frontend_semantic_change

context:
  branch: codex/next-round
  baseline_sha: af86321
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-862
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: cost tracking entry service
  module_owner: smart_construction_core service layer
  kernel_or_scenario: scenario
  backend_sub_layer_target: business_fact_layer
  backend_sub_layer_reason: >
    Failure is in accounting record materialization, a business-fact write path
    issue that must be fixed in backend service semantics.
  reason: >
    Remove runtime 500 and restore cost-chain closure.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-863.yaml
    - make verify.product.cost_entry_contract_guard
    - make verify.product.cost_list_block_guard
    - make verify.product.cost_summary_block_guard
    - make verify.product.project_flow.execution_cost

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - cost.tracking.record.create no longer fails on null currency/company context
  - governance/report artifacts for ITER-2026-04-02-863

rollback:
  - git restore addons/smart_construction_core/services/cost_tracking_entry_service.py
  - git restore agent_ops/tasks/ITER-2026-04-02-863.yaml
  - git restore agent_ops/reports/2026-04-02/report.ITER-2026-04-02-863.md
  - git restore agent_ops/state/task_results/ITER-2026-04-02-863.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-02-862
