task_id: ITER-2026-04-07-1280
title: Screen for dedicated outsider seed user path
status: active
type: screen
priority: medium
mode: screen

goal: >
  Define dedicated path for strict fixed-user outsider deny matrix by adding a
  stable outsider seed identity in customer data lane.

user_visible_outcome:
  - "形成 strict fixed-user deny 矩阵的可执行种子路径"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1280.yaml
    - docs/ops/governance/fixed_outsider_seed_path_screen_v1.md
  forbidden_paths:
    - addons/**

limits:
  max_files: 2
  max_output_lines: 120
  forbid_repo_wide_scan: true

change_rules:
  - screen-only

context:
  branch: codex/next-round
  explicit_user_authorization: true
  input_from: ITER-2026-04-07-1279

architecture:
  layer_target: Governance/Authorization Screen
  module: outsider seed path planning
  module_owner: smart_construction_custom
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene-orchestration layer
  backend_sub_layer_reason: planning only
  reason: strict fixed-user deny matrix needs stable outsider identity

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1280.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - outsider seed implementation screen doc

depends_on:
  - ITER-2026-04-07-1279

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1280.yaml
  - git restore docs/ops/governance/fixed_outsider_seed_path_screen_v1.md
