task_id: ITER-2026-04-10-1788
title: delivery-mode cleanup for form structure audit overlay

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在已确认契约与 tab 对齐后，将结构审计覆盖层降级为调试模式可见，
  恢复用户默认视图为交付态。

user_visible_outcome:
  - "普通用户页面不再显示结构投影审计块"
  - "调试模式仍可查看 contract->projection 对账信息"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1788.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1788.md
    - agent_ops/state/task_results/ITER-2026-04-10-1788.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend runtime rendering only
  - no backend semantic mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: ContractFormPage debug audit visibility policy
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    已完成“tab 来自契约且投影无损”验证，下一步应回到交付态默认展示，保留调试通道。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1788.yaml"
    - "rg -n \"structureAuditMode|showStructureProjectionSummary|VITE_FORM_STRUCTURE_AUDIT\" frontend/apps/web/src/pages/ContractFormPage.vue"
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - delivery-mode cleanup patch
  - iteration report and task result

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1787
