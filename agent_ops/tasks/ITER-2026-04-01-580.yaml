task_id: ITER-2026-04-01-580
title: Clarify record-view HUD context under the P1 continuity line
type: frontend
status: active
priority: high
mode: screen

goal: >
  Select the first bounded implementation under the record-view HUD continuity
  line, focusing on the smallest change that improves record-context
  readability after entering detail view.

user_visible_outcome:
  - "明确 HUD continuity 的第一刀是 record-context 文案澄清"
  - "优先提升详情态上下文可读性"
  - "保持 batch 只做归类，不直接实现"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 2
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - P1 screen stage only
  - consume only existing record-view HUD artifacts
  - do not reopen repository scan
  - do not implement in this batch

context:
  branch: codex/next-round
  baseline_sha: e430a7e
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-579
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: record-view HUD continuity screen
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The next continuity line is record-view HUD. The smallest bounded
    improvement is to clarify HUD title/message so the detail-view context is
    readable without changing record-view behavior or debug data content.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-580.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - screen_reads_new_product_files
  - screen_reopens_repo_scan
  - no_bounded_hud_candidate_remains

deliverables:
  - selected_record_view_hud_continuity_slice
  - governance/report artifacts for ITER-2026-04-01-580

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-580.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-580.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-580.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-579
