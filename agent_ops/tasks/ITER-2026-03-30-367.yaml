task_id: ITER-2026-03-30-367
title: Polish work-breakdown first screen for PM landing
type: code
status: in_progress
priority: medium

goal: >
  Finish the residual native PM tail by improving the first-screen quality of
  `工程结构`, so demo PM users land on a more self-explanatory grouped view
  instead of an undifferentiated flat list.

user_visible_outcome:
  - the work-breakdown page opens with clearer project grouping by default
  - the empty/help state explains the intended usage better
  - the native PM repair line closes without extra data or frontend work

scope:
  allowed_paths:
    - addons/smart_construction_core/views/support/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - keep this batch limited to action/search/help polish
  - do not change models, ACL, demo data, or frontend
  - prefer additive defaults over behavioral rewrites

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Scenario Product Layer
  module: work-breakdown landing polish
  module_owner: addons/smart_construction_core
  kernel_or_scenario: scenario
  reason: >
    The PM trio re-audit reduced the residual native usability tail to a single
    small issue: the work-breakdown page still lacks first-screen guidance and
    default focus even after demo data was seeded.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-367.yaml
    - make verify.smart_core

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_command_failed
  - work_breakdown_landing_regresses

deliverables:
  - improved default landing behavior for work-breakdown
  - report and task result artifacts for ITER-2026-03-30-367
  - delivery log checkpoint for the next native or custom-frontend target

rollback:
  - git restore addons/smart_construction_core/views/support/work_breakdown_views.xml
  - git restore agent_ops/tasks/ITER-2026-03-30-367.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-367.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-367.json

depends_on:
  - ITER-2026-03-30-366
