task_id: ITER-2026-03-30-338
title: Compress workspace.home lower capability browser into a compact action deck
type: code
status: in_progress
priority: high

goal: >
  Continue the usability productization mainline for workspace.home by reducing
  the lower-half capability-browser weight and turning the common entry area
  into a smaller, clearer action deck for normal users.

user_visible_outcome:
  - workspace.home lower area reads as a compact action deck instead of a large capability browser
  - normal users see a smaller set of high-value common entries with a clear path to full work pages
  - HUD/debug mode still keeps the broader scene browsing surface available

scope:
  allowed_paths:
    - frontend/apps/web/src/views/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/components/**
    - frontend/apps/web/src/app/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - workspace.home presentation cleanup only
  - no backend contract changes
  - no route or permission changes

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Frontend Layer
  module: workspace.home productized home view
  module_owner: frontend/apps/web/src/views/HomeView.vue
  kernel_or_scenario: scenario
  reason: >
    The workbench home page still carries a heavy lower-half capability browser.
    This batch compresses that area into a smaller action deck so the home page
    keeps users on the mainline path instead of reading like a capability
    catalog.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-338.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_command_failed
  - workspace_home_entry_flow_regressed

deliverables:
  - compact lower-half action deck in HomeView for normal users
  - report and task result artifacts for ITER-2026-03-30-338

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-338.yaml
  - git restore frontend/apps/web/src/views/HomeView.vue
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-338.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-338.json

depends_on:
  - ITER-2026-03-30-337

notes: >
  Keep existing scene entry actions, analytics hooks, and HUD diagnostics
  intact. This batch only changes how the normal user-facing lower-half home
  area is presented.
