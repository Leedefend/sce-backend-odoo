task_id: ITER-2026-04-03-912
title: Add graceful fallback when sc.evidence.summary.service missing
type: backend
status: active
priority: high
mode: implement

goal: >
  Prevent project.entry.context.resolve from returning 500 when
  sc.evidence.summary.service is not available in runtime.

user_visible_outcome:
  - "主入口语义 intent 在依赖缺失时不再 500，保持可用回退"

scope:
  allowed_paths:
    - addons/smart_construction_core/services/project_dashboard_service.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive_fallback_only
  - no_contract_field_remove
  - no_frontend_special_case

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-911
  priority_lane: usability_release_proof
  usability_battlefield: backend_semantics

architecture:
  layer_target: Product Release Usability Proof
  module: project dashboard semantic supplier
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This change only stabilizes semantic supply fallback path in orchestration
    when optional service provider is unavailable.
  reason: >
    Screen result confirmed project.entry.context.resolve 500 due to missing
    sc.evidence.summary.service hard dependency in ProjectDashboardService init.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-912.yaml
    - HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 GATEWAY_BASE_URLS=http://localhost E2E_LOGIN=admin E2E_PASSWORD=admin DB_NAME=sc_demo make verify.portal.project_dashboard_primary_entry_browser_smoke.host
    - HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 GATEWAY_BASE_URLS=http://localhost E2E_LOGIN=admin E2E_PASSWORD=admin DB_NAME=sc_demo make verify.product.main_entry_convergence.v1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed
  - uncertainty_in_fallback_scope

deliverables:
  - graceful_fallback_for_missing_evidence_summary_service

rollback:
  - git restore addons/smart_construction_core/services/project_dashboard_service.py
  - git restore agent_ops/tasks/ITER-2026-04-03-912.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-912.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-912.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-911
