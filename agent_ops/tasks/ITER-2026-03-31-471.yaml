task_id: ITER-2026-03-31-471
title: Audit business-admin display semantics against runtime authority
type: audit
status: active
priority: medium

goal: >
  Continue the Sichuan Baosheng delivery validation chain by auditing whether
  the current business-admin display semantics should be aligned with the
  already-correct runtime authority path.

user_visible_outcome:
  - "可以判断 business_admin 当前是展示残差还是模型残差"
  - "若需要后续修补，可直接进入一张窄实现批次"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - read-only governance and runtime audit only
  - do not modify model fields, role sync logic, XML, ACL, or data files in this batch
  - determine whether the current business-admin sc_role_profile display is acceptable or should be aligned

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom

architecture:
  layer_target: Customer Permission Governance
  module: Business-admin display semantics audit
  module_owner: customer delivery validation chain
  kernel_or_scenario: scenario
  reason: >
    ITER-2026-03-31-470 closed concrete first-batch flow acceptance for the
    delivered roles. The only visible residual is that admin still displays
    sc_role_profile=owner even though runtime authority is business-admin full.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-471.yaml
    - repository and runtime audit can classify business-admin display semantics as acceptable residual or actionable misalignment

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - business_admin_display_semantics_cannot_be_classified_from_repository_and_runtime_evidence
  - display_alignment_would_require_uncertain_contract_or_kernel_semantics

deliverables:
  - business-admin display-semantics audit report
  - task result artifact and delivery-log checkpoint for ITER-2026-03-31-471

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-471.yaml
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-471.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-471.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-31-470
