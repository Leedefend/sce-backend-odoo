task_id: ITER-2026-03-31-493
title: Execute scratch acceptance for the create-task recommendation branch
type: permission-governance
status: active
priority: high

goal: >
  Close the final uncovered recommendation branch by validating the create-task
  rule with a bounded runtime sample or an existing zero-task in-progress sample.

user_visible_outcome:
  - "补齐 创建任务 recommendation 分支的 runtime 验收"
  - "优先用现有样本；只有现有样本不存在时才考虑 scratch project"
  - "样本创建与清理仍须同批完成"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - runtime-audit batch
  - no code, ACL, record-rule, manifest, schema, or seed data changes
  - prefer existing in-progress zero-task samples over scratch project creation
  - if scratch project is required, keep it bounded and clean it up in the same batch

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom
  explicit_user_authorization: true

architecture:
  layer_target: Customer Delivery Flow Acceptance
  module: create-task recommendation acceptance sample execution
  module_owner: customer delivery permission surface
  kernel_or_scenario: scenario
  reason: >
    ITER-2026-03-31-492 closed the task in-progress branch, leaving create-task
    as the only uncovered recommendation branch in the current acceptance chain.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-493.yaml
    - "runtime audit confirms an in-progress zero-task sample emits 创建任务 and that any scratch sample is cleaned up in the same batch"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - no_zero_task_create_task_sample_can_be_classified_with_certainty
  - scratch_project_creation_hits_unexpected_runtime_guards

deliverables:
  - bounded runtime evidence for the create-task recommendation branch
  - governance/report artifacts for ITER-2026-03-31-493

rollback:
  - rerun the documented cleanup query for any scratch project/task sample
  - git restore agent_ops/tasks/ITER-2026-03-31-493.yaml
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-493.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-493.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-31-492
