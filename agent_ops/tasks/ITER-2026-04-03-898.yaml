task_id: ITER-2026-04-03-898
title: Add Playwright APIRequest fallback for host login handshake recovery
type: backend
status: active
priority: high
mode: implement

goal: >
  Recover host login-route handshake verification by adding Playwright
  APIRequest transport fallback when fetch/http-module probes fail.

user_visible_outcome:
  - "登录预检在网络栈失败时可切换到 Playwright 请求栈继续探测"
  - "主入口链路更快区分网络栈问题与真实登录契约问题"

scope:
  allowed_paths:
    - scripts/verify/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - handshake_transport_fallback_only
  - custom_frontend_only_guard
  - no_business_model_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-897
  priority_lane: usability_release_proof
  usability_battlefield: cross_stack_verification

architecture:
  layer_target: Product Release Usability Proof
  module: host login preflight handshake recovery
  module_owner: scene_orchestration_governance
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch changes verification transport orchestration only and keeps
    business facts untouched.
  reason: >
    Current preflight reaches TCP-ready origins but HTTP handshake still fails
    in current probes; Playwright APIRequest fallback is needed to classify or
    recover handshake on the same endpoint set.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-898.yaml
    - make verify.portal.project_dashboard_primary_entry_browser_smoke.host
    - make verify.product.main_entry_convergence.v1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - Playwright APIRequest fallback evidence in login preflight checks
  - updated host gate signal after handshake fallback activation

rollback:
  - git restore scripts/verify/project_dashboard_primary_entry_browser_smoke.mjs
  - git restore agent_ops/tasks/ITER-2026-04-03-898.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-898.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-898.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-897
