task_id: ITER-2026-04-01-598
title: Normalize HUD contract boolean empty-state placeholders
type: frontend
status: active
priority: high
mode: implement

goal: >
  Normalize empty-state display for HUD contract boolean rows so undefined
  values do not render as blank entries.

user_visible_outcome:
  - "契约布尔状态在缺值时不再空白"
  - "HUD 占位符语义更一致"
  - "true/false 语义保持不变"

scope:
  allowed_paths:
    - frontend/apps/web/src/app/action_runtime/useActionViewHudEntriesRuntime.ts
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - bounded P1 usability implementation
  - do not change HUD field coverage
  - keep boolean true/false behavior unchanged when value exists

context:
  branch: codex/next-round
  baseline_sha: a0c866c
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-597
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: record-view HUD contract boolean readability
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    Contract boolean rows can render blank when source values are undefined,
    which weakens HUD readability. Placeholder normalization keeps semantics but
    removes ambiguity.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-598.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.recordview_hud_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - dedicated_hud_smoke_still_fails

deliverables:
  - bounded_hud_contract_boolean_placeholder_fix
  - governance/report artifacts for ITER-2026-04-01-598

rollback:
  - git restore frontend/apps/web/src/app/action_runtime/useActionViewHudEntriesRuntime.ts
  - git restore agent_ops/tasks/ITER-2026-04-01-598.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-598.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-598.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-597
