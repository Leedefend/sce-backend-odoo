task_id: ITER-2026-04-07-1218
title: Screen explicit 8070 config source in runtime chain
type: governance
status: completed
priority: high
mode: screen

goal: >
  Locate and classify explicit 8070 configuration sources that override runtime
  fallback behavior in current verification chain.

user_visible_outcome:
  - "8070 来源定位清晰"
  - "后续配置修复可直接执行"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1218.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_environment_repair_lane_batch5_8070_config_source_screen_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 240
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen-only
  - no source code mutation

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_environment_repair_batch5_screen

architecture:
  layer_target: Governance Monitoring
  module: runtime config source screening
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch inspects runtime configuration source chain only.
  reason: >
    Resolve remaining ambiguity by identifying explicit 8070 providers before
    applying configuration-level repair.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1218.yaml
    - make verify.scene.legacy_auth.runtime_probe
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime environment repair lane batch5 config-source screen evidence

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1218.yaml
  - git restore docs/audit/native/native_runtime_environment_repair_lane_batch5_8070_config_source_screen_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1217
