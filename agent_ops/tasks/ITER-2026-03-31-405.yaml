task_id: ITER-2026-03-31-405
title: Reconcile ambiguous workbook users into review buckets
type: audit
status: completed
priority: high

goal: >
  Perform a second-pass reconciliation on the workbook users by isolating the
  ambiguous cases into review buckets: unmapped users, multi-department users,
  and project-department-only users.

user_visible_outcome:
  - "明确哪些用户还没有部门归属"
  - "明确哪些用户跨多个部门"
  - "明确哪些用户当前只落在项目部"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no implementation changes
  - derive review buckets from the existing workbook and confirmed rules
  - keep the output focused on reconciliation, not final master data claims

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Governance Audit
  module: customer user reconciliation
  module_owner: smart_construction_custom onboarding
  kernel_or_scenario: scenario
  reason: >
    After the first-pass user mapping, the next low-risk step is to isolate the
    remaining ambiguous user cases into review buckets that the owner can
    quickly confirm.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-405.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - reconciliation_buckets_unclear

deliverables:
  - review buckets for unmapped users, multi-department users, and project-department-only users
  - report and task result artifacts for ITER-2026-03-31-405
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-405.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-405.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-405.json

depends_on:
  - ITER-2026-03-31-404
