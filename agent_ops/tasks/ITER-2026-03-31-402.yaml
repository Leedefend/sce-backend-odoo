task_id: ITER-2026-03-31-402
title: Normalize customer maintenance workbook into department and project inputs
type: audit
status: completed
priority: high

goal: >
  Read the customer workbook provided in `tmp/用户维护 (1).xlsx` and normalize
  its contents into a structured summary split into department and project
  inputs for the next customer-delivery iteration.

user_visible_outcome:
  - "明确 Excel 里哪些记录属于部门"
  - "明确 Excel 里哪些记录属于项目"
  - "给出可直接继续录入系统的整理结果"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no business implementation changes
  - read from the provided workbook in tmp
  - normalize the output into department and project categories

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Governance Audit
  module: customer delivery input normalization
  module_owner: smart_construction_custom onboarding
  kernel_or_scenario: scenario
  reason: >
    The owner started providing customer delivery data. The next low-risk step
    is to normalize the workbook contents before mapping them into the customer
    customization module.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-402.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - workbook_structure_unclear

deliverables:
  - normalized summary of workbook contents split into department and project records
  - report and task result artifacts for ITER-2026-03-31-402
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-402.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-402.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-402.json

depends_on:
  - ITER-2026-03-31-401
