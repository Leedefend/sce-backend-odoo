task_id: ITER-2026-04-03-951
title: Recover intent runtime DB registry routing after sc_demo rebuild
type: backend
status: active
priority: high
mode: implement

goal: >
  Eliminate project.initiation.enter 500 caused by runtime registry drift
  (`cur_db=sc_prod_sim` while params.db=sc_demo) after demo DB rebuild.

user_visible_outcome:
  - "project 场景契约链路不再因 DB 注册表漂移报 500"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - runtime_recovery_commands_only
  - no_source_code_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-950
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: intent runtime DB registry routing
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch addresses runtime orchestration/state consistency only.
  reason: >
    Logs show intent router receives params.db=sc_demo but request runtime remains
    on sc_prod_sim, triggering KeyError project.project in handler.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-951.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim docker compose --env-file .env.prod.sim restart odoo
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim make verify.product.scene_contract_guard DB_NAME=sc_demo
    - ARTIFACTS_DIR=artifacts HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost E2E_LOGIN=admin E2E_PASSWORD=admin make verify.release.operator_surface.v1 DB_NAME=sc_demo

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - runtime_registry_alignment_recovered

rollback:
  - "ENV=test ENV_FILE=.env.prod.sim ... docker compose --env-file .env.prod.sim restart odoo"
  - git restore agent_ops/tasks/ITER-2026-04-03-951.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-951.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-951.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-950
