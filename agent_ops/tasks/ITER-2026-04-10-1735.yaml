task_id: ITER-2026-04-10-1735
title: form action truth-source dedup audit

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  落地 FORM-006 第一批：审计 form 契约动作在 buttons/toolbar/action_groups 等区域的重复承载，
  输出主从关系缺口证据。

user_visible_outcome:
  - "明确同一动作在多区域重复承载的冲突清单"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1735.yaml
    - scripts/verify/form_action_dedup_audit.py
    - artifacts/contract/form_action_dedup_v1.json
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1735.md
    - agent_ops/state/task_results/ITER-2026-04-10-1735.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 900

change_rules:
  - audit-first implementation only
  - no business model/schema changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: backend scene-orchestration contract supply governance
  module: form action dedup audit
  module_owner: smart_core
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批只审计动作语义组织，不变更业务事实。
  reason: >
    执行 FORM-006，识别动作在多个 surface 重复出现且缺少主从定义的风险。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1735.yaml"
    - "python3 scripts/verify/form_action_dedup_audit.py --json"

risk:
  level: medium-low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - action dedup audit script and artifact

rollback:
  - "git restore scripts/verify/form_action_dedup_audit.py"

depends_on:
  - ITER-2026-04-10-1734
