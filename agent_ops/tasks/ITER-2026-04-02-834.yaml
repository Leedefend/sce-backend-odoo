task_id: ITER-2026-04-02-834
title: Recover scene_admin smoke under fallback empty export surface
type: backend
status: active
priority: high
mode: implement

goal: >
  Prevent false-negative failure in scene_admin smoke when scenes.export is
  empty in fallback/test-seed runtime while API envelope remains valid.

user_visible_outcome:
  - "场景管理可用性验证在 fallback 运行态不中断"
  - "真实问题与误报问题分离，迭代效率提升"

scope:
  allowed_paths:
    - scripts/verify/scene_admin_smoke.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive_only
  - verify_tooling_only
  - no_business_semantics_change

context:
  branch: codex/next-round
  baseline_sha: 39ab54b
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-833
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: scene admin smoke fallback compatibility
  module_owner: verify scripts
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Usability verification should classify fallback empty export as non-blocking when envelope is valid.
  reason: >
    Remove false-negative blocker and keep focus on user-visible failures.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-834.yaml
    - make verify.e2e.scene_admin

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - scene_admin smoke handles fallback empty export as controlled skip
  - verification report and task result for ITER-2026-04-02-834

rollback:
  - git restore scripts/verify/scene_admin_smoke.py
  - git restore agent_ops/tasks/ITER-2026-04-02-834.yaml
  - git restore agent_ops/reports/2026-04-02/report.ITER-2026-04-02-834.md
  - git restore agent_ops/state/task_results/ITER-2026-04-02-834.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-02-833
