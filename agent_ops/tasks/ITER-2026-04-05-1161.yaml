task_id: ITER-2026-04-05-1161
title: Screen residual non-native capability drift from existing evidence
type: governance
status: active
priority: high
mode: execute

goal: >
  Classify residual non-native capability drift outside the current native
  release guard bundle using existing governance artifacts only.

user_visible_outcome:
  - "残余漂移 screen 分类报告"
  - "下一步 verify 入口明确"

scope:
  allowed_paths:
    - docs/refactor/residual_non_native_capability_drift_screen_v1.md
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - scripts/**
    - security/**
    - record_rules/**
    - addons/**/__manifest__.py

limits:
  max_files: 8
  max_candidates: 20
  max_output_lines: 320
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - governance_screen_only
  - no_new_repo_scan
  - no_runtime_code_change

context:
  branch: codex/next-round
  baseline_sha: da08944
  explicit_user_authorization: true
  input_from: continuous_iteration_1160_next_step
  priority_lane: governance_screen

architecture:
  layer_target: Governance Monitoring
  module: docs/refactor
  module_owner: platform
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: screen stage classifies risk based on existing orchestration governance evidence.
  reason: identify next verify-ready scope without reopening repo-wide scans.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-1161.yaml
    - make verify.architecture.native_capability_projection_release_guard_bundle
    - make verify.controller.boundary.guard

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - residual drift screen report

rollback:
  - git restore docs/refactor/residual_non_native_capability_drift_screen_v1.md
  - git restore agent_ops/tasks/ITER-2026-04-05-1161.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-1161.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-1161.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-1160

