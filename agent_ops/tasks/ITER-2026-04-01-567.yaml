task_id: ITER-2026-04-01-567
title: Decide the bounded follow-up for optimized route-preset visibility
type: frontend
status: active
priority: medium
mode: screen

goal: >
  Decide the bounded follow-up strategy for optimized route-preset visibility
  without reopening repository scan or implementing structural changes yet.

user_visible_outcome:
  - "把 optimized route-preset visibility 收敛成单一结构决策"
  - "明确后续实现边界，避免 route-preset 与当前条件重复"
  - "继续保持短上下文治理节奏"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 3
  max_candidates: 2
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - low-cost structural decision stage only
  - consume only existing scan/screen artifacts
  - do not rescan product files
  - do not implement in this batch

context:
  branch: codex/next-round
  baseline_sha: ed77101
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-566
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true

architecture:
  layer_target: Frontend Layer
  module: optimized route-preset visibility decision
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The fresh structural target has been selected. The next bounded step is to
    decide how optimized toolbar should preserve route-preset visibility
    without reintroducing the duplicate active-condition representation that
    earlier iterations intentionally removed.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-567.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - screen_reads_new_product_files
  - screen_reopens_repo_scan
  - no_bounded_route_preset_strategy_remains

deliverables:
  - bounded_route_preset_visibility_decision
  - governance/report artifacts for ITER-2026-04-01-567

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-567.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-567.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-567.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-566
