task_id: ITER-2026-04-10-1792
title: form notebook tab semantic label convergence

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  收敛 notebook 页签标题，避免机械或泛化 tab 名称，提升项目详情表单页签可理解性。

user_visible_outcome:
  - "页签标题更贴近业务语义（描述/设置/协作系统）"
  - "减少页签 1/2/3 等机械 fallback"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1792.yaml
    - frontend/apps/web/src/components/template/DetailShellLayout.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1792.md
    - agent_ops/state/task_results/ITER-2026-04-10-1792.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend template layer only
  - no backend contract mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: DetailShellLayout tab label normalization
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    在壳标题去噪后继续收敛页签标题，使 tab 标题与 section 语义一致。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1792.yaml"
    - "rg -n \"normalizeTabLabel|resolveSemanticTabLabel\" frontend/apps/web/src/components/template/DetailShellLayout.vue"
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - tab label semantic normalization patch
  - iteration report and task result

rollback:
  - "git restore frontend/apps/web/src/components/template/DetailShellLayout.vue"

depends_on:
  - ITER-2026-04-10-1791
