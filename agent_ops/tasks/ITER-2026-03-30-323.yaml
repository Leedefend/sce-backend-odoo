task_id: ITER-2026-03-30-323
title: Audit missing backend native composition extraction facts before adding optimization composition
type: docs
status: in_progress
priority: high

goal: >
  Produce a concrete backend-native extraction gap inventory that identifies
  which page composition facts already exist in Odoo native sources but are not
  yet fully extracted or normalized into current backend contracts, so the next
  implementation batch can fill native truth first before adding optimization
  composition.

user_visible_outcome:
  - repository contains a temporary backend native extraction gap inventory
  - the document separates native-available facts from current backend extraction output
  - the document defines the first implementation backlog for native composition completeness

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - docs/tmp/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - docs only
  - analysis only
  - no product code changes

context:
  branch: codex/next-round
  baseline_sha: a99fe49

architecture:
  layer_target: Documentation Layer
  module: backend native composition extraction gap inventory
  module_owner: docs/tmp
  kernel_or_scenario: scenario
  reason: >
    Before implementing optimization composition, backend must first close the
    gap between Odoo-native page facts and the currently exposed contract
    surface, so frontend can consume native truth rather than frontend-owned
    composition.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-323.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - task_contract_invalid

deliverables:
  - backend_native_extraction_gap_doc

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-323.yaml
  - git restore docs/tmp/backend_native_composition_extraction_gap_inventory_2026-03-30.md

depends_on:
  - ITER-2026-03-30-322

notes: >
  This batch does not implement code changes. It creates the concrete native
  extraction backlog that should be implemented before any optimization
  composition layer is introduced.
