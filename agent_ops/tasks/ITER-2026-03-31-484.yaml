task_id: ITER-2026-03-31-484
title: Audit cost-count sourcing mismatch in overview to next-action pipeline
type: customer-delivery-flow-acceptance
status: active
priority: high

goal: >
  Audit the cost-count sourcing mismatch found in ITER-2026-03-31-483 and
  determine whether the error comes from overview aggregation, next-action rule
  consumption, or runtime ACL shape.

user_visible_outcome:
  - "明确 cost.count 错配的根因"
  - "把问题收缩到具体服务或字段口径，而不是泛化为 recommendation 不稳定"
  - "为下一张窄实现批次提供可直接落地的修复点"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit-only batch unless a new explicitly authorized implementation task is opened
  - no ACL, record-rule, manifest, or schema changes
  - use repository facts plus runtime audit on sc_odoo
  - stop immediately if a newly found residual would require forbidden-path edits

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom
  explicit_user_authorization: true

architecture:
  layer_target: Customer Delivery Flow Acceptance
  module: overview-to-next-action cost-count mismatch audit
  module_owner: customer delivery permission surface
  kernel_or_scenario: scenario
  reason: >
    ITER-2026-03-31-483 found a recommendation correctness residual on the cost
    maintenance next-action. The next narrow batch is to isolate whether
    overview stats, rule consumption, or ACL behavior caused the mismatch.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-484.yaml
    - repository and runtime audit classify the cost-count mismatch root cause as PASS or PASS_WITH_RISK

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - runtime_audit_cannot_classify_with_confidence
  - newly_found_residual_requires_acl_or_record_rule_change

deliverables:
  - audit report for ITER-2026-03-31-484
  - result snapshot for ITER-2026-03-31-484
  - updated delivery context log

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-484.yaml
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-484.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-484.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-31-483
