task_id: ITER-2026-03-31-436
title: Classify taxonomy cleanup target for smart_construction_bootstrap
type: governance
status: active
priority: medium

goal: >
  Decide the most appropriate taxonomy cleanup target for
  smart_construction_bootstrap based on its actual responsibility and current
  repository module boundaries.

user_visible_outcome:
  - "明确 smart_construction_bootstrap 应该独立迁到平台命名域还是并入现有基座模块"
  - "为后续真实迁移批次确定边界"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - read-only repository audit
  - no code changes
  - no manifest changes

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Module Taxonomy Governance
  module: smart_construction_bootstrap
  module_owner: repository module taxonomy
  kernel_or_scenario: kernel
  reason: >
    The module ownership language has already been classified as platform-level,
    so the next low-risk step is to decide its correct destination before any
    real rename or migration batch is attempted.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-436.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - repository_boundaries_are_insufficient_to_classify_destination

deliverables:
  - implementation/report/result artifacts for ITER-2026-03-31-436

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-436.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-436.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-436.json

depends_on:
  - ITER-2026-03-31-435
