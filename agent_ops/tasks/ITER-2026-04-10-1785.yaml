task_id: ITER-2026-04-10-1785
title: prevent notebook page pruning in layout tree

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 layoutTrees 裁剪策略导致的 page 节点丢失，确保 notebook 页签节点不会在前置 prune 阶段被删除。

user_visible_outcome:
  - "结构投影摘要 tab 数恢复为 > 0"
  - "页面可见 notebook 页签结构"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1785.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1785.md
    - agent_ops/state/task_results/ITER-2026-04-10-1785.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend runtime logic only
  - no backend semantic mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: ContractFormPage layout tree prune
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    用户连续反馈 tab=0，需修复 page 节点在 prune 时被误删的根因。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1785.yaml"
    - "rg -n \"const prune =|section.kind === 'page'|parentKind\" frontend/apps/web/src/pages/ContractFormPage.vue"
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - page-prune protection patch
  - iteration report and task result

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1784
