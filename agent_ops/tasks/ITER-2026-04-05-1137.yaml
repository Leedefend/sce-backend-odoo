task_id: ITER-2026-04-05-1137
title: Repair real-env capability capture connectivity and endpoint probing
type: implementation
status: active
priority: high
mode: execute

goal: >
  Improve capability_payload_real_env_capture runtime resilience by adding
  deterministic endpoint probing and intent-url fallback selection so the
  capture flow can run on available local ports before declaring ENV_UNSTABLE.

user_visible_outcome:
  - "修复实环境抓取脚本连通性判定，优先自动选通可用入口再执行 off/on 抓取"

scope:
  allowed_paths:
    - scripts/verify/capability_payload_real_env_capture.py
    - docs/ops/iterations/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - artifacts/**
  forbidden_paths:
    - addons/**/security/**
    - security/**
    - record_rules/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 10
  max_candidates: 10
  max_output_lines: 260
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verification_script_only
  - no_runtime_code_semantic_change

context:
  branch: codex/next-round
  baseline_sha: da08944
  explicit_user_authorization: true
  input_from: user_approved_1137
  priority_lane: rollout_gating

architecture:
  layer_target: Governance Monitoring
  module: scripts/verify
  module_owner: platform
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: improve evidence capture path reliability without changing business semantics.
  reason: ensure controlled real-env capability capture can reach live runtime endpoints.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-1137.yaml
    - python3 -m py_compile scripts/verify/capability_payload_real_env_capture.py
    - python3 scripts/verify/capability_payload_real_env_capture.py --out-dir artifacts/capability_payload_capture_1137 --allow-env-unstable --request-timeout-sec 5
    - rg -n '"selected_intent_url"|"probe_attempts"|"env_status"' artifacts/capability_payload_capture_1137/latest/capture_report.json
    - make verify.controller.boundary.guard

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - scripts/verify/capability_payload_real_env_capture.py

rollback:
  - git restore scripts/verify/capability_payload_real_env_capture.py
  - git restore agent_ops/tasks/ITER-2026-04-05-1137.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-1137.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-1137.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-1136
