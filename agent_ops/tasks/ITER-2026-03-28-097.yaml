task_id: ITER-2026-03-28-097
title: Normalize native view semantic surface in contract governance
type: governance_task
status: pending
priority: high

goal: Make contract_governance explicitly preserve and normalize parser_contract, view_semantics, and native_view so the native parser semantic milestone remains stable across governance surfaces.
user_visible_outcome:
  - native parser semantic fields remain stable after contract governance normalization

scope:
  allowed_paths:
    - addons/smart_core/utils/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform core contract governance
  module: smart_core contract_governance native view surface
  reason: The parser batch now emits stable canonical semantics; governance must formally preserve that surface instead of leaving it implicit.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-097.yaml
    - python3 -m py_compile addons/smart_core/utils/contract_governance.py addons/smart_core/tests/test_native_view_contract_governance.py
    - python3 addons/smart_core/tests/test_native_view_contract_governance.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-28-097.yaml addons/smart_core/utils/contract_governance.py addons/smart_core/tests/test_native_view_contract_governance.py

depends_on:
  - ITER-2026-03-28-096
notes: "Governance alignment step after ui.contract semantic integration."
