task_id: ITER-2026-04-03-909
title: Add executable host permission-lane diagnostics for explicit gateway
type: backend
status: active
priority: high
mode: implement

goal: >
  Add a focused diagnostics script to classify loopback EPERM behavior before
  host primary-entry smoke execution.

user_visible_outcome:
  - "输出可执行的 permission-lane 诊断产物"
  - "将环境权限阻断与业务契约问题清晰分离"

scope:
  allowed_paths:
    - scripts/verify/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - diagnostics_script_only
  - no_business_model_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-908
  priority_lane: usability_release_proof
  usability_battlefield: cross_stack_verification

architecture:
  layer_target: Product Release Usability Proof
  module: host permission-lane diagnostics execution surface
  module_owner: scene_orchestration_governance
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch adds runtime diagnostics tooling only and does not modify
    business facts.
  reason: >
    Permission-lane blocker is deterministic; a standalone diagnostic script is
    needed to provide stable, actionable environment evidence.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-909.yaml
    - GATEWAY_BASE_URLS=http://localhost:8069 node scripts/verify/host_permission_lane_diag.mjs
    - HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 GATEWAY_BASE_URLS=http://localhost:8069 make verify.portal.project_dashboard_primary_entry_browser_smoke.host

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - host_permission_lane_diag artifact for gateway EPERM classification
  - updated host gate evidence linked with standalone permission diagnostics

rollback:
  - git restore scripts/verify/host_permission_lane_diag.mjs
  - git restore agent_ops/tasks/ITER-2026-04-03-909.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-909.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-909.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-908
