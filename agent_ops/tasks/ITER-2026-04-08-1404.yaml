task_id: ITER-2026-04-08-1404
title: Screen native parity candidates C1-C8 into P0/P1 implementation batches
status: active
type: governance-optimization
priority: high
mode: screen
batch: 1/1

goal: >
  对 scan 阶段 C1~C8 候选做分类筛选，输出结构/交互/验证覆盖缺口的
  P0/P1 优先级与实现批次顺序，不做代码实现。

user_visible_outcome:
  - "得到可直接执行的 tree/form/kanban 对齐分批计划"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-08-1404.yaml
    - agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1404.md
    - agent_ops/state/task_results/ITER-2026-04-08-1404.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*

limits:
  max_files: 4
  max_candidates: 15
  max_output_lines: 160
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - single-stage execution only
  - screen only classifies scan output
  - do not rescan repository
  - no implementation

context:
  branch: codex/next-round
  baseline_sha: 9109f99
  explicit_user_authorization: true

architecture:
  layer_target: Frontend parity governance screen
  module: native parity (tree/form/kanban)
  module_owner: frontend contract consumer + verify surface
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本阶段仅完成对齐任务编排分类，不改业务事实层。
  reason: >
    输出可执行的 P0/P1 批次顺序，支撑后续 verify 实施链。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-08-1404.yaml"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - limit_exceeded
  - forbidden_path_touched
  - stage_boundary_broken


deliverables:
  - screen-classified P0/P1 plan for native parity alignment

depends_on:
  - ITER-2026-04-08-1403

rollback:
  - "git restore agent_ops/tasks/ITER-2026-04-08-1404.yaml"
  - "git restore agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1404.md"
  - "git restore agent_ops/state/task_results/ITER-2026-04-08-1404.json"
  - "git restore docs/ops/iterations/delivery_context_switch_log_v1.md"
