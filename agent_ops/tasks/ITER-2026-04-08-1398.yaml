task_id: ITER-2026-04-08-1398
title: Verify and fix rights resolver inconsistency for permissions.effective.rights consumption
status: active
type: governance-optimization
priority: high
mode: verify
batch: 1/1

goal: >
  修复前端权限消费双口径（contractActionRuntime vs contractRecordRuntime）导致的
  read/write/create/unlink 判定不一致，确保 permissions.effective.rights 可配置并生效。

user_visible_outcome:
  - "同一 contract 在页面与记录态的权限判定一致"
  - "去除 effective 全 false 被错误放开的行为"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-08-1398.yaml
    - frontend/apps/web/src/app/contractRecordRuntime.ts
    - agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1398.md
    - agent_ops/state/task_results/ITER-2026-04-08-1398.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*

limits:
  max_files: 5
  max_output_lines: 220
  forbid_repo_wide_scan: true

change_rules:
  - verify stage executes declared checks only
  - no acl/financial semantics change
  - frontend remains generic consumer of backend permission facts

context:
  branch: codex/next-round
  baseline_sha: 3466cad
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract consumer semantics
  module: contractRecordRuntime rights resolver
  module_owner: frontend contract runtime
  kernel_or_scenario: scenario
  backend_sub_layer_target: business_fact
  backend_sub_layer_reason: >
    权限事实继续由后端 effective.rights 供给，本批仅修复前端消费偏差，不改业务事实。
  reason: >
    screen 阶段已确认高优冲突在前端双解析逻辑；本批执行最小修复对齐统一口径。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-08-1398.yaml"
    - "CONTRACT_PREFLIGHT_SKIP_DOCS=1 CONTRACT_PREFLIGHT_SKIP_GROUPED_GOV_BUNDLE=1 CONTRACT_PREFLIGHT_SKIP_SCENE_CAPABILITY_GUARD=1 CONTRACT_PREFLIGHT_STRICT_VIEW_TYPES=0 BASELINE_FREEZE_ENFORCE=0 make verify.contract.preflight DB_NAME=sc_demo E2E_LOGIN=admin E2E_PASSWORD=admin"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_command_failed
  - forbidden_path_touched


deliverables:
  - unified rights resolver behavior in record runtime
  - verification evidence under contract preflight

depends_on:
  - ITER-2026-04-08-1397

rollback:
  - "git restore frontend/apps/web/src/app/contractRecordRuntime.ts"
  - "git restore agent_ops/tasks/ITER-2026-04-08-1398.yaml"
  - "git restore agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1398.md"
  - "git restore agent_ops/state/task_results/ITER-2026-04-08-1398.json"
  - "git restore docs/ops/iterations/delivery_context_switch_log_v1.md"
