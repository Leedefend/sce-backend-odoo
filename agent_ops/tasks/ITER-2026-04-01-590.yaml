task_id: ITER-2026-04-01-590
title: Verify HUD key-signal ordering on the record-view continuity line
type: frontend
status: active
priority: high
mode: verify

goal: >
  Verify that the HUD key-signal ordering fix preserves strict typing and the
  dedicated recordview HUD smoke while improving continuity emphasis.

user_visible_outcome:
  - "确认 HUD 关键信号排序修复通过专用门禁"
  - "保持 HUD 字段与值不变"
  - "为下一张 continuity 任务提供干净状态"

scope:
  allowed_paths:
    - frontend/apps/web/src/app/action_runtime/useActionViewHudEntriesRuntime.ts
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - verify stage only
  - do not make new product edits in this batch
  - run only declared validation commands
  - keep scope on HUD ordering

context:
  branch: codex/next-round
  baseline_sha: 59536d3
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-589
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: record-view HUD verification
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The ordering-only HUD fix should improve emphasis without changing data
    semantics. Verification confirms the dedicated HUD smoke and strict typing
    remain green.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-590.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.recordview_hud_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - dedicated_hud_smoke_still_fails

deliverables:
  - verification_result_for_record_view_hud_ordering_fix
  - governance/report artifacts for ITER-2026-04-01-590

rollback:
  - git restore frontend/apps/web/src/app/action_runtime/useActionViewHudEntriesRuntime.ts
  - git restore agent_ops/tasks/ITER-2026-04-01-590.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-590.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-590.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-589
