task_id: ITER-2026-03-30-331
title: Implement backend optimization composition batch 1 on top of native truths
type: code
status: in_progress
priority: high

goal: >
  Implement the first backend optimization composition batch by adding additive
  optimization_composition truth for toolbar hierarchy and filter
  prioritization on top of completed native truths, without touching frontend
  or introducing batch action and guidance composition yet.

user_visible_outcome:
  - scene-ready payload includes additive optimization_composition contract
  - optimization_composition includes toolbar_sections, active_conditions, high_frequency_filters, and advanced_filters
  - system.init compact scene rows preserve optimization_composition

scope:
  allowed_paths:
    - addons/smart_core/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
  forbidden_paths:
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/__manifest__.py

change_rules:
  - backend only
  - additive contract change only
  - no frontend changes
  - no batch_actions or guidance implementation in this batch

context:
  branch: codex/next-round
  baseline_sha: 03bd2ef

architecture:
  layer_target: Platform Layer
  module: smart_core optimization composition batch 1
  module_owner: addons/smart_core
  kernel_or_scenario: kernel
  reason: >
    Native truths now cover search, list, batch capability, and form facts.
    Backend must add the smallest optimization composition layer for toolbar
    hierarchy and filter prioritization so frontend can stop owning those
    structures.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-331.yaml
    - python3 addons/smart_core/tests/test_scene_ready_contract_builder_semantic_consumption.py
    - python3 addons/smart_core/tests/test_system_init_payload_builder_semantics.py
    - make verify.smart_core

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_command_failed
  - contract_structure_breaks_existing_scene_ready_keys
  - optimization_composition_expands_beyond_batch1_scope

deliverables:
  - backend optimization_composition batch 1 implementation
  - updated tests covering optimization_composition preservation

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-331.yaml
  - git restore addons/smart_core/core/scene_ready_contract_builder.py
  - git restore addons/smart_core/core/system_init_payload_builder.py
  - git restore addons/smart_core/tests/test_scene_ready_contract_builder_semantic_consumption.py
  - git restore addons/smart_core/tests/test_system_init_payload_builder_semantics.py

depends_on:
  - ITER-2026-03-30-330

notes: >
  This batch only implements toolbar_sections, active_conditions,
  high_frequency_filters, and advanced_filters. It must not implement
  batch_actions or guidance.
