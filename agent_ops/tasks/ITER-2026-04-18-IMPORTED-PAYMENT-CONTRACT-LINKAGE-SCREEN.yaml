task_id: ITER-2026-04-18-IMPORTED-PAYMENT-CONTRACT-LINKAGE-SCREEN
title: Screen imported payment deterministic contract linkage gap
type: governance-screen
status: pass
priority: high
mode: screen

goal: >
  Classify imported payment requests that still lack contract_id and determine
  whether the gap can be reduced by deterministic project+partner+direction
  evidence, without writing payment records.

user_visible_outcome:
  - "先把剩余付款合同链接缺口分成可确定回放与不可确定歧义两类，再决定是否进入写入批次。"

scope:
  allowed_paths:
    - docs/ops/business_continuity/imported_payment_contract_linkage_screen_v1.md
    - docs/ops/iterations/delivery_context_switch_log_v1.md
    - agent_ops/tasks/ITER-2026-04-18-IMPORTED-PAYMENT-CONTRACT-LINKAGE-SCREEN.yaml
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**
    - scripts/**
    - security/**
    - record_rules/**
    - ir.model.access.csv
    - __manifest__.py

context:
  branch: codex/next-round
  baseline_sha: 0505b05
  prior_task: ITER-2026-04-18-IMPORTED-BUSINESS-CONTINUITY-GUARD

architecture:
  layer_target: Business Fact Screening
  module: imported payment contract linkage facts
  module_owner: construction industry business continuity
  kernel_or_scenario: scenario
  backend_sub_layer_target: business-fact layer
  backend_sub_layer_reason: >
    Missing contract_id on imported payment requests is a business-fact
    completeness question. This screen only classifies deterministic evidence
    and does not emit frontend or scene orchestration semantics.
  reason: >
    The imported continuity guard passed but reported remaining payment
    requests without deterministic contract linkage. A read-only screen is
    required before any replay/write task.

change_rules:
  - read-only database screening only
  - do not write payment, contract, settlement, account, ACL, manifest, frontend, or migration data
  - classify exact-one deterministic candidates separately from no-candidate and multi-candidate records
  - do not infer linkage from amount, name similarity, or free text

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-18-IMPORTED-PAYMENT-CONTRACT-LINKAGE-SCREEN.yaml
    - read-only Odoo shell classification against sc_demo

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - database_write_attempted
  - deterministic_rule_unclear

deliverables:
  - screen document with counts and next write/no-write recommendation

verification_result:
  status: PASS
  commands:
    - command: python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-18-IMPORTED-PAYMENT-CONTRACT-LINKAGE-SCREEN.yaml
      result: PASS
    - command: read-only Odoo shell classification against sc_demo
      result: PASS
      summary: >
        Missing contract_id payment requests: 17994; exact-one deterministic
        candidates by project+partner+direction: 0; no candidate: 11248;
        multiple candidates: 6746.

rollback:
  - git restore docs/ops/business_continuity/imported_payment_contract_linkage_screen_v1.md docs/ops/iterations/delivery_context_switch_log_v1.md agent_ops/tasks/ITER-2026-04-18-IMPORTED-PAYMENT-CONTRACT-LINKAGE-SCREEN.yaml

depends_on:
  - ITER-2026-04-18-IMPORTED-BUSINESS-CONTINUITY-GUARD
