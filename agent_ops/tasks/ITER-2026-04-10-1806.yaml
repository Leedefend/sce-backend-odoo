task_id: ITER-2026-04-10-1806
title: project form user governance de-prune tabs

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 project.form 在 user surface 下被治理层过度裁剪导致业务页签丢失的问题，保留原生布局结构完整性。

user_visible_outcome:
  - "project form 业务页签在 user surface 下可见，不再只剩描述/设置/协作"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1806.yaml
    - addons/smart_core/utils/contract_governance.py
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1806.md
    - agent_ops/state/task_results/ITER-2026-04-10-1806.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - governance policy only
  - keep parser and frontend unchanged
  - additive and reversible

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend contract semantic layer
  module: project form governance policy
  module_owner: addons/smart_core
  kernel_or_scenario: kernel
  reason: >
    parser/runtime 已能产出完整 notebook/page，但 user governance 的字段裁剪会连带裁掉多个业务 page，
    导致前端看到 tab 缺失。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1806.yaml"
    - 'rg -n "_govern_project_form_contract_for_user|_filter_project_form_layout" addons/smart_core/utils/contract_governance.py'
    - "make restart"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - project form user governance no longer prunes business tabs
  - iteration report and state

rollback:
  - "git restore addons/smart_core/utils/contract_governance.py"

depends_on:
  - ITER-2026-04-10-1805
