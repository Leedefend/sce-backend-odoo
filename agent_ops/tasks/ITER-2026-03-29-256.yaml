task_id: ITER-2026-03-29-256
title: Audit AppViewConfig lifecycle boundary concentration
type: audit_task
status: completed
priority: high

goal: Re-audit AppViewConfig after the recent backend cleanup line so the exact lifecycle concentration points, safe extraction seams, and stop-vs-continue decision can be documented without changing parse behavior.
user_visible_outcome:
  - a precise AppViewConfig lifecycle responsibility map
  - a list of safe extraction seams versus risky areas
  - a clearer basis for deciding whether the backend cleanup line should continue or stop

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
    - docs/architecture/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - audit_only
  - no_product_code_change
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform layer audit
  module: AppViewConfig parse and projection lifecycle
  reason: The refreshed backend chain now shows AppViewConfig as the densest remaining lifecycle concentration point, so the next safe move is to document its exact seams before deciding on any further refactor.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-256.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - audit_scope_uncertain

deliverables:
  - app_view_config_lifecycle_audit
  - safe_extraction_seams_matrix
  - stop_or_continue_recommendation

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-256.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-29-255

notes: "Audit only. Do not modify AppViewConfig or parse behavior in this batch."
