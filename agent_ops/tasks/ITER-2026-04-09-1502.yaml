task_id: ITER-2026-04-09-1502
title: batch5 verify menu fact visibility by role sample
type: governance-optimization
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  增加角色样本可见性校验脚本，验证管理/项目/财务角色菜单事实可见结果与事实字段完整性。

user_visible_outcome:
  - "不同角色菜单事实可见性可审计，明显暴露风险可识别"

scope:
  allowed_paths:
    - scripts/verify/menu_fact_visibility_sample_verify.py
    - artifacts/menu/menu_fact_visibility_sample_v1.json
    - agent_ops/tasks/ITER-2026-04-09-1502.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1502.md
    - agent_ops/state/task_results/ITER-2026-04-09-1502.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - frontend/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - record_rules/**

limits:
  max_files: 6
  max_output_lines: 320

change_rules:
  - verify facts-only visibility, no ACL mutation
  - do not enter record-rule deep business authorization

context:
  branch: codex/next-round
  baseline_sha: 9109f99
  explicit_user_authorization: true

architecture:
  layer_target: Platform fact layer verification
  module: Menu fact visibility sample verifier
  module_owner: scripts verify
  kernel_or_scenario: kernel
  backend_sub_layer_target: business_fact
  backend_sub_layer_reason: >
    本批只验证事实可见性结果，不修改业务或解释层。
  reason: >
    按 Batch5 要求，用管理/项目/财务三角色校验菜单事实可见性与字段一致性。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1502.yaml"
    - "python3 scripts/verify/menu_fact_visibility_sample_verify.py"
    - 'python3 -c "import json; from pathlib import Path; data=json.loads(Path(\"artifacts/menu/menu_fact_visibility_sample_v1.json\").read_text(encoding=\"utf-8\")); print(\"roles\", sorted([r.get(\"role\") for r in data.get(\"rows\", [])]))"'

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - role visibility sample verify script
  - role visibility sample artifact

rollback:
  - git restore scripts/verify/menu_fact_visibility_sample_verify.py
  - git restore artifacts/menu/menu_fact_visibility_sample_v1.json

depends_on:
  - ITER-2026-04-09-1501
