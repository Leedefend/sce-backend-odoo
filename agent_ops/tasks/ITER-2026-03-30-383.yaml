task_id: ITER-2026-03-30-383
title: Audit custom frontend supplement viability for workbench
type: audit
status: in_progress
priority: high

goal: >
  Determine whether the custom frontend supplement lane for `工作台` already has
  a viable minimal slice in the current codebase, using the native portal
  dashboard anchor as source truth.

user_visible_outcome:
  - "workbench supplement lane gets an explicit status: already viable or still missing"
  - "next supplement execution order can advance with less ambiguity"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; do not change frontend or backend code in this batch
  - use the native portal dashboard anchor as the publication source of truth
  - judge viability by target mapping, route normalization, and minimal usable surface

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: workbench supplement viability
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    The next active lane starts with `工作台`, but current evidence suggests the
    custom frontend may already contain a viable minimal landing surface that
    simply needs formal confirmation.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-383.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - supplement_mapping_scope_unclear

deliverables:
  - explicit viability decision for the workbench custom supplement lane
  - report and task result artifacts for ITER-2026-03-30-383
  - delivery log checkpoint for the next supplement batch

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-383.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-383.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-383.json

depends_on:
  - ITER-2026-03-30-382
