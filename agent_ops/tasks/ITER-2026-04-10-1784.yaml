task_id: ITER-2026-04-10-1784
title: preserve notebook tab projection in form runtime

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  针对“shell>0 但 tab=0”问题，修复 notebook/page 投影裁剪逻辑，
  让页签结构在前端稳定可见。

user_visible_outcome:
  - "结构投影摘要中的 tab 数 > 0"
  - "页面可见 notebook 页签标签"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1784.yaml
    - frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1784.md
    - agent_ops/state/task_results/ITER-2026-04-10-1784.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend runtime projection only
  - no backend semantic mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: detail layout notebook projection
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    用户给出摘要明确 tab=0，需要修复 notebook tab 生成阶段的过度过滤。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1784.yaml"
    - "rg -n \"buildNotebookShell|tabs =|tab.sections.length\" frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts"
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - notebook tab projection fix patch
  - iteration report and task result

rollback:
  - "git restore frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts"

depends_on:
  - ITER-2026-04-10-1783
