task_id: ITER-2026-03-28-105
title: Make runtime scene attach path explicitly consume scene semantic surface
type: governance_task
status: pending
priority: high

goal: Extend attach_release_surface_scene_contract so runtime payloads explicitly consume scene-level parser semantics instead of only attaching scene_contract_standard_v1 sidecar data.
user_visible_outcome:
  - runtime payloads now preserve released scene semantic surface in semantic_runtime and released_scene_semantic_surface

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration contract consumption
  module: smart_core scene_contract_builder attach path
  reason: After scene-level contracts consume parser semantics, the runtime attach path is the next backend consumer that must carry that semantic surface back into runtime payloads.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-105.yaml
    - python3 -m py_compile addons/smart_core/core/released_scene_semantic_surface_bridge.py addons/smart_core/core/scene_contract_builder.py addons/smart_core/tests/test_released_scene_semantic_surface_bridge.py addons/smart_core/tests/test_scene_contract_attach_semantics.py
    - python3 addons/smart_core/tests/test_released_scene_semantic_surface_bridge.py
    - python3 addons/smart_core/tests/test_scene_contract_attach_semantics.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-28-105.yaml addons/smart_core/core/released_scene_semantic_surface_bridge.py addons/smart_core/core/scene_contract_builder.py addons/smart_core/tests/test_released_scene_semantic_surface_bridge.py addons/smart_core/tests/test_scene_contract_attach_semantics.py

depends_on:
  - ITER-2026-03-28-104
notes: "Batch-3 sixth implementation: runtime scene attach path consumes released scene semantic surfaces."
