task_id: ITER-2026-03-31-432
title: Audit whether res.users ownership should remain platform-admin-only after enterprise maintenance split
type: governance
status: active
priority: medium

goal: >
  Audit the current repository facts around res.users ownership after company,
  department, and post maintenance were shifted to the business-admin path, and
  determine whether user maintenance should remain platform-admin-only or
  whether a separate governed ownership line is justified.

user_visible_outcome:
  - "用户设置 ownership 边界是否继续保留在平台管理员有明确结论"
  - "如果需要下放，明确需要单独治理的原因"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - read-only repository audit
  - no code changes
  - no ACL changes

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Customer Delivery Governance
  module: smart_enterprise_base / base user ownership
  module_owner: enterprise maintenance authority split
  kernel_or_scenario: scenario
  reason: >
    Company, department, and post ownership are now split cleanly, so the next
    low-risk step is to classify whether res.users ownership should remain on
    the platform-admin path.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-432.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - repository_facts_are_insufficient_to_classify_res_users_ownership
  - audit_discovers_new_high_risk_authority_gap

deliverables:
  - implementation/report/result artifacts for ITER-2026-03-31-432

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-432.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-432.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-432.json

depends_on:
  - ITER-2026-03-31-431
