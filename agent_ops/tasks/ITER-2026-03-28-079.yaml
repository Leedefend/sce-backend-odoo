task_id: ITER-2026-03-28-079
title: Define platform core view parsing batch-2 plan
type: governance_task
status: pending
priority: high

goal: Freeze the platform-core batch-2 plan for Odoo native view to structured contract parsing so the next iterations target substantive kernel capability instead of helper residue cleanup.
user_visible_outcome:
  - platform core view parsing gap is explicit
  - batch-2 queue for substantive parser optimization is ready

scope:
  allowed_paths:
    - docs/architecture/**
    - docs/ops/**
    - agent_ops/tasks/**
    - agent_ops/queue/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform kernel convergence batch-2
  module: smart_core native view parsing
  reason: Switch from low-risk helper cleanup to substantive kernel capability improvement on Odoo view parsing and structured contract output.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-079.yaml
    - rg -n "Current Gaps|Batch-2 Queue|Step 1|Step 2|Step 3" docs/architecture/odoo_view_structured_parse_gap_and_batch2_plan_v1.md
    - rg -n "platform-core-view-parse-batch-2" agent_ops/queue/platform_core_view_parse_batch_2.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - docs
  - report

rollback:
  - git restore docs/architecture/odoo_view_structured_parse_gap_and_batch2_plan_v1.md agent_ops/queue/platform_core_view_parse_batch_2.yaml agent_ops/tasks/ITER-2026-03-28-079.yaml

depends_on:
  - ITER-2026-03-28-078
notes: "Batch switch artifact: stop residue cleanup and enter platform core view parsing capability batch."
