task_id: ITER-2026-03-29-179
title: Expose bridge alignment in consumer-facing smart_scene diagnostics
type: governance_task
status: pending
priority: high

goal: Make smart_scene contract builder expose bridge-level consumer runtime alignment through stable consumer-facing diagnostics after parser bridge projection.
user_visible_outcome:
  - consumer_semantics.runtime now carries bridge_alignment derived from bridge assertions
  - consumer_runtime alias is refreshed after parser bridge and stays aligned across top-level diagnostics and scene_contract_v1 diagnostics

scope:
  allowed_paths:
    - addons/smart_scene/core/**
    - addons/smart_scene/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: scene layer
  module: smart_scene scene_contract_builder
  reason: parser bridge now emits consumer runtime assertions, but consumer-facing diagnostics need a stable post-bridge export surface for downstream product consumption.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-179.yaml
    - python3 -m py_compile addons/smart_scene/core/scene_contract_builder.py addons/smart_scene/tests/test_scene_contract_builder.py
    - python3 addons/smart_scene/tests/test_scene_contract_builder.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-179.yaml addons/smart_scene/core/scene_contract_builder.py addons/smart_scene/tests/test_scene_contract_builder.py

depends_on:
  - ITER-2026-03-29-178

notes: "Productization mainline: refresh export-side consumer diagnostics after parser bridge."
