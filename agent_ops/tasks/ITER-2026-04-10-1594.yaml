task_id: ITER-2026-04-10-1594
title: backend refactor v2 app availability and clickability semantics
type: governance
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  为 v2 app 模块输出补齐 is_clickable、availability_status、reason_code 语义，完成导航节点最小可执行契约闭环。

user_visible_outcome:
  - "v2 app 节点可直接表达可点状态与不可用原因"

scope:
  allowed_paths:
    - addons/smart_core/v2/**
    - scripts/verify/v2_rebuild_audit.py
    - docs/architecture/backend_core_refactor_blueprint_v1.md
    - agent_ops/tasks/ITER-2026-04-10-1594.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1594.md
    - agent_ops/state/task_results/ITER-2026-04-10-1594.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 14
  max_output_lines: 620

change_rules:
  - v2 independent rebuild only
  - additive contract semantics
  - no legacy runtime cutover

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Platform kernel independent rebuild layer
  module: smart_core v2 app availability semantics
  module_owner: smart_core
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批补齐导航语义组织字段，不涉及业务事实层。
  reason: >
    连续重构目标下，闭合前端纯消费所需最小导航状态契约。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1594.yaml"
    - "python3 -m py_compile addons/smart_core/v2/modules/app/services/catalog_service.py addons/smart_core/v2/modules/app/builders/catalog_builder.py"
    - "python3 scripts/verify/v2_rebuild_audit.py --json"
    - 'rg -n "is_clickable|availability_status|reason_code" addons/smart_core/v2/modules/app'
    - "rg -n \"app availability and clickability batch\" docs/architecture/backend_core_refactor_blueprint_v1.md"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - v2 app availability semantics

rollback:
  - git restore addons/smart_core/v2 docs/architecture/backend_core_refactor_blueprint_v1.md

depends_on:
  - ITER-2026-04-10-1593
