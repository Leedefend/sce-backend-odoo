task_id: ITER-2026-04-05-985
title: Scan platform entry occupation facts for smart_construction_core boundary Phase A-3
type: governance
status: active
priority: high
mode: scan

goal: >
  Collect fact-only evidence for platform entry occupation around critical API
  paths (/api/login, /api/session/get, /api/menu/tree, /api/capabilities/*,
  /api/scenes/*, /api/ops/*, /api/packs/*), including definition and references.

user_visible_outcome:
  - "形成平台主入口占用证据清单（定义源/引用点/潜在双源）"

scope:
  allowed_paths:
    - addons/smart_construction_core/**
    - addons/smart_core/**
    - addons/smart_construction_scene/**
    - frontend/**
    - docs/audit/boundary/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 14
  max_candidates: 80
  max_output_lines: 260
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: false
  allowed_roles:
    - executor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - single-stage execution only
  - scan_stage_only
  - fact_collection_without_solution_design
  - no_cross_stage_reasoning

context:
  branch: codex/next-round
  baseline_sha: f4102e4
  explicit_user_authorization: true
  input_from: ITER-2026-04-05-984
  priority_lane: smart_construction_core_boundary_phase_a_platform_entry_scan

architecture:
  layer_target: Agent Governance Boundary Scan
  module: platform entry occupation evidence
  module_owner: governance_audit
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch inventories entry ownership and dependency evidence only, with
    no runtime semantic changes.
  reason: >
    User requires explicit evidence for whether industry module occupies
    platform-critical API entry surfaces.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-985.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched
  - stage_boundary_broken

deliverables:
  - docs/audit/boundary/platform_entry_occupation.md

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-05-985.yaml
  - git restore docs/audit/boundary/platform_entry_occupation.md
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-985.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-985.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-984
