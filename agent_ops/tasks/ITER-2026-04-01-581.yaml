task_id: ITER-2026-04-01-581
title: Clarify record-view HUD title and context hint without changing HUD data
type: frontend
status: active
priority: high

goal: >
  Make the record-view HUD easier to understand by replacing the generic
  fallback title and adding a concise context hint for detail-view continuity.

user_visible_outcome:
  - "详情态 HUD 不再显示泛化英文标题"
  - "用户能更快理解 HUD 展示的是记录上下文"
  - "不改变 HUD 字段内容或调试数据来源"

scope:
  allowed_paths:
    - frontend/apps/web/src/views/ActionView.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - bounded P1 usability implementation
  - no backend contract change
  - do not change HUD data semantics

context:
  branch: codex/next-round
  baseline_sha: e430a7e
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-580
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: record-view HUD continuity
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    After entering a record from the native list, HUD continuity should be
    understandable without forcing the user to interpret a generic English
    debug label. This batch improves that readability only.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-581.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.v0_5.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - trusted_container_verify_failed
  - hud_readability_fix_requires_hud_schema_change

deliverables:
  - bounded_record_view_hud_readability_fix
  - governance/report artifacts for ITER-2026-04-01-581

rollback:
  - git restore frontend/apps/web/src/views/ActionView.vue
  - git restore agent_ops/tasks/ITER-2026-04-01-581.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-581.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-581.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-580
