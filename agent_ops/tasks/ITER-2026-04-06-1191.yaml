task_id: ITER-2026-04-06-1191
title: Screen minimal install-time seed materialization scope for native business-fact baseline
type: governance-optimization
status: completed
priority: high
mode: screen

goal: >
  Define a minimal install-time seed materialization scope for native business-fact
  baseline, with explicit high-risk boundaries and no implementation changes.

user_visible_outcome:
  - "seed 物化最小范围清单"
  - "manifest/data-load 高风险边界与验证门禁"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-06-1191.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/governance/native_seed_materialization_scope_v1.md
    - docs/audit/native/native_foundation_execution_sequence_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 140
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen only
  - no manifest/data-load implementation
  - define scope and gate only

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: post_1190_seed_materialization_screen

architecture:
  layer_target: Governance Monitoring
  module: seed materialization scope
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: business_fact
  backend_sub_layer_reason: >
    Seed materialization scope concerns install-time business facts and ownership truth.
  reason: >
    Continue sequential follow-up by preparing minimal seed materialization plan
    before any high-risk manifest/data-load execution batch.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-06-1191.yaml

risk:
  level: medium
  manual_approval_required: true

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - minimal seed materialization scope draft with gate criteria

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-06-1191.yaml
  - git restore docs/ops/governance/native_seed_materialization_scope_v1.md
  - git restore docs/audit/native/native_foundation_execution_sequence_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-06-1190
