task_id: ITER-2026-04-07-1225
title: Environment repair lane batch12 app-entry RemoteDisconnected evidence
type: governance
status: active
priority: high
mode: verify

goal: >
  Capture targeted app-entry diagnostics for `/api/scenes/my` while dev odoo is
  running, including request-triggered log window and probe outputs.

user_visible_outcome:
  - "RemoteDisconnected 的入口证据更细化"
  - "下一步可进入应用入口修复"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1225.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_environment_repair_lane_batch12_app_entry_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 260
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - evidence-only batch
  - no source code mutation

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_environment_repair_batch12

architecture:
  layer_target: Governance Monitoring
  module: app-entry diagnostics evidence
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch inspects runtime request/log relation around legacy endpoint entry.
  reason: >
    Determine whether request reaches app layer before connection close.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1225.yaml
    - make verify.scene.legacy_auth.runtime_probe
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime environment repair lane batch12 app-entry evidence

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1225.yaml
  - git restore docs/audit/native/native_runtime_environment_repair_lane_batch12_app_entry_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1224
