task_id: ITER-2026-04-09-1446
title: verify remaining write-intent boundary with pm finance sampling
status: active
type: governance
priority: high
mode: verify
batch: 1/1

goal: >
  对 execute_button、file_upload、api.data.write 三条写链路进行 PM/finance 双角色采样，
  判定是否仍存在“解释层/门禁层越界导致事实口径不一致”。

user_visible_outcome:
  - "给出剩余越界问题清单（若有）与根除结论"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-09-1446.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1446.md
    - agent_ops/state/task_results/ITER-2026-04-09-1446.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
    - artifacts/codex/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py

limits:
  max_files: 5
  max_output_lines: 160

change_rules:
  - verify stage only
  - no implementation changes
  - execute declared checks only

context:
  branch: codex/next-round
  baseline_sha: 9109f99
  explicit_user_authorization: true

architecture:
  layer_target: Boundary closure verification
  module: smart_core write-intent permission surface
  module_owner: smart_core backend contract stack
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批仅执行验证，不引入新的业务事实或编排语义。
  reason: >
    用户要求把越界“定死消除”，需要对关键写意图做角色采样闭环验证。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1446.yaml"
    - "make verify.portal.edit_tx_smoke.container DB_NAME=sc_demo E2E_LOGIN=sc_fx_pm E2E_PASSWORD=prod_like"
    - "make verify.portal.execute_button_smoke.container DB_NAME=sc_demo E2E_LOGIN=sc_fx_pm E2E_PASSWORD=prod_like"
    - "make verify.portal.file_upload_smoke.container DB_NAME=sc_demo E2E_LOGIN=sc_fx_pm E2E_PASSWORD=prod_like"
    - "make verify.portal.edit_tx_smoke.container DB_NAME=sc_demo E2E_LOGIN=sc_fx_fin E2E_PASSWORD=prod_like"
    - "make verify.portal.execute_button_smoke.container DB_NAME=sc_demo E2E_LOGIN=sc_fx_fin E2E_PASSWORD=prod_like"
    - "make verify.portal.file_upload_smoke.container DB_NAME=sc_demo E2E_LOGIN=sc_fx_fin E2E_PASSWORD=prod_like"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_command_failed
  - forbidden_path_touched
  - unverifiable_runtime

deliverables:
  - role-sampled write-intent verification matrix

depends_on:
  - ITER-2026-04-09-1445

rollback:
  - "git restore agent_ops/tasks/ITER-2026-04-09-1446.yaml"
  - "git restore agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1446.md"
  - "git restore agent_ops/state/task_results/ITER-2026-04-09-1446.json"
  - "git restore docs/ops/iterations/delivery_context_switch_log_v1.md"
