task_id: ITER-2026-03-30-318
title: Audit frontend-owned page composition on the list surface
type: docs
status: in_progress
priority: high

goal: >
  Record the first-step audit of list-page structures and actions that are still
  decided by the frontend instead of being explicitly driven by backend contract
  metadata.

user_visible_outcome:
  - repository contains a temporary audit of frontend-owned page composition
  - the audit identifies concrete frontend-only decisions with file evidence
  - the audit provides the clean handoff into contract-gap analysis

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - docs/tmp/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - docs only
  - audit only
  - no product code changes

context:
  branch: codex/next-round
  baseline_sha: 6e26cf1

architecture:
  layer_target: Documentation Layer
  module: frontend-owned list composition audit
  module_owner: docs/tmp
  kernel_or_scenario: scenario
  reason: >
    Before changing contracts or frontend structure, we need a concrete audit of
    which list-page structures and actions are still being composed directly in
    the frontend without explicit backend-driven page composition metadata.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-318.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - task_contract_invalid

deliverables:
  - frontend_owned_list_audit_doc

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-318.yaml
  - git restore docs/tmp/frontend_owned_list_composition_audit_2026-03-30.md

depends_on:
  - ITER-2026-03-30-317

notes: >
  This batch is the first step only: audit frontend-owned composition. Contract
  gap analysis and the improvement plan follow in later batches.
