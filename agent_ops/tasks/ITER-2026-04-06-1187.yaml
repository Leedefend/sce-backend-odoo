task_id: ITER-2026-04-06-1187
title: Policy-alignment for direct business-fact ACL/rule remediation under explicit user authorization
type: governance-optimization
status: completed
priority: high
mode: screen

goal: >
  Convert explicit user authorization into an executable high-risk lane by
  defining a compliant exception-matching path for ACL and record-rule changes.

user_visible_outcome:
  - "形成可实施的高风险任务入口"
  - "明确是否可直接改 ACL/record-rule 或需新增例外条款"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-06-1187.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_foundation_execution_sequence_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 6
  max_candidates: 8
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen only
  - no ACL/record-rule implementation
  - classify against existing narrow exceptions first

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: user_direct_execute_business_fact_risk_controllable

architecture:
  layer_target: Governance Monitoring
  module: high-risk policy alignment
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This stage organizes authorization semantics and implementation path only.
  reason: >
    User requested immediate implementation, but current target touches governed
    ACL/record-rule files that require explicit exception-path alignment.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-06-1187.yaml

risk:
  level: medium
  manual_approval_required: true

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched
  - no_matching_exception_path

deliverables:
  - exception-aligned implementation entry or explicit block reason

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-06-1187.yaml

depends_on:
  - ITER-2026-04-06-1186
