task_id: ITER-2026-04-07-1212
title: Runtime repair lane batch2 live probe re-validation
type: governance
status: completed
priority: high
mode: verify

goal: >
  Re-run escalated live strict probe after unreachable-classification hardening
  and capture whether `/api/scenes/my` now yields 401/403 or remains transport-failed.

user_visible_outcome:
  - "live re-probe 结果可审计"
  - "为下一步 runtime 处理提供明确分叉"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1212.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_repair_lane_batch2_live_probe_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 220
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - evidence-only batch
  - no source code mutation

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_repair_lane_batch2

architecture:
  layer_target: Governance Monitoring
  module: runtime live re-probe evidence
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch observes runtime behavior post-helper hardening.
  reason: >
    Decide whether runtime availability lane should escalate into environment
    remediation or can close with stable 401/403 evidence.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1212.yaml
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime repair lane batch2 live probe evidence

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1212.yaml
  - git restore docs/audit/native/native_runtime_repair_lane_batch2_live_probe_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1211
