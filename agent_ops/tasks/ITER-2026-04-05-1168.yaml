task_id: ITER-2026-04-05-1168
title: Frontend intent channel compliance for capability matrix API
type: implementation
status: active
priority: high
mode: execute

goal: >
  Remediate frontend intent-channel guard by removing forbidden direct
  /api/contract/capability_matrix call and routing capability matrix fetch
  through /api/v1/intent client path.

user_visible_outcome:
  - "verify.frontend.intent_channel.guard 通过"
  - "ci.preflight.contract 通过"

scope:
  allowed_paths:
    - frontend/apps/web/src/api/capabilityMatrix.ts
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - security/**
    - record_rules/**
    - addons/**/__manifest__.py
    - migrations/**

limits:
  max_files: 6
  max_candidates: 12
  max_output_lines: 220
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - frontend_intent_channel_compliance_only
  - keep_capability_matrix_payload_shape
  - no_backend_api_change

context:
  branch: codex/next-round
  baseline_sha: da08944
  explicit_user_authorization: true
  input_from: user_execute_1168
  priority_lane: ci_unblock

architecture:
  layer_target: Governance Monitoring
  module: frontend web api capability matrix fetch
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: frontend is switched to existing intent channel only, without introducing business facts.
  reason: resolve verify.frontend.intent_channel.guard stop condition.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-1168.yaml
    - make verify.frontend.intent_channel.guard
    - make ci.preflight.contract

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - intent-channel compliant frontend capability matrix api module

rollback:
  - git restore frontend/apps/web/src/api/capabilityMatrix.ts
  - git restore agent_ops/tasks/ITER-2026-04-05-1168.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-1168.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-1168.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-1167

