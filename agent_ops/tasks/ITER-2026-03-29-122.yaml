task_id: ITER-2026-03-29-122
title: Preserve semantic search surfaces in system init payload
type: governance_task
status: pending
priority: high

goal: Make system.init payload preserve semantic-driven search surface fields added by backend orchestration instead of trimming them away during startup compaction.
user_visible_outcome:
  - startup payload now keeps search mode and searchpanel semantics for released scene entries

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core system_init_payload_builder
  reason: System init already transports released scene runtime semantics, but startup compaction still drops semantic search surface fields such as mode and searchpanel. This iteration preserves those backend-orchestrated search semantics across startup payloads.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-122.yaml
    - python3 -m py_compile addons/smart_core/core/system_init_payload_builder.py addons/smart_core/tests/test_system_init_payload_builder_semantics.py
    - python3 addons/smart_core/tests/test_system_init_payload_builder_semantics.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-122.yaml addons/smart_core/core/system_init_payload_builder.py addons/smart_core/tests/test_system_init_payload_builder_semantics.py

depends_on:
  - ITER-2026-03-29-121

notes: "Batch-3 continuation: startup payload compaction should preserve semantic-driven search surfaces."
