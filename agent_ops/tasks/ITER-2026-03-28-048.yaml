task_id: ITER-2026-03-28-048
title: Define platform naming alignment strategy
type: governance_task
status: pending
priority: medium

goal: Align logical target naming with current module naming without triggering physical module renames in the current batch.
user_visible_outcome:
  - the repository has an explicit naming alignment strategy
  - logical platform naming is separated from physical module names

scope:
  allowed_paths:
    - docs/architecture/**
    - agent_ops/tasks/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform kernel governance
  module: naming alignment
  reason: Prevent premature module rename work while still aligning the architecture vocabulary used by docs and future tasks.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-048.yaml
    - rg -n "逻辑命名|物理模块名|两阶段策略" docs/architecture/platform_naming_alignment_v1.md

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore docs/architecture/platform_naming_alignment_v1.md agent_ops/tasks/ITER-2026-03-28-048.yaml

depends_on:
  - ITER-2026-03-28-047
notes: "Naming alignment only. No physical module rename in this batch."
