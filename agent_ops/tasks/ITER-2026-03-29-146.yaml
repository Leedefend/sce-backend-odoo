task_id: ITER-2026-03-29-146
title: Expand scene ready base fact metrics coverage
type: governance_task
status: pending
priority: high

goal: Make scene ready consumption metrics count the full canonical base_facts set already emitted by scene_dsl_compiler, not only the older search/workflow/validator/actions subset.
user_visible_outcome:
  - scene ready metrics now report base-fact coverage for views, fields and permissions in addition to the existing semantic surfaces

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core scene_ready_contract_builder
  reason: scene_dsl_compiler already emits canonical base_facts for views, fields and permissions, but scene_ready consumption metrics still ignore them. This iteration aligns scene_ready metrics with the upstream canonical base-fact surface.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-146.yaml
    - python3 -m py_compile addons/smart_core/core/scene_ready_contract_builder.py addons/smart_core/tests/test_scene_ready_consumption_metrics.py
    - python3 addons/smart_core/tests/test_scene_ready_consumption_metrics.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-146.yaml addons/smart_core/core/scene_ready_contract_builder.py addons/smart_core/tests/test_scene_ready_consumption_metrics.py

depends_on:
  - ITER-2026-03-29-145

notes: "Batch acceleration: continue grouped scene_ready metric alignment by expanding canonical base_facts coverage instead of splitting per-key micro iterations."
