task_id: ITER-2026-04-07-1224
title: Environment repair lane batch11 execute release 8069 binder and retry restore
type: governance
status: completed
priority: high
mode: execute

goal: >
  Temporarily release external 8069 binder (`odoo-paas-web`) and retry
  `make odoo.recreate` for dev stack to validate runtime restore viability.

user_visible_outcome:
  - "8069 端口冲突临时解除并复验"
  - "odoo.recreate 可行性明确"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1224.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_environment_repair_lane_batch11_release_recreate_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 260
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - runtime execute + evidence only
  - no source code mutation

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_environment_repair_batch11

architecture:
  layer_target: Governance Monitoring
  module: port release and runtime restore retry
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch executes runtime environment action and verifies restore behavior.
  reason: >
    Resolve identified 8069 collision path and validate whether dev odoo can
    bind/start once external owner is released.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1224.yaml
    - make verify.scene.legacy_auth.runtime_probe
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime environment repair lane batch11 release+recreate evidence

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1224.yaml
  - git restore docs/audit/native/native_runtime_environment_repair_lane_batch11_release_recreate_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1223
