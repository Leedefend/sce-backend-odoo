task_id: ITER-2026-03-29-220
title: Audit live project list fact contract
type: audit_task
status: pending
priority: high

goal: Fetch and summarize the live `project.project` list contract facts so the next list implementation batch is driven by real backend metadata instead of stale or wrapped snapshots.
user_visible_outcome:
  - the next list page batch can target real backend facts for columns, sort, filters, and actions
  - list-first implementation proceeds on a closed-loop basis instead of assumptions

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**
    - addons/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend fact audit
  module: live project list contract
  reason: The stored list snapshot is too wrapped/minimal to drive UI work, so we need the actual live backend contract facts for the project list page before implementing list-shell slimming.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-220.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - uncertain_runtime_state

deliverables:
  - audit_report
  - task_result

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-220.yaml agent_ops/reports/2026-03-29/report.ITER-2026-03-29-220.md agent_ops/state/task_results/ITER-2026-03-29-220.json

depends_on:
  - ITER-2026-03-29-219

notes: "Diagnosis-only batch: capture the live project list contract facts first, then implement list-shell slimming in the next batch."
