task_id: ITER-2026-04-18-LEGACY-PAYMENT-BASIS-EVIDENCE-SCREEN
title: Screen legacy payment basis evidence
type: governance-screen
status: pass
priority: high
mode: screen

goal: >
  Search the imported legacy database evidence for whether remaining payment
  requests without contract_id are contract-basis payments or enterprise daily
  expense payments, without writing business data.

user_visible_outcome:
  - "旧库证据用于区分合同付款与企业日常支出，避免把非合同付款错误挂到合同上。"

scope:
  allowed_paths:
    - docs/ops/business_continuity/legacy_payment_basis_evidence_screen_v1.md
    - docs/ops/iterations/delivery_context_switch_log_v1.md
    - agent_ops/tasks/ITER-2026-04-18-LEGACY-PAYMENT-BASIS-EVIDENCE-SCREEN.yaml
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**
    - scripts/**
    - security/**
    - record_rules/**
    - ir.model.access.csv
    - __manifest__.py

context:
  branch: codex/next-round
  baseline_sha: 74d414e
  prior_task: ITER-2026-04-18-IMPORTED-PAYMENT-CONTRACT-LINKAGE-SCREEN

architecture:
  layer_target: Business Fact Screening
  module: imported legacy payment basis facts
  module_owner: construction industry business continuity
  kernel_or_scenario: scenario
  backend_sub_layer_target: business-fact layer
  backend_sub_layer_reason: >
    Payment basis classification is business truth from the legacy data. It
    must be screened before any replay or model logic change.
  reason: >
    The owner clarified that real business includes both contract-basis
    payments and enterprise daily expenses. Remaining missing-contract payments
    may be valid non-contract expenses, so the old database evidence must be
    inspected before any linkage write.

change_rules:
  - read-only database and metadata inspection only
  - do not write payment, contract, settlement, account, ACL, manifest, frontend, or migration data
  - classify evidence source tables and fields before proposing replay
  - do not infer contract basis from amount/name similarity

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-18-LEGACY-PAYMENT-BASIS-EVIDENCE-SCREEN.yaml
    - read-only Odoo shell legacy evidence scan against sc_demo

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - database_write_attempted
  - legacy evidence remains insufficient for classification

deliverables:
  - screen document with legacy evidence sources, counts, and next-step classification

verification_result:
  status: PASS
  commands:
    - command: python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-18-LEGACY-PAYMENT-BASIS-EVIDENCE-SCREEN.yaml
      result: PASS
    - command: read-only Odoo shell legacy evidence scan against sc_demo
      result: PASS
      summary: >
        Remaining missing-contract rows split into actual outflow, outflow
        request, and receipt/income families. Deterministic contract-basis
        candidates exist, but non-contract/daily-expense and ambiguous rows must
        not be forced onto contracts.

rollback:
  - git restore docs/ops/business_continuity/legacy_payment_basis_evidence_screen_v1.md docs/ops/iterations/delivery_context_switch_log_v1.md agent_ops/tasks/ITER-2026-04-18-LEGACY-PAYMENT-BASIS-EVIDENCE-SCREEN.yaml

depends_on:
  - ITER-2026-04-18-IMPORTED-PAYMENT-CONTRACT-LINKAGE-SCREEN
