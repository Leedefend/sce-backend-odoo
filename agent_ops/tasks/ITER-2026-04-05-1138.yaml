task_id: ITER-2026-04-05-1138
title: Runtime endpoint recovery and trusted capability off/on capture retry
type: implementation
status: active
priority: high
mode: execute

goal: >
  Recover local runtime intent endpoint availability and rerun capability
  payload off/on capture to obtain trusted v1/v2 diff evidence when possible.

user_visible_outcome:
  - "修复本地 intent 入口可用性并重试 trusted capability v1/v2 差异抓取"

scope:
  allowed_paths:
    - docs/ops/iterations/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - artifacts/**
  forbidden_paths:
    - addons/**/security/**
    - security/**
    - record_rules/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 8
  max_candidates: 10
  max_output_lines: 260
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - runtime_recovery_and_evidence_only
  - no_business_semantic_change

context:
  branch: codex/next-round
  baseline_sha: da08944
  explicit_user_authorization: true
  input_from: user_approved_1138
  priority_lane: rollout_gating

architecture:
  layer_target: Governance Monitoring
  module: runtime_capture_evidence
  module_owner: platform
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: ensure rollout evidence pipeline can access runtime endpoint.
  reason: unblock trusted off/on capture evidence generation.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-1138.yaml
    - python3 scripts/verify/capability_payload_real_env_capture.py --out-dir artifacts/capability_payload_capture_1138 --allow-env-unstable --request-timeout-sec 5
    - rg -n '"env_status"|"probe_attempts"|"selected_intent_url"' artifacts/capability_payload_capture_1138/latest/capture_report.json
    - make verify.controller.boundary.guard

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - artifacts/capability_payload_capture_1138/latest/capture_report.json

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-05-1138.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-1138.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-1138.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-1137
