task_id: ITER-2026-04-09-1526
title: backend refactor B1 controller thin guard audit
type: governance
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在不修改 controller 逻辑的前提下建立 thin-guard 审计，识别超复杂入口并输出整改优先级。

user_visible_outcome:
  - "有可执行的 controller thin 审计证据，能量化哪些入口先做极薄化"

scope:
  allowed_paths:
    - scripts/verify/controller_thin_guard_audit.py
    - artifacts/architecture/controller_thin_guard_audit_v1.json
    - agent_ops/tasks/ITER-2026-04-09-1526.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1526.md
    - agent_ops/state/task_results/ITER-2026-04-09-1526.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - security/**
    - record_rules/**

limits:
  max_files: 6
  max_output_lines: 260

change_rules:
  - audit only
  - no controller runtime mutation

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Governance verification layer
  module: Controller thin guard
  module_owner: scripts verify
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批次只做架构守卫审计，不改业务事实。
  reason: >
    先建立 controller 超限可见性，再推进 B1 极薄化改造可降低重构风险。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1526.yaml"
    - "python3 scripts/verify/controller_thin_guard_audit.py"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - controller thin guard audit artifact

rollback:
  - git restore scripts/verify/controller_thin_guard_audit.py artifacts/architecture/controller_thin_guard_audit_v1.json

depends_on:
  - ITER-2026-04-09-1525

