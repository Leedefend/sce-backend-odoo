task_id: ITER-2026-04-09-1556
title: backend refactor B-line controller_thin_guard strict fail-gate enablement
type: governance
status: active
priority: medium
mode: implement
batch: 1/1

goal: >
  在 orm_hints 收敛为 0 前提下，将 controller_thin_guard 升级为严格门禁：over_threshold 或 orm_hints > 0 即 FAIL。

user_visible_outcome:
  - "verify.arch.controller_thin_guard 具备可阻断能力"

scope:
  allowed_paths:
    - scripts/verify/controller_thin_guard_audit.py
    - artifacts/architecture/controller_thin_guard_audit_v1.json
    - agent_ops/tasks/ITER-2026-04-09-1556.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1556.md
    - agent_ops/state/task_results/ITER-2026-04-09-1556.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 320

change_rules:
  - verify gate behavior only
  - no runtime code changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Governance verification layer
  module: controller_thin_guard
  module_owner: scripts verify
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    仅提升验证门禁强度，不改变业务事实。
  reason: >
    B 线 thinness 已收敛，切换到严格阻断防止回归。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1556.yaml"
    - "python3 -m py_compile scripts/verify/controller_thin_guard_audit.py"
    - "python3 scripts/verify/controller_thin_guard_audit.py"
    - "python3 - <<'PY'\nimport json\nobj=json.load(open('artifacts/architecture/controller_thin_guard_audit_v1.json','r',encoding='utf-8'))\nassert obj['status']=='PASS'\nassert obj['summary']['orm_hint_count']==0\nassert obj['summary']['over_threshold_count']==0\nprint('strict thin gate baseline PASS')\nPY"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - strict controller_thin_guard fail-gate

rollback:
  - git restore scripts/verify/controller_thin_guard_audit.py

depends_on:
  - ITER-2026-04-09-1555
