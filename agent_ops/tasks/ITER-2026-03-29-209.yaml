task_id: ITER-2026-03-29-209
title: Audit project form button governance injection
type: audit_task
status: pending
priority: high

goal: Determine whether the three live project form buttons are introduced before or during `apply_contract_governance` by comparing dispatcher output, finalized contract output, and governed delivery output.
user_visible_outcome:
  - the next backend fix batch will target the exact stage that injects noisy project form buttons
  - project form button cleanup can proceed without further guesswork

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**
    - addons/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: governance/backend root-cause audit
  module: project form button governance diagnosis
  reason: Parser extraction and get_contract_api() have already been ruled out, so this batch must isolate whether the live buttons appear in finalize_contract output or only after governance is applied.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-209.yaml
    - python3 -m py_compile agent_ops/scripts/project_form_button_governance_audit.py
    - python3 agent_ops/scripts/project_form_button_governance_audit.py --db sc_demo --login sc_fx_pm --container sc-backend-odoo-dev-odoo-1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - audit_report
  - task_result

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-209.yaml agent_ops/scripts/project_form_button_governance_audit.py agent_ops/reports/2026-03-29/report.ITER-2026-03-29-209.md agent_ops/state/task_results/ITER-2026-03-29-209.json

depends_on:
  - ITER-2026-03-29-208

notes: "Diagnosis-only batch: compare dispatcher output, finalized contract output, and governed contract output for project.project form buttons."
