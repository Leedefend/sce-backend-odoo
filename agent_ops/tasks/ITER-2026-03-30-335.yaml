task_id: ITER-2026-03-30-335
title: Remove temporary list contract debug panels after visual verification
type: code
status: in_progress
priority: medium

goal: >
  Remove temporary debug-only panels that were added to verify list contract
  consumption, while preserving the working optimization toolbar behavior and
  visual differentiation.

user_visible_outcome:
  - debug-only JSON panels no longer appear on the page
  - optimized toolbar behavior and styling remain unchanged

scope:
  allowed_paths:
    - frontend/apps/web/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
  forbidden_paths:
    - addons/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - cleanup only
  - no contract behavior changes
  - no visual rollback of the optimized toolbar

context:
  branch: codex/next-round
  baseline_sha: 03bd2ef

architecture:
  layer_target: Frontend Layer
  module: debug cleanup for contract toolbar verification
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    The list optimization toolbar has been verified as working and visually
    distinct. Temporary debug panels added during diagnosis should now be
    removed to restore a clean UI.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-335.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_command_failed
  - optimization_toolbar_regressed

deliverables:
  - removed temporary debug panels from ActionView and PageToolbar
  - updated report and task result artifacts

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-335.yaml
  - git restore frontend/apps/web/src/views/ActionView.vue
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue

depends_on:
  - ITER-2026-03-30-334

notes: >
  This batch removes only temporary debug UI. Keep the optimization toolbar
  consumer chain and presentation improvements intact.
