task_id: ITER-2026-03-30-375
title: Audit bootstrap-style core data ownership after seed migration
type: audit
status: in_progress
priority: medium

goal: >
  Audit the remaining bootstrap-style non-demo data files in
  `smart_construction_core/data` and determine whether they should remain in
  core or move to a more platform/governance-aligned owner.

user_visible_outcome:
  - explicit ownership judgment for the remaining bootstrap-style files
  - narrowed migration candidates after the scene/capability seed migration
  - a precise next batch recommendation

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only
  - do not change code or data in this batch
  - focus on ownership and runtime role of bootstrap files

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: bootstrap-style data ownership
  module_owner: agent_ops
  kernel_or_scenario: scenario
  reason: >
    After migrating the orchestration seeds, the remaining ownership question is
    concentrated on bootstrap-style files rather than demo or scene seeds.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-375.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - audit_scope_spills_into_code_changes

deliverables:
  - bootstrap-style ownership audit report
  - task result artifact for ITER-2026-03-30-375
  - delivery log checkpoint with the next cleanup recommendation

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-375.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-375.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-375.json

depends_on:
  - ITER-2026-03-30-374
