task_id: ITER-2026-04-07-1330
title: Runtime evidence closure sync after environment repair
status: active
type: verify
priority: high
mode: execute

goal: >
  Close the previously blocked runtime evidence line by validating config-center
  click-path evidence on the repaired sc_test baseline and synchronizing
  governance records.

user_visible_outcome:
  - "1326/1328 runtime 证据线在修复基线后完成收口同步"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1330.yaml
    - agent_ops/reports/2026-04-07/report.ITER-2026-04-07-1330.md
    - agent_ops/state/task_results/ITER-2026-04-07-1330.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - frontend/**
    - scripts/**

limits:
  max_files: 4
  max_output_lines: 260
  forbid_repo_wide_scan: true

change_rules:
  - verification-evidence-sync-only
  - no-business-code-change

context:
  branch: codex/next-round
  baseline_sha: ca23376
  explicit_user_authorization: true

architecture:
  layer_target: Runtime Verification Evidence
  module: config-center runtime evidence closure
  module_owner: ops runtime context
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene-orchestration layer
  backend_sub_layer_reason: >
    This batch only validates and synchronizes runtime evidence after
    environment repair.
  reason: >
    1329 repaired runtime prerequisites; this batch closes the verification line
    with refreshed evidence.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1330.yaml
    - "docker ps --format '{{.Names}} {{.Status}}' | rg 'sc-backend-odoo-dev-(db|odoo)-1'"
    - "E2E_BASE_URL=http://localhost:8069 DB_NAME=sc_test python3 scripts/verify/native_business_admin_config_center_runtime_clickpath_verify.py"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - refreshed runtime evidence verdict
  - governance sync report and result

depends_on:
  - ITER-2026-04-07-1329

rollback:
  - git restore agent_ops/reports/2026-04-07/report.ITER-2026-04-07-1330.md
  - git restore agent_ops/state/task_results/ITER-2026-04-07-1330.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
