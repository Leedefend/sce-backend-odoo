task_id: ITER-2026-03-31-393
title: Audit native-to-custom rendering and delivery logic alignment
type: audit
status: completed
priority: high

goal: >
  Verify whether the current native-to-custom parsing chain preserves the
  original capability semantics of native menu/action/view facts, with a focus
  on whether custom rendering results and delivery logic remain aligned with
  native capability intent instead of introducing product-side reinterpretation.

user_visible_outcome:
  - "明确原生能力解析到自定义前端后是否仍与原生能力对齐"
  - "明确哪些页面只是忠实承接，哪些页面已经发生交付逻辑偏移"
  - "按原生能力类型输出对齐 / 偏移结论"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no code changes
  - do not discuss scene-orchestration sufficiency as the primary explanation
  - compare native capability facts and custom rendered/delivery behavior directly
  - classify each audited surface as one of: faithfully_rendered, faithfully_handled, semantically_shifted

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: native to custom capability alignment
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    The active question is no longer whether scene orchestration is rich enough,
    but whether the current parsing and custom rendering pipeline still preserves
    native capability intent from menu/action/view facts through to delivered
    frontend behavior.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-393.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - native_capability_alignment_scope_unclear
  - audited_surface_requires_finance_governed_claim

deliverables:
  - native-to-custom alignment audit for key custom surfaces
  - explicit classification of rendering/delivery alignment
  - report and task result artifacts for ITER-2026-03-31-393
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-393.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-393.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-393.json

depends_on:
  - ITER-2026-03-31-392
