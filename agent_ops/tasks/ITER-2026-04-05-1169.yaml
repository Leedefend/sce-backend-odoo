task_id: ITER-2026-04-05-1169
title: Enforce scene_provider import boundary in app_shell
type: implementation
status: active
priority: high
mode: execute

goal: >
  Remove disallowed scene_provider import from app_shell fallback handler and
  keep fallback behavior compliant with scene provider guard so ci.preflight.contract can proceed.

user_visible_outcome:
  - "verify.scene.provider.guard 通过"
  - "ci.preflight.contract 继续通过到下一门禁"

scope:
  allowed_paths:
    - addons/smart_core/handlers/app_shell.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - security/**
    - record_rules/**
    - addons/**/__manifest__.py
    - migrations/**

limits:
  max_files: 6
  max_candidates: 12
  max_output_lines: 220
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - remove_scene_provider_direct_import
  - keep_fallback_contract_shape
  - no_industry_business_logic_injection

context:
  branch: codex/next-round
  baseline_sha: da08944
  explicit_user_authorization: true
  input_from: user_execute_1169
  priority_lane: ci_unblock

architecture:
  layer_target: Governance Monitoring
  module: smart_core app shell fallback handlers
  module_owner: platform
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: this batch only restores provider import boundary for fallback discovery path.
  reason: resolve verify.scene.provider.guard stop condition.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-1169.yaml
    - make verify.scene.provider.guard
    - make ci.preflight.contract

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - app_shell scene provider boundary compliant patch

rollback:
  - git restore addons/smart_core/handlers/app_shell.py
  - git restore agent_ops/tasks/ITER-2026-04-05-1169.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-1169.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-1169.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-1168

