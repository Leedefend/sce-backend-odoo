task_id: ITER-2026-04-03-936
title: Delivery policy guard drift scan (candidates only)
type: governance
status: active
priority: high
mode: scan

goal: >
  Collect bounded candidate evidence for delivery policy guard drift without
  concluding root cause in scan stage.

user_visible_outcome:
  - "明确列出 delivery policy drift 候选证据点"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 5
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - scan_only_no_conclusion
  - no_implementation_changes

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-935
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: delivery policy guard drift scan
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Scan stage gathers guard-evidence candidates only; no business fact changes.
  reason: >
    After menu-integrity fix, release chain now fails at policy guard strict
    menu set comparison.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-936.yaml
    - rg -n "menu policy drift|menu_keys|nav_menu_keys|scene policy drift|capability policy drift" scripts/verify/product_delivery_policy_guard.py
    - cat artifacts/backend/product_delivery_policy_guard.json

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - delivery_policy_drift_scan_candidates

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-936.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-936.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-936.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-935
