task_id: ITER-2026-04-01-529
title: Align MVP frontend smoke login token parsing with current login contract
type: verification-compat
status: active
priority: high

goal: >
  Restore trusted frontend smoke verification by updating MVP smoke scripts to
  accept the current login contract shape, where the token may appear under
  either `session.token` or `token`.

user_visible_outcome:
  - "v0_5 host smoke 恢复可信登录"
  - "MVP smoke 脚本与当前 login 契约一致"
  - "产品可用性 verify 批次可继续推进"

scope:
  allowed_paths:
    - scripts/verify/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - verify-tooling only
  - no product code changes
  - preserve smoke semantics except for current login token compatibility
  - align MVP scripts with the already-accepted login parsing pattern

context:
  branch: codex/next-round
  baseline_sha: 2a02eb6
  explicit_user_authorization: true

architecture:
  layer_target: Verification Tooling
  module: MVP frontend smoke login compatibility
  module_owner: scripts/verify
  kernel_or_scenario: kernel
  reason: >
    The active UI verify batch is blocked because verify.portal.v0_5.host still
    expects `data.token`, while the current frontend session logic accepts
    `session.token || token`. The smoke tooling must match that contract before
    UI verification can continue.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-529.yaml
    - make verify.portal.v0_5.host

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - smoke_login_contract_still_mismatched
  - v0_5_host_smoke_still_fails_for_the_same_reason

deliverables:
  - updated_mvp_smoke_login_parsing
  - governance/report artifacts for ITER-2026-04-01-529

rollback:
  - git restore scripts/verify/fe_mvp_list_smoke.js
  - git restore scripts/verify/fe_mvp_write_smoke.js
  - git restore agent_ops/tasks/ITER-2026-04-01-529.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-528
