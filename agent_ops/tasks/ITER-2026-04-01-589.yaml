task_id: ITER-2026-04-01-589
title: Reorder HUD entries so key continuity signals appear first
type: frontend
status: active
priority: high

goal: >
  Reorder the record-view HUD entries so the most important continuity signals
  appear first, without adding, removing, or renaming any HUD fields.

user_visible_outcome:
  - "HUD 先展示最关键的连续性信号"
  - "追踪 ID、写入模式、耗时与路由不再被技术字段淹没"
  - "不改 HUD 字段集合与值语义"

scope:
  allowed_paths:
    - frontend/apps/web/src/app/action_runtime/useActionViewHudEntriesRuntime.ts
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - bounded P1 usability implementation
  - do not change HUD field coverage
  - do not rename labels again in this batch

context:
  branch: codex/next-round
  baseline_sha: 59536d3
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-588
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: record-view HUD key-signal ordering
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    After HUD label readability, the next continuity gain is not more text but
    better ordering: users should see the active trace/write/latency/route
    signals before deeper technical group-window metadata.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-589.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.recordview_hud_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - dedicated_hud_smoke_still_fails
  - ordering_fix_requires_field_set_change

deliverables:
  - bounded_record_view_hud_ordering_fix
  - governance/report artifacts for ITER-2026-04-01-589

rollback:
  - git restore frontend/apps/web/src/app/action_runtime/useActionViewHudEntriesRuntime.ts
  - git restore agent_ops/tasks/ITER-2026-04-01-589.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-589.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-589.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-588
