task_id: ITER-2026-03-31-474
title: Audit finer-grained button and entry residuals on the corrected first-batch baseline
type: audit
status: active
priority: medium

goal: >
  Continue the Sichuan Baosheng delivery validation chain on the corrected
  write-path baseline by auditing whether any finer-grained first-batch buttons
  or alternate entry points still remain inconsistent for PM, finance,
  executive, or business-admin.

user_visible_outcome:
  - "可以判断首批主流程之外是否还残留细粒度按钮/入口不一致"
  - "若仍有问题，可直接定位到具体按钮或快捷入口"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - read-only repository and runtime audit only
  - do not modify code, XML, ACL, record rules, or data files in this batch
  - classify finer-grained button or alternate-entry residuals on top of the corrected representative write-path baseline

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom

architecture:
  layer_target: Customer Delivery Flow Acceptance
  module: finer-grained first-batch residual audit
  module_owner: customer delivery validation chain
  kernel_or_scenario: scenario
  reason: >
    ITER-2026-03-31-473 aligned the two known representative action residuals.
    The next efficient low-risk step is to inspect whether finer-grained
    buttons, quick actions, or alternate entry points still diverge from the
    corrected delivered boundary.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-474.yaml
    - repository and runtime audit can classify any remaining finer-grained first-batch entry residuals on sc_odoo

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - finer_grained_entry_residuals_cannot_be_classified_from_repository_and_runtime_evidence
  - classification_requires_uncertain_contract_or_kernel_semantics

deliverables:
  - finer-grained first-batch residual audit report
  - task result artifact and delivery-log checkpoint for ITER-2026-03-31-474

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-474.yaml

depends_on:
  - ITER-2026-03-31-473
