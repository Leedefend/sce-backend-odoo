task_id: ITER-2026-03-31-419
title: Freeze workbook system-role user mapping for customer bootstrap
type: audit
status: active
priority: high

goal: >
  Reconstruct and freeze the workbook-derived user-to-system-role mapping so
  the next additive bootstrap batch can attach `管理员角色` and `通用角色` to real
  users using the newly implemented authority path.

user_visible_outcome:
  - "明确哪些用户属于管理员角色"
  - "明确哪些用户属于通用角色"
  - "为下一批用户挂组提供可执行基线"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
    - tmp/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only
  - no addon implementation
  - freeze mapping from workbook and current customer decisions

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Delivery Audit
  module: smart_construction_custom customer bootstrap
  module_owner: customer-specific delivery customization
  kernel_or_scenario: scenario
  reason: >
    The business-system-admin authority path is now implemented. The next low-risk
    step is to freeze the workbook system-role membership by user before writing
    group attachments.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-419.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - workbook_role_membership_unclear
  - user_role_mapping_conflicts_with_frozen_customer_decisions

deliverables:
  - report and task result artifacts for ITER-2026-03-31-419
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-419.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-419.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-419.json

depends_on:
  - ITER-2026-03-31-417
