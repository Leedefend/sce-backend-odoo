task_id: ITER-2026-04-10-1797
title: layout alias traversal dedupe fix for tab duplication

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 layout 子节点遍历将 tabs/pages 别名重复计入的问题，恢复 tab 投影与业务事实一致。

user_visible_outcome:
  - "tab 数量不再因 pages/tabs 别名翻倍"
  - "结构摘要 page/tabs 与业务页签一致"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1797.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1797.md
    - agent_ops/state/task_results/ITER-2026-04-10-1797.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend consumer traversal only
  - no backend contract mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: ContractFormPage layout traversal
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    contract 中 notebook 同时包含 tabs/pages 别名，前端遍历双计导致 tab 重复与业务缺失观感。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1797.yaml"
    - 'rg -n "forEachLayoutChildren\(|normalizeLayoutChildNode\(" frontend/apps/web/src/pages/ContractFormPage.vue'
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - layout alias traversal dedupe patch
  - iteration report and state

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1796
