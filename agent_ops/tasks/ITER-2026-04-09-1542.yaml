task_id: ITER-2026-04-09-1542
title: backend refactor E1 envelope audit strict fail-gate enablement
type: governance
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在 candidate 已收敛为 0 的前提下，将 envelope consistency audit 升级为严格门禁：candidate_count>0 返回 FAIL。

user_visible_outcome:
  - "verify.contract.envelope_consistency_guard 具备可阻断能力"

scope:
  allowed_paths:
    - scripts/verify/envelope_consistency_audit.py
    - artifacts/architecture/envelope_consistency_audit_v1.json
    - agent_ops/tasks/ITER-2026-04-09-1542.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1542.md
    - agent_ops/state/task_results/ITER-2026-04-09-1542.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 320

change_rules:
  - verify gate behavior only
  - no runtime business code changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Governance verification layer
  module: envelope_consistency_audit
  module_owner: scripts verify
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    仅提升验证门禁强度，不改变业务事实或接口行为。
  reason: >
    审计已收敛，应切换到严格阻断防止未来回归。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1542.yaml"
    - "python3 -m py_compile scripts/verify/envelope_consistency_audit.py"
    - "python3 scripts/verify/envelope_consistency_audit.py"
    - "python3 - <<'PY'\nimport json\nobj=json.load(open('artifacts/architecture/envelope_consistency_audit_v1.json','r',encoding='utf-8'))\nassert obj['status']=='PASS'\nassert obj['summary']['inconsistent_candidate_count']==0\nprint('strict gate baseline PASS')\nPY"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - strict envelope consistency fail-gate

rollback:
  - git restore scripts/verify/envelope_consistency_audit.py

depends_on:
  - ITER-2026-04-09-1541
