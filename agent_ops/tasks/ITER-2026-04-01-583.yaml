task_id: ITER-2026-04-01-583
title: Repair recordview HUD smoke login-token compatibility
type: frontend
status: active
priority: high

goal: >
  Repair the dedicated recordview HUD smoke so it uses the current smoke-login
  defaults and token extraction shape already proven by the v0.5 list chain.

user_visible_outcome:
  - "recordview HUD smoke 不再因旧登录链失败"
  - "HUD continuity 线可重新获得可信验收门禁"
  - "修复范围仅限 verify/tooling，不回改业务逻辑"

scope:
  allowed_paths:
    - Makefile
    - scripts/verify/fe_recordview_hud_smoke.js
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - verify/tooling only
  - align with existing v0.5 smoke login conventions
  - do not change product code in this batch
  - do not broaden verify scope beyond recordview HUD smoke

context:
  branch: codex/next-round
  baseline_sha: e430a7e
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-582
  priority_lane: verification_recovery

architecture:
  layer_target: Frontend Layer
  module: recordview HUD smoke verification chain
  module_owner: frontend/apps/web verify tooling
  kernel_or_scenario: kernel
  reason: >
    The recordview HUD continuity line stopped on a verifier login-token
    mismatch. This batch restores the dedicated smoke to the same credential and
    token-handling conventions already used by the stable v0.5 list chain.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-583.yaml
    - make verify.portal.recordview_hud_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - dedicated_hud_smoke_still_fails
  - compatibility_fix_requires_product_code_change

deliverables:
  - repaired_recordview_hud_smoke_login_chain
  - governance/report artifacts for ITER-2026-04-01-583

rollback:
  - git restore Makefile
  - git restore scripts/verify/fe_recordview_hud_smoke.js
  - git restore agent_ops/tasks/ITER-2026-04-01-583.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-583.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-583.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-582
