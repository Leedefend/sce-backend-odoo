task_id: ITER-2026-03-30-356
title: Audit residual scene-oriented payload fields in smart_construction_core
type: code
status: in_progress
priority: high

goal: >
  After the direct scene seed cleanup, hook migration, and workspace-row
  cleanup, run a residual audit across smart_construction_core to locate any
  remaining scene-oriented payload fields still embedded in business facts or
  ext facts, so the cleanup line can finish with a concrete tail list instead
  of guesswork.

user_visible_outcome:
  - residual scene pollution in smart_construction_core is explicitly enumerated
  - the remaining cleanup work is reduced to a concrete tail list
  - later product work can proceed on a mostly cleaned fact-layer boundary

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - addons/**
    - frontend/**
    - scripts/verify/**
    - migrations/**
    - deploy/**

change_rules:
  - audit and reporting only
  - no implementation change in this batch
  - focus on scene-oriented payload fields embedded in business facts or ext facts

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Domain Layer Governance
  module: residual fact-layer scene audit
  module_owner: agent_ops + docs
  kernel_or_scenario: scenario
  reason: >
    Most active scene ownership has already migrated out of smart_construction_core.
    The remaining risk is hidden residual scene payload still embedded in
    business facts or extension facts.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-356.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - residual_scope_cannot_be_enumerated_clearly

deliverables:
  - residual scene-pollution tail list for smart_construction_core
  - report and task result artifacts for ITER-2026-03-30-356
  - delivery log checkpoint for the residual audit

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-356.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-356.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-356.json

depends_on:
  - ITER-2026-03-30-355
