task_id: ITER-2026-03-29-144
title: Guard canonical search surface in scene ready strict mode
type: governance_task
status: pending
priority: high

goal: Make scene ready strict contract guard treat canonical search_surface as a required semantic surface instead of checking only structural UI fields.
user_visible_outcome:
  - strict-mode scene-ready contracts now explicitly fail source completeness when search semantics are absent

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core scene_ready_contract_builder
  reason: Scene ready strict contract guard currently validates surface, view_modes, sections, projection and action surface, but still ignores canonical search_surface. This iteration aligns strict readiness with the search semantics already normalized elsewhere in the same builder.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-144.yaml
    - python3 -m py_compile addons/smart_core/core/scene_ready_contract_builder.py addons/smart_core/tests/test_scene_ready_strict_contract_guard.py
    - python3 addons/smart_core/tests/test_scene_ready_strict_contract_guard.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-144.yaml addons/smart_core/core/scene_ready_contract_builder.py addons/smart_core/tests/test_scene_ready_strict_contract_guard.py addons/smart_core/tests/test_scene_runtime_contract_chain.py

depends_on:
  - ITER-2026-03-29-143

notes: "Batch acceleration: keep scene_ready strict guard consistent with the canonical search semantic surface now used throughout backend orchestration."
