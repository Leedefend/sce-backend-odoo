task_id: ITER-2026-03-28-004
title: Validate dedicated fail queue stop
type: governance_task
status: pending
priority: high

goal: Prove that a fresh fail task causes queue execution to stop with FAIL on first encounter.
user_visible_outcome:
  - dedicated fail queue executes a fresh fail task
  - queue state records queue_stopped with FAIL
  - fail stop evidence is reproducible without mutating prior task results

scope:
  allowed_paths:
    - agent_ops/**
    - Makefile
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Governance/Tooling
  module: agent_ops queue + reports + docs/ops/releases/archive/temp
  reason: Exercise the queue fail-stop path with a fresh task result record.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-004.yaml
    - python3 -c "import sys; sys.exit(1)"

risk:
  level: low
  manual_approval_required: false
  threshold_overrides:
    max_changed_files: 200
    max_added_lines: 5000
    max_removed_lines: 1000

stop_conditions:
  - verify_failed

deliverables:
  - code
  - report

rollback:
  - git restore agent_ops Makefile docs/ops/iterations/delivery_context_switch_log_v1.md docs/ops/releases/archive/temp

depends_on:
  - ITER-2026-03-28-001
notes: "Fresh FAIL sample reserved for dedicated fail-queue execution."
