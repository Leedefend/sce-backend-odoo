task_id: ITER-2026-04-01-601
title: Align ActionView HUD title source with localized continuity fallback
type: frontend
status: active
priority: high
mode: implement

goal: >
  Remove the hardcoded English HUD title source in ActionView so existing
  localized title fallback logic can take effect consistently.

user_visible_outcome:
  - "HUD 标题不再固定为英文 View Context"
  - "列表/记录上下文标题回到既有中文回退逻辑"
  - "不改变 HUD 数据字段和交互行为"

scope:
  allowed_paths:
    - frontend/apps/web/src/views/ActionView.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - bounded P1 usability implementation
  - do not change HUD state fields
  - do not change request/response behavior

context:
  branch: codex/next-round
  baseline_sha: aca8eac
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-600
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: ActionView HUD title continuity
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    HUD panel already has localized fallback title logic, but it is bypassed by
    a hardcoded English title source. Restoring fallback priority improves
    user-visible continuity with minimal risk.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-601.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.recordview_hud_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - dedicated_hud_smoke_still_fails

deliverables:
  - bounded_action_view_hud_title_source_alignment_fix
  - governance/report artifacts for ITER-2026-04-01-601

rollback:
  - git restore frontend/apps/web/src/views/ActionView.vue
  - git restore agent_ops/tasks/ITER-2026-04-01-601.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-601.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-601.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-600
