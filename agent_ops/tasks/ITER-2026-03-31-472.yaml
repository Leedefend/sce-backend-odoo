task_id: ITER-2026-03-31-472
title: Audit representative write-path ownership for delivered first-batch roles
type: audit
status: active
priority: medium

goal: >
  Continue the Sichuan Baosheng delivery validation chain on sc_odoo by
  auditing whether representative first-batch write and approval paths now
  match the frozen PM / finance / executive / business-admin runtime boundary
  after the business-admin display residual has been classified.

user_visible_outcome:
  - "可以判断首批代表性写路径是否已经按角色边界正确落地"
  - "若仍有问题，可直接区分为权限残差、入口残差或数据前置条件残差"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - read-only governance and runtime audit only
  - do not modify model fields, role sync logic, XML, ACL, record rules, or data files in this batch
  - classify representative first-batch write and approval usability by delivered role using repository and runtime evidence

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom

architecture:
  layer_target: Customer Delivery Flow Acceptance
  module: Representative first-batch write-path ownership audit
  module_owner: customer delivery validation chain
  kernel_or_scenario: scenario
  reason: >
    ITER-2026-03-31-471 confirmed that the remaining business-admin issue is a
    controlled display residual rather than an authority defect. The next
    efficient low-risk step is to validate whether representative write and
    approval paths also match the frozen delivered role boundary.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-472.yaml
    - repository and runtime audit can classify representative first-batch write-path ownership for PM, finance, executive, and business-admin on sc_odoo

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - representative_write_path_cannot_be_classified_from_repository_and_runtime_evidence
  - write_path_boundary_requires_uncertain_contract_or_kernel_semantics

deliverables:
  - representative write-path ownership audit report
  - task result artifact and delivery-log checkpoint for ITER-2026-03-31-472

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-472.yaml

depends_on:
  - ITER-2026-03-31-471
