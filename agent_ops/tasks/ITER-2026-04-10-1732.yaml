task_id: ITER-2026-04-10-1732
title: form field truth source audit for native-alignment

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  落地 FORM-001：建立 form 字段真值源一致性审计，识别 layout.fieldInfo 与 fields 之间的
  类型/关系冲突，输出阻断级证据。

user_visible_outcome:
  - "明确标出会导致关系字段降级渲染的契约冲突项"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1732.yaml
    - scripts/verify/form_field_truth_source_audit.py
    - artifacts/contract/form_field_truth_source_v1.json
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1732.md
    - agent_ops/state/task_results/ITER-2026-04-10-1732.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 800

change_rules:
  - audit-first implementation only
  - no backend business model/schema changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: backend scene-orchestration contract supply governance
  module: form contract field truth-source audit
  module_owner: smart_core
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批只做契约供给一致性审计，不改业务事实与前端特化逻辑。
  reason: >
    用户提出 form 契约与原生对齐缺口，优先执行 FORM-001 识别关系字段降级风险。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1732.yaml"
    - "python3 scripts/verify/form_field_truth_source_audit.py --json"

risk:
  level: medium-low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - form field truth-source audit script and artifact

rollback:
  - "git restore scripts/verify/form_field_truth_source_audit.py"

depends_on:
  - ITER-2026-04-10-1731
