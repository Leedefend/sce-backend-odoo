task_id: ITER-2026-03-30-360
title: Remove leftover capability definition scene placeholder from smart_construction_core
type: code
status: in_progress
priority: medium

goal: >
  Finish the next low-risk residual cleanup by removing the leftover
  definition-time scene placeholder parameter from capability_registry so scene
  bindings live only in smart_construction_scene.

user_visible_outcome:
  - capability payload behavior remains stable
  - core capability definitions no longer carry scene placeholder inputs
  - the residual scene cleanup line continues without touching ACL or financial semantics

scope:
  allowed_paths:
    - addons/smart_construction_core/services/**
    - addons/smart_construction_scene/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - preserve capability payload shape and ordering
  - keep scene bindings only in smart_construction_scene after the batch
  - no ACL, financial, or manifest changes

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Scene Layer Migration
  module: capability definition cleanup
  module_owner: addons/smart_construction_core + addons/smart_construction_scene
  kernel_or_scenario: scenario
  reason: >
    After capability payload resolution moved to the scene layer, the remaining
    residual scene pollution is a definition-time placeholder parameter still
    present in capability_registry.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-360.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - capability_payload_shape_regresses
  - verification_failure

deliverables:
  - capability definition cleanup slice
  - report and task result artifacts for ITER-2026-03-30-360
  - delivery log checkpoint for the next residual cleanup batch

rollback:
  - git restore addons/smart_construction_core/services
  - git restore addons/smart_construction_scene
  - git restore agent_ops/tasks/ITER-2026-03-30-360.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-360.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-360.json

depends_on:
  - ITER-2026-03-30-359
