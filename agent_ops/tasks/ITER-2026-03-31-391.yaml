task_id: ITER-2026-03-31-391
title: Audit workbench classification against native fact ownership
type: audit
status: completed
priority: high

goal: >
  Decide whether the current `工作台` custom frontend surface should still be
  treated as a direct rendering of the native portal dashboard fact anchor, or
  should instead be formally classified as a product-level orchestration surface.

user_visible_outcome:
  - "明确工作台当前到底是什么：事实直出，还是产品编排面"
  - "明确是否还存在一致性问题，还是应该改成分类口径问题"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no code changes
  - evaluate both the native fact anchor and the current custom product surface
  - distinguish clearly between semantic drift and explicit reclassification

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: workbench classification
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    After closing capability-matrix consistency drift, the only remaining
    question is whether the current workbench should be repaired back toward the
    native portal-dashboard fact anchor or explicitly reclassified as a product
    orchestration surface.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-391.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - workbench_classification_scope_unclear

deliverables:
  - explicit classification decision for workbench
  - report and task result artifacts for ITER-2026-03-31-391
  - delivery log checkpoint with the next active line

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-391.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-391.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-391.json

depends_on:
  - ITER-2026-03-31-390
