task_id: ITER-2026-03-31-439
title: Add narrow AGENTS exception for bootstrap module migration batch
type: governance
status: active
priority: medium

goal: >
  Add a narrow stop-rule exception so the dedicated
  smart_construction_bootstrap -> smart_platform_bootstrap migration batch can
  legally touch addon manifests and related bootstrap references under explicit
  user authorization.

user_visible_outcome:
  - "为 bootstrap 模块迁移批次建立窄例外护栏"
  - "避免后续真实迁移批次被 __manifest__.py 停止规则直接拦住"

scope:
  allowed_paths:
    - AGENTS.md
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - governance-only batch
  - additive guard update only
  - no addon code changes
  - no manifest changes in this batch

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Execution Governance
  module: repository guardrails for bootstrap migration
  module_owner: task execution policy
  kernel_or_scenario: kernel
  reason: >
    The frozen migration plan now requires creating a new neutral bootstrap
    module and a compatibility shim, which will necessarily touch __manifest__.py.
    A narrow exception must exist before that high-risk implementation batch can
    begin.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-439.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - guard_scope_cannot_be_narrowed_without_opening_unrelated_manifest_changes

deliverables:
  - implementation/report/result artifacts for ITER-2026-03-31-439

rollback:
  - git restore AGENTS.md
  - git restore agent_ops/tasks/ITER-2026-03-31-439.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-439.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-439.json

depends_on:
  - ITER-2026-03-31-438
