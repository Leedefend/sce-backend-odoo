task_id: ITER-2026-04-10-1761
title: form field truth-source unification

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  落地 FORM-Batch1 / Task 101-2：统一字段真值源，`fields` 作为唯一字段元数据真值源，
  `layout.fieldInfo` 收敛为轻量布局渲染信息。

user_visible_outcome:
  - "layout.fieldInfo 不再承载类型真值，前端统一从 fields 消费字段元数据"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1761.yaml
    - addons/smart_core/app_config_engine/services/contract_service.py
    - scripts/verify/form_field_truth_source_audit.py
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1761.md
    - agent_ops/state/task_results/ITER-2026-04-10-1761.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 1000

change_rules:
  - backend contract semantic layer only
  - additive compatibility changes only
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend contract semantic layer
  module: ui.contract form field truth-source governance
  module_owner: smart_core + verify scripts
  kernel_or_scenario: kernel
  reason: >
    避免 layout.fieldInfo 与 fields 双真值源冲突，统一类型/relation/required/readonly 由 fields 真值驱动。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1761.yaml"
    - "python3 scripts/verify/form_field_truth_source_audit.py --json --input tmp/json/form.json"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - fields canonical truth-source patch
  - form_field_truth_source_v1 artifact

rollback:
  - "git restore addons/smart_core/app_config_engine/services/contract_service.py"
  - "git restore scripts/verify/form_field_truth_source_audit.py"

depends_on:
  - ITER-2026-04-10-1760
