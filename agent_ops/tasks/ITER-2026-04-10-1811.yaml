task_id: ITER-2026-04-10-1811
title: collaboration surface full-path root-cause fix

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  从业务事实层到消费层闭环修复消息/附件不可见问题：补齐后端 chatter/attachments 探测，并修正前端协作区显示门控。

user_visible_outcome:
  - "表单页面恢复消息/附件协作区，不因 align/native 条件被误隐藏"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1811.yaml
    - addons/smart_core/v2/services/ui_contract_service.py
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1811.md
    - agent_ops/state/task_results/ITER-2026-04-10-1811.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 1600

change_rules:
  - full-path root-cause fix
  - no business-model mutation
  - additive and reversible

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend semantic layer + frontend contract consumer
  module: v2 chatter surface detection and collaboration block gating
  module_owner: addons/smart_core + frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    runtime 原生 XML 含 oe_chatter 但 v2 contract 仍输出 chatter/attachments disabled，
    同时前端协作区受 preferNative 条件误拦截。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1811.yaml"
    - 'rg -n "_fill_chatter_attachment_surface|oe_chatter|mail_thread|has_chatter" addons/smart_core/v2/services/ui_contract_service.py'
    - 'rg -n "showCollaborationZoneBlock|semanticHasChatter|semanticHasAttachments|preferNativeFormSurface" frontend/apps/web/src/pages/ContractFormPage.vue'
    - "make restart"
    - "make frontend.restart"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - backend chatter/attachments detection fix
  - frontend collaboration visibility fix
  - iteration report and state

rollback:
  - "git restore addons/smart_core/v2/services/ui_contract_service.py frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1810
