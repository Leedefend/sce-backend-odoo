task_id: ITER-2026-04-01-539
title: Narrow advanced-filter toggle count to actionable hidden items
type: frontend
status: active
priority: medium

goal: >
  Make the optimized toolbar's advanced-filter expand CTA reflect actionable
  hidden items instead of mixing those counts with static metadata chips.

user_visible_outcome:
  - "高级筛选展开按钮的数量更贴近实际可操作项"
  - "静态分面说明不再抬高高级筛选数量"
  - "保持纯显示层修复"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - display-only enhancement
  - no behavior changes
  - narrow the advanced-filter count to actionable hidden filter items only

context:
  branch: codex/next-round
  baseline_sha: 2a02eb6
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-538

architecture:
  layer_target: Frontend Layer
  module: native metadata list toolbar consumer
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The advanced-filter expand CTA currently sums hidden filters and static
    metadata chips together. The next low-risk display fix is to align that
    number with actionable hidden items only.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-539.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - advanced_filter_count_cleanup_requires_behavior_change

deliverables:
  - advanced_filter_toggle_count_cleanup
  - governance/report artifacts for ITER-2026-04-01-539

rollback:
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue
  - git restore agent_ops/tasks/ITER-2026-04-01-539.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-539.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-539.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-538
