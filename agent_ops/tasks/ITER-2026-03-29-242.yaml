task_id: ITER-2026-03-29-242
title: Preserve project form notebook and page containers during pruning
type: implementation_task
status: done
priority: high

goal: Fix project.project governed form layout pruning so notebook/page containers are kept when they contain surviving tab/page children, even if sibling child arrays are empty.
user_visible_outcome:
  - project detail live contract preserves Settings and 协作/系统 tabs
  - frontend can render notebook/page structure from the governed contract without fallback flattening

scope:
  allowed_paths:
    - addons/smart_core/utils/**
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/__manifest__.py

change_rules:
  - additive_or_minimal_fix_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend governance projection
  module: smart_core project form layout pruning
  reason: The current pruning logic drops notebook/page nodes too early because parser output may contain empty children arrays alongside non-empty tabs/pages arrays.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-242.yaml
    - python3 -m py_compile addons/smart_core/utils/contract_governance.py agent_ops/scripts/project_form_layout_field_gap_audit.py
    - make verify.smart_core
    - make restart
    - bash -lc 'export E2E_BASE_URL=http://127.0.0.1:8070; python3 agent_ops/scripts/project_form_layout_field_gap_audit.py --db sc_demo --login sc_fx_pm --password prod_like --container sc-backend-odoo-dev-odoo-1 --action-id 449 --record-id 3'

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - notebook_and_page_still_missing

deliverables:
  - governance_fix
  - audit_report
  - task_result

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-242.yaml addons/smart_core/utils/contract_governance.py agent_ops/reports/2026-03-29/report.ITER-2026-03-29-242.md agent_ops/state/task_results/ITER-2026-03-29-242.json docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-29-241

notes: "Implementation batch: repair notebook/page keep-node logic in project form layout pruning."
