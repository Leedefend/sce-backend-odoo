task_id: ITER-2026-04-01-573
title: Select sort summary fallback semantics as the next P1 native list usability family
type: frontend
status: active
priority: high
mode: screen

goal: >
  Confirm the next P1 native list usability family after search visibility
  alignment, prioritizing the gap that most directly misstates current list
  state during continued operation.

user_visible_outcome:
  - "明确下一条 P1 列表主链任务是排序摘要语义"
  - "优先处理会误导当前列表状态理解的问题"
  - "保持 screen 只做归类，不直接实现"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 3
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - P1 screen stage only
  - consume only existing list mainline artifacts
  - do not reopen repository scan
  - do not implement in this batch

context:
  branch: codex/next-round
  baseline_sha: 5af85a3
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-572
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: native list mainline usability screen
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    After route-preset and search visibility fixes, the remaining most direct
    mainline confusion point is sort summary fallback: the toolbar can still
    imply a current sort state even when no explicit sort metadata is present.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-573.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - screen_reads_new_product_files
  - screen_reopens_repo_scan
  - no_p1_mainline_candidate_remains

deliverables:
  - selected_next_p1_sort_summary_family
  - governance/report artifacts for ITER-2026-04-01-573

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-573.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-573.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-573.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-572
