task_id: ITER-2026-03-31-413
title: Execute and audit customer bootstrap result in sc_demo
type: audit
status: active
priority: high

goal: >
  Execute the new customer bootstrap entries in sc_demo and audit the resulting
  company, department, and user state so the customer delivery chain advances
  from "implemented" to "actually applied and observed".

user_visible_outcome:
  - "确认 bootstrap 已在 sc_demo 实际执行"
  - "确认公司、部门、用户主部门结果已落库"
  - "明确 deferred_extra_departments 的实际保留情况"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit/runtime execution only
  - no code changes in this batch
  - execute the bootstrap through the repository's controlled Odoo shell path

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Runtime Audit
  module: smart_construction_custom customer bootstrap
  module_owner: customer-specific delivery customization
  kernel_or_scenario: scenario
  reason: >
    The bootstrap code is implemented and verified syntactically. The next
    low-risk step is to execute it in sc_demo and audit the actual result.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-413.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - bootstrap_runtime_execution_failed
  - bootstrap_runtime_result_unclear

deliverables:
  - report and task result artifacts for ITER-2026-03-31-413
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-413.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-413.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-413.json

depends_on:
  - ITER-2026-03-31-412
