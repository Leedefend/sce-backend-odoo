task_id: ITER-2026-04-10-1801
title: notebook projection anomaly fallback fix

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 contract 存在 notebook/page 但前端 projected tabs=0 的异常，增加投影容错确保页签可渲染。

user_visible_outcome:
  - "即使 layout 结构有异常，tab 不再掉为 0"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1801.yaml
    - frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1801.md
    - agent_ops/state/task_results/ITER-2026-04-10-1801.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend runtime projection only
  - no backend mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: detailLayoutRuntime notebook/page projection
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    用户提供摘要显示 notebook/page 存在但 projected tabs=0，需要容错补偿确保页面可交付。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1801.yaml"
    - 'rg -n "buildNotebookShell|orphan page|synthetic notebook" frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts'
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - notebook/page projection fallback patch
  - iteration report and task result

rollback:
  - "git restore frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts"

depends_on:
  - ITER-2026-04-10-1800
