task_id: ITER-2026-03-29-120
title: Make scene-ready search mode consume parser semantics
type: governance_task
status: pending
priority: high

goal: Make scene_ready search surfaces derive search mode from parser search semantics instead of leaving search behavior implicit.
user_visible_outcome:
  - scene_ready contracts now expose semantic-driven search mode for faceted vs filter-bar search

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core scene_ready_contract_builder
  reason: Scene-ready contracts already consume parser semantics for search fields, filters, and searchpanel, but the runtime search behavior is still implicit. This iteration makes search_surface.mode semantic-driven from parser search semantics.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-120.yaml
    - python3 -m py_compile addons/smart_core/core/scene_ready_search_semantic_bridge.py addons/smart_core/tests/test_scene_ready_search_semantic_bridge.py addons/smart_core/tests/test_scene_ready_search_surface_semantic_consumption.py
    - python3 addons/smart_core/tests/test_scene_ready_search_semantic_bridge.py
    - python3 addons/smart_core/tests/test_scene_ready_search_surface_semantic_consumption.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-120.yaml addons/smart_core/core/scene_ready_search_semantic_bridge.py addons/smart_core/tests/test_scene_ready_search_semantic_bridge.py addons/smart_core/tests/test_scene_ready_search_surface_semantic_consumption.py

depends_on:
  - ITER-2026-03-29-119

notes: "Batch-3 continuation: scene_ready search mode should be derived from parser search semantics."
