task_id: ITER-2026-04-10-1731
title: frontend notebook tabs consumer alignment for form detail

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在前端表单消费链中补齐 notebook.tabs 兼容映射，确保 tabs 在缺少显式 type/page
  标记时仍可稳定渲染为页签结构。

user_visible_outcome:
  - "项目详情页按契约中的 notebook.tabs 展示页签结构"

scope:
  allowed_paths:
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/tasks/ITER-2026-04-10-1731.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1731.md
    - agent_ops/state/task_results/ITER-2026-04-10-1731.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 700

change_rules:
  - frontend consumer alignment only
  - no new intent
  - no backend business model/schema changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: frontend contract-consumer runtime
  module: ContractFormPage layout tree parser
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批不修改后端，仅对齐前端对既有语义结构的消费。
  reason: >
    1730 verify 已确认后端通过 notebook.tabs 提供 tab 语义，本批修复前端对该结构的鲁棒消费。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1731.yaml"

risk:
  level: medium-low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - notebook.tabs consumer compatibility patch

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1730
