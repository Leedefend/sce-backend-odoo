task_id: ITER-2026-04-09-1539
title: backend refactor E1 tier1 delegated-envelope signal implementation
type: product-implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  对 Tier-1 候选补齐委托型 envelope 信号识别，避免审计将显式委托入口误判为 no_envelope_signal。

user_visible_outcome:
  - "envelope 审计对 platform_scenes_api/platform_ui_contract_api 的判定从 no_signal 收敛为 delegated_envelope"

scope:
  allowed_paths:
    - scripts/verify/envelope_consistency_audit.py
    - artifacts/architecture/envelope_consistency_audit_v1.json
    - agent_ops/tasks/ITER-2026-04-09-1539.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1539.md
    - agent_ops/state/task_results/ITER-2026-04-09-1539.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 320

change_rules:
  - verify-logic implementation only
  - no runtime controller/model changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Governance verification layer
  module: envelope_consistency_audit
  module_owner: scripts verify
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    仅修改审计识别逻辑，不改变业务事实或接口行为。
  reason: >
    依据 1538 screen 结果，先修正 Tier-1 误判来源，再进入其余候选治理。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1539.yaml"
    - "python3 -m py_compile scripts/verify/envelope_consistency_audit.py"
    - "python3 scripts/verify/envelope_consistency_audit.py"
    - "python3 - <<'PY'\nimport json\nobj=json.load(open('artifacts/architecture/envelope_consistency_audit_v1.json','r',encoding='utf-8'))\nrows={r['file']:r for r in obj['rows']}\nassert rows['addons/smart_core/controllers/platform_scenes_api.py']['envelope_shape']=='delegated_envelope'\nassert rows['addons/smart_core/controllers/platform_ui_contract_api.py']['envelope_shape']=='delegated_envelope'\nprint('tier1 delegated signals PASS')\nPY"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - envelope delegated-signal recognition for Tier-1 candidates

rollback:
  - git restore scripts/verify/envelope_consistency_audit.py

depends_on:
  - ITER-2026-04-09-1538
