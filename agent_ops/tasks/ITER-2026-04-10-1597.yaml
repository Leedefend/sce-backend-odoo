task_id: ITER-2026-04-10-1597
title: backend refactor v2 app contract field guard audit
type: governance
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  为 v2 app contract 增加字段完整性与枚举合法性 guard，确保输出结构可审计、可门禁。

user_visible_outcome:
  - "v2 app catalog/nav/open 合同字段统一且可验证"

scope:
  allowed_paths:
    - addons/smart_core/v2/**
    - scripts/verify/v2_*audit.py
    - docs/architecture/backend_core_refactor_blueprint_v1.md
    - docs/architecture/backend_core_v2_rebuild_spec_v1.md
    - agent_ops/tasks/ITER-2026-04-10-1597.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1597.md
    - agent_ops/state/task_results/ITER-2026-04-10-1597.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 14
  max_output_lines: 700

change_rules:
  - v2 independent rebuild only
  - additive contract guard audit
  - no legacy runtime cutover

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Platform kernel independent rebuild layer
  module: smart_core v2 app contract guard
  module_owner: smart_core
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批聚焦契约结构治理，不改变业务事实模型。
  reason: >
    在连续重构链中冻结 app contract 关键字段与枚举口径，防止后续漂移。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1597.yaml"
    - "python3 scripts/verify/v2_rebuild_audit.py --json"
    - "python3 scripts/verify/v2_app_reason_code_audit.py --json"
    - "python3 scripts/verify/v2_app_contract_guard_audit.py --json"
    - 'rg -n "target_type|delivery_mode|is_clickable|availability_status|reason_code" addons/smart_core/v2/modules/app/builders/catalog_builder.py'
    - "rg -n \"app contract field guard batch\" docs/architecture/backend_core_refactor_blueprint_v1.md"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - v2 app contract field guard audit

rollback:
  - git restore scripts/verify/v2_app_contract_guard_audit.py docs/architecture/backend_core_refactor_blueprint_v1.md docs/architecture/backend_core_v2_rebuild_spec_v1.md

depends_on:
  - ITER-2026-04-10-1596
