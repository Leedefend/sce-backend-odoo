task_id: ITER-2026-04-03-921
title: Clean sc_prod_sim duplicate custom user seed conflict
type: backend
status: active
priority: medium
mode: implement

goal: >
  Resolve duplicate login conflict in sc_prod_sim so module upgrade no longer
  fails on smart_construction_custom customer user seed records.

user_visible_outcome:
  - "sc_prod_sim 下 mod.upgrade 不再被 customer_users 重复登录冲突阻断"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - environment_data_cleanup_only
  - no_repository_source_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-920
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: runtime data hygiene for customer seed
  module_owner: operations_runtime
  kernel_or_scenario: scenario
  backend_sub_layer_target: business-fact
  backend_sub_layer_reason: >
    Conflict is concrete runtime user fact duplication in target DB.
  reason: >
    Mis-targeted upgrade path exposed duplicate res.users login conflict for
    smart_construction_custom customer_users seed in sc_prod_sim.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-921.yaml
    - docker exec sc-backend-odoo-prod-sim-db-1 psql -U odoo -d sc_prod_sim -c "select login,count(*) from res_users where login in ('wutao','yangdesheng','zhangwencui','lijianfeng','yelingyue','lidexue','chenshuai') group by login order by login;"
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim MODULE=smart_construction_core DB_NAME=sc_prod_sim CODEX_NEED_UPGRADE=1 CODEX_MODULES=smart_construction_core make mod.upgrade

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed
  - uncertain_cleanup_scope

deliverables:
  - sc_prod_sim_duplicate_user_conflict_cleared

rollback:
  - runtime rollback by restoring DB snapshot if needed
  - git restore agent_ops/tasks/ITER-2026-04-03-921.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-921.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-921.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-920
