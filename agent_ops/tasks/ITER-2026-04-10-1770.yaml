task_id: ITER-2026-04-10-1770
title: form render-profile consumption spec freeze

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  落地 FORM-Batch4 / Task 104-2：冻结 form render-profile 消费规范，
  明确主真值源、派生 surface、优先级与前端消费规则。

user_visible_outcome:
  - "产出稳定的 render-profile 消费文档，避免前后端解释漂移"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1770.yaml
    - docs/architecture/form_contract_render_profile_v1.md
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1770.md
    - agent_ops/state/task_results/ITER-2026-04-10-1770.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - documentation freeze only
  - no runtime behavior changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend contract semantic layer
  module: form render-profile consumption specification
  module_owner: smart_core + frontend consumer
  kernel_or_scenario: kernel
  reason: >
    render_surfaces 已落地，需要冻结消费口径，确保前端后续迭代按同一优先级消费。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1770.yaml"
    - "rg -n \"render_surfaces|layout_source|fields as truth source|modifiers priority|actions priority\" docs/architecture/form_contract_render_profile_v1.md"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - render-profile consumption spec freeze doc

rollback:
  - "git restore docs/architecture/form_contract_render_profile_v1.md"

depends_on:
  - ITER-2026-04-10-1769
