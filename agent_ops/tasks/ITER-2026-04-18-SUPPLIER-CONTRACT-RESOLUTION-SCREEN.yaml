task_id: ITER-2026-04-18-SUPPLIER-CONTRACT-RESOLUTION-SCREEN
title: Screen unresolved supplier contract resolution for imported payments
type: governance-screen
status: pass
priority: high
mode: screen

goal: >
  Screen unresolved legacy_supplier_contract_id evidence on imported payment
  request lines and classify whether it can deterministically resolve to target
  construction.contract records.

user_visible_outcome:
  - "确认单一旧供应商合同ID是否能安全解析成新系统合同，为后续是否回放合同依据提供边界。"

scope:
  allowed_paths:
    - docs/ops/business_continuity/supplier_contract_resolution_screen_v1.md
    - docs/ops/iterations/delivery_context_switch_log_v1.md
    - agent_ops/tasks/ITER-2026-04-18-SUPPLIER-CONTRACT-RESOLUTION-SCREEN.yaml
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**
    - scripts/**
    - security/**
    - record_rules/**
    - ir.model.access.csv
    - __manifest__.py

context:
  branch: codex/next-round
  baseline_sha: d4934c0
  prior_task: ITER-2026-04-18-LEGACY-PAYMENT-BASIS-EVIDENCE-SCREEN

architecture:
  layer_target: Business Fact Screening
  module: supplier contract resolution facts
  module_owner: construction industry business continuity
  kernel_or_scenario: scenario
  backend_sub_layer_target: business-fact layer
  backend_sub_layer_reason: >
    Resolving legacy supplier contract IDs to target contracts is business-fact
    evidence. This screen does not change backend semantics or UI structure.
  reason: >
    The previous screen found 1731 payment requests with a single unresolved
    legacy supplier contract ID. A read-only resolution screen is required
    before any high-risk write replay.

change_rules:
  - read-only database screening only
  - do not write payment, contract, settlement, account, ACL, manifest, frontend, or migration data
  - only classify exact-one target contract resolution as deterministic
  - require project, partner, and direction compatibility before write eligibility

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-18-SUPPLIER-CONTRACT-RESOLUTION-SCREEN.yaml
    - read-only Odoo shell supplier contract resolution scan against sc_demo

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - database_write_attempted
  - resolution_rule_unclear

deliverables:
  - screen document with resolution counts and write/no-write recommendation

verification_result:
  status: PASS
  commands:
    - command: python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-18-SUPPLIER-CONTRACT-RESOLUTION-SCREEN.yaml
      result: PASS
    - command: read-only Odoo shell supplier contract resolution scan against sc_demo
      result: PASS
      summary: >
        1731 payment requests with a single unresolved legacy supplier contract
        ID have no target construction.contract match and no ir.model.data match.
    - command: host-side manifest comparison with supplier_contract_external_id_manifest_v1.json
      result: PASS
      summary: >
        1556 unique legacy IDs are absent from the supplier contract external-id
        manifest; 0 loadable or blocked manifest matches.

rollback:
  - git restore docs/ops/business_continuity/supplier_contract_resolution_screen_v1.md docs/ops/iterations/delivery_context_switch_log_v1.md agent_ops/tasks/ITER-2026-04-18-SUPPLIER-CONTRACT-RESOLUTION-SCREEN.yaml

depends_on:
  - ITER-2026-04-18-LEGACY-PAYMENT-BASIS-EVIDENCE-SCREEN
