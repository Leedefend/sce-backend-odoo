task_id: ITER-2026-04-10-1765
title: form stat button separation closure in runtime contract

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  落地 FORM-Batch2 / Task 102-2：在 ui.contract 运行态中区分 button_box 与 stat_buttons，
  让统计按钮从通用按钮区分离并提供可消费语义。

user_visible_outcome:
  - "form contract 提供 stat_buttons，且与 button_box 不重复"
  - "stat button 输出 label/clickable/payload/stat_field/widget 语义"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1765.yaml
    - addons/smart_core/v2/services/ui_contract_service.py
    - scripts/verify/form_stat_button_audit.py
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1765.md
    - agent_ops/state/task_results/ITER-2026-04-10-1765.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 1200

change_rules:
  - backend contract semantic layer only
  - additive compatibility-safe changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend contract semantic layer
  module: form surface regions / stat button semantics
  module_owner: smart_core v2 service + verify script
  kernel_or_scenario: kernel
  reason: >
    当前 form contract 中 stat_buttons 长期为空，统计类按钮与 button_box 混用，
    前端无法稳定消费原生统计按钮语义。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1765.yaml"
    - "python3 scripts/verify/form_stat_button_audit.py --json --input tmp/json/form.json"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - stat button separation patch
  - runtime stat button audit artifact

rollback:
  - "git restore addons/smart_core/v2/services/ui_contract_service.py"
  - "git restore scripts/verify/form_stat_button_audit.py"

depends_on:
  - ITER-2026-04-10-1764
