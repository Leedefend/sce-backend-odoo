task_id: ITER-2026-03-31-438
title: Freeze transition plan for smart_construction_bootstrap taxonomy migration
type: governance
status: active
priority: medium

goal: >
  Freeze the lowest-risk transition plan for moving smart_construction_bootstrap
  into a neutral platform/bootstrap namespace, including the target module name,
  compatibility shim lifetime, and dependency/script/document migration order.

user_visible_outcome:
  - "明确最终目标模块名"
  - "明确兼容 shim 的保留条件与退出条件"
  - "明确 manifest / script / docs 的迁移顺序"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - read-only governance batch
  - no code changes
  - no manifest changes
  - no dependency changes

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Module Transition Governance
  module: smart_construction_bootstrap
  module_owner: repository taxonomy and upgrade path
  kernel_or_scenario: kernel
  reason: >
    The migration strategy is already chosen, so the next low-risk step is to
    freeze the exact transition plan before any real rename or relocation is
    attempted.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-438.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - repository_facts_are_insufficient_to_freeze_transition_order

deliverables:
  - implementation/report/result artifacts for ITER-2026-03-31-438

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-438.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-438.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-438.json

depends_on:
  - ITER-2026-03-31-437
