task_id: ITER-2026-04-10-1745
title: create-edit-readonly interaction regression verify

type: implementation
status: active
priority: high
mode: verify
batch: 1/1

goal: >
  对 create/edit/readonly 三态做交互回归验证，确认按钮可见性与写权限语义在 contract+consumer 层一致。

user_visible_outcome:
  - "输出三态交互回归证据并给出 PASS/BLOCKED"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1745.yaml
    - scripts/verify/form_render_profile_interaction_regression_audit.py
    - artifacts/contract/form_render_profile_interaction_regression_v1.json
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1745.md
    - agent_ops/state/task_results/ITER-2026-04-10-1745.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 900

change_rules:
  - verify-only batch
  - no business model/schema changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: render_profile interaction regression audit
  module_owner: frontend/apps/web + scripts/verify
  kernel_or_scenario: scenario
  reason: >
    1744 已修复消费链，需补一次三态交互回归证据，确保交付可验。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1745.yaml"
    - "python3 scripts/verify/form_render_profile_interaction_regression_audit.py --json"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - render profile interaction regression artifact

rollback:
  - "git restore scripts/verify/form_render_profile_interaction_regression_audit.py"

depends_on:
  - ITER-2026-04-10-1744
