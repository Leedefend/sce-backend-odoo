task_id: ITER-2026-04-07-1221
title: Environment repair lane batch8 runtime process and entry diagnostics
type: governance
status: completed
priority: high
mode: verify

goal: >
  Capture runtime process and entry diagnostics around 8069 handshake failures
  using compose service status and recent service logs.

user_visible_outcome:
  - "runtime 进程与入口日志证据补齐"
  - "服务链修复动作更聚焦"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1221.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_environment_repair_lane_batch8_process_entry_diagnostics_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 260
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - evidence-only batch
  - no source code mutation

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_environment_repair_batch8

architecture:
  layer_target: Governance Monitoring
  module: runtime process and entry diagnostics
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch inspects service health and entry logs around handshake window.
  reason: >
    Validate whether runtime failure is process/entry-chain related rather than
    pure network/listener mismatch.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1221.yaml
    - make verify.scene.legacy_auth.runtime_probe
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime environment repair lane batch8 process-entry diagnostics evidence

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1221.yaml
  - git restore docs/audit/native/native_runtime_environment_repair_lane_batch8_process_entry_diagnostics_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1220
