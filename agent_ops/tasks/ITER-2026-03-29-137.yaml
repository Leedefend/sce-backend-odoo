task_id: ITER-2026-03-29-137
title: Project search mode in scene merge policy
type: governance_task
status: pending
priority: high

goal: Make scene merge resolver treat search_policy.default_mode as a first-class search semantic during policy merge.
user_visible_outcome:
  - policy-driven faceted or filter_bar search mode now survives merge as a canonical scene search semantic

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core scene_merge_resolver
  reason: Scene merge resolver already handles default filters, group_by and searchpanel in policy merge, but still ignores policy-level search mode. This iteration brings default_mode into canonical merge behavior.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-137.yaml
    - python3 -m py_compile addons/smart_core/core/scene_merge_resolver.py addons/smart_core/tests/test_scene_merge_resolver_search_policy_mode.py
    - python3 addons/smart_core/tests/test_scene_merge_resolver_search_policy_mode.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-137.yaml addons/smart_core/core/scene_merge_resolver.py addons/smart_core/tests/test_scene_merge_resolver_search_policy_mode.py

depends_on:
  - ITER-2026-03-29-136

notes: "Batch-3 continuation: policy merge should no longer leave search mode outside the canonical search semantic surface."
