task_id: ITER-2026-04-10-1719
title: sidebar compact convergence for delivery shell

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  收敛左侧导航壳层密度，降低宽度与层级噪音，保持关键导航可用。

user_visible_outcome:
  - "左侧导航更紧凑，主内容区可视宽度提升"
  - "非核心侧栏信息默认收敛"

scope:
  allowed_paths:
    - frontend/apps/web/src/layouts/AppShell.vue
    - agent_ops/tasks/ITER-2026-04-10-1719.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1719.md
    - agent_ops/state/task_results/ITER-2026-04-10-1719.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 700

change_rules:
  - frontend shell sidebar compact only
  - no backend changes
  - no contract schema changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: sidebar compact delivery shell
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批仅前端展示密度收敛，不改业务事实语义。
  reason: >
    用户确认继续收敛，要求进一步压缩左侧导航占用并减少视觉噪音。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1719.yaml"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - compact sidebar shell behavior

rollback:
  - "git restore frontend/apps/web/src/layouts/AppShell.vue"

depends_on:
  - ITER-2026-04-10-1718
