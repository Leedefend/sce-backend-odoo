task_id: ITER-2026-04-01-536
title: Hide native default sort from the active-condition summary
type: frontend
status: active
priority: medium

goal: >
  Keep the native list toolbar's active-condition summary focused on
  user-applied state by hiding the native default sort chip while preserving
  visibility for non-default sort choices.

user_visible_outcome:
  - "当前条件不再因为默认排序而显得非空"
  - "用户手动改过排序时仍能看到排序状态"
  - "保持纯显示层修复"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - display-only enhancement
  - no behavior changes
  - preserve explicit sort visibility when the sort is not the native default

context:
  branch: codex/next-round
  baseline_sha: 2a02eb6
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-535

architecture:
  layer_target: Frontend Layer
  module: native metadata list toolbar consumer
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The current active-condition summary treats native default sort like an
    applied filter. The next low-risk display fix is to hide only that default
    sort state while keeping non-default sort visibility intact.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-536.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - default_sort_visibility_fix_requires_behavior_change

deliverables:
  - active_condition_default_sort_cleanup
  - governance/report artifacts for ITER-2026-04-01-536

rollback:
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue
  - git restore agent_ops/tasks/ITER-2026-04-01-536.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-536.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-536.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-535
