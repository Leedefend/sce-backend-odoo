task_id: ITER-2026-03-31-400
title: Audit smart_construction_custom ownership against boundary responsibilities
type: audit
status: completed
priority: high

goal: >
  Classify the contents of `smart_construction_custom` by boundary ownership so
  the module can be cleaned up later under a governed high-risk objective,
  without directly modifying security or ACL files in the current low-risk loop.

user_visible_outcome:
  - "明确 smart_construction_custom 里哪些内容符合当前边界职责"
  - "明确哪些内容应迁往独立角色治理、demo 装配或核心模块"
  - "给出后续受控改造顺序，不直接碰高风险权限文件"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/smart_construction_custom/**
    - addons/**/security/**
    - addons/**/ir.model.access.csv
    - addons/**/record_rules/**
    - addons/**/__manifest__.py
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no implementation changes
  - classify by ownership boundary instead of code-style opinion
  - explicitly respect the repository stop rule for security and ACL paths

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Governance Audit
  module: smart_construction_custom
  module_owner: security governance / demo bootstrap
  kernel_or_scenario: scenario
  reason: >
    The user wants this module aligned to the designed boundary duties, but the
    module currently consists mainly of security groups, ACLs, and post-init
    policy hooks. Those paths are high-risk under AGENTS.md, so the next valid
    step is an ownership audit rather than direct implementation.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-400.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - security_boundary_classification_unclear

deliverables:
  - ownership classification for groups, ACLs, server actions, hooks, and demo user mapping
  - report and task result artifacts for ITER-2026-03-31-400
  - delivery log checkpoint with a governed next-step suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-400.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-400.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-400.json

depends_on:
  - ITER-2026-03-31-399
