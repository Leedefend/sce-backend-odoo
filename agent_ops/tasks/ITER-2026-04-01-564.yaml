task_id: ITER-2026-04-01-564
title: Verify advanced-filter toggle count alignment on the native list surface
type: frontend
status: active
priority: medium
mode: verify

goal: >
  Verify that the advanced-filter toggle count alignment stays within the
  trusted frontend verification gate and does not alter runtime behavior.

user_visible_outcome:
  - "确认高级筛选数量对齐仍是低风险前端改动"
  - "保持 native list surface 的可信 smoke 绿色"
  - "为下一轮 fresh candidate 决策提供干净状态"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - verify stage only
  - do not make new product edits in this batch
  - only run declared validation commands
  - do not open new scan logic in this batch

context:
  branch: codex/next-round
  baseline_sha: 8992446
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-563
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true

architecture:
  layer_target: Frontend Layer
  module: native metadata list toolbar verification
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The advanced-filter count change is intended to remain a bounded
    display-only refinement. Verification must confirm that the trusted native
    list surface gates still pass.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-564.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.v0_5.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - trusted_container_verify_failed

deliverables:
  - verification_result_for_advanced_filter_count_alignment
  - governance/report artifacts for ITER-2026-04-01-564

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-564.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-564.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-564.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-563
