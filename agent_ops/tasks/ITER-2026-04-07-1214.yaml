task_id: ITER-2026-04-07-1214
title: Runtime environment repair execute with dedicated auth runtime probe
type: governance
status: completed
priority: high
mode: execute

goal: >
  Add a dedicated runtime auth probe utility to capture endpoint transport/status
  signals for `/api/scenes/my` across candidate local base URLs.

user_visible_outcome:
  - "runtime 探针可复用"
  - "环境修复证据可重复采集"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1214.yaml
    - scripts/verify/scene_legacy_auth_runtime_probe.py
    - Makefile
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_environment_repair_lane_batch1_probe_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 10
  max_candidates: 12
  max_output_lines: 240
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify tooling only
  - no business/acl semantics changes

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_environment_repair_batch1

architecture:
  layer_target: Governance Monitoring
  module: runtime probe tooling
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Utility gathers runtime endpoint transport/status evidence only.
  reason: >
    Provide deterministic input for environment repair lane without touching
    business code.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1214.yaml
    - make verify.scene.legacy_auth.runtime_probe
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime environment repair lane batch1 probe evidence

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1214.yaml
  - git restore scripts/verify/scene_legacy_auth_runtime_probe.py
  - git restore Makefile
  - git restore docs/audit/native/native_runtime_environment_repair_lane_batch1_probe_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1213
