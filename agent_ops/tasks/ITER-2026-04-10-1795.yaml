task_id: ITER-2026-04-10-1795
title: tab duplication and business-tab loss root fix

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 tab 重复观感和业务 tab 丢失：消除 tab 容器头部重复标题，并放宽 tab 过滤规则保留业务页签。

user_visible_outcome:
  - "tab 不再出现容器标题与页签名重复"
  - "有业务意义的 tab 不再被误过滤"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1795.yaml
    - frontend/apps/web/src/components/template/DetailShellLayout.vue
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1795.md
    - agent_ops/state/task_results/ITER-2026-04-10-1795.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 1200

change_rules:
  - frontend consumer runtime only
  - no backend contract mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: detail shell/tab projection and rendering
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    用户反馈 tab 明显重复且业务页签缺失，需要从渲染层重复和过滤策略两处做根因修复。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1795.yaml"
    - 'rg -n "shell.tabs|filteredTabs|normalizeTabLabel" frontend/apps/web/src/components/template/DetailShellLayout.vue frontend/apps/web/src/pages/ContractFormPage.vue'
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - tab duplication/loss root-fix patch
  - iteration report and state

rollback:
  - "git restore frontend/apps/web/src/components/template/DetailShellLayout.vue frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1794
