task_id: ITER-2026-03-31-445
title: Migrate baseline and demo verify scripts to smart_platform_bootstrap canonical semantics
type: implementation
status: active
priority: high

goal: >
  Update baseline and demo verification so smart_platform_bootstrap becomes the
  canonical bootstrap module signal, while still keeping compatibility awareness
  for smart_construction_bootstrap during the transition phase.

user_visible_outcome:
  - "verify.baseline / verify.demo 以 smart_platform_bootstrap 为主信号"
  - "兼容期仍能识别 smart_construction_bootstrap 的存在"

scope:
  allowed_paths:
    - scripts/verify/baseline.sh
    - scripts/verify/demo.sh
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - scripts/db/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - verify-only script migration batch
  - do not change reset/install scripts
  - keep compatibility awareness for the shim

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Platform Bootstrap Module Migration
  module: verify script semantics
  module_owner: repository taxonomy and upgrade path
  kernel_or_scenario: kernel
  reason: >
    Script ownership is frozen, so the next downstream transition step is to
    update verify semantics while keeping reset/install behavior unchanged.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-445.yaml
    - make verify.baseline DB_NAME=sc_demo
    - make verify.demo DB_NAME=sc_demo

risk:
  level: high
  manual_approval_required: false

stop_conditions:
  - verify_semantics_cannot_express_canonical_plus_compatibility_state_cleanly
  - verify_changes_require_reset_script_changes_in_same_batch

deliverables:
  - implementation/report/result artifacts for ITER-2026-03-31-445

rollback:
  - git restore scripts/verify/baseline.sh
  - git restore scripts/verify/demo.sh
  - git restore agent_ops/tasks/ITER-2026-03-31-445.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-445.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-445.json

depends_on:
  - ITER-2026-03-31-444
