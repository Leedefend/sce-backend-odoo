task_id: ITER-2026-04-01-522
title: Screen remaining natural candidates on the config-admin governance line
type: permission-governance
status: active
priority: medium

goal: >
  After closing scene/subscription, quota-import, and workflow, determine
  whether another natural uncovered low-risk config-admin family still remains
  on the current governance objective.

user_visible_outcome:
  - "继续扩展 config_admin / 平台治理面 覆盖"
  - "筛出是否还存在自然未覆盖的治理家族"
  - "避免把同一治理面内部重复家族重复计入"

scope:
  allowed_paths:
    - addons/smart_construction_core/**
    - addons/smart_construction_custom/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit-first governance screening batch
  - implementation changes are out of scope unless a later narrow batch is opened
  - avoid double-counting families already closed on the config-admin line

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom
  explicit_user_authorization: true

architecture:
  layer_target: Platform Governance Acceptance
  module: next uncovered config-admin family screen
  module_owner: platform governance surface
  kernel_or_scenario: scenario
  reason: >
    Three representative config-admin families are now closed. The next safe
    step is to determine whether another natural low-risk governance family
    remains or whether the current objective line is naturally closed.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-522.yaml
    - "repository screen narrows the remaining config-admin candidates"
    - "runtime screen confirms whether another representative governance family remains"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - remaining_candidates_require_acl_or_manifest_changes
  - runtime_cannot_classify_remaining_governance_candidates_with_confidence
  - the_current_config_admin_line_has_naturally_closed

deliverables:
  - next uncovered config-admin candidate screen
  - governance/report artifacts for ITER-2026-04-01-522

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-522.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-522.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-522.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-521
