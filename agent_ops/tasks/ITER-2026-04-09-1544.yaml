task_id: ITER-2026-04-09-1544
title: backend refactor A1 intent registry missing surfaces screen v1
type: governance
status: active
priority: high
mode: screen
batch: 1/1

goal: >
  对 intent_registry 缺口（missing_from_registry）做 screen 分类并冻结后续迁移顺序。

user_visible_outcome:
  - "intent registry 迁移有明确分层和批次顺序"

scope:
  allowed_paths:
    - artifacts/architecture/intent_registry_audit_v1.json
    - artifacts/architecture/intent_registry_missing_screen_v1.json
    - docs/architecture/intent_registry_missing_screen_v1.md
    - agent_ops/tasks/ITER-2026-04-09-1544.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1544.md
    - agent_ops/state/task_results/ITER-2026-04-09-1544.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 7
  max_output_lines: 320

change_rules:
  - screen stage only
  - classify from existing intent audit artifact only
  - no repo re-scan
  - no implementation changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Governance screening layer
  module: intent registry missing surfaces
  module_owner: architecture governance
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    仅做治理分类，不修改业务事实与运行时代码。
  reason: >
    为 A1 registry coverage 收敛提供低风险、可执行的分批路线。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1544.yaml"
    - "python3 - <<'PY'\nimport json\nobj=json.load(open('artifacts/architecture/intent_registry_missing_screen_v1.json','r',encoding='utf-8'))\nassert obj['status']=='PASS'\nassert obj['summary']['missing_count']==42\nprint('intent screen artifact PASS')\nPY"
    - "rg -n \"Tier-1|Tier-2|Tier-3|next_batch\" docs/architecture/intent_registry_missing_screen_v1.md"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - classification_uncertainty

deliverables:
  - intent registry missing surfaces screen classification

rollback:
  - git restore artifacts/architecture/intent_registry_missing_screen_v1.json docs/architecture/intent_registry_missing_screen_v1.md

depends_on:
  - ITER-2026-04-09-1543
