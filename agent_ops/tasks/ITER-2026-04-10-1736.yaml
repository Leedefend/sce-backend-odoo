task_id: ITER-2026-04-10-1736
title: form action source placement remediation

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 FORM-006 审计阻断：统一 form action 的 surface placement，避免 button_box 与 stat_buttons
  重复承载同一动作。

user_visible_outcome:
  - "同一动作在 form 契约中只由一个主区域承载"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1736.yaml
    - addons/smart_core/app_config_engine/services/contract_service.py
    - addons/smart_core/v2/services/ui_contract_service.py
    - artifacts/contract/form_action_dedup_v1.json
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1736.md
    - agent_ops/state/task_results/ITER-2026-04-10-1736.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 7
  max_output_lines: 900

change_rules:
  - backend contract placement remediation only
  - no business model/schema changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: backend scene-orchestration contract supply
  module: form action source and placement normalization
  module_owner: smart_core
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    只修复动作语义组织，不改业务事实。
  reason: >
    1735 审计确认 button_box/stat_buttons 重复承载同一动作，需要主从去重。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1736.yaml"
    - "python3 scripts/verify/form_action_dedup_audit.py --json"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - action placement dedup remediation patch

rollback:
  - "git restore addons/smart_core/app_config_engine/services/contract_service.py"
  - "git restore addons/smart_core/v2/services/ui_contract_service.py"

depends_on:
  - ITER-2026-04-10-1735
