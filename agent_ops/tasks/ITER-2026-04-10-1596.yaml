task_id: ITER-2026-04-10-1596
title: backend refactor v2 app reason_code enum audit and contract guard
type: governance
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  为 v2 app 契约增加 reason_code 枚举审计与 contract guard，固化可用性语义边界。

user_visible_outcome:
  - "v2 app reason_code 输出进入可审计、可门禁状态"

scope:
  allowed_paths:
    - addons/smart_core/v2/**
    - scripts/verify/v2_*audit.py
    - docs/architecture/backend_core_refactor_blueprint_v1.md
    - docs/architecture/backend_core_v2_rebuild_spec_v1.md
    - agent_ops/tasks/ITER-2026-04-10-1596.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1596.md
    - agent_ops/state/task_results/ITER-2026-04-10-1596.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 14
  max_output_lines: 620

change_rules:
  - v2 independent rebuild only
  - additive guard and enum audit
  - no legacy runtime cutover

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Platform kernel independent rebuild layer
  module: smart_core v2 app reason code governance
  module_owner: smart_core
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批只增加语义治理守卫，不改变业务事实层。
  reason: >
    连续重构模式下收紧契约语义边界，避免后续漂移。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1596.yaml"
    - "python3 scripts/verify/v2_rebuild_audit.py --json"
    - "python3 scripts/verify/v2_app_reason_code_audit.py --json"
    - 'rg -n "APP_KEY_MISSING|WORKSPACE_WORK_PARTIAL|reason_code" addons/smart_core/v2/modules/app'
    - "rg -n \"reason_code enum audit batch\" docs/architecture/backend_core_refactor_blueprint_v1.md"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - v2 app reason_code enum audit and contract guard

rollback:
  - git restore addons/smart_core/v2 scripts/verify/v2_app_reason_code_audit.py docs/architecture/backend_core_refactor_blueprint_v1.md docs/architecture/backend_core_v2_rebuild_spec_v1.md

depends_on:
  - ITER-2026-04-10-1595
