task_id: ITER-2026-04-01-569
title: Verify native list route-preset visibility after the usability-first fix
type: frontend
status: active
priority: high
mode: verify

goal: >
  Verify that the route-preset visibility fix stays within the trusted native
  list gate and keeps the recommended-filter context usable after the toolbar
  rendering adjustment.

user_visible_outcome:
  - "确认推荐筛选可见性修复通过可信门禁"
  - "保持 native list 主链可继续操作"
  - "为下一张 P1 主链筛选提供干净状态"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - verify stage only
  - do not make new product edits in this batch
  - run only declared validation commands
  - do not reopen repository scan in this batch

context:
  branch: codex/next-round
  baseline_sha: ed77101
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-568
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: native list toolbar route-preset verification
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The route-preset visibility follow-up is prioritized for usability rather
    than cosmetic cleanup. Verification must confirm that the list mainline
    stays green after the toolbar rendering change.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-569.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.v0_5.container

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - trusted_container_verify_failed

deliverables:
  - verification_result_for_route_preset_visibility_fix
  - governance/report artifacts for ITER-2026-04-01-569

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-569.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-569.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-569.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-568
