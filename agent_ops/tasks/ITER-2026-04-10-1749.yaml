task_id: ITER-2026-04-10-1749
title: form business semantic group label convergence

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  将 form 仍存在的弱语义分组标签（如“任务名称/已启用”）收敛为业务可交付标签，
  保障前端消费链直接展示业务语义。

user_visible_outcome:
  - "form 分组标题更贴近业务语义，可直接用于前端交付"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1749.yaml
    - addons/smart_core/app_config_engine/services/contract_service.py
    - addons/smart_core/v2/services/ui_contract_service.py
    - scripts/verify/form_layout_alignment_audit.py
    - artifacts/contract/form_layout_alignment_v1.json
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1749.md
    - agent_ops/state/task_results/ITER-2026-04-10-1749.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 8
  max_output_lines: 1000

change_rules:
  - contract semantic-label mapping only
  - no business model/schema changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: backend scene-orchestration contract supply
  module: form group semantic label mapping
  module_owner: smart_core
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批仅做语义组织供给，不涉及业务事实变更。
  reason: >
    最新 form.json 结构已完整，但分组仍存在弱语义标签，需要再收敛一轮以便前端直接消费。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1749.yaml"
    - "python3 scripts/verify/form_layout_alignment_audit.py --json"

risk:
  level: medium-low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - form business semantic group label mapping patch

rollback:
  - "git restore addons/smart_core/app_config_engine/services/contract_service.py"
  - "git restore addons/smart_core/v2/services/ui_contract_service.py"

depends_on:
  - ITER-2026-04-10-1748

