task_id: ITER-2026-03-28-028
title: Remove local scratch documents from the repository worktree
type: governance_task
status: pending
priority: low

goal: Keep the repository clean by excluding and removing local scratch documentation files that are not part of the governed delivery surface.
user_visible_outcome:
  - local scratch docs no longer appear in git status
  - future sessions will ignore the same scratch file names
  - the worktree is clean enough to resume governed continuous iteration

scope:
  allowed_paths:
    - .gitignore
    - agent_ops/tasks/**
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
    - CURRENT_COMPLETION_SUMMARY_2026-03-23.md
    - SANDBOX_SETUP_INSTRUCTIONS.md
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Governance Layer
  module: local scratch artifact cleanup
  reason: Remove non-governed local scratch docs so the repository can return to a clean governed execution state.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-028.yaml
    - git status --short

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore .gitignore agent_ops/tasks/ITER-2026-03-28-028.yaml docs/ops/iterations/delivery_context_switch_log_v1.md docs/ops/releases/archive/temp/TEMP_agent_ops_continuous_iteration_status_20260328.md

depends_on:
  - ITER-2026-03-28-027
notes: "Deletes local scratch docs after they were explicitly confirmed as disposable."
