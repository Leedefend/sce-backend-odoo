task_id: ITER-2026-04-03-972
title: Execute formal publish checklist gates on active branch
type: governance
status: active
priority: high
mode: verify

goal: >
  Execute the formal release publish checklist gate chain after delivery-engine
  recovery and produce final publishability verdict with evidence.

user_visible_outcome:
  - "给出当前分支是否可正式发布的最终门禁结论"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verification_and_reporting_only

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-971
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: formal publish checklist execution
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This is a verification/reporting batch over release gates and does not
    alter runtime semantics.
  reason: >
    User approved proceeding with formal publish checklist execution after
    recovery gates turned green.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-972.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim make restart
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost API_BASE_URL=http://127.0.0.1:18069 DB_NAME=sc_prod_sim E2E_LOGIN=svc_e2e_smoke E2E_PASSWORD=demo make verify.release.execution_protocol.v1
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost API_BASE_URL=http://127.0.0.1:18069 DB_NAME=sc_prod_sim E2E_LOGIN=svc_e2e_smoke E2E_PASSWORD=demo make verify.release.delivery_engine.v1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed

deliverables:
  - formal_publish_checklist_verdict

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-972.yaml

depends_on:
  - ITER-2026-04-03-971
