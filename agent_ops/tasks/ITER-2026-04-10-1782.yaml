task_id: ITER-2026-04-10-1782
title: fix form layout tree global dedup collapse

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 form layout tree 投影中的全局字段去重导致的结构塌缩问题，
  确保 notebook/page/group 不因重复字段被误剪枝。

user_visible_outcome:
  - "页签与分组不再因为重复字段被渲染为空"
  - "项目详情页能看到更完整的结构层级"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1782.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1782.md
    - agent_ops/state/task_results/ITER-2026-04-10-1782.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend runtime logic only
  - no backend semantic mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: form layout tree projection
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    用户反馈持续“没有页面结构”，排查发现 layoutTrees 全局 dedup 可能导致后续 page/group 字段被吃掉。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1782.yaml"
    - "rg -n \"layoutTrees|buildFieldNode|used.has|used.add\" frontend/apps/web/src/pages/ContractFormPage.vue"
    - "make frontend.restart"

risk:
  level: medium-low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - dedup collapse fix patch
  - iteration report and task result

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1781
