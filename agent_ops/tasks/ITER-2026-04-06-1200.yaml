task_id: ITER-2026-04-06-1200
title: Stage-A Batch3 broader key-entry evidence aggregation
type: governance
status: completed
priority: high
mode: execute

goal: >
  Execute Stage-A Batch3 with broader key-entry evidence aggregation while
  maintaining the same short-chain guard set.

user_visible_outcome:
  - "Stage-A Batch3 关键入口证据聚合"
  - "短链门禁稳定 PASS"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-06-1200.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_stage_a_regression_batch3_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 180
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - evidence-only updates
  - no runtime code changes

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_stage_a_batch3

architecture:
  layer_target: Governance Monitoring
  module: stage-a key-entry evidence aggregation
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch extends entry evidence coverage without changing business facts.
  reason: >
    Continue Stage-A roadmap with broader native entry evidence and stable short-chain gates.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-06-1200.yaml
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - stage-a batch3 key-entry evidence document

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-06-1200.yaml
  - git restore docs/audit/native/native_stage_a_regression_batch3_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-06-1199
