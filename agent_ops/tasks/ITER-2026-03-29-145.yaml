task_id: ITER-2026-03-29-145
title: Guard canonical action counts in scene ready strict mode
type: governance_task
status: pending
priority: high

goal: Make scene ready strict contract guard require canonical action_surface counts once action surfaces are materialized.
user_visible_outcome:
  - strict-mode scene-ready contracts now treat missing action count metadata as incomplete canonical action surface

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core scene_ready_contract_builder
  reason: Scene ready strict contract guard already requires primary_actions, groups and selection_mode, but still ignores the canonical action counts that the same builder materializes. This iteration keeps strict readiness aligned with the full action surface shape.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-145.yaml
    - python3 -m py_compile addons/smart_core/core/scene_ready_contract_builder.py addons/smart_core/tests/test_scene_ready_strict_contract_guard.py
    - python3 addons/smart_core/tests/test_scene_ready_strict_contract_guard.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-145.yaml addons/smart_core/core/scene_ready_contract_builder.py addons/smart_core/tests/test_scene_ready_strict_contract_guard.py

depends_on:
  - ITER-2026-03-29-144

notes: "Batch acceleration: continue grouped strict-guard alignment inside scene_ready by requiring canonical action count metadata together with the rest of action_surface."
