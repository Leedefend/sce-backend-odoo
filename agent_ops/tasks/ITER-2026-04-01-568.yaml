task_id: ITER-2026-04-01-568
title: Restore route-preset visibility on the native list toolbar under usability-first priority
type: frontend
status: active
priority: high

goal: >
  Preserve route-preset visibility on the native list toolbar in both optimized
  and non-optimized compositions so users do not lose the active recommended
  filter context while navigating the list.

user_visible_outcome:
  - "推荐筛选在 native list toolbar 中不再消失"
  - "优化态下保留独立 route-preset 可见性与清除入口"
  - "不把 preset 重新塞回当前条件 chips"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - usability-first structural follow-up
  - do not modify backend contracts
  - keep route-preset visibility separate from active-condition chips

context:
  branch: codex/next-round
  baseline_sha: ed77101
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-567
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: native list toolbar route-preset visibility
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    Recommended filter context is part of the list usability mainline. If the
    toolbar hides the route preset, users lose both provenance and the clear
    affordance needed to continue operating confidently.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-568.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.v0_5.container

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - trusted_container_verify_failed
  - route_preset_fix_requires_backend_contract_change

deliverables:
  - bounded_route_preset_visibility_fix
  - governance/report artifacts for ITER-2026-04-01-568

rollback:
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue
  - git restore agent_ops/tasks/ITER-2026-04-01-568.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-568.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-568.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-567
