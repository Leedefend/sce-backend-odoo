task_id: ITER-2026-03-31-404
title: Normalize workbook users into department post and system-role mapping
type: audit
status: completed
priority: high

goal: >
  Convert the current customer workbook into a user-mapping draft that assigns
  each visible user to department, post, and system-role layers, with explicit
  identification of project-department-only members where possible.

user_visible_outcome:
  - "形成用户归属映射草案"
  - "明确用户对应的部门、岗位、系统角色"
  - "尽量识别仅属于项目部的人员"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no implementation changes
  - use the confirmed department/post/role rules from ITER-2026-03-31-403
  - derive a user mapping draft from the workbook without pretending it is final master data

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Governance Audit
  module: customer user mapping normalization
  module_owner: smart_construction_custom onboarding
  kernel_or_scenario: scenario
  reason: >
    After freezing the enterprise structure draft, the next low-risk step is to
    map the user workbook into department, post, and system-role assignments so
    later import and customization work can proceed.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-404.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - user_mapping_rules_unclear

deliverables:
  - normalized user mapping draft for the workbook
  - report and task result artifacts for ITER-2026-03-31-404
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-404.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-404.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-404.json

depends_on:
  - ITER-2026-03-31-403
