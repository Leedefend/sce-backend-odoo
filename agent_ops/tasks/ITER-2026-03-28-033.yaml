task_id: ITER-2026-03-28-033
title: Classify and submit load_contract mainline cleanup slices 031 and 032
type: governance_task
status: pending
priority: medium

goal: Reduce repo-level diff pressure by grouping the reviewed load_contract mainline cleanup slices into a bounded local commit that preserves task evidence and restores a clean continuation point.
user_visible_outcome:
  - load_contract mainline cleanup for slices 031 and 032 is preserved in a single bounded commit
  - task reports and iteration state for 031 and 032 remain auditable after submission
  - the branch can continue from a clean worktree instead of repeating diff_too_large on the same reviewed delta

scope:
  allowed_paths:
    - addons/smart_core/handlers/load_contract.py
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - agent_ops/reports/**
    - agent_ops/state/**
    - agent_ops/tasks/**
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Governance Layer
  module: load_contract mainline cleanup submission
  reason: Commit the reviewed 031 and 032 runtime-mainline cleanup slices so repo-level guard can stop treating the same accepted delta as in-flight risk.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-033.yaml
    - git status --short
    - git log --oneline -n 3

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git revert --no-edit HEAD

depends_on:
  - ITER-2026-03-28-032
notes: "Commit only the reviewed 031/032 load_contract cleanup artifacts and their audit trail."
