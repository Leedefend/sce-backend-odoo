task_id: ITER-2026-04-01-614
title: Verify architecture-corrected lifecycle copy consumer changes
type: frontend
status: active
priority: high
mode: verify

goal: >
  Verify semantic-consumer correction keeps strict typing and project lifecycle
  acceptance baseline green.

user_visible_outcome:
  - "确认架构纠偏后门禁通过"
  - "保持生命周期可用性收益"
  - "为下一张后端语义任务提供干净基线"

scope:
  allowed_paths:
    - frontend/apps/web/src/composables/useStatus.ts
    - frontend/apps/web/src/pages/ListPage.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - verify stage only
  - do not make new product edits in this batch
  - run only declared validation commands

context:
  branch: codex/next-round
  baseline_sha: 4ba974e
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-613
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_project_lifecycle_usability

architecture:
  layer_target: Frontend Layer
  module: lifecycle semantic-consumer correction verification
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    Confirm architecture correction does not regress acceptance baseline and
    keeps lifecycle usability chain executable.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-614.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.project.management.acceptance

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - project_management_acceptance_failed

deliverables:
  - verification_result_for_semantic_consumer_correction
  - governance/report artifacts for ITER-2026-04-01-614

rollback:
  - git restore frontend/apps/web/src/composables/useStatus.ts
  - git restore frontend/apps/web/src/pages/ListPage.vue
  - git restore agent_ops/tasks/ITER-2026-04-01-614.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-614.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-614.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-613
