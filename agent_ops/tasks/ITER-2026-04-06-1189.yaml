task_id: ITER-2026-04-06-1189
title: Execute minimal business-fact ACL/record-rule closure under approved narrow exception
type: governance
status: completed
priority: high
mode: execute

goal: >
  After approval of Route-B narrow exception, execute minimal closure for step-5/6:
  deduplicate project.budget ACL and add minimal project/task/budget/cost-ledger
  record rules in controlled scope.

user_visible_outcome:
  - "project.budget ACL 重复项去重"
  - "项目/任务/预算/成本台账最小 record-rule 闭环"

scope:
  allowed_paths:
    - addons/smart_construction_core/security/ir.model.access.csv
    - addons/smart_construction_core/security/sc_record_rules.xml
    - agent_ops/tasks/ITER-2026-04-06-1189.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_foundation_blockers_v1.md
    - docs/audit/native/native_foundation_execution_sequence_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 10
  max_output_lines: 180
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive/minimal changes only
  - no financial semantics changes
  - no manifest changes

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  route_b_exception_required: true
  input_from: route_b_execute_ready_template

architecture:
  layer_target: Governance Monitoring
  module: business-fact ACL and rule closure
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: business_fact
  backend_sub_layer_reason: >
    Changes target authority facts and data-access truth for core business models.
  reason: >
    Close audited gaps in native business-fact governance after exception approval.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-06-1189.yaml
    - make verify.scene.legacy_auth.smoke.semantic
    - make verify.scene.legacy_contract.guard

risk:
  level: high
  manual_approval_required: true

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched
  - exception_not_approved

deliverables:
  - minimal ACL dedup + minimal record-rule closure

rollback:
  - git restore addons/smart_construction_core/security/ir.model.access.csv
  - git restore addons/smart_construction_core/security/sc_record_rules.xml
  - git restore docs/audit/native/native_foundation_blockers_v1.md
  - git restore docs/audit/native/native_foundation_execution_sequence_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-06-1188
