task_id: ITER-2026-04-03-905
title: Execute chained host gate with widened runtime-probe cache window
type: backend
status: active
priority: high
mode: implement

goal: >
  Validate chained host gate behavior using widened runtime-probe cache window
  to reuse recent PASS and reduce nested launch instability.

user_visible_outcome:
  - "链式验收优先复用 recent PASS runtime probe 结果"
  - "减少同链路二次探针启动带来的随机失败"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verification_and_reporting_only
  - runtime_probe_cache_window_tuning_only

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-904
  priority_lane: usability_release_proof
  usability_battlefield: cross_stack_verification

architecture:
  layer_target: Product Release Usability Proof
  module: host runtime-probe cache policy execution
  module_owner: scene_orchestration_governance
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch executes verification under tuned orchestration parameters only,
    without changing business facts.
  reason: >
    Previous batch added cache short-circuit; this batch validates chained gate
    behavior with a wider cache window and explicit gateway binding.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-905.yaml
    - HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 make verify.portal.host_browser_runtime_probe
    - HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 GATEWAY_BASE_URLS=http://localhost:8069 make verify.portal.project_dashboard_primary_entry_browser_smoke.host

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - tuned-cache-window host runtime probe evidence
  - chained host gate evidence under cache-only nested probe policy

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-905.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-905.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-905.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-904
