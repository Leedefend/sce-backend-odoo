task_id: ITER-2026-03-29-138
title: Project default sort in scene merge policy
type: governance_task
status: pending
priority: high

goal: Make scene merge resolver treat search_policy.default_sort as a first-class search semantic during policy merge.
user_visible_outcome:
  - policy-driven default sort now survives merge as a canonical search_surface field

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core scene_merge_resolver
  reason: Scene merge resolver already handles default filters, default group_by, default searchpanel and default mode in policy merge, but still ignores policy-level default_sort. This iteration brings default_sort into canonical merge behavior.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-138.yaml
    - python3 -m py_compile addons/smart_core/core/scene_merge_resolver.py addons/smart_core/tests/test_scene_merge_resolver_search_policy_default_sort.py
    - python3 addons/smart_core/tests/test_scene_merge_resolver_search_policy_default_sort.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-138.yaml addons/smart_core/core/scene_merge_resolver.py addons/smart_core/tests/test_scene_merge_resolver_search_policy_default_sort.py

depends_on:
  - ITER-2026-03-29-137

notes: "Batch-3 continuation: keep search policy aligned with canonical search_surface by merging default_sort at policy stage."
