task_id: ITER-2026-04-09-1540
title: backend refactor E1 fix route delegation detection for non-single-return methods
type: product-implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 envelope 审计中 route delegation 识别对“非单语句 return”方法漏检问题，恢复 Tier-1 断言通过。

user_visible_outcome:
  - "platform_ui_contract_api 由 no_envelope_signal 收敛为 delegated_envelope"

scope:
  allowed_paths:
    - scripts/verify/envelope_consistency_audit.py
    - artifacts/architecture/envelope_consistency_audit_v1.json
    - agent_ops/tasks/ITER-2026-04-09-1540.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1540.md
    - agent_ops/state/task_results/ITER-2026-04-09-1540.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 320

change_rules:
  - verify logic fix only
  - no runtime business code changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Governance verification layer
  module: envelope_consistency_audit
  module_owner: scripts verify
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    仅修复审计识别逻辑，不修改业务事实或接口行为。
  reason: >
    对 1539 FAIL 做修复恢复，确保 Tier-1 screen 对应实现批次闭环。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1540.yaml"
    - "python3 -m py_compile scripts/verify/envelope_consistency_audit.py"
    - "python3 scripts/verify/envelope_consistency_audit.py"
    - "python3 - <<'PY'\nimport json\nobj=json.load(open('artifacts/architecture/envelope_consistency_audit_v1.json','r',encoding='utf-8'))\nrows={r['file']:r for r in obj['rows']}\nassert rows['addons/smart_core/controllers/platform_scenes_api.py']['envelope_shape']=='delegated_envelope'\nassert rows['addons/smart_core/controllers/platform_ui_contract_api.py']['envelope_shape']=='delegated_envelope'\nprint('tier1 delegated signals PASS')\nPY"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - fixed route delegation detection for envelope audit

rollback:
  - git restore scripts/verify/envelope_consistency_audit.py

depends_on:
  - ITER-2026-04-09-1539
