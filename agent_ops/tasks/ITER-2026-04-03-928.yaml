task_id: ITER-2026-04-03-928
title: Operator surface heading drift scan (candidates only)
type: governance
status: active
priority: high
mode: scan

goal: >
  Collect bounded candidate evidence for operator-surface smoke failure without
  concluding root cause in this stage.

user_visible_outcome:
  - "明确列出 operator surface 失败候选点与证据位置"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 5
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - scan_only_no_conclusion
  - no_implementation_changes

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-927
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: operator surface heading expectation scan
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This stage gathers scenario-level evidence only, without changing business facts
    or frontend model-specific behavior.
  reason: >
    927 acceptance failed on heading expectation; scan stage must collect bounded
    candidates before screen classification.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-928.yaml
    - rg -n "发布控制台|operator|heading|waitForOperatorPage" scripts/verify/release_operator_surface_browser_smoke.mjs
    - cat artifacts/codex/release-operator-surface-browser-smoke/20260404T085255Z/summary.json

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - operator_surface_scan_candidates

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-928.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-928.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-928.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-927
