task_id: ITER-2026-03-30-294
title: Reflect native sort source in list header subtitle
type: frontend
status: in_progress
priority: medium

goal: >
  Align the list header subtitle with the toolbar by showing whether the current
  sort follows native default metadata or a current runtime sort state.

user_visible_outcome:
  - list header subtitle becomes more self-explanatory
  - header and toolbar sort semantics stay consistent
  - no new sort interaction is introduced

scope:
  allowed_paths:
    - frontend/apps/web/src/app/action_runtime/**
    - agent_ops/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/views/**
    - frontend/apps/web/src/pages/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend runtime helper only
  - reuse existing sort label and source label
  - no backend changes
  - no new interactions

context:
  branch: codex/next-round
  baseline_sha: 65d6932

architecture:
  layer_target: Frontend Layer
  module: action view display runtime
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The toolbar now explains sort origin, but the list header subtitle still
    uses a generic sort string. The next low-risk step is to align subtitle text
    with the same metadata-derived sort-source semantics.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-294.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - subtitle_change_requires_cross_component_refactor

deliverables:
  - subtitle_sort_source_alignment
  - iteration_report

rollback:
  - git restore frontend/apps/web/src/app/action_runtime/useActionViewDisplayComputedRuntime.ts
  - git restore agent_ops/tasks/ITER-2026-03-30-294.yaml

depends_on:
  - ITER-2026-03-30-292

notes: >
  Keep the change inside the display runtime helper to avoid unnecessary surface
  churn.
