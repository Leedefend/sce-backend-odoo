task_id: ITER-2026-03-30-299
title: Add active-condition count label to list toolbar
type: frontend
status: in_progress
priority: medium

goal: >
  Make the active-condition section consistent with other toolbar metadata
  groups by showing how many conditions are currently active.

user_visible_outcome:
  - users can scan the current active-condition load at a glance
  - toolbar labels become visually consistent across metadata sections
  - no new behavior is introduced

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/**
    - agent_ops/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/views/**
    - frontend/apps/web/src/pages/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - display-only enhancement
  - no backend changes
  - no new interactions

context:
  branch: codex/next-round
  baseline_sha: 65d6932

architecture:
  layer_target: Frontend Layer
  module: native metadata list toolbar consumer
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    All other metadata sections now expose counts. The active-condition section
    should align with the same scanability pattern without changing semantics.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-299.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - active_state_count_requires_behavior_change

deliverables:
  - active_condition_count_label
  - iteration_report

rollback:
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue
  - git restore agent_ops/tasks/ITER-2026-03-30-299.yaml

depends_on:
  - ITER-2026-03-30-297

notes: >
  Derive the count only from existing activeStateChips.
