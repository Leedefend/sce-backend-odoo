task_id: ITER-2026-04-07-1217
title: Environment repair lane batch4 execute default base-url fallback correction
type: governance
status: completed
priority: high
mode: execute

goal: >
  Correct verify runtime default base-url fallback from 8070 to 8069 when no
  explicit env configuration is provided.

user_visible_outcome:
  - "默认运行端口与实际监听更一致"
  - "减少无配置场景下 8070 超时噪音"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1217.yaml
    - scripts/verify/python_http_smoke_utils.py
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_environment_repair_lane_batch4_fallback_port_fix_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 240
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify helper runtime fallback only
  - preserve explicit env precedence

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_environment_repair_batch4

architecture:
  layer_target: Governance Monitoring
  module: verify runtime base-url fallback
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch adjusts runtime verify helper fallback behavior only.
  reason: >
    Align no-env default with observed local listener surface and reduce false
    timeout signals from unreachable 8070 default path.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1217.yaml
    - make verify.scene.legacy_auth.runtime_probe
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime fallback port correction evidence

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1217.yaml
  - git restore scripts/verify/python_http_smoke_utils.py
  - git restore docs/audit/native/native_runtime_environment_repair_lane_batch4_fallback_port_fix_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1216
