task_id: ITER-2026-04-02-768
title: Screen project-journey view continuity slice
type: backend
status: active
priority: high
mode: screen

goal: >
  Select next low-risk view continuity slice for project journey after
  write-conflict and edit transaction paths PASS.

user_visible_outcome:
  - "项目管理闭环视图连续性下一步明确"
  - "加载/列表/看板链路继续按用户体验主线收口"

scope:
  allowed_paths:
    - scripts/verify/**
    - Makefile
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen stage only
  - no implementation
  - usability-mainline-first

context:
  branch: codex/next-round
  baseline_sha: 881958d
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-767
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: project-journey view continuity slice selection
  module_owner: verify scripts / make targets
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Continue backend-first usability verification for generic frontend rendering.
  reason: >
    Keep journey closure momentum after consistency safety pass.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-768.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed
  - no_candidate_selected

deliverables:
  - selected_view_continuity_slice
  - governance/report artifacts for ITER-2026-04-02-768

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-02-768.yaml

depends_on:
  - ITER-2026-04-02-767
