task_id: ITER-2026-04-08-1357
title: Home block frontend runtime evidence and fallback hardening Batch 2/3
status: active
type: feature
priority: high
mode: execute
batch: 2/3

goal: >
  Capture role-based homepage runtime evidence and harden fallback behavior when
  home_blocks contract rows do not map to renderable homepage entries.

user_visible_outcome:
  - "首页在 contract 映射不足时不再空白，并输出 PM/Finance/Outsider 可视差异证据"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-08-1357.yaml
    - frontend/apps/web/src/views/HomeView.vue
    - agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1357.md
    - agent_ops/state/task_results/ITER-2026-04-08-1357.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - scripts/verify/**
    - security/**
    - record_rules/**
    - addons/**/__manifest__.py
    - addons/**/ir.model.access.csv

limits:
  max_files: 5
  max_output_lines: 280
  forbid_repo_wide_scan: true

change_rules:
  - frontend-minimal-fallback-hardening
  - no-new-component
  - no-role-hardcode

context:
  branch: codex/next-round
  baseline_sha: ca23376
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract consumer
  module: homepage rendering fallback
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    Batch 1 enabled home_blocks override, but runtime evidence shows empty-entry
    risk when contract keys are not renderable in current payload.

acceptance:
  commands:
    - "Runtime evidence capture only; no verify expansion in this batch"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - role_hardcode_detected

deliverables:
  - fallback hardening for non-renderable home_blocks overrides
  - role runtime evidence checkpoint for PM/Finance/Outsider

depends_on:
  - ITER-2026-04-08-1356

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-08-1357.yaml
  - git restore frontend/apps/web/src/views/HomeView.vue
  - git restore agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1357.md
  - git restore agent_ops/state/task_results/ITER-2026-04-08-1357.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
