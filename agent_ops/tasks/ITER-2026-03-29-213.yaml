task_id: ITER-2026-03-29-213
title: Audit live project form button flow stages
type: audit_task
status: pending
priority: high

goal: Compare the same project form button family across dispatcher output, finalize_contract output, and ui.contract live output so the next repair lands on the exact stage that freezes the `Create project` label.
user_visible_outcome:
  - the next backend fix will target the real stage that emits the wrong project form button label
  - button cleanup iterations will stop guessing between assembler, finalize, and ui.contract delivery

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**
    - addons/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: governance/backend root-cause audit
  module: project form button flow stages
  reason: Two implementation attempts failed, so this batch must pin the exact stage where the `action_view_tasks` label becomes `Create project`.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-213.yaml
    - python3 -m py_compile agent_ops/scripts/project_form_button_flow_audit.py
    - python3 agent_ops/scripts/project_form_button_flow_audit.py --db sc_demo --login sc_fx_pm --container sc-backend-odoo-dev-odoo-1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - audit_report
  - task_result

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-213.yaml agent_ops/scripts/project_form_button_flow_audit.py agent_ops/reports/2026-03-29/report.ITER-2026-03-29-213.md agent_ops/state/task_results/ITER-2026-03-29-213.json

depends_on:
  - ITER-2026-03-29-210

notes: "Diagnosis-only batch: compare the suspicious button family across dispatch, finalize, and ui.contract stages."
