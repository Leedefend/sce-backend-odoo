task_id: ITER-2026-03-28-045
title: Classify and submit load_contract cleanup slice 044
type: governance_task
status: pending
priority: medium

goal: Reduce repo-level diff pressure by grouping the reviewed load_contract cleanup slice 044 into a bounded local commit with audit evidence, restoring a clean continuation point for the next runtime-mainline batch.
user_visible_outcome:
  - load_contract cleanup for slice 044 is preserved in a bounded local commit
  - task report and iteration state for 044 remain auditable after submission
  - the branch can continue from a clean worktree without replaying the same accepted delta

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/handlers/load_contract.py
    - addons/smart_core/tests/**
    - agent_ops/reports/**
    - agent_ops/state/**
    - agent_ops/tasks/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Governance Layer
  module: load_contract cleanup submission
  reason: Commit the reviewed 044 load_contract cleanup slice so the repo guard does not keep counting the same accepted delta as in-flight work.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-045.yaml
    - git status --short
    - git log --oneline -n 3

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git revert --no-edit HEAD

depends_on:
  - ITER-2026-03-28-044
notes: "Commit only the reviewed 044 load_contract cleanup artifacts and their audit trail."
