task_id: ITER-2026-03-28-017
title: Align system_init verify scripts with current login contract
type: bugfix_task
status: pending
priority: high

goal: Update system_init live verify scripts so they read the current login token contract and can validate the system_init refactor against the running environment.
user_visible_outcome:
  - system_init verify scripts accept the current login response shape
  - live system_init verifies can run against the local environment
  - the first code batch can be revalidated instead of remaining blocked by outdated verification assumptions

scope:
  allowed_paths:
    - scripts/verify/**
    - agent_ops/tasks/**
    - agent_ops/queue/**
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Verification Governance
  module: scripts/verify system_init live guards
  reason: Remove outdated login-contract assumptions from system_init live verifies so they test the runtime refactor rather than fail on unrelated auth response shape drift.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-017.yaml
    - python3 -m py_compile scripts/verify/system_init_snapshot_equivalence.py scripts/verify/system_init_runtime_context_stability.py
    - make verify.system_init.snapshot_equivalence
    - make verify.system_init.runtime_context.stability

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore scripts/verify/system_init_snapshot_equivalence.py scripts/verify/system_init_runtime_context_stability.py agent_ops/tasks/ITER-2026-03-28-017.yaml agent_ops/queue/platform_kernel_refactor_prep_queue.yaml docs/ops/iterations/delivery_context_switch_log_v1.md docs/ops/releases/archive/temp/TEMP_agent_ops_continuous_iteration_status_20260328.md

depends_on:
  - ITER-2026-03-28-016
notes: "Verification alignment follow-up for the first system.init code batch."
