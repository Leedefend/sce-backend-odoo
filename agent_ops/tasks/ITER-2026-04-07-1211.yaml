task_id: ITER-2026-04-07-1211
title: Runtime repair lane execute for unreachable error classification hardening
type: governance
status: completed
priority: high
mode: execute

goal: >
  Harden runtime unreachable classification in legacy auth smoke verify path so
  RemoteDisconnected-like transport failures are treated consistently.

user_visible_outcome:
  - "运行态不可达分类更稳健"
  - "strict/fallback 语义保持不变"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1211.yaml
    - scripts/verify/scene_legacy_auth_smoke.py
    - scripts/verify/scene_legacy_auth_smoke_semantic_verify.py
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_repair_lane_batch1_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 9
  max_candidates: 12
  max_output_lines: 220
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify-helper additive hardening only
  - preserve strict default and explicit-fallback semantics

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_repair_lane_batch1

architecture:
  layer_target: Governance Monitoring
  module: runtime verify helper hardening
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Patch is limited to runtime verification helper behavior classification.
  reason: >
    Resolve observed live probe RemoteDisconnected ambiguity without changing
    business facts, endpoints, or permission rules.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1211.yaml
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime repair lane batch1 evidence

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1211.yaml
  - git restore scripts/verify/scene_legacy_auth_smoke.py
  - git restore scripts/verify/scene_legacy_auth_smoke_semantic_verify.py
  - git restore docs/audit/native/native_runtime_repair_lane_batch1_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1210
