task_id: ITER-2026-03-29-259
title: Audit project kanban sidebar exposure chain
type: audit_task
status: in_progress
priority: high

goal: Trace the scene-driven sidebar exposure chain for the project kanban entry so we can determine whether the missing入口 is caused by menu/scene contract facts, backend scene resolution, or frontend sidebar consumer filtering.
user_visible_outcome:
  - a precise explanation of why the project kanban entry is absent from the current product sidebar
  - a narrowed implementation target for restoring the entry without model-specific hacks
  - a documented boundary between menu facts and frontend sidebar exposure

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
    - docs/architecture/**
    - frontend/apps/web/src/**
  forbidden_paths:
    - addons/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - audit_first
  - no_backend_contract_change
  - no_acl_change
  - no_record_rule_change
  - no_model_specific_hardcode

architecture:
  layer_target: frontend layer audit
  module: sidebar/menu exposure chain for project kanban
  reason: The kanban page is renderable now, but the user-facing gap has shifted to navigation exposure. We need a fact-based chain audit before changing menu or sidebar behavior.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-259.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - exposure_chain_uncertain

deliverables:
  - sidebar_exposure_gap_report
  - next_fix_target

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-259.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-29-258

notes: "Audit the exposure chain first. Do not patch menu behavior until the gap is localized."
