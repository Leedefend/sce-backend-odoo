task_id: ITER-2026-04-05-1153
title: Native capability intent/runtime exposure phase-4 contract freeze
type: governance
status: active
priority: high
mode: execute

goal: >
  Freeze phase-4 platform contract for native capability intent/runtime exposure
  unification under app_config_engine capability core, before implementation.

user_visible_outcome:
  - "原生能力 intent/runtime 绑定规范冻结"
  - "phase-4 实施边界与验收基线明确"

scope:
  allowed_paths:
    - docs/refactor/native_capability_intent_runtime_exposure_spec_v1.md
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - scripts/**
    - security/**
    - record_rules/**
    - addons/**/__manifest__.py

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 320
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - docs_governance_only
  - no_runtime_code_change

context:
  branch: codex/next-round
  baseline_sha: da08944
  explicit_user_authorization: true
  input_from: continuous_iteration_1152_next_step
  priority_lane: native_capability_phase4_contract

architecture:
  layer_target: Governance Monitoring
  module: docs/refactor
  module_owner: platform
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: freeze consumption-safe intent/runtime exposure semantics before code migration.
  reason: prevent semantic drift while expanding native capability runtime binding surface.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-1153.yaml
    - make verify.architecture.native_capability_projection_release_guard_bundle
    - make verify.controller.boundary.guard

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - phase-4 intent/runtime exposure spec

rollback:
  - git restore docs/refactor/native_capability_intent_runtime_exposure_spec_v1.md
  - git restore agent_ops/tasks/ITER-2026-04-05-1153.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-1153.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-1153.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-1152

