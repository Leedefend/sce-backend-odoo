task_id: ITER-2026-04-02-861
title: Verify constrained-runtime freeze surrogate gate
type: frontend
status: active
priority: high
mode: verify

goal: >
  Under host-browser network constraints, validate a freeze surrogate gate by
  combining release second-slice prepared checks with container cross-stack
  contract smoke.

user_visible_outcome:
  - "在受限环境下仍有可执行的 freeze 近似门禁"
  - "产品阻断与环境阻断继续分离"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify-only
  - no_business_code_change

context:
  branch: codex/next-round
  baseline_sha: 30fdd08
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-860
  priority_lane: usability_verification_mainline
  usability_battlefield: cross_stack_custom_frontend

architecture:
  layer_target: Product Usability Closure
  module: constrained-runtime freeze surrogate
  module_owner: verify chain
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Preserve closure confidence using reachable container cross-stack checks
    when host browser path is runtime-blocked.
  reason: >
    Keep release-quality iteration moving under known environment limitations.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-861.yaml
    - make verify.release.second_slice_prepared
    - make verify.portal.cross_stack_contract_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - constrained-runtime freeze surrogate verification result
  - governance/report artifacts for ITER-2026-04-02-861

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-02-861.yaml
  - git restore agent_ops/reports/2026-04-02/report.ITER-2026-04-02-861.md
  - git restore agent_ops/state/task_results/ITER-2026-04-02-861.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-02-860
