task_id: ITER-2026-03-30-366
title: Re-audit repaired PM trio for default and first-screen quality
type: audit
status: in_progress
priority: medium

goal: >
  Re-audit the newly repaired PM trio (`投标管理`、`工程资料`、`工程结构`)
  after demo fact seeding, so the next batch can decide whether any remaining
  issues are only default filters/help wording or whether the native line should
  move on to other pages.

user_visible_outcome:
  - the repaired PM trio is validated as either good-enough or still needing small default/filter polish
  - the native PM usability line avoids over-fixing after the seed batch
  - the next repair target is selected from evidence instead of habit

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
    - artifacts/codex/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only in this batch
  - focus on first-screen quality, default filters, help text, and immediate PM comprehension
  - do not reopen scene or portal ownership questions in this native page audit

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: repaired PM trio quality re-audit
  module_owner: addons/smart_construction_demo + addons/smart_construction_core
  kernel_or_scenario: scenario
  reason: >
    After the seed batch passed, the next low-risk step is to verify whether the
    PM-visible trio is now sufficiently usable or still needs only small
    default/filter/help refinements before the native value line moves on.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-366.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - repaired_trio_quality_cannot_be_classified_with_confidence

deliverables:
  - re-audit result for tender/documents/work-breakdown after demo seeding
  - report and task result artifacts for ITER-2026-03-30-366
  - delivery log checkpoint with the next native or custom-frontend target

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-366.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-366.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-366.json

depends_on:
  - ITER-2026-03-30-365
