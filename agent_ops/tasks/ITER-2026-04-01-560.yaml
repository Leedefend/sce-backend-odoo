task_id: ITER-2026-04-01-560
title: Preserve the primary toolbar when sort summary remains visible
type: frontend
status: active
priority: medium

goal: >
  Keep the primary toolbar mounted in optimization mode when the search section
  is hidden but the sort summary block is still visible.

user_visible_outcome:
  - "搜索区被隐藏时，排序摘要不再跟着整个主工具栏一起消失"
  - "保持修改范围只在主工具栏可见性门槛"
  - "不改变排序行为或按钮回调"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - bounded structural enhancement
  - no behavior changes
  - modify only the primary toolbar visibility gate

context:
  branch: codex/next-round
  baseline_sha: 8194c19
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-559

architecture:
  layer_target: Frontend Layer
  module: primary toolbar visibility gating
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The decision batch confirmed a bounded structural follow-up exists. The
    change is limited to preserving the toolbar when sort summary remains
    relevant even if optimization hides the search section.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-560.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.v0_5.container

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - trusted_container_verify_failed
  - visibility_followup_requires_broader_structural_change

deliverables:
  - bounded_primary_toolbar_visibility_fix
  - governance/report artifacts for ITER-2026-04-01-560

rollback:
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue
  - git restore agent_ops/tasks/ITER-2026-04-01-560.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-560.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-560.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-559
