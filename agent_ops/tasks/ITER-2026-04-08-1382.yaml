task_id: ITER-2026-04-08-1382
title: Fix scene ready consumption trend guard false drop checks
status: active
type: governance
priority: high
mode: execute
batch: 1/1

goal: >
  Remove false trend-drop failures when scene_ready_consumption metrics are not
  enabled in current payload, while preserving strict checks when enabled.

user_visible_outcome:
  - "scene_ready_consumption_trend_guard 不再误报 1.0 drop"
  - "preflight 继续推进到下一真实门禁"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-08-1382.yaml
    - scripts/verify/scene_ready_consumption_trend_guard.py
    - agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1382.md
    - agent_ops/state/task_results/ITER-2026-04-08-1382.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*

limits:
  max_files: 5
  max_output_lines: 220
  forbid_repo_wide_scan: true

change_rules:
  - additive-only
  - no-acl-table-change
  - no-financial-semantic-change

context:
  branch: codex/next-round
  baseline_sha: 3466cad
  explicit_user_authorization: true

architecture:
  layer_target: Verification guard logic layer
  module: scene_ready_consumption_trend_guard
  module_owner: platform verification governance
  kernel_or_scenario: scenario
  reason: >
    Guard currently applies drop-rate checks even when live payload reports
    consumption disabled/unavailable, causing false failures.

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-08-1382.yaml"
    - "make verify.scene.ready.consumption_trend.guard DB_NAME=sc_demo E2E_LOGIN=admin E2E_PASSWORD=admin"
    - "CONTRACT_PREFLIGHT_SKIP_DOCS=1 CONTRACT_PREFLIGHT_SKIP_GROUPED_GOV_BUNDLE=1 CONTRACT_PREFLIGHT_SKIP_SCENE_CAPABILITY_GUARD=1 CONTRACT_PREFLIGHT_STRICT_VIEW_TYPES=0 BASELINE_FREEZE_ENFORCE=0 make verify.contract.preflight DB_NAME=sc_demo E2E_LOGIN=admin E2E_PASSWORD=admin"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_command_failed

deliverables:
  - trend guard drop-check gating by enabled/live metrics
  - updated preflight evidence

depends_on:
  - ITER-2026-04-08-1381

rollback:
  - "git restore scripts/verify/scene_ready_consumption_trend_guard.py"
  - "git restore agent_ops/tasks/ITER-2026-04-08-1382.yaml"
  - "git restore agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1382.md"
  - "git restore agent_ops/state/task_results/ITER-2026-04-08-1382.json"
  - "git restore docs/ops/iterations/delivery_context_switch_log_v1.md"
