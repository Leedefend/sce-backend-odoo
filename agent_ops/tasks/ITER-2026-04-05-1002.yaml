task_id: ITER-2026-04-05-1002
title: Verify login session menu smoke after P0 route ownership migration
type: backend
status: active
priority: high
mode: verify

goal: >
  Run runtime smoke verification for `/api/login`, `/api/session/get`, and
  `/api/menu/tree` after platform ownership migration.

user_visible_outcome:
  - "得到迁移后 login/session/menu 运行链是否可用的明确结论"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 160
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify_only
  - no_source_code_change

context:
  branch: codex/next-round
  baseline_sha: f4102e4
  explicit_user_authorization: true
  input_from: ITER-2026-04-05-1001
  priority_lane: smart_construction_core_boundary_remediation_p0_verify

architecture:
  layer_target: Runtime Verification
  module: frontend api smoke chain
  module_owner: operations_runtime
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Verification reads runtime route behavior only.
  reason: >
    User explicitly requested execution of smoke verification after migration.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-1002.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim make verify.frontend_api

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_failed

deliverables:
  - frontend_api_smoke_verdict

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-05-1002.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-1002.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-1002.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-1001
