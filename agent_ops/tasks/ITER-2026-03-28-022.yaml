task_id: ITER-2026-03-28-022
title: Extract runtime fetch workspace collection helper
type: refactor_task
status: pending
priority: medium

goal: Move runtime_fetch workspace collection extraction into a shared smart_core helper so runtime entrypoints carry less inline shaping logic and the collection policy becomes explicit.
user_visible_outcome:
  - runtime_fetch uses a dedicated core helper for workspace collection extraction
  - workspace collection allowlist lives in one platform-owned helper
  - existing system_init live verifies still pass after the extraction

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/handlers/runtime_fetch.py
    - agent_ops/tasks/**
    - agent_ops/queue/**
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Platform Layer
  module: smart_core runtime fetch collection extraction
  reason: Continue narrowing runtime entrypoint responsibilities by moving workspace collection shaping out of the handler and into a reusable core helper.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-022.yaml
    - python3 -m py_compile addons/smart_core/handlers/runtime_fetch.py addons/smart_core/core/runtime_workspace_collection_helper.py
    - make verify.system_init.snapshot_equivalence
    - make verify.system_init.runtime_context.stability

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore addons/smart_core/handlers/runtime_fetch.py addons/smart_core/core/runtime_workspace_collection_helper.py agent_ops/tasks/ITER-2026-03-28-022.yaml agent_ops/queue/platform_kernel_refactor_prep_queue.yaml docs/ops/iterations/delivery_context_switch_log_v1.md docs/ops/releases/archive/temp/TEMP_agent_ops_continuous_iteration_status_20260328.md

depends_on:
  - ITER-2026-03-28-021
notes: "Third narrow runtime-mainline cleanup slice after baseline recovery and execution lock hardening."
