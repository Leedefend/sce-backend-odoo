task_id: ITER-2026-04-09-1537
title: backend refactor E1 envelope consistency audit rule convergence
type: governance
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  收敛 envelope 审计规则，从“返回函数形态扫描”升级为“ok/data/error/meta/effect 字段一致性审计”。

user_visible_outcome:
  - "envelope 审计结果可真实反映已完成的统一改造"

scope:
  allowed_paths:
    - scripts/verify/envelope_consistency_audit.py
    - artifacts/architecture/envelope_consistency_audit_v1.json
    - agent_ops/tasks/ITER-2026-04-09-1537.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1537.md
    - agent_ops/state/task_results/ITER-2026-04-09-1537.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 320

change_rules:
  - audit-rule refactor only
  - no business endpoint logic changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Governance verification layer
  module: envelope_consistency_audit
  module_owner: scripts verify
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    仅升级审计判据，不改变业务事实或接口行为。
  reason: >
    E1 已完成实现侧改造，需要同步审计口径避免指标失真。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1537.yaml"
    - "python3 -m py_compile scripts/verify/envelope_consistency_audit.py"
    - "python3 scripts/verify/envelope_consistency_audit.py"
    - "rg -n \"ok_keys|fail_keys|required_envelope_keys|envelope_shape\" artifacts/architecture/envelope_consistency_audit_v1.json"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - envelope consistency audit rule convergence

rollback:
  - git restore scripts/verify/envelope_consistency_audit.py

depends_on:
  - ITER-2026-04-09-1536
