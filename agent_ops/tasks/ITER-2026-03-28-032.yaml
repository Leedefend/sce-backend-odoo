task_id: ITER-2026-03-28-032
title: Move load_contract view_type normalization into the shared entry helper
type: refactor_task
status: pending
priority: medium

goal: Extract load_contract view_type parsing and fallback normalization into the shared core helper so the handler keeps less inline request-shaping logic while preserving the current contract behavior.
user_visible_outcome:
  - load_contract still accepts string and list view_type inputs
  - load_contract still infers view_type from menu or action context when the client omits it
  - invalid requested view_type values are still rejected with the same error semantics

scope:
  allowed_paths:
    - addons/smart_core/handlers/load_contract.py
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - agent_ops/tasks/**
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Platform Layer
  module: smart_core load_contract view_type normalization
  reason: Continue narrowing load_contract transitional logic by moving request and fallback view-type normalization into the shared platform helper before broader runtime mainline cleanup.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-032.yaml
    - python3 -m py_compile addons/smart_core/handlers/load_contract.py addons/smart_core/core/load_contract_entry_context.py addons/smart_core/tests/test_load_contract_entry_context.py
    - python3 addons/smart_core/tests/test_load_contract_entry_context.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore addons/smart_core/handlers/load_contract.py addons/smart_core/core/load_contract_entry_context.py addons/smart_core/tests/test_load_contract_entry_context.py agent_ops/tasks/ITER-2026-03-28-032.yaml docs/ops/iterations/delivery_context_switch_log_v1.md docs/ops/releases/archive/temp/TEMP_agent_ops_continuous_iteration_status_20260328.md

depends_on:
  - ITER-2026-03-28-031
notes: "Stay inside request normalization only; do not change contract schema or downstream builder semantics."
