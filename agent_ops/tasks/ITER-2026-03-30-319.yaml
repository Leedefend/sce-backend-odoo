task_id: ITER-2026-03-30-319
title: Analyze native capability versus contract gaps for the list surface
type: docs
status: in_progress
priority: high

goal: >
  Produce the second-step gap analysis that compares Odoo-native list/search
  capabilities, current backend contract exposure, and the missing page
  composition metadata needed to remove frontend-owned orchestration.

user_visible_outcome:
  - repository contains a gap-analysis document
  - the document separates native capability, current contract, and missing composition metadata
  - the document gives a direct basis for a follow-up implementation plan

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - docs/tmp/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - docs only
  - analysis only
  - no product code changes

context:
  branch: codex/next-round
  baseline_sha: 6e26cf1

architecture:
  layer_target: Documentation Layer
  module: list surface contract gap analysis
  module_owner: docs/tmp
  kernel_or_scenario: scenario
  reason: >
    After auditing frontend-owned page composition, we need a precise analysis
    of which capabilities already exist in Odoo and backend runtime, which are
    already exposed by current contracts, and which composition metadata is
    still missing.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-319.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - task_contract_invalid

deliverables:
  - list_surface_contract_gap_doc

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-319.yaml
  - git restore docs/tmp/list_surface_native_vs_contract_gap_analysis_2026-03-30.md

depends_on:
  - ITER-2026-03-30-318

notes: >
  This batch is the second step only: capability-versus-contract gap analysis.
  The final implementation plan comes in the next batch.
