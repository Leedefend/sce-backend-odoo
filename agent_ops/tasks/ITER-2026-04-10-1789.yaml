task_id: ITER-2026-04-10-1789
title: delivery semantic naming convergence for form sections

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  将表单分组的兜底命名从机械标题收敛到业务语义标题，提升交付态可理解性。

user_visible_outcome:
  - "信息分组标题更接近业务语义（主体信息/管理信息/设置/协作系统）"
  - "减少机械命名（如 信息分组 N）出现频率"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1789.yaml
    - frontend/apps/web/src/components/template/DetailShellLayout.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1789.md
    - agent_ops/state/task_results/ITER-2026-04-10-1789.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend template rendering only
  - no backend semantic mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: DetailShellLayout fallback naming semantics
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    tab 与契约已对齐，下一步进入交付收口，优先提升分组语义可读性。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1789.yaml"
    - "rg -n \"resolveSemanticSectionTitle|fallbackSectionTitle\" frontend/apps/web/src/components/template/DetailShellLayout.vue"
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - section semantic naming polish patch
  - iteration report and task result

rollback:
  - "git restore frontend/apps/web/src/components/template/DetailShellLayout.vue"

depends_on:
  - ITER-2026-04-10-1788
