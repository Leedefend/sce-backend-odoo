task_id: ITER-2026-03-28-096
title: Preserve native view semantics through ui.contract model/view contract flow
type: governance_task
status: pending
priority: high

goal: Ensure ui.contract model/view contract consumption explicitly projects the standardized native view parser contract and top-level semantics so the parser milestone reaches the main readonly contract entrypoint.
user_visible_outcome:
  - ui.contract model/view responses now preserve canonical parser contract and native view semantics for the active view

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/handlers/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform core contract integration
  module: smart_core ui.contract readonly contract flow
  reason: Complete the parser-semantic integration by ensuring the main readonly contract entrypoint explicitly preserves canonical parser output instead of relying on incidental passthrough.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-096.yaml
    - python3 -m py_compile addons/smart_core/handlers/ui_contract.py addons/smart_core/tests/test_ui_contract_projection.py addons/smart_core/core/native_view_contract_projection.py
    - python3 addons/smart_core/tests/test_ui_contract_projection.py
    - python3 addons/smart_core/tests/test_native_view_contract_projection.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-28-096.yaml addons/smart_core/handlers/ui_contract.py addons/smart_core/tests/test_ui_contract_projection.py addons/smart_core/core/native_view_contract_projection.py

depends_on:
  - ITER-2026-03-28-095
notes: "Readonly contract entrypoint integration step after canonical load_contract/load_view projection."
