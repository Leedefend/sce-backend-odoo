task_id: ITER-2026-03-30-302
title: Align search mode label styling with native metadata count pattern
type: frontend
status: in_progress
priority: medium

goal: >
  Improve toolbar consistency by making the search-mode section follow the same
  metadata-label pattern as the other native metadata blocks.

user_visible_outcome:
  - search mode reads as a first-class native metadata section
  - toolbar labeling feels more consistent across sections
  - no interaction or backend behavior changes

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/**
    - agent_ops/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/views/**
    - frontend/apps/web/src/pages/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - display-only enhancement
  - no backend changes
  - no new interactions

context:
  branch: codex/next-round
  baseline_sha: 65d6932

architecture:
  layer_target: Frontend Layer
  module: native metadata list toolbar consumer
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    Search mode is already surfaced, but its section label is the only metadata
    block without a structural hint. A small consistency pass keeps the toolbar
    easier to scan.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-302.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - search_mode_label_change_requires_behavior_change

deliverables:
  - search_mode_label_alignment
  - iteration_report

rollback:
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue
  - git restore agent_ops/tasks/ITER-2026-03-30-302.yaml

depends_on:
  - ITER-2026-03-30-299

notes: >
  Keep the change inside PageToolbar and do not invent any count semantics for
  search mode; only align the label pattern.
