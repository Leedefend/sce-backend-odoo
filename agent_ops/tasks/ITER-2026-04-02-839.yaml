task_id: ITER-2026-04-02-839
title: Align project execution action contract guard with extended task fields
type: backend
status: active
priority: high
mode: implement

goal: >
  Update project execution action contract guard to support extended response
  fields (`task_id`, task state transitions, lifecycle hints) while preserving
  required contract checks.

user_visible_outcome:
  - "执行动作契约校验与当前后端返回一致"
  - "不再因守卫过旧导致假失败"

scope:
  allowed_paths:
    - scripts/verify/product_project_execution_action_contract_guard.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive_only
  - verify_guard_only
  - no_business_semantics_change

context:
  branch: codex/next-round
  baseline_sha: c7a219e
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-838
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: project execution action contract guard
  module_owner: verify scripts
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Guard must reflect backend contract truth for execution action path.
  reason: >
    Preserve strong contract checks while accepting approved extended fields.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-839.yaml
    - make verify.product.project_execution_action_contract_guard

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - execution action contract guard supports extended task fields without drift false-positives
  - verification report and task result for ITER-2026-04-02-839

rollback:
  - git restore scripts/verify/product_project_execution_action_contract_guard.py
  - git restore agent_ops/tasks/ITER-2026-04-02-839.yaml
  - git restore agent_ops/reports/2026-04-02/report.ITER-2026-04-02-839.md
  - git restore agent_ops/state/task_results/ITER-2026-04-02-839.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-02-838
