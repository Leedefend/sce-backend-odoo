task_id: ITER-2026-04-01-559
title: Assess primary-toolbar visibility gating for a structural follow-up
type: frontend
status: active
priority: medium
mode: screen

goal: >
  Assess the remaining primary-toolbar visibility candidate and decide whether
  it can be decomposed into a bounded structural follow-up without violating
  current architecture and low-cost execution rules.

user_visible_outcome:
  - "明确 primary-toolbar visibility 问题是否可拆成单独结构批次"
  - "避免把结构性问题伪装成低风险显示层改动"
  - "为后续是否继续实现提供正式决策依据"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 2
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - reporter
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - structural decision batch only
  - do not modify product code in this batch
  - only assess the remaining primary-toolbar visibility candidate
  - do not reopen broader repository scans

context:
  branch: codex/next-round
  baseline_sha: 8194c19
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-558
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true

architecture:
  layer_target: Frontend Layer
  module: primary toolbar visibility gating
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The display-only toolbar slices are exhausted. The only remaining known
    candidate is structural, so the next valid step is an explicit decision
    batch that determines whether a bounded structural follow-up exists.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-559.yaml

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - decision_requires_broader_architectural_change
  - candidate_cannot_be_bounded
  - screen_output_exceeds_limit

deliverables:
  - decision
  - bounded_followup_scope_or_stop
  - reason
  - governance/report artifacts for ITER-2026-04-01-559

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-559.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-559.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-559.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-558
