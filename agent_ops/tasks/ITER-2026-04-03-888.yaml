task_id: ITER-2026-04-03-888
title: Converge PM landing semantics to backend project-management primary entry
type: backend
status: active
priority: high
mode: implement

goal: >
  Fix backend role-surface landing semantics for PM so system.init primary route
  aligns with project-management usability path in custom frontend, without
  frontend model-specific branching.

user_visible_outcome:
  - "登录后可进入项目管理主入口语义链路"
  - "主入口可用性验证不再依赖前端特判"

scope:
  allowed_paths:
    - addons/smart_construction_scene/core_extension.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - backend_semantic_alignment_only
  - no_frontend_model_branching
  - no_business_model_change
  - no_contract_field_change

context:
  branch: codex/next-round
  baseline_sha: d156083
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-887
  priority_lane: usability_release_proof
  usability_battlefield: backend_orchestration

architecture:
  layer_target: Product Release Usability Proof
  module: PM role surface entry semantic policy
  module_owner: smart_construction_scene
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration_governance
  backend_sub_layer_reason: >
    Entry and landing semantics are scenario orchestration policy, should be
    fixed in backend role-surface policy instead of frontend branching.
  reason: >
    Host verification shows PM startup landing is biased to projects.intake and
    blocks project-management chain, causing real-user entry instability.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-888.yaml
    - make verify.portal.project_dashboard_primary_entry_browser_smoke.host
    - make verify.product.main_entry_convergence.v1

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - PM role-surface backend semantics aligned to project-management primary entry
  - updated host usability verification evidence

rollback:
  - git restore addons/smart_construction_scene/core_extension.py
  - git restore agent_ops/tasks/ITER-2026-04-03-888.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-888.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-888.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-887
