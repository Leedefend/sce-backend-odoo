task_id: ITER-2026-04-02-770
title: Screen project-journey tree-kanban continuity slice
type: backend
status: active
priority: high
mode: screen

goal: >
  Select one next low-risk slice between tree-view and kanban-view continuity
  for project-management user journey.

user_visible_outcome:
  - "树视图/看板视图下一步单切片明确"
  - "避免一次并发验证导致上下文扩张"

scope:
  allowed_paths:
    - scripts/verify/**
    - Makefile
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen stage only
  - no implementation
  - usability-mainline-first

context:
  branch: codex/next-round
  baseline_sha: bd298af
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-769
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: tree-kanban continuity slice selection
  module_owner: verify scripts / make targets
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Continue backend-first usability validation for generic frontend rendering.
  reason: >
    Maintain single-slice progression for view-mode closure.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-770.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed
  - no_candidate_selected

deliverables:
  - selected_tree_or_kanban_slice
  - governance/report artifacts for ITER-2026-04-02-770

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-02-770.yaml

depends_on:
  - ITER-2026-04-02-769
