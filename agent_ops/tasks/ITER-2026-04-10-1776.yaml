task_id: ITER-2026-04-10-1776
title: cleanup non-user projects and bind imported projects to user enterprise

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  清理 sc_demo 中非用户项目数据，并将本轮导入项目统一绑定到用户企业。

user_visible_outcome:
  - "数据库中非用户项目数据被清理（优先删除，失败则归档）"
  - "导入项目统一绑定到目标用户企业"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1776.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1776.md
    - agent_ops/state/task_results/ITER-2026-04-10-1776.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
    - tmp/customer_project_cleanup_1776.json
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 800

change_rules:
  - db data operation only
  - no schema/acl/manifest change
  - keep business semantics unchanged

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: runtime data operation layer
  module: project data cleanup and enterprise binding
  module_owner: project.project runtime data
  kernel_or_scenario: scenario
  reason: >
    用户要求将非用户项目数据清理，并把刚导入项目统一绑定到用户企业，保证交付数据面向真实企业。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1776.yaml"
    - "COMPOSE_PROJECT_NAME=sc-backend-odoo-dev PROJECT=sc-backend-odoo-dev DB_NAME=sc_demo bash scripts/ops/odoo_shell_exec.sh < /tmp/iter1776_cleanup_exec.py"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - cleanup summary json
  - report and task result

rollback:
  - "恢复方式：根据 tmp/customer_project_cleanup_1776.json 中 deleted_ids / archived_ids 做反向恢复（需单独批次）。"

depends_on:
  - ITER-2026-04-10-1775
