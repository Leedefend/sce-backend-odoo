task_id: ITER-2026-04-09-1527
title: backend refactor B1 first thin-controller implementation slice
type: product-implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  对 intent_dispatcher 做第一步极薄化实现切片：将请求归一/分发/响应组装迁移到 helper，缩短路由方法体。

user_visible_outcome:
  - "intent controller 主入口更薄，行为保持一致，且可通过 thin guard 量化改进"

scope:
  allowed_paths:
    - addons/smart_core/controllers/intent_dispatcher.py
    - agent_ops/tasks/ITER-2026-04-09-1527.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1527.md
    - agent_ops/state/task_results/ITER-2026-04-09-1527.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/smart_core/core/intent_router.py
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 320

change_rules:
  - preserve runtime semantics
  - thin-controller extraction only
  - no permission policy rewrite

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Controller protocol adapter layer
  module: intent dispatcher thin slice
  module_owner: smart_core controllers
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批次仅收口协议适配层，不触及业务事实与策略语义。
  reason: >
    B1 第一批通过 helper 提取降低 controller 方法复杂度，为后续 dispatcher 纯分发化打基础。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1527.yaml"
    - "python3 scripts/verify/controller_thin_guard_audit.py"
    - 'rg -n "def _execute_intent_request\\(|def handle_intent\\(" addons/smart_core/controllers/intent_dispatcher.py'

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - first controller-thin implementation slice

rollback:
  - git restore addons/smart_core/controllers/intent_dispatcher.py

depends_on:
  - ITER-2026-04-09-1526
