task_id: ITER-2026-03-31-396
title: Audit custom business flow pages against business-fact structure fields and delivery logic
type: audit
status: completed
priority: high

goal: >
  Verify whether the current custom frontend business flow pages remain aligned
  with business facts at three levels: page structure, business fields, and
  delivery logic.

user_visible_outcome:
  - "明确业务主流程页的页面结构是否与业务事实对齐"
  - "明确业务字段是否与业务事实对齐"
  - "明确交付逻辑是否与业务事实对齐"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no code changes
  - focus only on active business flow pages, not product workbench or governance-only pages
  - classify alignment separately for structure, fields, and delivery logic
  - keep finance-governed claims outside the positive alignment set

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: custom business flow pages vs business facts
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    The next active objective is to directly verify whether the custom frontend
    business flow pages still match business facts in structure, field surface,
    and delivery behavior, without reopening product-surface ownership debates.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-396.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - business_flow_page_scope_unclear
  - audited_flow_requires_finance_governed_claim

deliverables:
  - alignment audit for custom business flow pages
  - per-page structure / field / delivery verdicts
  - report and task result artifacts for ITER-2026-03-31-396
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-396.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-396.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-396.json

depends_on:
  - ITER-2026-03-31-395
