task_id: ITER-2026-04-07-1206
title: Runtime availability evidence under strict default auth smoke policy
type: governance
status: completed
priority: high
mode: verify

goal: >
  Produce auditable runtime availability evidence showing strict default policy
  behavior for legacy auth smoke in current environment.

user_visible_outcome:
  - "严格默认策略下运行态不可达证据明确"
  - "短链门禁持续 PASS"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1206.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_availability_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 200
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - evidence-only verify batch
  - no source code mutation

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_availability_evidence

architecture:
  layer_target: Governance Monitoring
  module: runtime strict-policy evidence
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch focuses on runtime verification evidence and policy observation only.
  reason: >
    Confirm strict-default auth smoke behavior in real runtime availability
    conditions and keep short-chain guard outcomes stable.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1206.yaml
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime availability evidence document

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1206.yaml
  - git restore docs/audit/native/native_runtime_availability_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1205
