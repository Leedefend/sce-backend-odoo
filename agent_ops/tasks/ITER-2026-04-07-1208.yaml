task_id: ITER-2026-04-07-1208
title: Screen runtime probe permission blocker and recovery condition
type: governance
status: completed
priority: high
mode: screen

goal: >
  Screen and classify runtime probe permission blocker for `/api/scenes/my`
  reachable validation, and define non-invasive recovery conditions.

user_visible_outcome:
  - "运行探测权限阻塞已分类"
  - "恢复条件可执行且不越权"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1208.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_probe_permission_screen_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 200
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen/evidence only
  - no code mutation

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_permission_screen

architecture:
  layer_target: Governance Monitoring
  module: runtime probe blocker screen
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch only classifies runtime verification constraint and recovery path.
  reason: >
    Low-cost governance screen to avoid invalid retries and define the next
    eligible verification trigger.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1208.yaml
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime probe permission screen document

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1208.yaml
  - git restore docs/audit/native/native_runtime_probe_permission_screen_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1207
