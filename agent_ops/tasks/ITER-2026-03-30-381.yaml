task_id: ITER-2026-03-30-381
title: Audit config-oriented native pages against business-fact usability goal
type: audit
status: in_progress
priority: medium

goal: >
  Audit the remaining non-finance native config-oriented pages and decide
  whether they belong inside the current business-fact usability objective or
  should be treated as a separate admin/configuration lane.

user_visible_outcome:
  - config-oriented native pages get an explicit in-scope or out-of-scope decision
  - the business-fact usability goal remains focused on real PM operational value

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; do not modify config pages in this batch
  - evaluate by PM operational value rather than mere non-emptiness
  - keep conclusions explicit about inclusion or exclusion from the active goal

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: config-oriented native page usability boundary
  module_owner: smart_construction_core
  kernel_or_scenario: scenario
  reason: >
    After the project-centric operational pages were confirmed usable, the
    remaining low-risk native surfaces are configuration-oriented and need an
    explicit boundary decision against the current business-fact usability goal.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-381.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - config_page_boundary_unclear

deliverables:
  - scope decision for the remaining config-oriented native pages
  - report and task result artifacts for ITER-2026-03-30-381
  - delivery log checkpoint for the next usability lane

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-381.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-381.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-381.json

depends_on:
  - ITER-2026-03-30-380
