task_id: ITER-2026-04-10-1804
title: form tab projection loss root fix

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  根因修复 detail shell 投影链路：当 layout_tree 已包含 notebook/page 时，确保 detail_shell_raw 可稳定生成 tabs。

user_visible_outcome:
  - "结构摘要中 projected tabs 与 layout page 计数对齐，不再出现 delta>0"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1804.yaml
    - frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1804.md
    - agent_ops/state/task_results/ITER-2026-04-10-1804.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend runtime projection only
  - root-cause fix, no backend fallback expansion
  - additive and reversible

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: detail layout tree to shell projection
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    当前 pipeline(layout_tree) notebook/page>0 但 pipeline(detail_shell_raw) tab=0，
    说明 tabs 在前端投影阶段丢失，需要修复 layout tree 到 shell 的页面回收逻辑。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1804.yaml"
    - 'rg -n "collectPageNodesRecursively|buildDetailShellViewsFromTree|synthetic notebook" frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts'
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - stable tab projection from views.form.layout
  - iteration report and state

rollback:
  - "git restore frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts"

depends_on:
  - ITER-2026-04-10-1803
