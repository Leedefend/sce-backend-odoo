task_id: ITER-2026-03-30-365
title: Seed minimal demo facts for tender documents and work breakdown pages
type: code
status: in_progress
priority: high

goal: >
  Turn the highest-value data-thin native PM pages into usable demo surfaces by
  seeding minimal original business facts for `投标管理`、`工程资料`、`工程结构`
  in the already loaded smart_construction_demo dataset, without changing
  manifests or touching high-risk domains.

user_visible_outcome:
  - demo PM users see at least one meaningful record in tender management
  - demo PM users see at least one meaningful record in engineering documents
  - demo PM users see at least a minimal WBS tree in work breakdown

scope:
  allowed_paths:
    - addons/smart_construction_demo/data/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - only extend already loaded demo data files; do not add manifest entries
  - keep the seed minimal and project-centric
  - do not alter core business semantics or ACL

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Scenario Product Layer
  module: demo fact seeding for PM-visible native pages
  module_owner: addons/smart_construction_demo
  kernel_or_scenario: scenario
  reason: >
    The native value-readiness audit narrowed the next repair target to three
    PM-visible pages that are structurally usable but data-thin. The lowest-risk
    correction is to seed minimal demo facts in the existing demo dataset.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-365.yaml
    - make mod.upgrade MODULE=smart_construction_demo DB_NAME=sc_demo
    - make verify.smart_core

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - demo_seed_breaks_upgrade
  - verify_command_failed

deliverables:
  - minimal demo tender/document/WBS records in an already loaded smart_construction_demo data file
  - report and task result artifacts for ITER-2026-03-30-365
  - delivery log checkpoint for the next native data-value slice

rollback:
  - git restore addons/smart_construction_demo/data/scenario/s60_project_cockpit/10_cockpit_business_facts.xml
  - git restore agent_ops/tasks/ITER-2026-03-30-365.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-365.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-365.json

depends_on:
  - ITER-2026-03-30-364
