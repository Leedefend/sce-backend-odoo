task_id: ITER-2026-04-03-944
title: Edition route fallback guard timeout scan (candidates only)
type: governance
status: active
priority: high
mode: scan

goal: >
  Collect bounded candidate evidence for edition route fallback guard timeout
  without concluding root cause in scan stage.

user_visible_outcome:
  - "列出 route_fallback_guard 超时候选证据"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 5
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - scan_only_no_conclusion
  - no_implementation_changes

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-943
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: edition route fallback guard timeout scan
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Scan stage gathers timeout evidence only; no business fact mutation.
  reason: >
    After session_context guard fix, release chain now blocks at route_fallback guard login navigation timeout.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-944.yaml
    - rg -n "submitLogin|waitForURL|networkidle|edition=preview|my.work.summary" scripts/verify/edition_route_fallback_guard.mjs
    - cat artifacts/codex/edition-route-fallback-guard/20260404T135358Z/summary.json

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - edition_route_fallback_timeout_scan_candidates

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-944.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-944.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-944.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-943
