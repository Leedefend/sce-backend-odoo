task_id: ITER-2026-04-18-NO-CONTRACT-PAYMENT-OPERATION-GAP-SCREEN
title: Screen no-contract payment operation gap after contract replay
type: governance-screen
status: pass_with_risk
priority: high
mode: screen

goal: >
  Screen whether remaining no-contract payment records can continue under new
  system operations after deterministic contract replay.

user_visible_outcome:
  - "确认无合同日常支出付款是否会被新系统办理动作阻断，并明确后续修复边界。"

scope:
  allowed_paths:
    - docs/ops/business_continuity/no_contract_payment_operation_gap_screen_v1.md
    - docs/ops/iterations/delivery_context_switch_log_v1.md
    - agent_ops/tasks/ITER-2026-04-18-NO-CONTRACT-PAYMENT-OPERATION-GAP-SCREEN.yaml
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**
    - scripts/**
    - security/**
    - record_rules/**
    - ir.model.access.csv
    - __manifest__.py

context:
  branch: codex/next-round
  baseline_sha: c8c533f
  prior_task: ITER-2026-04-18-CONTRACT-REPLAY-WRITE-EXACT-ONE

architecture:
  layer_target: Business Fact Screening
  module: no-contract payment operation gap
  module_owner: construction industry business continuity
  kernel_or_scenario: scenario
  backend_sub_layer_target: business-fact layer
  backend_sub_layer_reason: >
    Whether no-contract payments may be submitted is a payment business rule.
    The gap must be diagnosed at backend business-fact semantics, not patched
    by frontend rendering or scene orchestration.
  reason: >
    After exact-one contract replay, remaining no-contract payments are valid
    business facts for daily expense and non-contract outflows. The current
    operation handlers still appear to require contract_id on submit.

change_rules:
  - read-only screen only
  - do not modify payment, settlement, account, ACL, manifest, frontend, or migration data
  - if fixing requires payment business files, stop and report the required high-risk task line

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-18-NO-CONTRACT-PAYMENT-OPERATION-GAP-SCREEN.yaml
    - static read-only inspection of payment submit and available-actions guards

risk:
  level: high
  manual_approval_required: true

stop_conditions:
  - forbidden_path_touched
  - payment_business_semantics_change_required
  - frontend_special_case_suggested

deliverables:
  - gap screen document with stop decision and next required high-risk backend task

verification_result:
  status: PASS_WITH_RISK
  commands:
    - command: python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-18-NO-CONTRACT-PAYMENT-OPERATION-GAP-SCREEN.yaml
      result: PASS
    - command: static read-only inspection of payment submit and available-actions guards
      result: PASS_WITH_RISK
      summary: >
        payment.request.action_submit and payment.request.available_actions
        still require contract_id before submit. This blocks no-contract daily
        spending and requires a dedicated high-risk payment business semantics
        task before code can be changed.

rollback:
  - git restore docs/ops/business_continuity/no_contract_payment_operation_gap_screen_v1.md docs/ops/iterations/delivery_context_switch_log_v1.md agent_ops/tasks/ITER-2026-04-18-NO-CONTRACT-PAYMENT-OPERATION-GAP-SCREEN.yaml

depends_on:
  - ITER-2026-04-18-CONTRACT-REPLAY-WRITE-EXACT-ONE
