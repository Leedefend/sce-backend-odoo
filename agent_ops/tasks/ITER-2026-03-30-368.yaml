task_id: ITER-2026-03-30-368
title: Audit finance-generated native pages for demo PM first-screen value
type: audit
status: in_progress
priority: medium

goal: >
  Continue the native usability line after the PM trio closes by auditing the
  finance-generated native pages `资金台账` and `付款记录`, focusing on whether
  demo PM users get meaningful first-screen value from current business facts,
  actions, views, and default conditions.

user_visible_outcome:
  - clear judgment on whether `资金台账` and `付款记录` are demo-usable today
  - explicit separation between reachability and real first-screen value
  - a narrowed next repair target only if a low-risk factual gap remains

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no code or data edits in this batch
  - base conclusions on model/action/view/menu/data-prerequisite facts
  - do not widen this batch into portal/custom-frontend targets

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: finance-generated native value readiness audit
  module_owner: agent_ops
  kernel_or_scenario: scenario
  reason: >
    After the native PM trio line closed, the next low-risk step is to assess
    whether the remaining finance-generated native pages already provide usable
    first-screen value or still need factual/data remediation.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-368.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - audit_scope_spills_into_code_changes

deliverables:
  - audit report for `资金台账` and `付款记录`
  - task result artifact for ITER-2026-03-30-368
  - delivery log checkpoint with the next low-risk recommendation

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-368.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-368.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-368.json

depends_on:
  - ITER-2026-03-30-367
