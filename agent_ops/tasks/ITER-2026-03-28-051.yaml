task_id: ITER-2026-03-28-051
title: Create platform kernel alignment guard baseline
type: governance_task
status: pending
priority: medium

goal: Establish a first-pass platform-kernel alignment guard that verifies required baseline documents and AGENTS bindings exist before later code-layer alignment batches run.
user_visible_outcome:
  - the repository has a runnable platform-kernel alignment guard
  - missing baseline documents or agent bindings cause a clear PASS/FAIL result

scope:
  allowed_paths:
    - scripts/verify/**
    - Makefile
    - agent_ops/policies/**
    - agent_ops/tasks/**
    - docs/architecture/**
    - AGENTS.md
    - agent_ops/prompts/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: governance layer
  module: platform kernel alignment guard
  reason: Convert the architecture-alignment baseline from document-only guidance into a minimal runnable audit gate.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-051.yaml
    - python3 scripts/verify/platform_kernel_alignment_guard.py

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore scripts/verify/platform_kernel_alignment_guard.py Makefile agent_ops/tasks/ITER-2026-03-28-051.yaml

depends_on:
  - ITER-2026-03-28-050
notes: "First-pass guard only. No deep static analysis in this round."
