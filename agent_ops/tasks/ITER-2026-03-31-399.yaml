task_id: ITER-2026-03-31-399
title: Re-audit projects.intake alignment after direct form handoff
type: audit
status: completed
priority: medium

goal: >
  Re-check whether `projects.intake` is now sufficiently aligned with business
  facts after the direct handoff change, and identify any remaining residual
  gap without reopening broad implementation work.

user_visible_outcome:
  - "明确 `projects.intake` 修复后还剩多少偏差"
  - "判断当前状态是否已足够接受"
  - "如果还有剩余差距，限制在最小描述范围内"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no code changes
  - compare the repaired scene handoff behavior against the existing fact-layer form contract
  - keep the conclusion narrow to `projects.intake`

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: projects.intake post-repair alignment
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    After ITER-2026-03-31-398 repaired the strongest frontend-originated drift
    by handing the scene route to the existing form surface, the next low-risk
    step is to re-audit the remaining alignment gap before choosing any further
    work.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-399.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - post_repair_alignment_unclear

deliverables:
  - focused alignment verdict for repaired `projects.intake`
  - report and task result artifacts for ITER-2026-03-31-399
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-399.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-399.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-399.json

depends_on:
  - ITER-2026-03-31-398
