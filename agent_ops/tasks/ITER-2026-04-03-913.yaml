task_id: ITER-2026-04-03-913
title: Screen why sc_demo entry resolves to /my-work fallback
type: governance
status: active
priority: high
mode: screen

goal: >
  Classify why project.entry.context.resolve returns empty project_context in sc_demo
  and falls back to /my-work after 500 fix.

user_visible_outcome:
  - "明确 sc_demo 主入口为何落到 /my-work 的可执行原因"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 2
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen_only_no_code_change
  - no_cross_stage_reasoning
  - backend_semantics_first

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-912
  priority_lane: usability_release_proof
  usability_battlefield: backend_semantics

architecture:
  layer_target: Product Release Usability Proof
  module: project entry semantic supply
  module_owner: scene_orchestration_governance
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Focus on semantic-supply classification for entry route selection.
  reason: >
    500 is fixed, but runtime now returns fallback route /my-work with empty
    project context for sc_demo.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-913.yaml
    - HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 GATEWAY_BASE_URLS=http://localhost E2E_LOGIN=admin E2E_PASSWORD=admin DB_NAME=sc_demo make verify.portal.project_dashboard_primary_entry_browser_smoke.host

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed
  - uncertain_classification

deliverables:
  - fallback_reason_classification
  - next_implement_slice_suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-913.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-913.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-913.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-912
