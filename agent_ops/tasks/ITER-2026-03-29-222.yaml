task_id: ITER-2026-03-29-222
title: Audit live project detail fact contract
type: audit_task
status: pending
priority: high

goal: Fetch and summarize the live `project.project` form contract facts so the next detail implementation batch can target real backend metadata for layout, statusbar, modifiers, and actions.
user_visible_outcome:
  - the next detail page batch can focus on concrete contract gaps instead of visual guesswork
  - detail-page work continues on a fact-first closed-loop basis

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**
    - addons/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend fact audit
  module: live project detail contract
  reason: List-first delivery is now grounded in live facts. The next detail batch should likewise start from the live form contract rather than stale screenshots or wrapped snapshots.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-222.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - uncertain_runtime_state

deliverables:
  - audit_report
  - task_result

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-222.yaml agent_ops/reports/2026-03-29/report.ITER-2026-03-29-222.md agent_ops/state/task_results/ITER-2026-03-29-222.json

depends_on:
  - ITER-2026-03-29-221

notes: "Diagnosis-only batch: capture live form facts first, then implement detail-page structure parity in the next batch."
