task_id: ITER-2026-03-28-098
title: Guard native view semantic surface in live load_view smoke
type: governance_task
status: pending
priority: high

goal: Extend the live load_view smoke so it fails if parser_contract, view_semantics, or native_view disappear from the real endpoint response.
user_visible_outcome:
  - real load_view smoke now guards canonical native parser semantics instead of only checking layout presence

scope:
  allowed_paths:
    - scripts/verify/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform core verification guard
  module: live load_view smoke semantic contract guard
  reason: The parser-semantic milestone is now wired through real handlers; the live smoke must guard that surface so future changes cannot silently strip it.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-098.yaml
    - make verify.portal.load_view_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-28-098.yaml scripts/verify/fe_load_view_smoke.js

depends_on:
  - ITER-2026-03-28-097
notes: "Live verification guard for the canonical native parser semantic surface."
