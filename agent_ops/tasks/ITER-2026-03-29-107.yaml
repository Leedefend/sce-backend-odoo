task_id: ITER-2026-03-29-107
title: Preserve scene semantic surfaces through contract governance
type: governance_task
status: pending
priority: high

goal: Extend contract_governance so scene_contract_standard_v1, scene_contract_v1, semantic_runtime, and released_scene_semantic_surface keep normalized parser semantic surfaces instead of losing shape in governance mode.
user_visible_outcome:
  - scene-level semantic surfaces remain stable after governance normalization

scope:
  allowed_paths:
    - addons/smart_core/utils/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend contract governance
  module: smart_core contract_governance
  reason: After scene and runtime layers start carrying parser semantics, governance must normalize and preserve those surfaces or the backend chain will still lose them during contract processing.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-107.yaml
    - python3 -m py_compile addons/smart_core/utils/contract_governance.py addons/smart_core/tests/test_scene_semantic_contract_governance.py
    - python3 addons/smart_core/tests/test_native_view_contract_governance.py
    - python3 addons/smart_core/tests/test_scene_semantic_contract_governance.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-107.yaml addons/smart_core/utils/contract_governance.py addons/smart_core/tests/test_scene_semantic_contract_governance.py

depends_on:
  - ITER-2026-03-28-106
notes: "Batch-3 governance continuation: preserve scene semantic surfaces through contract governance."
