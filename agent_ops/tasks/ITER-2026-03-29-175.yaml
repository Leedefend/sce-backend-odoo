task_id: ITER-2026-03-29-175
title: Validate consumer runtime alias in smart_scene contract diagnostics
type: governance_task
status: pending
priority: high

goal: Make smart_scene schema and builder shape validation explicitly guard diagnostics.consumer_runtime as an object.
user_visible_outcome:
  - final scene contracts now validate the consumer-facing runtime diagnostics alias
  - invalid consumer_runtime payloads are rejected by both schema and builder shape validation

scope:
  allowed_paths:
    - addons/smart_scene/core/**
    - addons/smart_scene/schemas/**
    - addons/smart_scene/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: scene layer
  module: smart_scene contract schema + scene_contract_builder
  reason: smart_scene now exposes a stable consumer-facing runtime diagnostics alias, but contract validation still does not explicitly guard that alias. This iteration adds low-risk schema and shape checks for diagnostics.consumer_runtime.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-175.yaml
    - python3 -m py_compile addons/smart_scene/schemas/scene_contract_schema.py addons/smart_scene/core/scene_contract_builder.py addons/smart_scene/tests/test_scene_contract_schema.py
    - python3 addons/smart_scene/tests/test_scene_contract_schema.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-175.yaml addons/smart_scene/schemas/scene_contract_schema.py addons/smart_scene/core/scene_contract_builder.py addons/smart_scene/tests/test_scene_contract_schema.py

depends_on:
  - ITER-2026-03-29-174

notes: "Batch acceleration: validate diagnostics.consumer_runtime in schema and shape guards."
