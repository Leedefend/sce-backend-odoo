task_id: ITER-2026-03-31-397
title: Audit whether flow-page mixing originates from backend contract or frontend realization
type: audit
status: completed
priority: high

goal: >
  Determine, with direct repository evidence, whether the current mixing/drift in
  custom business flow pages originates from backend contract/runtime outputs or
  from frontend realization choices.

user_visible_outcome:
  - "明确哪些混入来自后端 contract/runtime"
  - "明确哪些偏移来自前端承接实现"
  - "给出逐页归因，不再靠推测"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no code changes
  - use direct evidence from backend contract/runtime definitions and frontend page implementation
  - produce per-page attribution instead of a single blended conclusion
  - keep finance-governed behavior outside positive correctness claims

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: source of flow-page mixing
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    The active question is now causal attribution: whether current flow-page
    mixing comes from backend contract/runtime itself or from frontend
    realization decisions. This must be answered with direct evidence.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-397.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - attribution_scope_unclear

deliverables:
  - per-page attribution for `projects.intake` and `project.management`
  - report and task result artifacts for ITER-2026-03-31-397
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-397.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-397.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-397.json

depends_on:
  - ITER-2026-03-31-396
