task_id: ITER-2026-04-01-520
title: Classify the quota-import family on the config-admin governance line
type: permission-governance
status: active
priority: medium

goal: >
  Continue the config-admin governance objective after scene/subscription
  closure by formally classifying the quota-import family and confirming its
  repository/runtime boundary.

user_visible_outcome:
  - "继续扩展 config_admin / 平台治理面 覆盖"
  - "正式分类 quota import 家族"
  - "确认其菜单、action 与模型权限边界是否 clean"

scope:
  allowed_paths:
    - addons/smart_construction_core/**
    - addons/smart_construction_custom/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit-first governance batch
  - implementation changes are out of scope unless a later narrow batch is opened
  - prefer a pure config-admin family with no delivered-role ambiguity

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom
  explicit_user_authorization: true

architecture:
  layer_target: Platform Governance Acceptance
  module: quota-import family audit
  module_owner: platform governance surface
  kernel_or_scenario: scenario
  reason: >
    ITER-2026-04-01-519 established scene/subscription as the first clean
    representative config-admin family. The next safe step is to classify
    quota import as another governance-side family.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-520.yaml
    - "repository audit confirms quota import menu and action ownership"
    - "runtime audit classifies quota import entrypoints without high-risk side effects"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - quota_import_requires_acl_or_manifest_changes
  - runtime_cannot_classify_quota_import_with_confidence
  - quota_import_boundary_crosses_into_financial_domain

deliverables:
  - quota-import family classification
  - governance/report artifacts for ITER-2026-04-01-520

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-520.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-520.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-520.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-519
