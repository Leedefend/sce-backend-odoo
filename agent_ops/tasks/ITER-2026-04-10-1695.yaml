task_id: ITER-2026-04-10-1695
title: page convergence launch checkpoint

type: implementation
status: active
priority: high
mode: verify
batch: 1/1

goal: >
  启动页面收敛执行轮次，复验正式入口页面链路与可观测证据资产，确保进入交付前试用状态。

user_visible_outcome:
  - "页面收敛链路保持稳定可用"
  - "正式入口评审资产可直接复用"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1695.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1695.md
    - agent_ops/state/task_results/ITER-2026-04-10-1695.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
    - artifacts/delivery/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 500

change_rules:
  - verification and reporting only

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: page convergence launch verification
  module_owner: scripts/verify + delivery docs
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批仅验证页面收敛状态，不改业务事实。
  reason: >
    用户确认问题已解决并要求启动页面收敛，先执行一轮可交付验证检查点。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1695.yaml"
    - "python3 scripts/verify/v2_app_governance_gate_audit.py --json"
    - "BASE_URL=http://127.0.0.1:5174 DB_NAME=sc_demo E2E_LOGIN=wutao E2E_PASSWORD=demo API_BASE_URL=http://127.0.0.1:8069 node scripts/verify/unified_system_menu_click_usability_smoke.mjs"
    - "python3 scripts/verify/formal_entry_page_review_audit.py"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - page convergence launch checkpoint report

rollback:
  - "N/A (verification/reporting batch)"

depends_on:
  - ITER-2026-04-10-1694
