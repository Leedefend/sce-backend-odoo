task_id: ITER-2026-03-30-326
title: Complete backend native batch capability truth for scene-ready contracts
type: code
status: in_progress
priority: high

goal: >
  Complete backend-native action and batch capability truth by exposing stable
  batch capability facts in scene-ready contracts and compact system.init
  payloads, without introducing page action composition or frontend changes.

user_visible_outcome:
  - scene-ready payload includes additive batch capability truth under action_surface
  - action_surface explicitly declares can_delete, can_archive, can_activate, selection_required, native_basis
  - system.init preserves additive batch capability truth in compact scene rows

scope:
  allowed_paths:
    - addons/smart_core/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
  forbidden_paths:
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/__manifest__.py

change_rules:
  - backend only
  - native capability truth only
  - no page action grouping redesign
  - no optimization composition
  - no frontend changes

context:
  branch: codex/next-round
  baseline_sha: a99fe49

architecture:
  layer_target: Platform Layer
  module: smart_core action and batch native extraction
  module_owner: addons/smart_core
  kernel_or_scenario: kernel
  reason: >
    After search_surface and list_surface native truths are complete, backend
    must expose explicit batch capability truth so frontend can stop inferring
    archive/activate/delete behavior from active-field heuristics.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-326.yaml
    - python3 addons/smart_core/tests/test_scene_ready_action_semantic_bridge.py
    - python3 addons/smart_core/tests/test_scene_ready_action_surface_semantic_consumption.py
    - python3 addons/smart_core/tests/test_system_init_payload_builder_semantics.py
    - make verify.smart_core

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_command_failed
  - contract_structure_breaks_existing_scene_ready_keys

deliverables:
  - backend batch capability truth implementation
  - updated tests covering scene-ready and system.init preservation

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-326.yaml
  - git restore addons/smart_core/core/scene_ready_action_semantic_bridge.py
  - git restore addons/smart_core/core/scene_ready_contract_builder.py
  - git restore addons/smart_core/core/system_init_payload_builder.py
  - git restore addons/smart_core/tests/test_scene_ready_action_semantic_bridge.py
  - git restore addons/smart_core/tests/test_scene_ready_action_surface_semantic_consumption.py
  - git restore addons/smart_core/tests/test_system_init_payload_builder_semantics.py

depends_on:
  - ITER-2026-03-30-325

notes: >
  This batch only fills native action and batch capability truth. It must not
  add toolbar sections, primary versus secondary operation composition, or
  guidance design.
