task_id: ITER-2026-04-02-780
title: Implement one2many-edit relation fallback alignment
type: backend
status: active
priority: high
mode: implement

goal: >
  Fix one2many-edit smoke failure when one2many descriptor omits relation_field
  under current load_view contract projection.

user_visible_outcome:
  - "one2many 编辑门禁不再被 relation_field 缺失误阻断"
  - "仍保持对真实编辑能力的验证"

scope:
  allowed_paths:
    - scripts/verify/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - implement stage only
  - additive-only
  - verify-script-only

context:
  branch: codex/next-round
  baseline_sha: 1324dc8
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-779
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: one2many edit verify fallback
  module_owner: verify scripts
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Align backend contract consumption without frontend specialization.
  reason: >
    Recover one2many-edit gate with generic compatibility fallback.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-780.yaml
    - node --check scripts/verify/fe_one2many_edit_smoke.js

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - one2many_edit_verify_fallback_fix
  - governance/report artifacts for ITER-2026-04-02-780

rollback:
  - git restore scripts/verify/fe_one2many_edit_smoke.js
  - git restore agent_ops/tasks/ITER-2026-04-02-780.yaml
  - git restore agent_ops/reports/2026-04-02/report.ITER-2026-04-02-780.md
  - git restore agent_ops/state/task_results/ITER-2026-04-02-780.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-02-779
