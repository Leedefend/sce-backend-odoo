task_id: ITER-2026-04-10-1717
title: action list shell header compact convergence

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  将 action 列表页外层壳层头部收敛为单层工作区，移除与列表页重复的大标题卡片与面包屑噪音。

user_visible_outcome:
  - "列表页上方外层壳层显著简化"
  - "页面进入后直接呈现单层表格工作区"

scope:
  allowed_paths:
    - frontend/apps/web/src/layouts/AppShell.vue
    - agent_ops/tasks/ITER-2026-04-10-1717.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1717.md
    - agent_ops/state/task_results/ITER-2026-04-10-1717.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 800

change_rules:
  - shell header compact convergence only
  - no backend or contract semantic changes
  - no new intent or route expansion

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: action list shell header convergence
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批仅收敛前端壳层展示密度，不改变业务事实语义。
  reason: >
    用户反馈列表页仍有外层壳层冗余，需要继续收敛到接近原生单层工作区。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1717.yaml"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - compact shell header behavior for action list page

rollback:
  - "git restore frontend/apps/web/src/layouts/AppShell.vue"

depends_on:
  - ITER-2026-04-10-1701
