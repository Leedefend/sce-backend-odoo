task_id: ITER-2026-04-06-1203
title: Stage-B Batch2 targeted regression matrix and visibility evidence refresh
type: governance
status: completed
priority: high
mode: verify

goal: >
  Execute Stage-B Batch2 by running the targeted short-chain regression matrix
  and refreshing dictionary visibility evidence without expanding scope.

user_visible_outcome:
  - "Stage-B Batch2 门禁矩阵稳定 PASS"
  - "字典可见性证据刷新"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-06-1203.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_stage_b_batch2_regression_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 200
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify/evidence refresh only
  - no business fact mutation in this batch

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_stage_b_batch2

architecture:
  layer_target: Governance Monitoring
  module: stage-b regression gate
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch validates runtime/contract verification gates and evidence only;
    no business fact write path is introduced.
  reason: >
    Keep Stage-B progression controlled by proving regression matrix stability and
    visibility evidence continuity after Batch1 seed extension.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-06-1203.yaml
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - stage-b batch2 regression and visibility evidence

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-06-1203.yaml
  - git restore docs/audit/native/native_stage_b_batch2_regression_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-06-1202
