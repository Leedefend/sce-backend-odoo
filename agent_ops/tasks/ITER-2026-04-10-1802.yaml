task_id: ITER-2026-04-10-1802
title: page assembler runtime-user view parsing fix

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 view contract 仍在 su_env 解析的问题，确保 form XML 继承链按真实用户上下文生效。

user_visible_outcome:
  - "ui.contract form 布局与原生用户态一致"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1802.yaml
    - addons/smart_core/app_config_engine/services/assemblers/page_assembler.py
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1802.md
    - agent_ops/state/task_results/ITER-2026-04-10-1802.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - backend contract assembly only
  - no acl/rule/manifest changes
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend contract semantic layer
  module: page_assembler view contract generation path
  module_owner: smart_core
  kernel_or_scenario: scenario
  reason: >
    用户指出原生 XML 需真实用户上下文；当前路径仍使用 su_env 解析 view，导致继承链失真。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1802.yaml"
    - "rg -n app.view.config addons/smart_core/app_config_engine/services/assemblers/page_assembler.py"
    - "make restart"
    - "make frontend.restart"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - runtime-user-first view parsing patch
  - iteration report and task result

rollback:
  - "git restore addons/smart_core/app_config_engine/services/assemblers/page_assembler.py"

depends_on:
  - ITER-2026-04-10-1800
