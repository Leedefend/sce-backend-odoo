task_id: ITER-2026-04-10-1810
title: restore chatter attachment block in align mode

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  恢复结构对齐模式下的协作区可见性，确保消息/附件功能不会在优化阶段被隐藏。

user_visible_outcome:
  - "对齐模式下仍可看到讨论区与附件区状态/入口"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1810.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1810.md
    - agent_ops/state/task_results/ITER-2026-04-10-1810.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend visibility fix only
  - keep existing optimization scope
  - additive and reversible

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: collaboration zone visibility
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    当前 align 模式硬编码隐藏协作区，误伤消息/附件能力，需要恢复。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1810.yaml"
    - 'rg -n "showCollaborationZoneBlock|semanticHasChatter|semanticHasAttachments" frontend/apps/web/src/pages/ContractFormPage.vue'
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - collaboration zone visible in align mode
  - iteration report and state

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1809
