task_id: ITER-2026-04-10-1755
title: frontend form header-action statusbar native-order parity

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复前端对 form 头部动作区的重排与裁剪，确保 statusbar/动作按后端 contract
  原顺序消费，提升与原生 form 头部的一致性。

user_visible_outcome:
  - "项目详情页头部动作顺序与数量与契约一致，不再被前端优先级算法改写"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1755.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - scripts/verify/form_render_profile_frontend_consumer_audit.py
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1755.md
    - agent_ops/state/task_results/ITER-2026-04-10-1755.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 900

change_rules:
  - frontend contract-consumer root-cause fix only
  - no backend contract/schema mutation
  - no ACL/financial semantics change

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: frontend contract-consumer runtime
  module: form header action/statusbar consumption order
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    用户持续要求页面结构对齐，本批收敛头部动作区消费策略，去除前端重排和数量裁剪。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1755.yaml"
    - "python3 scripts/verify/form_render_profile_frontend_consumer_audit.py --json"

risk:
  level: medium-low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - frontend header action/statusbar parity patch

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"
  - "git restore scripts/verify/form_render_profile_frontend_consumer_audit.py"

depends_on:
  - ITER-2026-04-10-1754
