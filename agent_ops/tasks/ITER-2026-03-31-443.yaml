task_id: ITER-2026-03-31-443
title: Migrate smart_construction_seed dependency to smart_platform_bootstrap
type: implementation
status: active
priority: high

goal: >
  Execute the first downstream transition step after the bootstrap semantic
  split by migrating smart_construction_seed to depend on
  smart_platform_bootstrap instead of smart_construction_bootstrap.

user_visible_outcome:
  - "smart_construction_seed 直接依赖新中性 bootstrap 模块"
  - "bootstrap 迁移顺序的 manifest 阶段开始落地"

scope:
  allowed_paths:
    - addons/smart_construction_seed/__manifest__.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/smart_platform_bootstrap/**
    - addons/smart_construction_bootstrap/**
    - scripts/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - dedicated manifest migration batch
  - only migrate dependent manifest edge
  - do not touch reset/install scripts in this batch
  - do not touch verify scripts or docs in this batch

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Platform Bootstrap Module Migration
  module: smart_construction_seed manifest dependency
  module_owner: repository taxonomy and upgrade path
  kernel_or_scenario: kernel
  reason: >
    The semantic split is already verified, so the next frozen transition step
    is to migrate the first dependent manifest edge to the new neutral bootstrap
    module.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-443.yaml
    - make mod.upgrade CODEX_NEED_UPGRADE=1 MODULE=smart_construction_seed DB_NAME=sc_demo
    - make verify.smart_core

risk:
  level: high
  manual_approval_required: false

stop_conditions:
  - manifest_dependency_transition_breaks_seed_upgrade_with_uncertainty
  - downstream_reference_behavior_requires_script_changes_in_same_batch

deliverables:
  - implementation/report/result artifacts for ITER-2026-03-31-443

rollback:
  - git restore addons/smart_construction_seed/__manifest__.py
  - git restore agent_ops/tasks/ITER-2026-03-31-443.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-443.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-443.json

depends_on:
  - ITER-2026-03-31-442
