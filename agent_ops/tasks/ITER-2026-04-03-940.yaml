task_id: ITER-2026-04-03-940
title: Edition session context guard timeout scan (candidates only)
type: governance
status: active
priority: high
mode: scan

goal: >
  Collect bounded candidate evidence for edition session-context guard timeout
  without concluding root cause in scan stage.

user_visible_outcome:
  - "列出 session_context_guard 超时候选证据点"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 5
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - scan_only_no_conclusion
  - no_implementation_changes

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-939
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: edition session context guard timeout scan
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Scan stage gathers timeout evidence only; no business fact or contract mutation.
  reason: >
    After policy-guard fix, release chain now blocks at edition session context guard timeout.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-940.yaml
    - rg -n "waitForURL|networkidle|edition=preview|system.init|my.work.summary" scripts/verify/edition_session_context_guard.mjs
    - cat artifacts/codex/edition-session-context-guard/20260404T133542Z/summary.json

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - edition_session_context_timeout_scan_candidates

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-940.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-940.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-940.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-939
