task_id: ITER-2026-03-31-475
title: Audit project-form object buttons against delivered role boundaries
type: audit
status: active
priority: medium

goal: >
  Continue the Sichuan Baosheng delivery validation chain by auditing whether
  project-form object buttons and stat buttons still resolve to actions that
  exceed the corrected delivered role boundary for PM, finance, executive, or
  business-admin.

user_visible_outcome:
  - "可以判断项目表单 object button 是否还会把角色送入越界 action"
  - "若仍有问题，可直接定位到具体按钮方法与目标 action"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - read-only repository and runtime audit only
  - do not modify code, XML, ACL, record rules, or data files in this batch
  - classify project-form object-button residuals using method targets and runtime evidence

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom

architecture:
  layer_target: Customer Delivery Flow Acceptance
  module: project-form object-button residual audit
  module_owner: customer delivery validation chain
  kernel_or_scenario: scenario
  reason: >
    ITER-2026-03-31-474 found no new quick-action or alternate-entry leakage.
    The next low-risk step is to inspect project-form object buttons and the
    actions they return, because those methods can still bypass the visible
    action matrix if not aligned.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-475.yaml
    - repository and runtime audit can classify project-form object-button targets on sc_odoo

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - project_form_object_button_targets_cannot_be_classified_from_repository_and_runtime_evidence
  - classification_requires_uncertain_contract_or_kernel_semantics

deliverables:
  - project-form object-button residual audit report
  - task result artifact and delivery-log checkpoint for ITER-2026-03-31-475

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-475.yaml

depends_on:
  - ITER-2026-03-31-474
