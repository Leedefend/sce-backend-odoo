task_id: ITER-2026-03-29-219
title: Audit list parity baseline
type: audit_task
status: pending
priority: high

goal: Establish a list-first parity baseline by checking whether backend list metadata is already sufficient and which frontend list capabilities are already consumed vs overexposed.
user_visible_outcome:
  - next list batch can target the shortest path to a usable Odoo-like list page
  - implementation focus moves from metadata doubt to concrete frontend list-shell gaps

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**
    - addons/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: frontend contract consumer audit
  module: list parity baseline
  reason: The product path is now list -> detail -> kanban. Before changing list UI, we need a concrete baseline of which list contract capabilities are already consumed and which surfaces are simply overexposed.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-219.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - uncertain_scope

deliverables:
  - audit_report
  - task_result

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-219.yaml agent_ops/reports/2026-03-29/report.ITER-2026-03-29-219.md agent_ops/state/task_results/ITER-2026-03-29-219.json

depends_on:
  - ITER-2026-03-29-218

notes: "Diagnosis-only batch: inspect ActionView/ListPage list consumer chain and define the next minimal list implementation batch."
