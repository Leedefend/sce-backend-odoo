task_id: ITER-2026-04-02-811
title: Recover workbench-tiles smoke fallback compatibility
type: backend
status: active
priority: high
mode: implement

goal: >
  Align workbench tiles smoke with scene-fallback compatibility behavior to avoid false negative failures.

user_visible_outcome:
  - "语义门禁在场景回退模式下不误报"
  - "可用性验证结果更贴近真实用户路径"

scope:
  allowed_paths:
    - scripts/verify/fe_workbench_tiles_smoke.js
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - frontend/**
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive_only
  - verify_script_only
  - no business semantic changes

context:
  branch: codex/next-round
  baseline_sha: a32be47
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-810
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: workbench tiles smoke compatibility
  module_owner: verify scripts
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Keep verification aligned with backend scene delivery fallback semantics.
  reason: >
    Current failure is caused by fallback runtime mode, not direct user-path regression.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-811.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - contract_breaking_change

deliverables:
  - workbench_tiles_smoke_fallback_compatibility_recovered
  - governance/report artifacts for ITER-2026-04-02-811

rollback:
  - git restore scripts/verify/fe_workbench_tiles_smoke.js
  - git restore agent_ops/tasks/ITER-2026-04-02-811.yaml
  - git restore agent_ops/reports/2026-04-02/report.ITER-2026-04-02-811.md
  - git restore agent_ops/state/task_results/ITER-2026-04-02-811.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-02-810
