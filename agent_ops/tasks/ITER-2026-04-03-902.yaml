task_id: ITER-2026-04-03-902
title: Stabilize host browser runtime probe with broader fallback trigger
type: backend
status: active
priority: high
mode: implement

goal: >
  Improve host browser runtime probe resilience by broadening fallback trigger
  conditions for full Chrome launch when headless shell fails at runtime.

user_visible_outcome:
  - "主机浏览器运行时探针在沙箱类失败时可自动切换回退路径"
  - "显式网关验收可减少被 runtime probe 误阻断"

scope:
  allowed_paths:
    - scripts/verify/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - runtime_probe_fallback_only
  - no_business_model_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-901
  priority_lane: usability_release_proof
  usability_battlefield: cross_stack_verification

architecture:
  layer_target: Product Release Usability Proof
  module: host browser runtime probe fallback policy
  module_owner: scene_orchestration_governance
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch adjusts runtime verification launch strategy only and does not
    alter business facts.
  reason: >
    Explicit-gateway verification was blocked by host runtime probe launch
    fatal; fallback trigger must include sandbox/permission launch failures.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-902.yaml
    - make verify.portal.host_browser_runtime_probe
    - GATEWAY_BASE_URLS=http://localhost:8069 make verify.portal.project_dashboard_primary_entry_browser_smoke.host

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - broader fallback trigger for host browser runtime probe
  - updated explicit-gateway host gate evidence after runtime probe hardening

rollback:
  - git restore scripts/verify/host_browser_runtime_probe.mjs
  - git restore agent_ops/tasks/ITER-2026-04-03-902.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-902.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-902.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-901
