task_id: ITER-2026-04-10-1805
title: action-specific form parser root path fix

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 action-specific form 被强制 fallback 解析导致结构缩水的问题，恢复 primary parser 为主、fallback 仅兜底。

user_visible_outcome:
  - "form 契约可保留原生 notebook/page 结构，不再被 fallback 路径压缩"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1805.yaml
    - addons/smart_core/app_config_engine/models/app_view_config.py
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1805.md
    - agent_ops/state/task_results/ITER-2026-04-10-1805.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - backend parser routing only
  - root-cause fix first, no frontend special case
  - additive and reversible

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend contract semantic layer
  module: app.view.config parser routing
  module_owner: addons/smart_core
  kernel_or_scenario: kernel
  reason: >
    运行时验证显示原生 form 含 11 个 page，而 contract 仅 3 个 page；
    关键原因是 action-specific form 被优先送入 fallback 解析链。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1805.yaml"
    - 'rg -n "action-specific view detected|force_fallback|parse_with_primary_parser" addons/smart_core/app_config_engine/models/app_view_config.py'
    - "make restart"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - action-specific form parser route fixed
  - iteration report and state

rollback:
  - "git restore addons/smart_core/app_config_engine/models/app_view_config.py"

depends_on:
  - ITER-2026-04-10-1804
