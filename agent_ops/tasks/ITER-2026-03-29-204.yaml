task_id: ITER-2026-03-29-204
title: Preserve form field modifiers in governed contract
type: backend_task
status: pending
priority: high

goal: Restore `views.form.field_modifiers` in the live governed `ui.contract` output by fixing the form whitelist that currently strips parsed modifiers during final contract projection.
user_visible_outcome:
  - project form contract exposes usable field-level modifiers again
  - frontend can consume readonly/required/invisible semantics from backend contract without fallback heuristics

scope:
  allowed_paths:
    - agent_ops/**
    - addons/smart_core/**
    - docs/ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend contract projection layer
  module: governed form contract whitelist
  reason: Root-cause audit proved field_modifiers are already present in parser output and arch_parsed, but are stripped by governed form whitelist before final contract delivery.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-204.yaml
    - python3 -m py_compile addons/smart_core/app_config_engine/models/contract_mixin.py addons/smart_core/tests/test_contract_mixin_governed_form.py
    - python3 addons/smart_core/tests/test_contract_mixin_governed_form.py
    - bash -lc 'export E2E_BASE_URL=http://127.0.0.1:8070; python3 agent_ops/scripts/project_form_live_contract_audit.py --db sc_demo --login sc_fx_pm --password prod_like --expect-field-modifiers present'

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code
  - tests

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-204.yaml addons/smart_core/app_config_engine/models/contract_mixin.py addons/smart_core/tests/test_contract_mixin_governed_form.py agent_ops/reports/2026-03-29/report.ITER-2026-03-29-204.md agent_ops/state/task_results/ITER-2026-03-29-204.json

depends_on:
  - ITER-2026-03-29-203

notes: "Minimal fix batch: preserve form field_modifiers in governed contract projection, then validate against live project form ui.contract."
