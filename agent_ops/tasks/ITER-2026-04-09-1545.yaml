task_id: ITER-2026-04-09-1545
title: backend refactor A1 tier1 intent registry entries implementation
type: product-implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  实现 intent registry Tier-1（app/meta/system.inspect/ui.enhanced）缺口注册，收敛 missing surfaces。

user_visible_outcome:
  - "intent registry 缺口减少，Tier-1 公开面可追溯"

scope:
  allowed_paths:
    - addons/smart_core/intents/registry_entries/__init__.py
    - addons/smart_core/intents/registry_entries/tier1_surface.py
    - artifacts/architecture/intent_registry_audit_v1.json
    - artifacts/architecture/intent_registry_taxonomy_audit_v1.json
    - agent_ops/tasks/ITER-2026-04-09-1545.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1545.md
    - agent_ops/state/task_results/ITER-2026-04-09-1545.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 8
  max_output_lines: 320

change_rules:
  - additive registry entries only
  - no handler/controller/runtime logic changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Intent governance layer
  module: intent registry entries
  module_owner: smart_core intents
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    仅补齐意图总账本，不改变业务事实。
  reason: >
    按 1544 screen 冻结顺序先执行 Tier-1 注册迁移。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1545.yaml"
    - "python3 scripts/verify/intent_registry_audit.py"
    - "python3 scripts/verify/intent_registry_taxonomy_audit.py"
    - "python3 - <<'PY'\nimport json\nobj=json.load(open('artifacts/architecture/intent_registry_audit_v1.json','r',encoding='utf-8'))\nmissing=obj['coverage']['missing_from_registry']\nfor name in ['app.catalog','app.nav','app.open','meta.describe_model','meta.intent_catalog','system.init.inspect','ui.contract.enhanced','ui.contract.model.view']:\n    assert name not in missing, name\nprint('tier1 registry closure PASS; remaining missing=',len(missing))\nPY"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - Tier-1 intent registry entries

rollback:
  - git restore addons/smart_core/intents/registry_entries/__init__.py addons/smart_core/intents/registry_entries/tier1_surface.py

depends_on:
  - ITER-2026-04-09-1544
