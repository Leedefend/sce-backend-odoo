task_id: ITER-2026-03-30-336
title: Remove temporary contract-mode banner copy from optimized list toolbar
type: code
status: in_progress
priority: medium

goal: >
  Remove the explicit contract-mode banner copy that was added for visual
  verification, while keeping the optimized toolbar grouping and styling.

user_visible_outcome:
  - the toolbar no longer shows extra contract-mode hint copy
  - optimized filter grouping remains in place

scope:
  allowed_paths:
    - frontend/apps/web/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
  forbidden_paths:
    - addons/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - remove banner copy only
  - keep optimized grouping behavior
  - no contract or routing changes

context:
  branch: codex/next-round
  baseline_sha: 03bd2ef

architecture:
  layer_target: Frontend Layer
  module: optimized page toolbar presentation cleanup
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    The optimized toolbar now renders correctly, but the explicit contract-mode
    hint text was an implementation-side prompt and should be removed.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-336.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_command_failed
  - optimized_toolbar_regressed

deliverables:
  - removed contract-mode banner copy from PageToolbar
  - updated report and task result artifacts

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-336.yaml
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue

depends_on:
  - ITER-2026-03-30-335

notes: >
  This batch keeps the optimized toolbar structure and only removes the explicit
  hint/banner copy.
