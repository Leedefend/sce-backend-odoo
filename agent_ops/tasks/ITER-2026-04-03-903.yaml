task_id: ITER-2026-04-03-903
title: Stabilize chained host runtime probe by adding fallback retry order
type: backend
status: active
priority: high
mode: implement

goal: >
  Reduce non-deterministic host browser runtime probe failures in chained make
  execution by hardening fallback retry order and launch argument profile.

user_visible_outcome:
  - "链式验收中的浏览器运行时探针更稳定"
  - "显式网关链路可更稳定进入 preflight 阶段"

scope:
  allowed_paths:
    - scripts/verify/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - runtime_probe_launch_strategy_only
  - no_business_model_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-902
  priority_lane: usability_release_proof
  usability_battlefield: cross_stack_verification

architecture:
  layer_target: Product Release Usability Proof
  module: host runtime probe chained stability
  module_owner: scene_orchestration_governance
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch only tunes verification runtime launch orchestration and does
    not alter any business facts.
  reason: >
    Current chained host gate may pass standalone probe but fail nested probe;
    launch strategy needs deterministic fallback retry behavior.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-903.yaml
    - make verify.portal.host_browser_runtime_probe
    - GATEWAY_BASE_URLS=http://localhost:8069 make verify.portal.project_dashboard_primary_entry_browser_smoke.host

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - hardened runtime probe fallback retry behavior
  - updated explicit-gateway host gate evidence after chained stability tuning

rollback:
  - git restore scripts/verify/host_browser_runtime_probe.mjs
  - git restore agent_ops/tasks/ITER-2026-04-03-903.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-903.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-903.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-902
