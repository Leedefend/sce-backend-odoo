task_id: ITER-2026-04-10-1694
title: budget menu INTERNAL_ERROR order field fallback fix

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复“预算/成本”页面点击时报 INTERNAL_ERROR 的后端根因，避免 api.data 列表查询因无效排序字段崩溃。

user_visible_outcome:
  - "预算/成本入口可正常打开"
  - "后端不再抛 Invalid field 'parent_path'"

scope:
  allowed_paths:
    - addons/smart_core/handlers/api_data.py
    - agent_ops/tasks/ITER-2026-04-10-1694.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1694.md
    - agent_ops/state/task_results/ITER-2026-04-10-1694.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 600

change_rules:
  - root-cause fix only
  - no schema/model changes
  - no feature expansion

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend intent handler runtime
  module: api.data list ordering guard
  module_owner: smart_core handlers
  kernel_or_scenario: scenario
  backend_sub_layer_target: business_fact
  backend_sub_layer_reason: >
    修复真实查询执行错误，不涉及前端展示语义。
  reason: >
    日志显示 project.budget 列表查询使用无效字段 parent_path 排序导致 500，需要在 handler 层做排序字段兼容兜底。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1694.yaml"
    - "python3 scripts/verify/v2_app_governance_gate_audit.py --json"
    - "BASE_URL=http://127.0.0.1:5174 DB_NAME=sc_demo E2E_LOGIN=wutao E2E_PASSWORD=demo API_BASE_URL=http://127.0.0.1:8069 node scripts/verify/unified_system_menu_click_usability_smoke.mjs"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - stable budget menu list query behavior

rollback:
  - "git restore addons/smart_core/handlers/api_data.py"

depends_on:
  - ITER-2026-04-10-1693
