task_id: ITER-2026-04-08-1396
title: Scan permissions.effective.rights vs UI contract inconsistency candidates
status: active
type: governance-optimization
priority: high
mode: scan
batch: 1/1

goal: >
  在低成本 scan 阶段仅收集候选证据，定位 permissions.effective.rights
  与 UI 契约不一致的可能来源，不做结论与修复。

user_visible_outcome:
  - "输出可进入 screen 阶段的候选证据清单"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-08-1396.yaml
    - agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1396.md
    - agent_ops/state/task_results/ITER-2026-04-08-1396.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*

limits:
  max_files: 4
  max_candidates: 12
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: false
  allowed_roles:
    - executor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - single-stage execution only
  - do not mix scan screen verify responsibilities
  - scan only finds candidates and does not conclude root cause

context:
  branch: codex/next-round
  baseline_sha: 3466cad
  explicit_user_authorization: true
  usability_battlefield: backend

architecture:
  layer_target: Backend semantic-supply diagnosis
  module: permissions.effective.rights contract path
  module_owner: smart_core handlers + frontend contract consumer
  kernel_or_scenario: scenario
  backend_sub_layer_target: business_fact
  backend_sub_layer_reason: >
    permissions.effective.rights 属于权限业务事实，不是前端展示编排字段。
  reason: >
    先定位候选来源，再进入 screen 阶段做归因分类，避免跨阶段推理。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-08-1396.yaml"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - limit_exceeded
  - forbidden_path_touched
  - stage_boundary_broken


deliverables:
  - scan candidate list for screen stage

depends_on:
  - ITER-2026-04-08-1395

rollback:
  - "git restore agent_ops/tasks/ITER-2026-04-08-1396.yaml"
  - "git restore agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1396.md"
  - "git restore agent_ops/state/task_results/ITER-2026-04-08-1396.json"
  - "git restore docs/ops/iterations/delivery_context_switch_log_v1.md"
