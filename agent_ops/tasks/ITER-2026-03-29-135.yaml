task_id: ITER-2026-03-29-135
title: Track search mode overrides in scene merge resolver
type: governance_task
status: pending
priority: high

goal: Make scene merge resolver treat search_surface.mode as a first-class merged search semantic and track provider overrides.
user_visible_outcome:
  - merged scene contracts no longer silently override faceted/filter_bar search mode during provider merge

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core scene_merge_resolver
  reason: Scene merge resolver already treats searchpanel as a first-class search semantic, but provider merge still ignores search_surface.mode conflict tracking. This iteration normalizes search mode as part of canonical merge behavior.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-135.yaml
    - python3 -m py_compile addons/smart_core/core/scene_merge_resolver.py addons/smart_core/tests/test_scene_merge_resolver_search_mode_semantics.py
    - python3 addons/smart_core/tests/test_scene_merge_resolver_search_mode_semantics.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-135.yaml addons/smart_core/core/scene_merge_resolver.py addons/smart_core/tests/test_scene_merge_resolver_search_mode_semantics.py

depends_on:
  - ITER-2026-03-29-134

notes: "Batch-3 continuation: search mode should not be silently overridden during provider merge once faceted/filter_bar semantics are canonicalized upstream."
