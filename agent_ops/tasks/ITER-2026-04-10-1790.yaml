task_id: ITER-2026-04-10-1790
title: form shell projection default-container convergence

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  收敛 detail shell 投影中的 default 容器噪音，避免页面结构出现无语义 default shell。

user_visible_outcome:
  - "结构投影摘要中 default shell 显著减少或消失"
  - "页面区块结构更接近原生语义分层"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1790.yaml
    - frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1790.md
    - agent_ops/state/task_results/ITER-2026-04-10-1790.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend runtime projection only
  - no backend contract mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: detailLayoutRuntime shell projection
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    在 tab 结构已恢复后，继续修复 default shell 投影噪音，提升结构语义一致性。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1790.yaml"
    - "rg -n \"startsContainer|kind === 'default'\" frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts"
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - default shell projection convergence patch
  - iteration report and task result

rollback:
  - "git restore frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts"

depends_on:
  - ITER-2026-04-10-1789
