task_id: ITER-2026-04-09-1499
title: batch2 parse menu action raw bindings
type: governance-optimization
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在菜单事实层补齐 action 原始绑定解析，输出 action_type/action_id/action_model/action_exists
  与 act_window 原始字段事实，不进入 route/scene 解释。

user_visible_outcome:
  - "每个有 action 的菜单都能追溯标准化 action 事实，解析失败明确标记"

scope:
  allowed_paths:
    - addons/smart_core/delivery/menu_fact_service.py
    - scripts/verify/menu_fact_export.py
    - artifacts/menu/menu_fact_snapshot_v1.json
    - agent_ops/tasks/ITER-2026-04-09-1499.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1499.md
    - agent_ops/state/task_results/ITER-2026-04-09-1499.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - frontend/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - record_rules/**

limits:
  max_files: 7
  max_output_lines: 280

change_rules:
  - fact-layer only; no route/scene/delivery-mode fields
  - parse failure must be explicit (no silent swallow)
  - no ACL/business-fact mutation

context:
  branch: codex/next-round
  baseline_sha: 9109f99
  explicit_user_authorization: true

architecture:
  layer_target: Platform fact layer
  module: Menu action binding fact parser
  module_owner: smart_core delivery
  kernel_or_scenario: kernel
  backend_sub_layer_target: business_fact
  backend_sub_layer_reason: >
    菜单 action 绑定是平台事实，不属于前端导航解释语义。
  reason: >
    延续 Batch1 扫描结果，补齐 action 事实字段，为后续异常审计与解释层消费提供统一输入。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1499.yaml"
    - 'rg -n "action_type|action_id|action_model|action_exists|action_meta" addons/smart_core/delivery/menu_fact_service.py'
    - 'python3 -c "import json; from pathlib import Path; p=Path(\"artifacts/menu/menu_fact_snapshot_v1.json\"); data=json.loads(p.read_text(encoding=\"utf-8\")); print(\"has_flat\", isinstance(data.get(\"flat\"), list))"'

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - menu action raw binding parser
  - enriched menu fact snapshot structure

rollback:
  - git restore addons/smart_core/delivery/menu_fact_service.py
  - git restore scripts/verify/menu_fact_export.py
  - git restore artifacts/menu/menu_fact_snapshot_v1.json

depends_on:
  - ITER-2026-04-09-1498
