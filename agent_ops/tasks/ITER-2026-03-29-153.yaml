task_id: ITER-2026-03-29-153
title: Compact top-level runtime semantic surfaces in system init
type: governance_task
status: pending
priority: high

goal: Make top-level semantic_runtime and released_scene_semantic_surface in the minimal system.init payload follow the same canonical semantic-surface compaction rules already used for scene_ready scenes.
user_visible_outcome:
  - minimal startup payloads now keep top-level runtime semantic surfaces consistently, without passing through oversized or irregular semantic blobs

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core system_init_payload_builder
  reason: system.init now preserves canonical semantic surfaces in scene_ready scenes, but the top-level semantic_runtime and released_scene_semantic_surface are still copied wholesale. This iteration aligns top-level runtime semantic payloads with the same canonical compaction policy.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-153.yaml
    - python3 -m py_compile addons/smart_core/core/system_init_payload_builder.py addons/smart_core/tests/test_system_init_payload_builder_semantics.py
    - python3 addons/smart_core/tests/test_system_init_payload_builder_semantics.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-153.yaml addons/smart_core/core/system_init_payload_builder.py addons/smart_core/tests/test_system_init_payload_builder_semantics.py

depends_on:
  - ITER-2026-03-29-152

notes: "Batch acceleration: continue the direct downstream runtime chain by reusing canonical compaction rules for top-level semantic payloads."
