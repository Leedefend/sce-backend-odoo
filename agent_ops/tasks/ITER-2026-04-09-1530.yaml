task_id: ITER-2026-04-09-1530
title: backend refactor B2 validator schema-map placeholder
type: product-implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在 dispatcher validator 层引入 intent-level schema map 占位机制，为后续 request schema 强校验留出稳定挂点。

user_visible_outcome:
  - "每个请求可给出 schema key 口径，后续可无痛接入严格 schema 校验"

scope:
  allowed_paths:
    - addons/smart_core/controllers/intent_dispatcher.py
    - agent_ops/tasks/ITER-2026-04-09-1530.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1530.md
    - agent_ops/state/task_results/ITER-2026-04-09-1530.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/smart_core/core/intent_router.py
    - security/**
    - record_rules/**
    - "**/__manifest__.py"

limits:
  max_files: 5
  max_output_lines: 300

change_rules:
  - additive only
  - no strict validation gating yet

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Dispatcher orchestration layer
  module: validator schema-map placeholder
  module_owner: smart_core controllers
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    当前仅建立调度层校验映射挂点，不涉及业务事实变更。
  reason: >
    先冻结 schema_key 出口，再逐批接入 request_schema 强校验策略。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1530.yaml"
    - "python3 -m py_compile addons/smart_core/controllers/intent_dispatcher.py"
    - 'rg -n "INTENT_REQUEST_SCHEMA_MAP|schema_key" addons/smart_core/controllers/intent_dispatcher.py'

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - validator schema map placeholder

rollback:
  - git restore addons/smart_core/controllers/intent_dispatcher.py

depends_on:
  - ITER-2026-04-09-1529

