task_id: ITER-2026-04-02-763
title: Screen custom-frontend project journey usability slice
type: backend
status: active
priority: high
mode: screen

goal: >
  Select next custom-frontend usability slice along user journey:
  create project -> execute project lifecycle -> verify closure.

user_visible_outcome:
  - "从用户视角推进项目创建到管理闭环可用性"
  - "只选择一个可执行低风险切片"

scope:
  allowed_paths:
    - scripts/verify/**
    - Makefile
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen stage only
  - no implementation
  - usability-mainline-first
  - architecture-boundary-first

context:
  branch: codex/next-round
  baseline_sha: 4015259
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-762
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: project journey usability slice selection
  module_owner: verify scripts / make targets
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    User-journey usability gaps must be resolved in backend orchestration/contract path.
  reason: >
    Frontend stays generic rendering and interaction only.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-763.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed
  - no_candidate_selected

deliverables:
  - selected_project_journey_slice
  - governance/report artifacts for ITER-2026-04-02-763

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-02-763.yaml

depends_on:
  - ITER-2026-04-02-762
