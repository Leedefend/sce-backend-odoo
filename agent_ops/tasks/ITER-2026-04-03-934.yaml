task_id: ITER-2026-04-03-934
title: Delivery menu integrity drift verify (declared guard check)
type: governance
status: active
priority: high
mode: verify

goal: >
  Execute declared menu-integrity guard verify check to confirm reproducibility
  and finalize governance decision input.

user_visible_outcome:
  - "明确 menu integrity guard 在 sc_demo 的可复现结果"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify_only_declared_checks
  - no_implementation_changes

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-933
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: delivery menu integrity drift verify
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Verify stage runs selected guard check only and emits decision input.
  reason: >
    Screen stage selected menu-integrity guard replay as decisive verification.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-934.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim E2E_LOGIN=admin E2E_PASSWORD=admin make verify.product.delivery_menu_integrity_guard DB_NAME=sc_demo
    - cat artifacts/backend/product_delivery_menu_integrity_guard.json

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - delivery_menu_drift_verify_decision_input

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-934.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-934.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-934.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-933
