task_id: ITER-2026-03-29-208
title: Diagnose project form button source path
type: audit_task
status: pending
priority: high

goal: Determine exactly where the suspicious `Create project` button enters the governed `project.project` form contract by comparing raw view extraction, parser output, arch_parsed, and final contract delivery.
user_visible_outcome:
  - the next backend batch will target the real button-source defect instead of trimming frontend actions blindly
  - project form button cleanup can be based on contract fact parity

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**
    - addons/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: governance/backend root-cause audit
  module: project form button source diagnosis
  reason: The live project form button noise has been narrowed to backend contract facts, so this batch must identify which backend stage introduces the suspicious button before any implementation change.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-208.yaml
    - python3 -m py_compile agent_ops/scripts/project_form_button_source_audit.py
    - python3 agent_ops/scripts/project_form_button_source_audit.py --db sc_demo --login sc_fx_pm --container sc-backend-odoo-dev-odoo-1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - audit_report
  - task_result

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-208.yaml agent_ops/scripts/project_form_button_source_audit.py agent_ops/reports/2026-03-29/report.ITER-2026-03-29-208.md agent_ops/state/task_results/ITER-2026-03-29-208.json

depends_on:
  - ITER-2026-03-29-207

notes: "Diagnosis-only batch: compare raw form button extraction, parser output, arch_parsed snapshots, and final contract delivery for project.project form."
