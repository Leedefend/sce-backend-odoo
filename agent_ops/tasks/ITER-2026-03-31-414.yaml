task_id: ITER-2026-03-31-414
title: Audit post and system-role attachment path for customer bootstrap
type: audit
status: active
priority: high

goal: >
  Audit the current repository support for customer post attachment and
  system-role attachment so the next implementation batch can extend the
  bootstrap chain without guessing field ownership or group targets.

user_visible_outcome:
  - "明确岗位信息应该落到哪个模型/字段"
  - "明确系统角色应该挂到哪些现有 groups"
  - "明确下一批实现是否仍然属于低风险 additive 变更"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only in this batch
  - no addon or security file changes
  - use repository facts to identify the existing post and group attachment path

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Runtime and Ownership Audit
  module: smart_construction_custom customer bootstrap
  module_owner: customer-specific delivery customization
  kernel_or_scenario: scenario
  reason: >
    Company, department, and primary-department user bootstrap have already been
    executed successfully in sc_demo. The next low-risk step is to determine
    the exact repository-owned path for posts and system-role attachment before
    writing those fields or group relations.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-414.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - post_attachment_owner_unclear
  - system_role_group_target_unclear
  - attachment_path_requires_acl_or_security_changes

deliverables:
  - report and task result artifacts for ITER-2026-03-31-414
  - delivery log checkpoint with the exact next implementation suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-414.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-414.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-414.json

depends_on:
  - ITER-2026-03-31-413
