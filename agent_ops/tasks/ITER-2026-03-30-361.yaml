task_id: ITER-2026-03-30-361
title: Audit telemetry-only scene dimensions after payload cleanup
type: audit
status: in_progress
priority: medium

goal: >
  Audit the remaining telemetry-only scene_key usages after the user-facing
  payload cleanup is complete, so the team can explicitly decide which scene
  dimensions remain acceptable analytics metadata and which still need removal.

user_visible_outcome:
  - the residual scene cleanup line is closed with an explicit telemetry decision
  - user-facing payload pollution and telemetry-only dimensions are no longer mixed together
  - the next implementation batch can focus only on real remaining leaks

scope:
  allowed_paths:
    - addons/smart_construction_core/handlers/**
    - addons/smart_construction_core/services/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit first, do not mutate telemetry semantics blindly
  - distinguish analytics-only scene dimensions from user-facing payload leaks
  - no ACL, financial, or manifest changes

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: telemetry scene dimension audit
  module_owner: addons/smart_construction_core
  kernel_or_scenario: scenario
  reason: >
    After the payload cleanup slices passed, the remaining scene-key usages are
    concentrated in telemetry and reporting paths and now need an explicit
    governance decision rather than blind removal.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-361.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - telemetry_audit_cannot_distinguish_analytics_from_payload_usage

deliverables:
  - telemetry-only scene dimension audit result
  - report and task result artifacts for ITER-2026-03-30-361
  - delivery log checkpoint for the next cleanup or retention decision

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-361.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-361.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-361.json

depends_on:
  - ITER-2026-03-30-360
