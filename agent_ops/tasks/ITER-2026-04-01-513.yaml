task_id: ITER-2026-04-01-513
title: Reclassify dictionary and quota-center family on cleaned runtime
type: permission-governance
status: active
priority: medium

goal: >
  Re-audit the dictionary / quota-center / business-dictionary family after the
  quota-center entry-path fix, and decide whether the family can now be
  classified as a clean representative non-first-batch family.

user_visible_outcome:
  - "复判 dictionary / quota center 家族"
  - "明确修复后该家族是否已经 clean"
  - "不再重复进入实现面，除非出现新的 residual"

scope:
  allowed_paths:
    - addons/smart_construction_core/**
    - addons/smart_construction_custom/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit-first reclassification batch
  - no further entry-path implementation unless a new residual is discovered
  - reuse the fixed runtime path from ITER-2026-04-01-512

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom
  explicit_user_authorization: true

architecture:
  layer_target: Customer Delivery Flow Acceptance
  module: dictionary and quota-center family reclassification
  module_owner: customer delivery scenario surface
  kernel_or_scenario: scenario
  reason: >
    ITER-2026-04-01-512 closed the quota-center delivered-role executability
    residual. The next eligible low-risk step is to reclassify the parent
    dictionary/quota family on the cleaned runtime.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-513.yaml
    - "repository and runtime facts classify the dictionary/quota family on the fixed entry path"
    - "at least one representative dictionary/quota entry surface is re-audited without high-risk side effects"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - runtime_still_cannot_classify_dictionary_quota_family_with_confidence
  - a_new_residual_requires_scope_expansion
  - audit_requires_acl_or_manifest_changes

deliverables:
  - dictionary/quota family reclassification
  - governance/report artifacts for ITER-2026-04-01-513

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-513.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-513.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-513.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-512
