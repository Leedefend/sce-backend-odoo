task_id: ITER-2026-03-29-177
title: Audit consumer runtime bridge consistency in smart_scene parser bridge
type: governance_task
status: pending
priority: high

goal: Make smart_scene parser bridge emit explicit consumer runtime consistency assertions alongside consumer_runtime projection.
user_visible_outcome:
  - top-level diagnostics and scene_contract_v1 diagnostics now include consumer_runtime_assertions after parser bridge projection
  - parser_semantic_surface now carries bridge-level consumer runtime consistency assertions for downstream audit

scope:
  allowed_paths:
    - addons/smart_scene/core/**
    - addons/smart_scene/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: scene layer
  module: smart_scene scene_parser_semantic_bridge
  reason: consumer_runtime is already preserved through parser bridge, but downstream consumers still need a stable bridge-level audit result instead of re-computing alias/runtime alignment themselves.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-177.yaml
    - python3 -m py_compile addons/smart_scene/core/scene_parser_semantic_bridge.py addons/smart_scene/tests/test_scene_parser_semantic_bridge.py
    - python3 addons/smart_scene/tests/test_scene_parser_semantic_bridge.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-177.yaml addons/smart_scene/core/scene_parser_semantic_bridge.py addons/smart_scene/tests/test_scene_parser_semantic_bridge.py

depends_on:
  - ITER-2026-03-29-176

notes: "Batch acceleration: add bridge-level audit for consumer_runtime projection and alignment."
