task_id: ITER-2026-04-02-762
title: Verify one-shot custom-frontend compatibility slice
type: backend
status: active
priority: high
mode: verify

goal: >
  Verify one-shot compatibility fix across login token validation and
  scene-style publish smoke gates with nav fallback compatibility.

user_visible_outcome:
  - "登录 token 校验链路可用"
  - "发布场景门禁在 nav 回退形态可用"
  - "主线继续推进"

scope:
  allowed_paths:
    - scripts/verify/**
    - Makefile
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - verify-only
  - no new implementation
  - run declared checks only

context:
  branch: codex/next-round
  baseline_sha: cd15128
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-761
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: custom-frontend compatibility verify
  module_owner: verify scripts
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Verify scene schema contract for custom frontend consumption.
  reason: >
    Continue user-journey usability gates.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-762.yaml
    - make verify.portal.scene_contract_smoke.container
    - make verify.portal.scene_default_sort_smoke.container
    - make verify.portal.scene_list_profile_smoke.container
    - make verify.portal.scene_target_smoke.container
    - make verify.portal.scene_targets_resolve_smoke.container
    - make verify.portal.scene_filters_semantic_smoke.container
    - make verify.portal.scene_tiles_semantic_smoke.container
    - make verify.portal.scene_versioning_smoke.container
    - make verify.portal.scene_schema_smoke.container
    - make verify.portal.scene_semantic_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - verified_custom_frontend_login_scene_compat_slice
  - governance/report artifacts for ITER-2026-04-02-762

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-02-762.yaml
  - git restore agent_ops/reports/2026-04-02/report.ITER-2026-04-02-762.md
  - git restore agent_ops/state/task_results/ITER-2026-04-02-762.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-02-761
