task_id: ITER-2026-04-10-1780
title: frontend form structure forced projection recovery

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复前端布局节点识别与容器映射，让 contract 中的 group/notebook/page 结构稳定投影到页面，
  解决“页面结构始终没有出来”。

user_visible_outcome:
  - "group/notebook/page 结构节点可见"
  - "含 string/col/children 的容器不再被忽略"
  - "项目详情页结构层级明显增强"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1780.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1780.md
    - agent_ops/state/task_results/ITER-2026-04-10-1780.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend contract-consumer runtime only
  - no backend semantic mutation
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: form layout node normalization and projection
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    用户持续反馈结构未出来，需优先增强节点 kind 推断逻辑，避免结构节点在消费链被降级丢失。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1780.yaml"
    - "rg -n \"normalizeLayoutNodeKind|pages|tabs|children|attrs.string\" frontend/apps/web/src/pages/ContractFormPage.vue"
    - "make frontend.restart"

risk:
  level: medium-low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - frontend layout projection recovery patch
  - iteration report and task result

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1779
