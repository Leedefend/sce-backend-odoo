task_id: ITER-2026-03-30-369
title: Audit treasury-ledger generation prerequisites in demo flow
type: audit
status: in_progress
priority: medium

goal: >
  Explain why `资金台账` is still empty in `sc_demo` after the finance-generated
  page audit by tracing its allowed creation paths and comparing them with the
  current demo business facts.

user_visible_outcome:
  - a precise explanation of why `资金台账` has zero rows in `sc_demo`
  - a narrowed next step framed as data-gap, trigger-gap, or intentional deferral
  - no speculative coding before the prerequisite chain is clear

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no model, view, data, or frontend edits
  - use live `sc_demo` facts when needed, but keep this batch read-only
  - keep the conclusion anchored in business trigger facts rather than scene or portal semantics

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: treasury-ledger prerequisite audit
  module_owner: agent_ops
  kernel_or_scenario: scenario
  reason: >
    The finance-generated native audit reduced the remaining gap to a single
    empty page, so the next low-risk step is to classify whether the absence of
    treasury rows comes from missing demo facts, missing trigger execution, or
    an intentionally unfulfilled native path.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-369.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - audit_scope_spills_into_code_changes

deliverables:
  - treasury-ledger prerequisite audit report
  - task result artifact for ITER-2026-03-30-369
  - delivery log checkpoint with the next low-risk recommendation

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-369.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-369.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-369.json

depends_on:
  - ITER-2026-03-30-368
