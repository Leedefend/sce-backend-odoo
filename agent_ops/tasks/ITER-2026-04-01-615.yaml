task_id: ITER-2026-04-01-615
title: Screen and codify backend-first battlefield rule for lifecycle usability scheduling
type: governance-optimization
status: active
priority: high
mode: screen

goal: >
  Convert user architecture constraint into explicit scheduler preconditions so
  lifecycle usability work defaults to backend semantic supply.

user_visible_outcome:
  - "后续调度默认后端语义优先"
  - "前端模型特判被制度层禁止"
  - "调度前提从口头约束变为文档约束"

scope:
  allowed_paths:
    - AGENTS.md
    - docs/ops/**
    - agent_ops/templates/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 8
  max_candidates: 3
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - single-stage screen only
  - governance codification only
  - no business code changes

context:
  branch: codex/next-round
  baseline_sha: 36bbed6
  explicit_user_authorization: true
  priority_lane: P1_project_lifecycle_usability

architecture:
  layer_target: Agent Governance
  module: lifecycle usability scheduler boundary codification
  module_owner: agent_ops governance surface
  kernel_or_scenario: scenario
  reason: >
    User requested the boundary to be mandatory scheduler precondition.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-615.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - stage_boundary_broken

deliverables:
  - codified_backend_first_scheduler_rule
  - governance/report artifacts for ITER-2026-04-01-615

rollback:
  - git restore AGENTS.md
  - git restore docs/ops/codex_low_cost_iteration_policy_v1.md
  - git restore agent_ops/templates/task_low_cost.yaml
  - git restore agent_ops/tasks/ITER-2026-04-01-615.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-615.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-615.json

depends_on:
  - ITER-2026-04-01-614
