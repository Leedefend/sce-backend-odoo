task_id: ITER-2026-04-03-881
title: Continue low-risk parallel usability closure verification
type: backend
status: active
priority: medium
mode: verify

goal: >
  Maintain usability-first continuous iteration with backend-first low-risk
  parallel closure verification under constrained-runtime baseline.

user_visible_outcome:
  - "创建到管理闭环可用性持续稳定"
  - "低风险并行验证效率持续保持"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify-only
  - no_business_code_change

context:
  branch: codex/next-round
  baseline_sha: 1e10ebe
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-880
  priority_lane: usability_verification_mainline
  usability_battlefield: backend_plus_cross_stack

architecture:
  layer_target: Product Usability Closure
  module: low-risk parallel closure cadence
  module_owner: verify chain
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Backend execution/contract checks remain the closure source; frontend is a
    generic contract consumer without semantic inference.
  reason: >
    Keep throughput and confidence high without breaking architecture boundary.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-881.yaml
    - make verify.product.project_flow.execution_cost
    - make verify.product.project_flow.execution_payment
    - make verify.product.project_flow.execution_settlement
    - make verify.portal.cross_stack_contract_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - low-risk parallel closure verification result
  - governance/report artifacts for ITER-2026-04-03-881

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-881.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-881.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-881.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-880
