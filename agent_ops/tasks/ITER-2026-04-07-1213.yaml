task_id: ITER-2026-04-07-1213
title: Screen dedicated runtime environment repair lane
type: governance
status: completed
priority: high
mode: screen

goal: >
  Define a dedicated runtime environment repair lane for `/api/scenes/my`
  RemoteDisconnected behavior with clear scope and stop boundaries.

user_visible_outcome:
  - "环境修复专线边界可执行"
  - "后续实现批次可直接进入"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1213.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_environment_repair_lane_screen_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 220
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen-only classification
  - no code mutation

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_environment_repair_screen

architecture:
  layer_target: Governance Monitoring
  module: runtime environment repair screening
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Screen focuses on environment/service behavior around legacy endpoint runtime.
  reason: >
    Move from repeated evidence loops to a bounded remediation lane definition.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1213.yaml
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime environment repair lane screen document

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1213.yaml
  - git restore docs/audit/native/native_runtime_environment_repair_lane_screen_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1212
