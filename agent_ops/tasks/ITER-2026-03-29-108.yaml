task_id: ITER-2026-03-29-108
title: Make scene-ready entries explicitly expose parser semantics
type: governance_task
status: pending
priority: high

goal: Extend scene_ready_contract_builder so each scene-ready entry explicitly exposes parser semantic surfaces instead of keeping them only inside meta, surface, or render_hints internals.
user_visible_outcome:
  - scene_ready entries now carry stable parser_semantic_surface, semantic_view, semantic_page, and view_type fields

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: scene-ready contract consumption
  module: smart_core scene_ready_contract_builder
  reason: After parser semantics reach scene-ready orchestration internals, scene-ready entries themselves must explicitly expose that surface for downstream runtime/backend consumers.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-108.yaml
    - python3 -m py_compile addons/smart_core/core/scene_ready_entry_semantic_bridge.py addons/smart_core/core/scene_ready_contract_builder.py addons/smart_core/tests/test_scene_ready_entry_semantic_bridge.py
    - python3 addons/smart_core/tests/test_scene_ready_entry_semantic_bridge.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-108.yaml addons/smart_core/core/scene_ready_entry_semantic_bridge.py addons/smart_core/core/scene_ready_contract_builder.py addons/smart_core/tests/test_scene_ready_entry_semantic_bridge.py

depends_on:
  - ITER-2026-03-29-107
notes: "Batch-3 continuation: scene-ready entries explicitly expose parser semantics."
