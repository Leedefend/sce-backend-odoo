task_id: ITER-2026-04-10-1807
title: detail shell normalization convergence pass

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在 tab 已完整恢复前提下，收敛 detail shell/section 投影，使前端结构输出更贴近原生 form 层次。

user_visible_outcome:
  - "结构摘要 shell 标题与 section 分布更稳定，减少空壳/技术壳噪音"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1807.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1807.md
    - agent_ops/state/task_results/ITER-2026-04-10-1807.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend projection normalization only
  - no backend or contract mutation
  - additive and reversible

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: ContractFormPage detail shell normalization
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    当前 tabs 已达 11 并对账通过，但 shell/section 仍含空壳与标题噪音，需做消费端收口。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1807.yaml"
    - 'rg -n "normalizedDetailShells|normalizeShellSummaryTitle|isRenderableSection" frontend/apps/web/src/pages/ContractFormPage.vue'
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - refined shell normalization in structure align mode
  - iteration report and state

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1806
