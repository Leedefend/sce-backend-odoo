task_id: ITER-2026-04-09-1552
title: backend refactor B-line controller thinness orm-hint screen v1
type: governance
status: active
priority: medium
mode: screen
batch: 1/1

goal: >
  对 controller_thin_guard 的 ORM hints 做 B 线 screen 分类，冻结后续实现顺序。

user_visible_outcome:
  - "controller thinness 改造有分层优先级与 next_batch 标记"

scope:
  allowed_paths:
    - artifacts/architecture/controller_thin_guard_audit_v1.json
    - artifacts/architecture/controller_thin_screen_v1.json
    - docs/architecture/controller_thin_screen_v1.md
    - agent_ops/tasks/ITER-2026-04-09-1552.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1552.md
    - agent_ops/state/task_results/ITER-2026-04-09-1552.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 7
  max_output_lines: 320

change_rules:
  - screen stage only
  - classify from existing audit artifact only
  - no runtime code changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Governance screening layer
  module: controller thinness candidates
  module_owner: architecture governance
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    仅治理分类，不修改业务事实。
  reason: >
    A1 已闭环，B 线需先 screen 再 implement。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1552.yaml"
    - "python3 - <<'PY'\nimport json\nobj=json.load(open('artifacts/architecture/controller_thin_screen_v1.json','r',encoding='utf-8'))\nassert obj['status']=='PASS'\nassert obj['summary']['candidate_count']==4\nprint('controller thin screen PASS')\nPY"
    - "rg -n \"Tier-1|Tier-2|Tier-3|next_batch\" docs/architecture/controller_thin_screen_v1.md"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - classification_uncertainty

deliverables:
  - controller thinness candidate screen classification

rollback:
  - git restore artifacts/architecture/controller_thin_screen_v1.json docs/architecture/controller_thin_screen_v1.md

depends_on:
  - ITER-2026-04-09-1551
