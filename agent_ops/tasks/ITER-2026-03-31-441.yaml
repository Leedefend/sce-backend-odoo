task_id: ITER-2026-03-31-441
title: Redesign bootstrap semantics for platform baseline versus one-time DB initialization
type: governance
status: active
priority: high

goal: >
  Redesign the bootstrap logic around smart_platform_bootstrap so that
  repeat-safe platform baseline behavior is separated from one-time fresh-DB
  initialization behavior before any further physical migration continues.

user_visible_outcome:
  - "明确哪些 bootstrap 逻辑应可重复重放"
  - "明确哪些逻辑只能在 fresh DB 首次初始化时执行"
  - "为后续实现批次冻结新的 hook/module 语义"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - read-only governance batch
  - no addon changes
  - no manifest changes

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Platform Bootstrap Semantics Governance
  module: smart_platform_bootstrap / smart_construction_bootstrap
  module_owner: platform bootstrap semantics and migration path
  kernel_or_scenario: kernel
  reason: >
    The failed migration proved that the legacy hook mixes repeat-safe platform
    defaults with one-time database mutations, so the next step is to redesign
    semantics around actual operational need rather than preserving old
    implementation shape.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-441.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - repository_facts_are_insufficient_to_separate_repeat_safe_and_one_time_bootstrap_logic

deliverables:
  - implementation/report/result artifacts for ITER-2026-03-31-441

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-441.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-441.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-441.json

depends_on:
  - ITER-2026-03-31-440
