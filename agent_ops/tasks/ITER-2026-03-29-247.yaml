task_id: ITER-2026-03-29-247
title: Audit backend intent dispatch parse and contract assembly boundaries
type: audit_task
status: in_progress
priority: high

goal: Trace the backend request chain across intent handling, dispatch, parsing, and contract assembly so each module boundary is explicit and duplicate or ambiguous responsibilities are identified without changing the overall architecture.
user_visible_outcome:
  - a clear backend chain map from intent entry to final contract assembly
  - a gap matrix that identifies duplicated, ambiguous, or misplaced responsibilities across the chain
  - a sequenced next-iteration plan for low-risk cleanup batches

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
    - docs/architecture/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - audit_only
  - no_product_code_change
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform layer audit
  module: intent handling dispatch parsing and contract assembly
  reason: Before starting a continuous cleanup chain, the current backend flow must be traced end to end so boundary ownership is explicit and duplicate or fuzzy responsibilities are removed in later targeted iterations.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-247.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - audit_scope_uncertain

deliverables:
  - backend_chain_audit_report
  - boundary_gap_matrix
  - next_iteration_queue

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-247.yaml docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-29-246

notes: "Continuous iteration bootstrap for backend chain clarification. No code changes in this batch."
