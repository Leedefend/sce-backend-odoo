task_id: ITER-2026-03-29-245
title: Trim empty detail group shells and repeated labels
type: implementation_task
status: done
priority: medium

goal: Polish the detail renderer by removing empty structural group shells and suppressing repeated generic labels after hierarchy restoration.
user_visible_outcome:
  - project detail no longer shows empty structural shells such as blank core groups
  - restored structure remains intact while repeated generic labels are reduced

scope:
  allowed_paths:
    - frontend/apps/web/src/**
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - addons/**
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**/__tests__/**

change_rules:
  - additive_or_minimal_fix_only
  - no_schema_change
  - no_backend_contract_change
  - no_model_specific_frontend_hardcode

architecture:
  layer_target: frontend detail renderer
  module: detailLayoutRuntime and DetailShellLayout
  reason: Structure is already aligned enough for delivery; the remaining gap is low-risk renderer cleanup for empty group shells and repeated generic labels.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-245.yaml
    - bash -lc 'source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20 >/dev/null && pnpm -C frontend/apps/web typecheck:strict'

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed

deliverables:
  - detail_cleanup_fix
  - task_report
  - task_result

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-245.yaml frontend/apps/web/src/app/runtime/detailLayoutRuntime.ts frontend/apps/web/src/components/template/DetailShellLayout.vue docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-29-244

notes: "Frontend-only batch: remove empty group shells and reduce repeated generic labels."
