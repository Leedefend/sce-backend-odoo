task_id: ITER-2026-04-06-1201
title: Stage-B pre-screen for medium-risk extension boundary and gate set
type: governance-optimization
status: completed
priority: high
mode: screen

goal: >
  Define Stage-B medium-risk extension boundary, allowed file surface, and
  mandatory gate set before any Stage-B execute batch.

user_visible_outcome:
  - "Stage-B 中风险边界清单"
  - "Stage-B 执行门禁命令集合"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-06-1201.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_stage_b_prescreen_boundary_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 180
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen only
  - no Stage-B implementation in this batch

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_stage_b_prescreen

architecture:
  layer_target: Governance Monitoring
  module: stage-b boundary screening
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch is governance screening for execution boundary and gate set.
  reason: >
    Stage-A evidence is complete; Stage-B requires explicit medium-risk boundary
    before execute tasks.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-06-1201.yaml

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched
  - stage_boundary_broken

deliverables:
  - stage-b boundary and mandatory gate definition

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-06-1201.yaml
  - git restore docs/audit/native/native_stage_b_prescreen_boundary_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-06-1200
