task_id: ITER-2026-03-29-229
title: Audit list and detail contract gap matrix
type: audit_task
status: pending
priority: high

goal: Build a repeatable list/detail gap matrix that compares live backend page facts against current frontend consumer coverage so the next structure-improvement batch is driven by explicit gaps.
user_visible_outcome:
  - the next page-structure batch targets concrete list/detail gaps instead of intuition
  - the team gets a reusable audit entrypoint for list/detail contract-consumer alignment

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**
    - addons/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend fact audit + frontend consumer audit
  module: list/detail contract gap matrix
  reason: Before continuing page implementation, we need a single audit that states which live list/detail facts already exist and whether the current frontend consumers actually cover them.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-229.yaml
    - python3 -m py_compile agent_ops/scripts/list_detail_gap_audit.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - uncertain_runtime_state

deliverables:
  - audit_report
  - task_result
  - reusable_audit_script

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-229.yaml agent_ops/scripts/list_detail_gap_audit.py agent_ops/reports/2026-03-29/report.ITER-2026-03-29-229.md agent_ops/state/task_results/ITER-2026-03-29-229.json docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-29-220
  - ITER-2026-03-29-222

notes: "Diagnosis-only batch: compare live backend list/detail facts against current frontend consumer coverage before implementing the next page-structure batch."
