task_id: ITER-2026-03-28-082
title: Move form parsing onto native view parse pipeline
type: governance_task
status: pending
priority: high
goal: Move existing form parsing onto the new native view parse pipeline and narrow the legacy path.
user_visible_outcome:
  - form parsing uses unified pipeline
scope:
  allowed_paths:
    - addons/smart_core/view/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change
architecture:
  layer_target: platform kernel convergence batch-2
  module: smart_core native view form parser pipeline
  reason: Deliver the first substantive platform parser capability increment.
acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-082.yaml
risk:
  level: low
  manual_approval_required: false
stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision
deliverables:
  - code
rollback:
  - git restore agent_ops/tasks/ITER-2026-03-28-082.yaml
depends_on:
  - ITER-2026-03-28-081
notes: "Batch-2 Step 3."
