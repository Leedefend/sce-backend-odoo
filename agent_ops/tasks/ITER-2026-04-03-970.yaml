task_id: ITER-2026-04-03-970
title: Align delivery integrity guards with unified system menu semantics
type: backend
status: active
priority: high
mode: implement

goal: >
  Update delivery menu/policy integrity guards to validate unified system-menu
  projection semantics instead of legacy release.* menu-key identity.

user_visible_outcome:
  - "发布门禁在统一系统菜单策略下恢复可执行且不误报"

scope:
  allowed_paths:
    - scripts/verify/product_delivery_menu_integrity_guard.py
    - scripts/verify/product_delivery_policy_guard.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 7
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - guard_semantics_alignment_only
  - no_business_fact_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-969
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: release guard semantics
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    The change aligns verification semantics for navigation projection and
    policy-to-runtime mapping; no business fact changes are introduced.
  reason: >
    Release gate is blocked by legacy guard expectations (`release.*` keys)
    after unified system-menu projection rollout.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-970.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim make restart
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim E2E_LOGIN=svc_e2e_smoke E2E_PASSWORD=demo make verify.product.delivery_menu_integrity_guard
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim E2E_LOGIN=svc_e2e_smoke E2E_PASSWORD=demo make verify.product.delivery_policy_guard
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost API_BASE_URL=http://127.0.0.1:18069 DB_NAME=sc_prod_sim E2E_LOGIN=svc_e2e_smoke E2E_PASSWORD=demo make verify.release.delivery_engine.v1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - delivery_guard_semantics_aligned_with_unified_menu

rollback:
  - git restore scripts/verify/product_delivery_menu_integrity_guard.py
  - git restore scripts/verify/product_delivery_policy_guard.py
  - git restore agent_ops/tasks/ITER-2026-04-03-970.yaml

depends_on:
  - ITER-2026-04-03-969
