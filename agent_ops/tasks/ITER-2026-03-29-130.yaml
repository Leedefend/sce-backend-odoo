task_id: ITER-2026-03-29-130
title: Count searchpanel semantics in scene dsl surface profile
type: governance_task
status: pending
priority: high

goal: Make scene dsl compiler surface profile count searchpanel semantics alongside filters and group_by.
user_visible_outcome:
  - scene compile metadata now reflects faceted search usage instead of undercounting search semantics

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core scene_dsl_compiler
  reason: Scene dsl compiler still reports search usage with a legacy surface profile that ignores faceted searchpanel semantics. This iteration makes compile metadata consistent with the parser-driven search contract.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-130.yaml
    - python3 -m py_compile addons/smart_core/core/scene_dsl_compiler.py addons/smart_core/tests/test_scene_dsl_compiler_surface_profile.py
    - python3 addons/smart_core/tests/test_scene_dsl_compiler_surface_profile.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-130.yaml addons/smart_core/core/scene_dsl_compiler.py addons/smart_core/tests/test_scene_dsl_compiler_surface_profile.py

depends_on:
  - ITER-2026-03-29-129

notes: "Batch-3 continuation: compile-stage semantic profile should count faceted searchpanel semantics."
