task_id: ITER-2026-04-01-606
title: Screen next lifecycle slice for created-project to first-management-action continuity
type: frontend
status: active
priority: high
mode: screen

goal: >
  Select the next bounded lifecycle-first slice that improves continuity from
  "project created" to "first management action".

user_visible_outcome:
  - "明确创建后下一步管理动作引导切片"
  - "继续降低项目主链操作中断概率"
  - "保持低风险前端边界"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 3
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - screen stage only
  - prioritize created-to-manage continuity
  - do not reopen repo-wide scan
  - do not implement in this batch

context:
  branch: codex/next-round
  baseline_sha: 3e10bd1
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-605
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_project_lifecycle_usability

architecture:
  layer_target: Frontend Layer
  module: project lifecycle continuity screen
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    After empty-list create guidance, the next leverage point is explicit
    guidance from list rows to first management actions.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-606.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - no_bounded_candidate_remains

deliverables:
  - selected_next_created_to_manage_continuity_slice
  - governance/report artifacts for ITER-2026-04-01-606

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-606.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-606.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-606.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-605
