task_id: ITER-2026-03-29-159
title: Normalize smart_scene workflow and validation summaries
type: governance_task
status: pending
priority: high

goal: Make smart_scene project canonical workflow and validation profile counts into record_state_summary using existing orchestration vocabulary.
user_visible_outcome:
  - scene contracts now expose workflow and validation summary counts consistently in record_state_summary
  - scene contracts now preserve required validation fields in scene permissions summaries, not only as disabled submit reasons

scope:
  allowed_paths:
    - addons/smart_scene/core/**
    - addons/smart_scene/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: scene layer
  module: smart_scene scene_engine
  reason: smart_scene already projects workflow transitions and validation gating into permissions, but it still lacks the profile summary counts already used in orchestration snapshots. This iteration aligns scene-layer summaries with existing workflow/validation vocabulary.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-159.yaml
    - python3 -m py_compile addons/smart_scene/core/scene_engine.py addons/smart_scene/tests/test_scene_engine_semantics.py
    - python3 addons/smart_scene/tests/test_scene_engine_semantics.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-159.yaml addons/smart_scene/core/scene_engine.py addons/smart_scene/tests/test_scene_engine_semantics.py

depends_on:
  - ITER-2026-03-29-158

notes: "Batch acceleration: align smart_scene record_state_summary with existing workflow and validation profile vocabulary."
