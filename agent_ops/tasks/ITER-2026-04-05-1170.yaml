task_id: ITER-2026-04-05-1170
title: Stabilize scene contract schema guard live-fetch timeout fallback
type: implementation
status: active
priority: high
mode: execute

goal: >
  Unblock ci.preflight.contract by making scene_contract_v1.field_schema guard
  default to state/snapshot fallback when live fetch times out, while retaining
  live-first behavior and warning evidence.

user_visible_outcome:
  - "verify.scene.contract_v1.field_schema.guard 在超时场景稳定通过"
  - "ci.preflight.contract 继续推进"

scope:
  allowed_paths:
    - scripts/verify/scene_contract_v1_field_schema_guard.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - security/**
    - record_rules/**
    - addons/**/__manifest__.py
    - migrations/**

limits:
  max_files: 6
  max_candidates: 10
  max_output_lines: 220
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - guard_logic_only
  - live_first_then_fallback
  - no_business_contract_schema_change

context:
  branch: codex/next-round
  baseline_sha: da08944
  explicit_user_authorization: true
  input_from: user_execute_1170
  priority_lane: ci_unblock

architecture:
  layer_target: Governance Monitoring
  module: verify scene contract v1 field schema guard
  module_owner: platform
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: adjust guard fallback policy for deterministic verification in restricted runtime conditions.
  reason: resolve live-fetch timeout stop condition in verify.scene.contract_v1.field_schema.guard.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-1170.yaml
    - make verify.scene.contract_v1.field_schema.guard
    - make ci.preflight.contract

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - timeout-safe scene_contract_v1 schema guard fallback behavior

rollback:
  - git restore scripts/verify/scene_contract_v1_field_schema_guard.py
  - git restore agent_ops/tasks/ITER-2026-04-05-1170.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-1170.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-1170.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-1169

