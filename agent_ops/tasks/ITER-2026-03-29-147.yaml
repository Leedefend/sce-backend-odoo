task_id: ITER-2026-03-29-147
title: Count canonical permission surface in scene ready metrics
type: governance_task
status: pending
priority: high

goal: Make scene ready consumption metrics count meaningful canonical permission_surface signals alongside the already-counted base_facts.permissions coverage.
user_visible_outcome:
  - scene ready metrics now expose whether permission semantics are actually consumed, instead of only reporting that permission facts existed upstream

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: backend orchestration
  module: smart_core scene_ready_contract_builder
  reason: scene_ready metrics now count canonical base_facts.permissions but still ignore the matching canonical permission_surface. This iteration keeps surface-level consumption metrics aligned with the permission semantics already present in the builder.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-147.yaml
    - python3 -m py_compile addons/smart_core/core/scene_ready_contract_builder.py addons/smart_core/tests/test_scene_ready_consumption_metrics.py
    - python3 addons/smart_core/tests/test_scene_ready_consumption_metrics.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-147.yaml addons/smart_core/core/scene_ready_contract_builder.py addons/smart_core/tests/test_scene_ready_consumption_metrics.py

depends_on:
  - ITER-2026-03-29-146

notes: "Batch acceleration: continue grouped scene_ready metrics alignment by adding the missing canonical permission surface hit/rate tracking."
