task_id: ITER-2026-04-03-969
title: Wire unified menu click smoke into release gate
type: backend
status: active
priority: high
mode: implement

goal: >
  Integrate unified system menu click usability smoke into formal Makefile
  release verification chain so publish decision includes per-leaf click safety.

user_visible_outcome:
  - "发布门禁自动包含系统菜单逐叶子点击可用性检查"

scope:
  allowed_paths:
    - Makefile
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - release_gate_wiring_only
  - no_business_fact_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-968
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: release gate orchestration
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This change only modifies verification orchestration semantics in release
    gating; it does not alter business-fact payloads.
  reason: >
    User requires executable release decision confidence. Existing release gate
    must include the new unified menu per-leaf click smoke.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-969.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim make restart
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost API_BASE_URL=http://127.0.0.1:18069 DB_NAME=sc_prod_sim E2E_LOGIN=svc_e2e_smoke E2E_PASSWORD=demo make verify.portal.unified_system_menu_click_usability_smoke.host
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost API_BASE_URL=http://127.0.0.1:18069 DB_NAME=sc_prod_sim E2E_LOGIN=svc_e2e_smoke E2E_PASSWORD=demo make verify.release.delivery_engine.v1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - release_gate_wired_with_unified_menu_click_smoke

rollback:
  - git restore Makefile
  - git restore agent_ops/tasks/ITER-2026-04-03-969.yaml

depends_on:
  - ITER-2026-04-03-968
