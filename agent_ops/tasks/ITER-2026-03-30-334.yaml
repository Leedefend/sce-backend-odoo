task_id: ITER-2026-03-30-334
title: Make list optimization toolbar visually distinct from legacy fallback
type: code
status: in_progress
priority: high

goal: >
  Make the backend-driven optimization toolbar on projects.list visually
  distinguishable from the legacy fallback toolbar, without changing contract
  semantics or the consumer chain.

user_visible_outcome:
  - users can visually tell that the list page is rendering the contract-driven toolbar
  - high-frequency filters and advanced filters are clearly separated

scope:
  allowed_paths:
    - frontend/apps/web/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
  forbidden_paths:
    - addons/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - presentation only
  - no backend contract changes
  - no new toolbar semantics

context:
  branch: codex/next-round
  baseline_sha: 03bd2ef

architecture:
  layer_target: Frontend Layer
  module: page toolbar contract presentation
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    The projects.list page already consumes optimization_composition correctly,
    but the rendered toolbar looks too similar to the legacy fallback layout.
    This batch only improves visible differentiation.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-334.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_command_failed
  - contract_semantics_changed

deliverables:
  - clearer optimization toolbar presentation in PageToolbar
  - updated report and task result artifacts

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-334.yaml
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue

depends_on:
  - ITER-2026-03-30-333

notes: >
  This batch must not change optimization_composition meaning, ordering, or
  fallback eligibility. It is a visibility and affordance improvement only.
