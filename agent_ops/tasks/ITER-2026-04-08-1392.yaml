task_id: ITER-2026-04-08-1392
title: Align scene meta trace smoke with current hud payload trace field placement
status: active
type: governance
priority: high
mode: execute
batch: 1/1

goal: >
  Fix false mismatch failures in scene_meta_trace_smoke by validating
  scene_channel at meta trace level while matching hud payload keys by contract.

user_visible_outcome:
  - "scene_meta_trace_smoke 不再因 scene_channel hud缺失误报"
  - "preflight 继续推进到下一真实门禁"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-08-1392.yaml
    - scripts/verify/scene_meta_trace_smoke.py
    - agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1392.md
    - agent_ops/state/task_results/ITER-2026-04-08-1392.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*

limits:
  max_files: 5
  max_output_lines: 220
  forbid_repo_wide_scan: true

change_rules:
  - additive-only
  - no-acl-table-change
  - no-financial-semantic-change

context:
  branch: codex/next-round
  baseline_sha: 3466cad
  explicit_user_authorization: true

architecture:
  layer_target: Verification guard logic layer
  module: scene_meta_trace_smoke
  module_owner: platform verification governance
  kernel_or_scenario: scenario
  reason: >
    Current hud payload exposes selector fields, while scene_channel remains
    in meta trace; smoke currently compares mismatched surfaces directly.

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-08-1392.yaml"
    - "make verify.scene.meta.trace.smoke DB_NAME=sc_demo E2E_LOGIN=admin E2E_PASSWORD=admin"
    - "CONTRACT_PREFLIGHT_SKIP_DOCS=1 CONTRACT_PREFLIGHT_SKIP_GROUPED_GOV_BUNDLE=1 CONTRACT_PREFLIGHT_SKIP_SCENE_CAPABILITY_GUARD=1 CONTRACT_PREFLIGHT_STRICT_VIEW_TYPES=0 BASELINE_FREEZE_ENFORCE=0 make verify.contract.preflight DB_NAME=sc_demo E2E_LOGIN=admin E2E_PASSWORD=admin"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_command_failed

deliverables:
  - scene meta trace smoke assertions aligned with current hud field placement
  - updated preflight evidence

depends_on:
  - ITER-2026-04-08-1391

rollback:
  - "git restore scripts/verify/scene_meta_trace_smoke.py"
  - "git restore agent_ops/tasks/ITER-2026-04-08-1392.yaml"
  - "git restore agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1392.md"
  - "git restore agent_ops/state/task_results/ITER-2026-04-08-1392.json"
  - "git restore docs/ops/iterations/delivery_context_switch_log_v1.md"
