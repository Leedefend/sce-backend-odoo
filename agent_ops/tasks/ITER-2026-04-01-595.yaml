task_id: ITER-2026-04-01-595
title: Normalize HUD route fallback placeholder for continuity readability
type: frontend
status: active
priority: high
mode: implement

goal: >
  Normalize the HUD `当前路由` empty-state display to use the same placeholder
  style as other HUD entries, reducing blank-row ambiguity in record-view.

user_visible_outcome:
  - "HUD 空路由不再显示空白值"
  - "显示一致的占位符语义"
  - "不改变字段覆盖和行为逻辑"

scope:
  allowed_paths:
    - frontend/apps/web/src/app/action_runtime/useActionViewHudEntriesRuntime.ts
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - bounded P1 usability implementation
  - do not change HUD field coverage
  - do not change HUD behavior semantics

context:
  branch: codex/next-round
  baseline_sha: 5555e15
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-594
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: record-view HUD route fallback readability
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    HUD currently uses an empty string for missing route path while most entries
    use a visible placeholder. This creates avoidable ambiguity during quick
    debugging and continuity checks.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-595.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.recordview_hud_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - dedicated_hud_smoke_still_fails

deliverables:
  - bounded_hud_route_placeholder_consistency_fix
  - governance/report artifacts for ITER-2026-04-01-595

rollback:
  - git restore frontend/apps/web/src/app/action_runtime/useActionViewHudEntriesRuntime.ts
  - git restore agent_ops/tasks/ITER-2026-04-01-595.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-595.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-595.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-594
