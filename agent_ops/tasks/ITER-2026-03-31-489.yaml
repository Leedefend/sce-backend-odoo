task_id: ITER-2026-03-31-489
title: Design controlled pending-payment acceptance sample strategy
type: permission-governance
status: active
priority: high

goal: >
  Under explicit high-risk authorization, design a reversible acceptance-sample
  strategy for the uncovered pending-payment next-action branch, including the
  exact runtime mutation boundary, sample marker, verification path, and rollback.

user_visible_outcome:
  - "明确 pending-payment recommendation 分支的受控验收样本策略"
  - "把高风险点收缩到单一 scratch payment.request 样本，而不是扩散到真实财务链"
  - "只有在回滚和验证路径都明确后，才允许进入实际 runtime 造样"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit-and-strategy-only batch
  - no code, ACL, record-rule, manifest, schema, or seed data changes
  - runtime mutation is not allowed in this batch; this batch only defines the controlled sample strategy
  - the strategy must identify exact create fields, exact state target, exact verification query, and exact cleanup query

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom
  explicit_user_authorization: true

architecture:
  layer_target: Customer Delivery Flow Acceptance
  module: pending-payment controlled acceptance sample strategy
  module_owner: customer delivery permission surface
  kernel_or_scenario: scenario
  reason: >
    ITER-2026-03-31-488 narrowed the remaining blocker to the pending-payment
    branch specifically, and the user explicitly selected the high-risk route
    for a dedicated acceptance-sample strategy.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-489.yaml
    - "repository and runtime audit define one concrete, reversible pending-payment acceptance sample strategy with exact verification and cleanup steps"

risk:
  level: high
  manual_approval_required: false

stop_conditions:
  - no_single_reversible_pending_payment_sample_strategy_can_be_stated_with_certainty
  - the_strategy_requires_unbounded_finance_side_effects

deliverables:
  - controlled pending-payment acceptance sample strategy
  - governance/report artifacts for ITER-2026-03-31-489

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-489.yaml
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-489.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-489.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-31-488
