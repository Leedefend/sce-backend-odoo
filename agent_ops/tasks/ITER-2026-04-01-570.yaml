task_id: ITER-2026-04-01-570
title: Select the next P1 native list mainline usability family after route-preset recovery
type: frontend
status: active
priority: high
mode: screen

goal: >
  Select the next P1 native list mainline usability family after route-preset
  visibility recovery, prioritizing the ability to continue list operations
  over display-only polish.

user_visible_outcome:
  - "明确下一条 P1 列表主链可用性任务"
  - "优先处理会影响继续操作理解的缺口"
  - "保持 screen 只做主线归类，不扩展到无关模块"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 4
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - P1 screen stage only
  - consume only existing toolbar and delivery artifacts
  - do not reopen repository scan
  - do not implement in this batch

context:
  branch: codex/next-round
  baseline_sha: ed77101
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-569
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: native list mainline usability screen
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    Route-preset continuity is restored. The next usability-first step should
    target the remaining list-mainline gap most likely to confuse or block the
    user from continuing search, sort, open-record, or save-return flows.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-570.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - screen_reads_new_product_files
  - screen_reopens_repo_scan
  - no_p1_mainline_candidate_remains

deliverables:
  - selected_next_p1_mainline_usability_family
  - governance/report artifacts for ITER-2026-04-01-570

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-570.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-570.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-570.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-569
