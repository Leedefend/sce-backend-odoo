task_id: ITER-2026-03-31-450
title: Audit Sichuan Baosheng delivery initialization data on non-demo sc_odoo
type: implementation
status: active
priority: high

goal: >
  Verify the Sichuan Baosheng customer-delivery initialization chain on the
  clean non-demo sc_odoo runtime, and determine exactly which customer-specific
  company, department, post, user, role, and entry data has already landed.

user_visible_outcome:
  - "明确 sc_odoo 是否已经具备四川保盛完整交付初始化数据"
  - "逐项列出已进库与未进库的客户数据"
  - "为后续按客户完整流程验证提供事实基线"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - read-only audit batch
  - no repository code changes beyond governance artifacts
  - query runtime facts only

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司

architecture:
  layer_target: Customer Delivery Runtime Audit
  module: sc_odoo non-demo runtime
  module_owner: customer delivery verification chain
  kernel_or_scenario: scenario
  reason: >
    The user wants subsequent validation to follow the full Sichuan Baosheng
    delivery flow, so the next step is to audit whether the customer-specific
    initialization data has actually landed in the clean non-demo runtime.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-450.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - sc_odoo_runtime_cannot_be_inspected_with_certainty
  - customer_specific_data_state_is_ambiguous

deliverables:
  - implementation/report/result artifacts for ITER-2026-03-31-450

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-450.yaml
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-450.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-450.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-31-446
