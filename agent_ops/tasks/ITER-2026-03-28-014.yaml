task_id: ITER-2026-03-28-014
title: Build system_init runtime trace inventory
type: governance_task
status: pending
priority: high

goal: Trace current system.init handoff points so the first code batch can isolate where base facts end, where scene/runtime assembly begins, and where fallback or mixed authority still exists.
user_visible_outcome:
  - a system_init runtime trace inventory document exists
  - key handoff points are mapped from handler to payload builders and scene/runtime hooks
  - the first code convergence slice has a bounded trace target

scope:
  allowed_paths:
    - docs/architecture/**
    - agent_ops/tasks/**
    - agent_ops/queue/**
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Platform Layer
  module: docs/architecture system.init runtime trace inventory
  reason: Bound the first implementation slice by tracing current system.init authority handoff zones before editing platform code.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-014.yaml
    - test -f docs/architecture/system_init_runtime_trace_inventory_v1.md
    - rg -n "Handoff Points|Base Facts Boundary|Scene Assembly Boundary|Fallback Zones|First Code Slice Boundary" docs/architecture/system_init_runtime_trace_inventory_v1.md

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore docs/architecture/system_init_runtime_trace_inventory_v1.md agent_ops/tasks/ITER-2026-03-28-014.yaml agent_ops/queue/platform_kernel_refactor_prep_queue.yaml docs/ops/iterations/delivery_context_switch_log_v1.md docs/ops/releases/archive/temp/TEMP_agent_ops_continuous_iteration_status_20260328.md

depends_on:
  - ITER-2026-03-28-013
notes: "First implementation-prep runtime trace task for the selected system.init slice."
