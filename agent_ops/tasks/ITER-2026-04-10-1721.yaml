task_id: ITER-2026-04-10-1721
title: form route shell topbar flatten batch

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在表单路由下隐藏外层壳层大头部，避免与表单页内部头部重复，形成单层编辑工作区。

user_visible_outcome:
  - "表单页顶部大横条消失"
  - "进入表单后直接看到编辑主体"

scope:
  allowed_paths:
    - frontend/apps/web/src/layouts/AppShell.vue
    - agent_ops/tasks/ITER-2026-04-10-1721.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1721.md
    - agent_ops/state/task_results/ITER-2026-04-10-1721.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 700

change_rules:
  - shell topbar flatten for form routes only
  - no backend changes
  - no intent/contract changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: form route shell topbar flatten
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批仅收敛壳层展示结构，不触达业务事实。
  reason: >
    用户要求表单视图继续收敛，需要移除与内部表单头重复的外层壳层头部。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1721.yaml"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - flattened shell topbar behavior on form routes

rollback:
  - "git restore frontend/apps/web/src/layouts/AppShell.vue"

depends_on:
  - ITER-2026-04-10-1720
