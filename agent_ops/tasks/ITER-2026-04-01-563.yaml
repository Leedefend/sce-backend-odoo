task_id: ITER-2026-04-01-563
title: Align advanced-filter toggle count with the actual hidden option inventory
type: frontend
status: active
priority: medium

goal: >
  Update the advanced-filter toggle copy so its count reflects every hidden
  option family already shown inside the expanded advanced-filter panel.

user_visible_outcome:
  - "高级筛选按钮数量与展开后的隐藏项总数一致"
  - "保持修改仅限 PageToolbar 的计数文案"
  - "不改变筛选行为、展开逻辑或回调"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - bounded display-only wording/count alignment
  - no behavior changes
  - modify only advanced-filter count composition in PageToolbar

context:
  branch: codex/next-round
  baseline_sha: 8992446
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-562

architecture:
  layer_target: Frontend Layer
  module: advanced-filter toggle count alignment
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The fresh scan and screen isolated a display-only count mismatch: the
    advanced-filter CTA currently excludes hidden search-panel options from the
    count even though those options appear in the expanded panel.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-563.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - count_alignment_requires_behavior_change

deliverables:
  - bounded_advanced_filter_count_alignment
  - governance/report artifacts for ITER-2026-04-01-563

rollback:
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue
  - git restore agent_ops/tasks/ITER-2026-04-01-563.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-563.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-563.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-562
