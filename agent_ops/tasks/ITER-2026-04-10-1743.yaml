task_id: ITER-2026-04-10-1743
title: frontend render_profile consumer alignment audit

type: implementation
status: active
priority: medium
mode: verify
batch: 1/1

goal: >
  审计前端是否消费后端 `render_profile` 并映射详情页编辑/只读行为，避免三态后端能力未被前端承接。

user_visible_outcome:
  - "输出前端 render_profile 消费覆盖率与阻断结论"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1743.yaml
    - scripts/verify/form_render_profile_frontend_consumer_audit.py
    - artifacts/contract/form_render_profile_frontend_consumer_v1.json
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1743.md
    - agent_ops/state/task_results/ITER-2026-04-10-1743.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 900

change_rules:
  - audit-only batch
  - no business model/schema changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: frontend contract-consumer runtime
  module: render_profile consumer alignment audit
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    1742 后端三态已恢复，需要核验前端是否正确消费该语义。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1743.yaml"
    - "python3 scripts/verify/form_render_profile_frontend_consumer_audit.py --json"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - frontend render_profile consumer coverage audit artifact

rollback:
  - "git restore scripts/verify/form_render_profile_frontend_consumer_audit.py"

depends_on:
  - ITER-2026-04-10-1742
