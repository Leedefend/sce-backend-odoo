task_id: ITER-2026-03-28-054
title: Identify project workspace shell versus industry block boundary
type: governance_task
status: pending
priority: medium

goal: Freeze the boundary between common project/workspace shell capabilities and scenario-specific dashboard block semantics before any code-layer convergence in dashboard/runtime helpers.
user_visible_outcome:
  - the repository has a documented workspace shell versus industry block boundary
  - later helper extraction can target shared shell/runtime code without absorbing scenario semantics into the kernel

scope:
  allowed_paths:
    - docs/architecture/**
    - docs/ops/**
    - agent_ops/tasks/**
    - agent_ops/queue/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform kernel governance
  module: project workspace shell boundary inventory
  reason: Freeze dashboard shell versus scenario block ownership before runtime/dashboard helper convergence continues.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-054.yaml
    - rg -n "common shell|scenario block|暂缓收敛" docs/architecture/project_workspace_shell_boundary_v1.md

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore docs/architecture/project_workspace_shell_boundary_v1.md agent_ops/tasks/ITER-2026-03-28-054.yaml

depends_on:
  - ITER-2026-03-28-053
notes: "Document-only boundary freeze for dashboard/workspace shell versus scenario-specific block semantics."
