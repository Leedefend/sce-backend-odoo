task_id: ITER-2026-03-29-203
title: Diagnose live project form field modifiers root cause
type: audit_task
status: pending
priority: high

goal: Determine exactly why live `project.project` form `ui.contract` still returns empty `views.form.field_modifiers` by comparing runtime parser paths instead of continuing speculative backend fixes.
user_visible_outcome:
  - the next backend batch will target the real missing link in the project form contract chain
  - no more guesswork between parser output, fallback output, and final contract delivery

scope:
  allowed_paths:
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - frontend/**
    - addons/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: governance/backend root-cause audit
  module: project form field modifiers diagnosis
  reason: The first backend implementation attempt failed live verification, so this batch must isolate the real missing link before any further code change.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-203.yaml
    - python3 -m py_compile agent_ops/scripts/project_form_parser_path_audit.py
    - python3 agent_ops/scripts/project_form_parser_path_audit.py --db sc_demo --login sc_fx_pm --container sc-backend-odoo-dev-odoo-1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - audit_report
  - task_result

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-203.yaml agent_ops/scripts/project_form_parser_path_audit.py agent_ops/reports/2026-03-29/report.ITER-2026-03-29-203.md agent_ops/state/task_results/ITER-2026-03-29-203.json

depends_on:
  - ITER-2026-03-29-201

notes: "Diagnosis-only batch: compare live parser paths and capture whether field_modifiers is missing in primary parser output, fallback output, or final contract delivery."
