task_id: ITER-2026-03-30-281
title: Fix list search placeholder field source
type: bugfix
status: in_progress
priority: high

goal: >
  Correct the list search placeholder runtime so it reads native searchable fields
  from the actual resolved scene-ready shape instead of always falling back to the
  generic placeholder.

user_visible_outcome:
  - list search placeholder now reflects native searchable fields when present
  - the previous metadata-driven placeholder feature actually becomes visible

scope:
  allowed_paths:
    - agent_ops/**
    - frontend/apps/web/**
  forbidden_paths:
    - addons/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - bugfix only
  - no backend contract changes

context:
  branch: codex/next-round
  baseline_sha: 65d6932

architecture:
  layer_target: Frontend Layer
  module: list metadata consumer
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The previous placeholder enhancement reads a non-existent field on the resolved
    list surface, so the UI silently falls back to generic text.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-281.yaml
    - bash -lc 'source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20 >/dev/null && pnpm -C frontend/apps/web typecheck:strict'

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed
  - runtime_shape_unclear

deliverables:
  - fixed_search_placeholder_runtime
  - iteration_report

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-281.yaml
  - git restore frontend/apps/web/src/views/ActionView.vue

depends_on:
  - ITER-2026-03-30-280

notes: >
  Keep the fix minimal: align the field source with the actual resolver output.
