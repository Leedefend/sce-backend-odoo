task_id: ITER-2026-03-28-034
title: Extract runtime fetch bootstrap surface assembly into a shared helper
type: refactor_task
status: pending
priority: medium

goal: Move the runtime_fetch bootstrap payload, extension merge, and surface-apply sequence into a dedicated smart_core helper so runtime_fetch carries less inline system_init-style assembly logic without changing returned semantics.
user_visible_outcome:
  - runtime_fetch keeps returning the same bootstrap-shaped context for downstream page and collection handlers
  - extension hooks and merged extension facts are still applied before runtime surface shaping
  - runtime_fetch context builder becomes narrower and easier to align with later system_init convergence

scope:
  allowed_paths:
    - addons/smart_core/core/**
    - addons/smart_core/handlers/runtime_fetch.py
    - addons/smart_core/tests/**
    - agent_ops/tasks/**
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Platform Layer
  module: smart_core runtime_fetch bootstrap assembly
  reason: Continue runtime-mainline cleanup by isolating the runtime_fetch bootstrap and surface assembly sequence into a reusable platform helper before wider system_init alignment.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-034.yaml
    - python3 -m py_compile addons/smart_core/core/runtime_fetch_bootstrap_helper.py addons/smart_core/core/runtime_fetch_context_builder.py addons/smart_core/tests/test_runtime_fetch_bootstrap_helper.py
    - python3 addons/smart_core/tests/test_runtime_fetch_bootstrap_helper.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore addons/smart_core/core/runtime_fetch_bootstrap_helper.py addons/smart_core/core/runtime_fetch_context_builder.py addons/smart_core/tests/test_runtime_fetch_bootstrap_helper.py agent_ops/tasks/ITER-2026-03-28-034.yaml docs/ops/iterations/delivery_context_switch_log_v1.md docs/ops/releases/archive/temp/TEMP_agent_ops_continuous_iteration_status_20260328.md

depends_on:
  - ITER-2026-03-28-033
notes: "Keep this slice runtime_fetch-only; do not move system_init itself in the same batch."
