task_id: ITER-2026-04-03-919
title: Capture 5xx fetch intent_hint in smoke summary
type: backend
status: active
priority: medium
mode: implement

goal: >
  Capture actionable request-level hint for 5xx fetch resources in smoke summary
  by extracting intent name from request payload when available.

user_visible_outcome:
  - "5xx 资源产物直接给出 intent_hint，支持定点修复"

scope:
  allowed_paths:
    - scripts/verify/project_dashboard_primary_entry_browser_smoke.mjs
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive_observability_only
  - no_runtime_semantic_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-918
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: smoke observability reporter
  module_owner: verification_governance
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Reporter-only enhancement for deterministic diagnostics.
  reason: >
    5xx resource URL is known but failing intent payload is not yet visible.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-919.yaml
    - HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 GATEWAY_BASE_URLS=http://localhost E2E_LOGIN=admin E2E_PASSWORD=admin DB_NAME=sc_demo make verify.portal.project_dashboard_primary_entry_browser_smoke.host

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - smoke_http_5xx_intent_hint_evidence

rollback:
  - git restore scripts/verify/project_dashboard_primary_entry_browser_smoke.mjs
  - git restore agent_ops/tasks/ITER-2026-04-03-919.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-919.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-919.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-918
