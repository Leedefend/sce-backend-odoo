task_id: ITER-2026-03-28-055
title: Define common project code alignment wave-1 plan
type: governance_task
status: pending
priority: medium

goal: Convert the candidate map and boundary freeze into a wave-1 code alignment plan that only targets low-risk shared helpers and read-model utilities.
user_visible_outcome:
  - the repository has an approved low-risk common-project code alignment wave-1 plan
  - the next continuous batch can implement bounded helper extractions instead of ad hoc refactors

scope:
  allowed_paths:
    - docs/architecture/**
    - docs/ops/**
    - agent_ops/tasks/**
    - agent_ops/queue/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform kernel governance
  module: common project code alignment wave-1 plan
  reason: Turn the current architecture baseline into a bounded next implementation batch with explicit low-risk helper extraction targets.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-055.yaml
    - rg -n "wave-1|low-risk|do not touch" docs/architecture/common_project_code_alignment_wave1_plan_v1.md

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore docs/architecture/common_project_code_alignment_wave1_plan_v1.md agent_ops/tasks/ITER-2026-03-28-055.yaml

depends_on:
  - ITER-2026-03-28-054
notes: "This closes batch-1 planning and opens the next code-layer convergence queue."
