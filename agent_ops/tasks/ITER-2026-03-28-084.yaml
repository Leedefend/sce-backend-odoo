task_id: ITER-2026-03-28-084
title: Register kanban parser in native view registry
type: governance_task
status: pending
priority: high

goal: Add a minimal structured kanban parser and register it in the native view parser registry so the native parser subsystem covers the three most common Odoo view types.
user_visible_outcome:
  - kanban parsing is available through native view parser registry

scope:
  allowed_paths:
    - addons/smart_core/view/**
    - addons/smart_core/tests/**
    - docs/ops/**
    - agent_ops/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform kernel convergence batch-2
  module: smart_core native view kanban parser
  reason: Expand the native parser subsystem to cover kanban as the third common Odoo view type on the new pipeline.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-084.yaml
    - python3 -m py_compile addons/smart_core/view/kanban_parser.py addons/smart_core/view/native_view_parser_registry.py addons/smart_core/tests/test_native_view_kanban_parser.py
    - python3 addons/smart_core/tests/test_native_view_kanban_parser.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-28-084.yaml addons/smart_core/view/kanban_parser.py addons/smart_core/view/native_view_parser_registry.py addons/smart_core/tests/test_native_view_kanban_parser.py

depends_on:
  - ITER-2026-03-28-083
notes: "Batch-2 next wave: minimal kanban parsing through the registry."
