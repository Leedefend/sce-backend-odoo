task_id: ITER-2026-04-02-868
title: Align settlement summary guard with extended summary semantics
type: backend
status: active
priority: high
mode: implement

goal: >
  Update settlement summary guard to enforce required keys while allowing
  approved extension fields in the settlement summary payload.

user_visible_outcome:
  - "结算摘要门禁不再被兼容扩展字段误阻断"
  - "结算链路闭环验证恢复稳定"

scope:
  allowed_paths:
    - scripts/verify/product_settlement_summary_contract_guard.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive_only
  - verify_guard_only
  - no_business_semantics_change

context:
  branch: codex/next-round
  baseline_sha: 49bcd8c
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-867
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: settlement summary contract guard
  module_owner: verify scripts
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Guard should freeze required settlement summary shape and allow bounded
    extension keys added by backend summary enrichment.
  reason: >
    Remove strict-key false negatives and keep settlement verification stable.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-868.yaml
    - make verify.product.settlement_summary_contract_guard
    - make verify.product.project_flow.execution_settlement

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - settlement summary guard aligned to required+optional key model
  - governance/report artifacts for ITER-2026-04-02-868

rollback:
  - git restore scripts/verify/product_settlement_summary_contract_guard.py
  - git restore agent_ops/tasks/ITER-2026-04-02-868.yaml
  - git restore agent_ops/reports/2026-04-02/report.ITER-2026-04-02-868.md
  - git restore agent_ops/state/task_results/ITER-2026-04-02-868.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-02-867
