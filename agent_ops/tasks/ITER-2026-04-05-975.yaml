task_id: ITER-2026-04-05-975
title: Recover release protocol by using writable artifacts path under real-user lane
type: governance
status: active
priority: high
mode: verify

goal: >
  Re-run release execution protocol on sc_prod_sim with real customer verification
  lane and writable artifact directory so host permission issue no longer blocks gate.

user_visible_outcome:
  - "在真实用户验证通道下给出发布协议是否通过的明确结论"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verification_and_reporting_only

context:
  branch: codex/next-round
  baseline_sha: 7de445f
  explicit_user_authorization: true
  input_from: ITER-2026-04-05-974
  priority_lane: delivery_simulation_alignment

architecture:
  layer_target: Delivery Simulation Runtime Alignment
  module: release execution protocol runtime environment
  module_owner: operations_runtime
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch only adjusts verification execution envelope (artifact path)
    and re-checks release gate status; no business fact is changed.
  reason: >
    Previous batch confirmed real-user lane works but release protocol failed
    due to `/codex` write permission. Need environment-level re-verification.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-05-975.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim make restart
    - ARTIFACTS_DIR=artifacts ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost API_BASE_URL=http://127.0.0.1:18069 DB_NAME=sc_prod_sim E2E_LOGIN=wutao E2E_PASSWORD=demo make verify.release.execution_protocol.v1

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed

deliverables:
  - release_execution_protocol_verdict_under_real_user_lane

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-05-975.yaml
  - git restore agent_ops/reports/2026-04-05/report.ITER-2026-04-05-975.md
  - git restore agent_ops/state/task_results/ITER-2026-04-05-975.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-05-974
