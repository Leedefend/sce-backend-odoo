task_id: ITER-2026-04-07-1219
title: Environment repair lane batch6 execute switch dev ODOO_PORT to 8069
type: governance
status: completed
priority: high
mode: execute

goal: >
  Align dev environment port configuration with observed listener baseline by
  switching `.env.dev` ODOO_PORT from 8070 to 8069.

user_visible_outcome:
  - "dev 默认运行端口与监听一致"
  - "runtime probe 默认链减少 8070 timeout"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1219.yaml
    - .env.dev
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_runtime_environment_repair_lane_batch6_dev_port_alignment_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 220
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - environment config alignment only
  - no business code changes

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_runtime_environment_repair_batch6

architecture:
  layer_target: Governance Monitoring
  module: dev runtime port alignment
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch adjusts environment runtime wiring only.
  reason: >
    Implement chosen low-risk branch B from batch5 decision to align dev runtime
    port with listener baseline.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1219.yaml
    - make verify.scene.legacy_auth.runtime_probe
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - runtime environment repair lane batch6 dev-port alignment evidence

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1219.yaml
  - git restore .env.dev
  - git restore docs/audit/native/native_runtime_environment_repair_lane_batch6_dev_port_alignment_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-07-1218
