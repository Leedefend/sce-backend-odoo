task_id: ITER-2026-04-02-864
title: Align cost list block guard with extended response envelope
type: backend
status: active
priority: high
mode: implement

goal: >
  Update cost list block guard to validate required envelope keys while
  allowing approved extension fields (for example project_context).

user_visible_outcome:
  - "成本列表门禁不再被兼容扩展字段误阻断"
  - "成本链路闭环门禁恢复稳定"

scope:
  allowed_paths:
    - scripts/verify/product_cost_list_block_guard.py
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive_only
  - verify_guard_only
  - no_business_semantics_change

context:
  branch: codex/next-round
  baseline_sha: af86321
  explicit_user_authorization: true
  input_from: ITER-2026-04-02-863
  priority_lane: usability_verification_mainline
  usability_battlefield: backend

architecture:
  layer_target: Backend Usability
  module: cost list block contract guard
  module_owner: verify scripts
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Guard should freeze required response shape and allow bounded extension keys.
  reason: >
    Remove strict-key false negatives after backend response enrichment.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-02-864.yaml
    - make verify.product.cost_list_block_guard
    - make verify.product.cost_entry_contract_guard
    - make verify.product.cost_summary_block_guard
    - make verify.product.project_flow.execution_cost

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - cost list block guard supports required+optional envelope keys
  - governance/report artifacts for ITER-2026-04-02-864

rollback:
  - git restore scripts/verify/product_cost_list_block_guard.py
  - git restore agent_ops/tasks/ITER-2026-04-02-864.yaml
  - git restore agent_ops/reports/2026-04-02/report.ITER-2026-04-02-864.md
  - git restore agent_ops/state/task_results/ITER-2026-04-02-864.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-02-863
