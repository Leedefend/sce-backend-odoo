task_id: ITER-2026-04-10-1769
title: form render profile surface closure

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  落地 FORM-Batch4 / Task 104-1：补齐 create/edit/readonly 三态真实 surface，
  让 form contract 不再只有 render_profile 名称而是可消费差异表面。

user_visible_outcome:
  - "contract 包含 render_surfaces.create/edit/readonly"
  - "三态在 fields/actions/subviews 维度存在可审计差异"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1769.yaml
    - addons/smart_core/v2/services/ui_contract_service.py
    - scripts/verify/form_render_profile_closure_audit.py
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1769.md
    - agent_ops/state/task_results/ITER-2026-04-10-1769.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 1200

change_rules:
  - backend contract semantic layer only
  - additive compatibility-safe changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend contract semantic layer
  module: form create/edit/readonly render profile closure
  module_owner: smart_core v2 service + verify script
  kernel_or_scenario: kernel
  reason: >
    当前 contract 仍以单表面 + render_profile 标记为主，需输出三态真实表面，
    支撑前端稳定消费 profile 差异。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1769.yaml"
    - "python3 scripts/verify/form_render_profile_closure_audit.py --json --input tmp/json/form.json"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - render profile surface closure patch
  - runtime render profile audit artifact

rollback:
  - "git restore addons/smart_core/v2/services/ui_contract_service.py"
  - "git restore scripts/verify/form_render_profile_closure_audit.py"

depends_on:
  - ITER-2026-04-10-1768
