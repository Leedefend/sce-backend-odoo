task_id: ITER-2026-03-31-406
title: Freeze customer user mapping baseline after owner confirmation
type: audit
status: completed
priority: high

goal: >
  Freeze the current customer user mapping as the accepted baseline after the
  owner confirmed that the remaining 9 special users already match real
  business needs.

user_visible_outcome:
  - "用户映射基线正式冻结"
  - "9 个特殊用户按现状接受"
  - "后续可直接进入系统映射或导入准备"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no implementation changes
  - treat the owner-confirmed 9-user review set as accepted baseline data
  - produce a frozen baseline conclusion rather than reopening reconciliation

context:
  branch: codex/next-round
  baseline_sha: c23f8b2

architecture:
  layer_target: Governance Audit
  module: customer user mapping baseline
  module_owner: smart_construction_custom onboarding
  kernel_or_scenario: scenario
  reason: >
    The owner confirmed that the previously isolated 4 + 3 + 2 special users
    already match actual business needs. The next low-risk step is to freeze
    the current user mapping as the accepted onboarding baseline.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-406.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - owner_confirmation_not_reflected

deliverables:
  - frozen baseline conclusion for the customer user mapping
  - report and task result artifacts for ITER-2026-03-31-406
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-406.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-406.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-406.json

depends_on:
  - ITER-2026-03-31-405
