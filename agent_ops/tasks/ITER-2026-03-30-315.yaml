task_id: ITER-2026-03-30-315
title: Humanize remaining raw metadata labels on the list page
type: frontend
status: in_progress
priority: high

goal: >
  Remove the remaining technical-looking metadata wording on the list page so
  Odoo-native search metadata reads as business-facing labels rather than raw
  keys or internal source markers.

user_visible_outcome:
  - search mode labels read as user-facing text
  - search-panel dimensions fall back to field labels instead of raw field keys
  - route preset source wording no longer leaks internal source markers

scope:
  allowed_paths:
    - frontend/apps/web/src/views/**
    - frontend/apps/web/src/components/page/**
    - agent_ops/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - display cleanup only
  - preserve current list interactions
  - no backend changes

context:
  branch: codex/next-round
  baseline_sha: 57f41c7

architecture:
  layer_target: Frontend Layer
  module: list page metadata presentation
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    The list page already consumes native Odoo metadata, but a few display
    fallbacks still expose raw technical tokens. This batch finishes the
    business-facing presentation layer without changing behavior.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-315.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - cleanup_requires_backend_contract_change

deliverables:
  - list_metadata_display_cleanup
  - iteration_report

rollback:
  - git restore frontend/apps/web/src/views/ActionView.vue
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue
  - git restore agent_ops/tasks/ITER-2026-03-30-315.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-03-30-314

notes: >
  Keep all contract consumption paths intact. Only adjust user-facing labels
  and fallback wording on the list surface.
