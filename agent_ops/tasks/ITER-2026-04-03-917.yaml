task_id: ITER-2026-04-03-917
title: Capture >=500 resource URLs in project dashboard smoke summary
type: backend
status: active
priority: medium
mode: implement

goal: >
  Improve smoke observability by capturing response URL/status for resource
  requests with HTTP status >= 500.

user_visible_outcome:
  - "烟测产物可定位具体 500 资源 URL，不再只有泛化 console 文本"

scope:
  allowed_paths:
    - scripts/verify/project_dashboard_primary_entry_browser_smoke.mjs
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive_observability_only
  - no_semantic_behavior_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-916
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: smoke observability reporter
  module_owner: verification_governance
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Verification reporter enhancement only; no business-fact mutation.
  reason: >
    Current smoke summary records generic console 500 text but omits failing
    resource URL, blocking deterministic root-cause classification.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-917.yaml
    - HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 GATEWAY_BASE_URLS=http://localhost E2E_LOGIN=admin E2E_PASSWORD=admin DB_NAME=sc_demo make verify.portal.project_dashboard_primary_entry_browser_smoke.host

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - smoke_summary_http_500_resource_evidence

rollback:
  - git restore scripts/verify/project_dashboard_primary_entry_browser_smoke.mjs
  - git restore agent_ops/tasks/ITER-2026-04-03-917.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-917.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-917.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-916
