task_id: ITER-2026-03-31-395
title: Freeze PM surface ownership baseline after workbench acceptance
type: audit
status: completed
priority: high

goal: >
  Freeze the current PM-facing surface ownership baseline after accepting the
  custom workbench as intentional product behavior, so future implementation can
  distinguish faithful native handling surfaces from custom product surfaces and
  read-only governance surfaces without reopening the same debate.

user_visible_outcome:
  - "固化当前 PM 面的承接归属基线"
  - "明确哪些面是原生忠实承接，哪些是产品面，哪些是只读治理面"
  - "给后续实现一个不会反复争论的归属矩阵"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - addons/**
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; no code changes
  - use existing audit evidence from 392 through 394
  - freeze an explicit ownership matrix for PM-facing surfaces
  - include next-step implications for future implementation work

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: PM surface ownership baseline
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    After deciding to keep the custom workbench as-is, the next low-risk step is
    to freeze the ownership baseline for PM-facing surfaces so later work can
    proceed from a stable classification instead of re-litigating native vs
    custom intent.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-31-395.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - ownership_baseline_scope_unclear

deliverables:
  - PM surface ownership matrix
  - implementation-facing baseline statement for future batches
  - report and task result artifacts for ITER-2026-03-31-395
  - delivery log checkpoint with next suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-31-395.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-31/report.ITER-2026-03-31-395.md
  - git restore agent_ops/state/task_results/ITER-2026-03-31-395.json

depends_on:
  - ITER-2026-03-31-394
