task_id: ITER-2026-04-01-557
title: Add a concise hint for hidden clears near the reset CTA
type: frontend
status: active
priority: medium

goal: >
  Add a compact hint near the active-condition reset CTA so users understand
  that clearing conditions also removes hidden tracked state, without changing
  reset behavior.

user_visible_outcome:
  - "清空全部条件旁边会提示同时清除隐藏条件"
  - "减少 route/group/filter 隐藏状态被一起清除的理解歧义"
  - "保持纯显示层修复"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - display-only enhancement
  - no behavior changes
  - add only a concise hint near the existing reset CTA

context:
  branch: codex/next-round
  baseline_sha: 8194c19
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-556

architecture:
  layer_target: Frontend Layer
  module: native metadata list toolbar consumer
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The reset CTA wording is already clearer, but a small hint can further
    explain hidden clears without changing the reset logic itself.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-557.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - hidden_clear_hint_requires_behavior_change

deliverables:
  - hidden_clear_scope_hint
  - governance/report artifacts for ITER-2026-04-01-557

rollback:
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue
  - git restore agent_ops/tasks/ITER-2026-04-01-557.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-557.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-557.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-556
