task_id: ITER-2026-04-09-1547
title: backend refactor A1 tier2 intent registry first slice implementation
type: product-implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  实现 Tier-2 第一批（execute/file/load/permission/page）intent registry entries，持续收敛 missing surfaces。

user_visible_outcome:
  - "Tier-2 首批 intent 可在 registry 中追溯"

scope:
  allowed_paths:
    - addons/smart_core/intents/registry_entries/__init__.py
    - addons/smart_core/intents/registry_entries/tier2_slice1.py
    - artifacts/architecture/intent_registry_audit_v1.json
    - artifacts/architecture/intent_registry_taxonomy_audit_v1.json
    - agent_ops/tasks/ITER-2026-04-09-1547.yaml
    - agent_ops/reports/2026-04-09/report.ITER-2026-04-09-1547.md
    - agent_ops/state/task_results/ITER-2026-04-09-1547.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 8
  max_output_lines: 320

change_rules:
  - additive registry entries only
  - no runtime handler/controller changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Intent governance layer
  module: intent registry entries
  module_owner: smart_core intents
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    仅补齐意图注册总账本，不改业务事实。
  reason: >
    按 1544 冻结顺序推进 Tier-2 首批可低风险收敛项。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-09-1547.yaml"
    - "python3 scripts/verify/intent_registry_audit.py"
    - "python3 scripts/verify/intent_registry_taxonomy_audit.py"
    - "python3 - <<'PY'\nimport json\na=json.load(open('artifacts/architecture/intent_registry_audit_v1.json','r',encoding='utf-8'))\nmissing=a['coverage']['missing_from_registry']\nfor name in ['execute_button','file.download','file.upload','load_contract','load_metadata','load_view','permission.check','page.contract']:\n    assert name not in missing, name\nprint('tier2 slice1 closure PASS; remaining missing=',len(missing))\nPY"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - Tier-2 slice-1 intent registry entries

rollback:
  - git restore addons/smart_core/intents/registry_entries/__init__.py addons/smart_core/intents/registry_entries/tier2_slice1.py

depends_on:
  - ITER-2026-04-09-1546
