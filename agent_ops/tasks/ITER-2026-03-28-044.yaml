task_id: ITER-2026-03-28-044
title: Extract load_contract request flag normalization into the shared helper
type: refactor_task
status: pending
priority: medium

goal: Move load_contract request flag normalization for force_refresh, version, and if_none_match into the shared entry helper so the handler keeps less inline request parsing while preserving current semantics.
user_visible_outcome:
  - load_contract still parses force_refresh truthy values the same way
  - load_contract still normalizes version and if_none_match the same way
  - handler request parsing shrinks again without changing contract output

scope:
  allowed_paths:
    - addons/smart_core/handlers/load_contract.py
    - addons/smart_core/core/**
    - addons/smart_core/tests/**
    - agent_ops/tasks/**
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Platform Layer
  module: smart_core load_contract request flag normalization
  reason: Continue load_contract handler cleanup by moving request-level flag parsing into the shared entry helper before broader mainline convergence.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-044.yaml
    - python3 -m py_compile addons/smart_core/handlers/load_contract.py addons/smart_core/core/load_contract_entry_context.py addons/smart_core/tests/test_load_contract_entry_context.py
    - python3 addons/smart_core/tests/test_load_contract_entry_context.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore addons/smart_core/handlers/load_contract.py addons/smart_core/core/load_contract_entry_context.py addons/smart_core/tests/test_load_contract_entry_context.py agent_ops/tasks/ITER-2026-03-28-044.yaml docs/ops/iterations/delivery_context_switch_log_v1.md docs/ops/releases/archive/temp/TEMP_agent_ops_continuous_iteration_status_20260328.md

depends_on:
  - ITER-2026-03-28-043
notes: "Keep the batch at request flag normalization only; do not change generated contract payloads."
