task_id: ITER-2026-04-10-1733
title: form field truth source remediation for layout fieldInfo

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 FORM-001 阻断：确保 layout.fieldInfo 的 type/relation 从 canonical fields 元信息继承，
  避免关系字段降级为 char。

user_visible_outcome:
  - "many2one/one2many/many2many 在 layout.fieldInfo 中不再降级"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1733.yaml
    - addons/smart_core/app_config_engine/services/view_Parser/base.py
    - addons/smart_core/app_config_engine/services/view_Parser/parsers Tree Form.py
    - addons/smart_core/app_config_engine/services/contract_service.py
    - addons/smart_core/v2/services/ui_contract_service.py
    - artifacts/contract/form_field_truth_source_v1.json
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1733.md
    - agent_ops/state/task_results/ITER-2026-04-10-1733.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 7
  max_output_lines: 900

change_rules:
  - backend contract parser fix only
  - keep fields as canonical truth source
  - no business model/schema changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: backend scene-orchestration contract supply
  module: app_config_engine view parser fieldInfo generation
  module_owner: smart_core
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    修复结构语义供给一致性，不变更业务事实。
  reason: >
    1732 审计确认关系字段在 layout.fieldInfo 中存在降级，需统一 canonical 字段真值源。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1733.yaml"
    - "python3 scripts/verify/form_field_truth_source_audit.py --json"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - parser truth-source remediation patch

rollback:
  - "git restore addons/smart_core/app_config_engine/services/view_Parser/base.py"
  - "git restore addons/smart_core/app_config_engine/services/view_Parser/parsers Tree Form.py"

depends_on:
  - ITER-2026-04-10-1732
