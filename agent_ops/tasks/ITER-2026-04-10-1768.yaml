task_id: ITER-2026-04-10-1768
title: form action single-source closure

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  落地 FORM-Batch3 / Task 103-3：统一 form 动作真值源，收敛 buttons/header/buttons_box/
  stat_buttons/action_groups 的主从关系与去重规则。

user_visible_outcome:
  - "同一动作在不同 surface 不再冲突表达"
  - "action_groups 与 form surface 的动作 identity 与 label 一致"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1768.yaml
    - addons/smart_core/v2/services/ui_contract_service.py
    - scripts/verify/form_action_single_source_audit.py
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1768.md
    - agent_ops/state/task_results/ITER-2026-04-10-1768.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 1200

change_rules:
  - backend contract semantic layer only
  - additive compatibility-safe changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend contract semantic layer
  module: form action single-source governance
  module_owner: smart_core v2 service + verify script
  kernel_or_scenario: kernel
  reason: >
    当前动作 identity 在部分场景仅按 name 去重会发生误合并，且 action_groups 与 form
    surfaces 的真值关系不稳定，需要统一主从来源与冲突收敛规则。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1768.yaml"
    - "python3 scripts/verify/form_action_single_source_audit.py --json --input tmp/json/form.json"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - action single-source closure patch
  - runtime action single-source audit artifact

rollback:
  - "git restore addons/smart_core/v2/services/ui_contract_service.py"
  - "git restore scripts/verify/form_action_single_source_audit.py"

depends_on:
  - ITER-2026-04-10-1767
