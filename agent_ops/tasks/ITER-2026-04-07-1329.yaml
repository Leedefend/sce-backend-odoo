task_id: ITER-2026-04-07-1329
title: Environment base-chain repair for runtime verification
status: active
type: fix
priority: critical
mode: execute

goal: >
  Repair runtime base-chain blockers (database health + install chain prerequisites)
  so config-center runtime click-path verification can be executed reliably.

user_visible_outcome:
  - "恢复可用数据库与安装链，形成可复跑运行态验证前置条件"
  - "在 clean 库回跑配置中心 runtime click-path 并输出可信结论"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1329.yaml
    - agent_ops/reports/2026-04-07/report.ITER-2026-04-07-1329.md
    - agent_ops/state/task_results/ITER-2026-04-07-1329.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - frontend/**
    - scripts/**

limits:
  max_files: 4
  max_output_lines: 320
  forbid_repo_wide_scan: true

change_rules:
  - runtime-environment-repair-only
  - no-business-code-change
  - single-compose-project

context:
  branch: codex/next-round
  baseline_sha: ca23376
  explicit_user_authorization: true

architecture:
  layer_target: Runtime Verification Environment Guard
  module: config-center runtime verification environment base-chain repair
  module_owner: ops runtime context
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene-orchestration layer
  backend_sub_layer_reason: >
    This batch only restores runtime verification prerequisites and does not
    alter business facts or frontend behavior.
  reason: >
    Previous iteration failed due to database health corruption and broken
    installation preconditions; these must be repaired before runtime evidence
    can be trusted.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1329.yaml
    - "docker ps --format '{{.Names}} {{.Status}}' | rg 'sc-backend-odoo-dev-(db|odoo)-1'"
    - "COMPOSE_PROJECT_NAME=sc-backend-odoo-dev make mod.install DB_NAME=sc_test MODULE=smart_enterprise_base"
    - "COMPOSE_PROJECT_NAME=sc-backend-odoo-dev make mod.install DB_NAME=sc_test MODULE=smart_construction_core"
    - "E2E_BASE_URL=http://localhost:8069 DB_NAME=sc_test python3 scripts/verify/native_business_admin_config_center_runtime_clickpath_verify.py"

risk:
  level: high
  manual_approval_required: true

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched
  - database_unhealthy_unrecoverable

deliverables:
  - environment base-chain repair evidence
  - runtime click-path verification result
  - governance report and task result

depends_on:
  - ITER-2026-04-07-1328

rollback:
  - git restore agent_ops/reports/2026-04-07/report.ITER-2026-04-07-1329.md
  - git restore agent_ops/state/task_results/ITER-2026-04-07-1329.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
