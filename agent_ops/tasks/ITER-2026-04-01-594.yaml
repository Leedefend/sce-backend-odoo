task_id: ITER-2026-04-01-594
title: Select the next bounded record-view HUD continuity slice after contract-status ordering
type: frontend
status: active
priority: high
mode: screen

goal: >
  Select the next bounded record-view HUD continuity slice with immediate
  readability gain after contract-status ordering, without changing HUD field
  coverage.

user_visible_outcome:
  - "明确下一条 HUD continuity 可执行切片"
  - "优先处理用户最容易困惑的 HUD 显示细节"
  - "保持字段集合不变"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 3
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - screen stage only
  - consume only existing HUD continuity artifacts
  - do not reopen repository scan
  - do not implement in this batch

context:
  branch: codex/next-round
  baseline_sha: 5555e15
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-593
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: record-view HUD continuity screen
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The previous ordering batches stabilized key continuity and contract health.
    The next bounded slice should target residual display ambiguity that still
    interrupts first-glance interpretation in HUD.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-594.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - screen_reads_new_product_files
  - screen_reopens_repo_scan
  - no_bounded_hud_candidate_remains

deliverables:
  - selected_next_record_view_hud_slice_after_contract_status_ordering
  - governance/report artifacts for ITER-2026-04-01-594

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-594.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-594.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-594.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-593
