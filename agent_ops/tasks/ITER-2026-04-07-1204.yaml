task_id: ITER-2026-04-07-1204
title: Stage-B Batch3 strict-mode fallback policy evidence
type: governance
status: completed
priority: high
mode: verify

goal: >
  Produce strict-mode runtime policy evidence for legacy auth smoke semantics,
  confirming default no-fallback behavior and explicit-fallback-only path.

user_visible_outcome:
  - "严格模式 fallback 策略有可审计证据"
  - "Stage-B 短链门禁继续 PASS"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1204.yaml
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/audit/native/native_stage_b_batch3_strict_mode_evidence_v1.md
    - docs/audit/native/native_next_stage_roadmap_v1.md
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - record_rules/**
    - migrations/**

limits:
  max_files: 8
  max_candidates: 12
  max_output_lines: 200
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - evidence-only verify batch
  - no source behavior mutation

context:
  branch: codex/next-round
  baseline_sha: a2d0e90
  explicit_user_authorization: true
  input_from: execute_stage_b_batch3

architecture:
  layer_target: Governance Monitoring
  module: strict-mode policy verification
  module_owner: platform
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Batch validates runtime verification semantics and policy evidence only.
  reason: >
    Ensure legacy auth smoke gate semantics remain strict by default and fallback
    remains explicit-only, with auditable evidence.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1204.yaml
    - make verify.scene.legacy_contract.guard
    - make verify.test_seed_dependency.guard
    - make verify.scene.legacy_auth.smoke.semantic

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched

deliverables:
  - stage-b batch3 strict-mode evidence package

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-07-1204.yaml
  - git restore docs/audit/native/native_stage_b_batch3_strict_mode_evidence_v1.md
  - git restore docs/audit/native/native_next_stage_roadmap_v1.md
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-06-1203
