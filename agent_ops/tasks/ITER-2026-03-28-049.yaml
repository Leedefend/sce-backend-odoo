task_id: ITER-2026-03-28-049
title: Build execution baseline for platform kernel alignment
type: governance_task
status: pending
priority: medium

goal: Create an implementation-grade execution baseline document that separates target architecture from current executable rules, assets, and phased alignment strategy.
user_visible_outcome:
  - the repository has an execution baseline document usable by Codex and developers
  - current core assets, boundaries, and alignment phases are frozen in one operational document

scope:
  allowed_paths:
    - docs/architecture/**
    - agent_ops/tasks/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform kernel governance
  module: execution baseline
  reason: Bind future automatic iteration work to an implementation baseline instead of broad target-architecture slogans.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-049.yaml
    - rg -n "当前系统架构|双轨前端承接策略|Codex 自动迭代体系|三阶段实施路线" docs/architecture/execution_baseline_v1.md

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore docs/architecture/execution_baseline_v1.md agent_ops/tasks/ITER-2026-03-28-049.yaml

depends_on:
  - ITER-2026-03-28-048
notes: "Implementation baseline only. No code-layer kernel migration in this task."
