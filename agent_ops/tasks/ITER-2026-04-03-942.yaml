task_id: ITER-2026-04-03-942
title: Edition session context guard timeout verify (declared check)
type: governance
status: active
priority: high
mode: verify

goal: >
  Verify reproducibility of edition session context guard timeout on sc_demo.

user_visible_outcome:
  - "确认 session_context_guard 超时阻断可复现"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify_only_declared_checks
  - no_implementation_changes

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-941
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: edition session context guard timeout verify
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Verify-stage replay only; no implementation changes.
  reason: >
    Screen stage selected deterministic replay of session context guard.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-942.yaml
    - ARTIFACTS_DIR=artifacts HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost E2E_LOGIN=admin E2E_PASSWORD=admin make verify.edition.session_context_guard DB_NAME=sc_demo

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - edition_session_context_timeout_verify_decision_input

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-942.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-942.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-942.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-941
