task_id: ITER-2026-04-10-1809
title: form visibility optimization after restore

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在直接还原基础上做轻量优化：结构对齐模式仅恢复安全语义门控（technical/hidden），保持业务 tab 内容完整可见。

user_visible_outcome:
  - "页面可读性提升，同时不再出现业务 tab 空白回归"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1809.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1809.md
    - agent_ops/state/task_results/ITER-2026-04-10-1809.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - optimize on top of restored behavior
  - keep tab integrity first
  - no backend changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: field visibility policy in align mode
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    直接还原已达成，需在不破坏 tab 内容完整性的前提下恢复最小语义门控，提高可读性。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1809.yaml"
    - 'rg -n "isFieldVisible\(|projectDetailStructureAlignMode\.value" frontend/apps/web/src/pages/ContractFormPage.vue'
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - refined align-mode visibility behavior
  - iteration report and state

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1808
