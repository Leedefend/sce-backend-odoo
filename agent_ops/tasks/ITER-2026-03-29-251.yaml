task_id: ITER-2026-03-29-251
title: Clarify governance naming and canonical delivery sequencing
type: refactor_task
status: in_progress
priority: high

goal: Make the distinction between view-runtime filtering and delivery-surface governance explicit in code, and expose a canonical finalize to delivery pipeline name without changing behavior or architecture.
user_visible_outcome:
  - backend delivery chain is easier to understand and safer to continue refactoring
  - governance naming becomes less ambiguous
  - canonical finalize to delivery sequence is explicit in code

scope:
  allowed_paths:
    - addons/smart_core/app_config_engine/**
    - addons/smart_core/utils/**
    - agent_ops/**
    - docs/ops/**
  forbidden_paths:
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*

change_rules:
  - additive_or_refactor_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change
  - preserve_contract_output

architecture:
  layer_target: platform layer
  module: governance naming and delivery sequencing
  reason: after thinning handler duplication and extracting page policy helpers, the remaining major ambiguity is naming and sequencing between view-runtime filter and delivery-surface governance.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-29-251.yaml
    - make verify.smart_core

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_command_failed
  - output_contract_changed_unintentionally

deliverables:
  - governance_boundary_clarity
  - canonical_delivery_pipeline_name
  - regression_report

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-29-251.yaml docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore addons/smart_core/app_config_engine/services/contract_governance_filter.py addons/smart_core/app_config_engine/models/app_view_config.py addons/smart_core/app_config_engine/services/contract_service.py

depends_on:
  - ITER-2026-03-29-250

notes: "Non-behavioral cleanup only. Make naming and sequencing explicit, not different."
