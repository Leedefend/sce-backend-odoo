task_id: ITER-2026-04-01-516
title: Classify the business-evidence family as the next uncovered secondary-flow candidate
type: permission-governance
status: active
priority: medium

goal: >
  Continue the non-first-batch objective by classifying the business-evidence
  family on repository/runtime facts, now that workflow has been removed as a
  false-positive candidate.

user_visible_outcome:
  - "明确 business evidence 家族的入口与权限边界"
  - "判断它是否能并入 clean representative family 集合"
  - "不进入 evidence 生产逻辑或 financial 语义实现"

scope:
  allowed_paths:
    - addons/smart_construction_core/**
    - addons/smart_construction_custom/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit-first classification batch
  - no implementation changes unless a later narrow batch is opened
  - focus on canonical evidence action and runtime read/write semantics only

context:
  branch: codex/next-round
  baseline_sha: c23f8b2
  target_db: sc_odoo
  customer_name: 四川保盛建设集团有限公司
  customer_module: smart_construction_custom
  explicit_user_authorization: true

architecture:
  layer_target: Customer Delivery Flow Acceptance
  module: business evidence family audit
  module_owner: customer delivery scenario surface
  kernel_or_scenario: scenario
  reason: >
    ITER-2026-04-01-515 isolated business evidence as the next true candidate
    after workflow was diagnosed as a config-admin false-positive.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-516.yaml
    - "repository audit classifies the business-evidence action/model boundary"
    - "runtime audit classifies at least one representative evidence entry surface without high-risk side effects"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - runtime_cannot_classify_business_evidence_with_confidence
  - a_new_residual_requires_scope_expansion
  - audit_requires_acl_or_manifest_changes

deliverables:
  - business-evidence family classification
  - governance/report artifacts for ITER-2026-04-01-516

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-516.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-516.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-516.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-515
