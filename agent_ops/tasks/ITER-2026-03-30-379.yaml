task_id: ITER-2026-03-30-379
title: Re-anchor business-fact usability audit priority after ownership cleanup
type: audit
status: completed
priority: high

goal: >
  Re-anchor the active product objective on business-fact usability after the
  core data ownership cleanup line, separating low-risk native usability work
  from finance-governed trigger gaps.

user_visible_outcome:
  - current usability objective has an explicit next-step priority order
  - finance-governed gaps are separated from low-risk native page confirmation

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; do not implement product changes in this batch
  - use existing verified reports as the source of truth
  - separate low-risk usability continuation from finance-governed gaps

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: business fact usability prioritization
  module_owner: smart_construction_core
  kernel_or_scenario: scenario
  reason: >
    After the ownership cleanup line completed, the active objective needs to
    return to verifying which native business surfaces are truly usable, while
    keeping already-identified finance trigger gaps out of the low-risk lane.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-379.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - usability_priority_becomes_unclear

deliverables:
  - explicit next-step priority order for business-fact usability work
  - report and task result artifacts for ITER-2026-03-30-379
  - delivery log checkpoint for the resumed usability line

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-379.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-379.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-379.json

depends_on:
  - ITER-2026-03-30-378
