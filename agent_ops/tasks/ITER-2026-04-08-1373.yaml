task_id: ITER-2026-04-08-1373
title: Integrate permission runtime probe into verify.contract preflight
status: active
type: governance
priority: high
mode: execute
batch: 1/1

goal: >
  Wire permission runtime UID probe into an existing make verify.contract.*
  aggregate target, so contract regression can be checked in one command.

user_visible_outcome:
  - "make verify.contract.preflight 覆盖 permissions.effective.rights 回归"
  - "合同权限语义与运行态事实持续可验证"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-08-1373.yaml
    - Makefile
    - agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1373.md
    - agent_ops/state/task_results/ITER-2026-04-08-1373.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*

limits:
  max_files: 5
  max_output_lines: 260
  forbid_repo_wide_scan: true

change_rules:
  - additive-only
  - no-acl-table-change
  - no-financial-semantic-change

context:
  branch: codex/next-round
  baseline_sha: 3466cad
  explicit_user_authorization: true

architecture:
  layer_target: Verification contract-preflight layer
  module: Makefile verify.contract.preflight aggregation
  module_owner: platform verification governance
  kernel_or_scenario: scenario
  reason: >
    User requested one-click daily regression; permission runtime UID probe must
    become part of existing verify.contract preflight chain.

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-08-1373.yaml"
    - "make verify.contract.permission_runtime_uid_probe DB_NAME=sc_demo E2E_LOGIN=admin E2E_PASSWORD=admin"
    - "CONTRACT_PREFLIGHT_STRICT_VIEW_TYPES=0 BASELINE_FREEZE_ENFORCE=0 make verify.contract.preflight DB_NAME=sc_demo E2E_LOGIN=admin E2E_PASSWORD=admin"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_command_failed

deliverables:
  - verify.contract permission probe make target
  - verify.contract.preflight integration

depends_on:
  - ITER-2026-04-08-1372

rollback:
  - "git restore Makefile"
  - "git restore agent_ops/tasks/ITER-2026-04-08-1373.yaml"
  - "git restore agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1373.md"
  - "git restore agent_ops/state/task_results/ITER-2026-04-08-1373.json"
  - "git restore docs/ops/iterations/delivery_context_switch_log_v1.md"
