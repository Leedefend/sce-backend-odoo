task_id: ITER-2026-04-01-603
title: Screen first project lifecycle usability slice from create-to-closure objective
type: frontend
status: active
priority: high
mode: screen

goal: >
  Shift scheduling to end-user lifecycle usability and select the first bounded
  slice that directly improves "create project -> manage project" continuity.

user_visible_outcome:
  - "调度优先级转为项目全流程可用性"
  - "明确第一条可执行的闭环入口改进"
  - "停止继续 HUD 微观优化优先级"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 6
  max_candidates: 3
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - screen stage only
  - prioritize end-user lifecycle path
  - do not reopen repo-wide scan
  - do not implement in this batch

context:
  branch: codex/next-round
  baseline_sha: 0872fae
  explicit_user_authorization: true
  input_from: user_directive_lifecycle_usability_first
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_project_lifecycle_usability

architecture:
  layer_target: Frontend Layer
  module: project lifecycle usability screen
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    User explicitly redirected the objective to project lifecycle closure
    usability. First slice must improve create-entry discoverability on actual
    project usage path.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-603.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - no_bounded_candidate_remains

deliverables:
  - selected_first_project_lifecycle_usability_slice
  - governance/report artifacts for ITER-2026-04-01-603

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-603.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-603.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-603.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on: []
