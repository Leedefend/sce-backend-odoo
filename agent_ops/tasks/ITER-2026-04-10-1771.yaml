task_id: ITER-2026-04-10-1771
title: frontend form render-surfaces consumer switch

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  将前端 form 页面消费主路径切换到 `render_surfaces.<profile>`，
  并将 x2many 编辑行为对齐 profile-level subview policies。

user_visible_outcome:
  - "表单动作优先消费 render_surfaces.<profile>.actions"
  - "x2many 新增/编辑/删除受 render_surfaces.<profile>.subviews.policies 控制"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1771.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1771.md
    - agent_ops/state/task_results/ITER-2026-04-10-1771.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend contract-consumer runtime only
  - additive compatibility-safe changes
  - no backend semantic changes in this batch

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: ContractFormPage profile-surface consumer
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    render_surfaces contract 已就绪，需要完成前端消费切换，避免继续消费旧 form surface
    造成 profile 行为偏差。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1771.yaml"
    - "rg -n \"render_surfaces|activeRenderSurface|one2manyPolicies\" frontend/apps/web/src/pages/ContractFormPage.vue"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - frontend render-surfaces consumer switch patch

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1770
