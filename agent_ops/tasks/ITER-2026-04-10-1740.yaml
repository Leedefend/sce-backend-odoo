task_id: ITER-2026-04-10-1740
title: form parser root-cause remediation for x2many subviews

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 FORM-005 根因：在 form 解析器层稳定产出 x2many subviews，避免仅靠编排层兜底。

user_visible_outcome:
  - "form 契约由解析器直接给出 task_ids 与 collaborator_ids 的 subview 承载"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1740.yaml
    - addons/smart_core/app_config_engine/services/view_Parser/parsers Tree Form.py
    - artifacts/contract/form_subview_relation_v1.json
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1740.md
    - agent_ops/state/task_results/ITER-2026-04-10-1740.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 900

change_rules:
  - parser-level root-cause fix only
  - no business model/schema changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: backend scene-orchestration contract supply
  module: form parser x2many subview extraction
  module_owner: smart_core
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批仅修复 contract 结构语义供给路径，不变更业务事实。
  reason: >
    用户要求根因修复，需在 view parser 阶段保证 x2many subviews 稳定输出。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1740.yaml"
    - "python3 scripts/verify/form_subview_relation_audit.py --json"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - parser-level x2many subview extraction remediation patch

rollback:
  - "git restore addons/smart_core/app_config_engine/services/view_Parser/parsers Tree Form.py"

depends_on:
  - ITER-2026-04-10-1739
