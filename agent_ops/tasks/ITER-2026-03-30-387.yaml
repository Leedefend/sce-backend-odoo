task_id: ITER-2026-03-30-387
title: Audit capability matrix supplement closure after custom frontend implementation
type: audit
status: in_progress
priority: high

goal: >
  Verify that the `能力矩阵` custom frontend supplement lane is now effectively
  closed after ITER-2026-03-30-386, and confirm that no remaining user-visible
  portal fallback is required for the active product objective.

user_visible_outcome:
  - "能力矩阵补位线被明确标记为已闭合或仍存在剩余缺口"
  - "portal 锚点、自定义路由、自定义页面三者之间的关系被明确记录"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only; do not modify frontend or backend code in this batch
  - judge closure using the implemented custom page, route normalization, and portal anchor behavior
  - focus on the active product objective, not on reviving native portal frontend

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: capability matrix supplement closure
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    After implementing the minimal capability-matrix custom page, the next
    low-risk step is to confirm whether the supplement lane is now fully
    closed for the current product objective.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-387.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - supplement_closure_scope_unclear

deliverables:
  - explicit closure decision for the capability matrix supplement lane
  - report and task result artifacts for ITER-2026-03-30-387
  - delivery log checkpoint with the next active line

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-387.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-387.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-387.json

depends_on:
  - ITER-2026-03-30-386
