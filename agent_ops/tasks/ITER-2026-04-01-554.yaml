task_id: ITER-2026-04-01-554
title: Rename the reset CTA to signal a full clear
type: frontend
status: active
priority: medium

goal: >
  Clarify that the active-condition reset CTA clears all tracked conditions by
  updating its wording, without changing reset behavior.

user_visible_outcome:
  - "重置按钮更清楚地表达会清空全部条件"
  - "减少隐藏状态被一起清除的理解歧义"
  - "保持纯显示层修复"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - frontend only
  - display-only enhancement
  - no behavior changes
  - update reset CTA wording only

context:
  branch: codex/next-round
  baseline_sha: 8194c19
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-553

architecture:
  layer_target: Frontend Layer
  module: native metadata list toolbar consumer
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    The reset CTA already clears multiple tracked conditions. The next low-risk
    display fix is to align the wording with that existing behavior.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-554.yaml
    - pnpm -C frontend/apps/web typecheck:strict

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - reset_wording_clarity_requires_behavior_change

deliverables:
  - reset_all_wording_clarity
  - governance/report artifacts for ITER-2026-04-01-554

rollback:
  - git restore frontend/apps/web/src/components/page/PageToolbar.vue
  - git restore agent_ops/tasks/ITER-2026-04-01-554.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-554.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-554.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-553
