task_id: ITER-2026-04-07-1328
title: Environment unification for config-center runtime verification
status: active
type: verify
priority: high
mode: execute

goal: >
  Unify runtime execution topology to a single compose project and allowed DB,
  then rerun config-center runtime click-path verification on a clean database.

user_visible_outcome:
  - "统一环境后在 sc_test 完成运行态点击链验证"
  - "确认 runtime 证据是否可达 PASS"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-07-1328.yaml
    - agent_ops/reports/2026-04-07/report.ITER-2026-04-07-1328.md
    - agent_ops/state/task_results/ITER-2026-04-07-1328.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - addons/**
    - frontend/**
    - scripts/**

limits:
  max_files: 4
  max_output_lines: 320
  forbid_repo_wide_scan: true

change_rules:
  - runtime-verification-only
  - single-compose-project
  - no-code-change

context:
  branch: codex/next-round
  baseline_sha: ca23376
  explicit_user_authorization: true

architecture:
  layer_target: Runtime Verification Environment Guard
  module: config-center runtime verification environment unification
  module_owner: ops runtime context
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene-orchestration layer
  backend_sub_layer_reason: >
    This batch resolves runtime environment consistency for verification only.
  reason: >
    Remove compose/dbfilter/db-target mismatch before re-evaluating runtime
    operability evidence.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-07-1328.yaml
    - "COMPOSE_PROJECT_NAME=sc-backend-odoo clean sc_test and install smart_enterprise_base + smart_construction_core"
    - "E2E_BASE_URL=http://localhost:8069 DB_NAME=sc_test python3 scripts/verify/native_business_admin_config_center_runtime_clickpath_verify.py"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - acceptance_failed
  - forbidden_path_touched
  - runtime_clickpath_fail_after_unification

deliverables:
  - unified-runtime verification evidence
  - governance report and task result

depends_on:
  - ITER-2026-04-07-1327

rollback:
  - git restore agent_ops/reports/2026-04-07/report.ITER-2026-04-07-1328.md
  - git restore agent_ops/state/task_results/ITER-2026-04-07-1328.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
