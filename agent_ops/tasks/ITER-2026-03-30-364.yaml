task_id: ITER-2026-03-30-364
title: Audit value and data readiness of remaining native act-window pages
type: audit
status: in_progress
priority: medium

goal: >
  Continue the native-fact usability line after the execution-structure bridge
  fix by auditing the remaining native `act_window` preview pages for business
  value readiness: seeded data, default filters, empty-state quality, and demo
  PM minimum closure.

user_visible_outcome:
  - the next repair batch can target pages that open successfully but still feel empty or low-value for demo PM
  - native reachability and business-value readiness are separated cleanly
  - the team gets a narrow list of remaining native pages that still need data/default/filter work

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
    - artifacts/codex/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - audit only in this batch; do not mutate models, views, frontend, or runtime behavior
  - evaluate remaining native pages by seeded data, default filters, and first-business-value quality
  - keep portal and scene anchors out of this native page audit

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Governance Audit
  module: native act-window value readiness audit
  module_owner: addons/smart_construction_core
  kernel_or_scenario: scenario
  reason: >
    After raw reachability has been classified and the execution-structure entry
    bridge has been repaired, the next low-risk step is to identify which
    remaining native act-window pages still lack business value for the demo PM
    role because of data, defaults, or empty-state problems.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-364.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - native_value_readiness_cannot_be_classified_with_confidence

deliverables:
  - native act-window value readiness audit for the published preview set
  - report and task result artifacts for ITER-2026-03-30-364
  - delivery log checkpoint for the next high-value native repair batch

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-364.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-364.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-364.json

depends_on:
  - ITER-2026-03-30-363
