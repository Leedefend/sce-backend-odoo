task_id: ITER-2026-03-30-359
title: Move capability and projection scene-key ownership behind smart_construction_scene
type: code
status: in_progress
priority: high

goal: >
  Continue the residual scene cleanup by removing scene-key ownership from
  capability and projection payload builders in smart_construction_core, while
  leaving business capability facts and projection facts intact.

user_visible_outcome:
  - capability and projection payloads stop deciding scene semantics directly in smart_construction_core where a scene-layer seam can own them
  - business capability lists and projection rows remain available
  - the residual fact-layer scene cleanup advances from P2 into P3 without changing ACL or financial semantics

scope:
  allowed_paths:
    - addons/smart_construction_core/services/**
    - addons/smart_construction_scene/**
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - frontend/**
    - migrations/**
    - deploy/**

change_rules:
  - preserve current capability/projection business facts
  - move scene ownership behind scene-layer or shared scene resolution seams
  - no ACL, financial, or manifest changes

context:
  branch: codex/next-round
  baseline_sha: ddcc2e6

architecture:
  layer_target: Scene Layer Migration
  module: capability and projection scene-key cleanup
  module_owner: addons/smart_construction_core + addons/smart_construction_scene
  kernel_or_scenario: scenario
  reason: >
    After the enter-handler and my_work cleanup slices passed, the remaining
    user-facing scene pollution is concentrated in capability and projection
    services that still bake scene_key into business payloads.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-359.yaml

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - capability_or_projection_payload_regresses
  - verification_failure

deliverables:
  - first P3 cleanup slice for capability/projection scene-key ownership
  - report and task result artifacts for ITER-2026-03-30-359
  - delivery log checkpoint for the next residual cleanup batch

rollback:
  - git restore addons/smart_construction_core/services
  - git restore addons/smart_construction_scene
  - git restore agent_ops/tasks/ITER-2026-03-30-359.yaml
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md
  - git restore agent_ops/reports/2026-03-30/report.ITER-2026-03-30-359.md
  - git restore agent_ops/state/task_results/ITER-2026-03-30-359.json

depends_on:
  - ITER-2026-03-30-358
