task_id: ITER-2026-04-10-1812
title: collaboration display gate final unlock

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在已确认后端 chatter/attachments enabled 的前提下，去除前端 collaboration 区块对 preferNative 的显示拦截，确保用户界面可见。

user_visible_outcome:
  - "讨论区/附件区在普通页面模式也可见，不再受 preferNative 条件影响"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1812.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1812.md
    - agent_ops/state/task_results/ITER-2026-04-10-1812.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend display gating only
  - keep backend unchanged
  - additive and reversible

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: collaboration block visibility gate
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    实测 API 已返回 chatter/attachments enabled=true，当前仍不显示，根因是前端 preferNative 显示门控。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1812.yaml"
    - 'rg -n "showCollaborationZoneBlock|preferNativeFormSurface|semanticHasChatter|semanticHasAttachments" frontend/apps/web/src/pages/ContractFormPage.vue'
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - collaboration block visible when chatter/attachments enabled
  - iteration report and state

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1811
