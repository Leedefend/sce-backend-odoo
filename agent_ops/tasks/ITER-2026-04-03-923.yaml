task_id: ITER-2026-04-03-923
title: Integrate customer seed external-id guard into module upgrade preflight
type: backend
status: active
priority: medium
mode: implement

goal: >
  Integrate customer seed external-id guard into mod.upgrade preflight with
  warning-by-default mode and optional strict blocking mode.

user_visible_outcome:
  - "mod.upgrade 前置自动执行外部ID检查，默认告警，可切严格阻断"

scope:
  allowed_paths:
    - scripts/mod/upgrade.sh
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 5
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - additive_preflight_guard_only
  - default_non_blocking

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-922
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: module-upgrade preflight guard orchestration
  module_owner: operations_runtime
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This is execution preflight behavior, not business-fact mutation.
  reason: >
    Prevent recurrence of customer user seed external-id conflicts during module upgrades.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-923.yaml
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim CODEX_NEED_UPGRADE=1 CODEX_MODULES=smart_construction_core CUSTOMER_SEED_EXTERNAL_ID_GUARD=warn make mod.upgrade MODULE=smart_construction_core
    - ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim DB_NAME=sc_prod_sim CODEX_NEED_UPGRADE=1 CODEX_MODULES=smart_construction_core CUSTOMER_SEED_EXTERNAL_ID_GUARD=strict make mod.upgrade MODULE=smart_construction_core

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - mod_upgrade_preflight_guard_integration

rollback:
  - git restore scripts/mod/upgrade.sh
  - git restore agent_ops/tasks/ITER-2026-04-03-923.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-923.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-923.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-922
