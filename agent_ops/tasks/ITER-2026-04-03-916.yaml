task_id: ITER-2026-04-03-916
title: Screen frontend resource 500 console noise after entry convergence
type: governance
status: active
priority: medium
mode: screen

goal: >
  Classify the remaining frontend console 500 resource noise after entry
  convergence without changing runtime behavior.

user_visible_outcome:
  - "明确 console 500 的资源来源与后续修复切口"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 2
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen_only_no_code_change
  - verify_noise_classification_only

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-915
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: frontend resource noise classification
  module_owner: verification_governance
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Focus on runtime evidence classification, no semantic changes in this batch.
  reason: >
    Entry contract is now converged but smoke summary still shows occasional
    frontend console 500 resource messages.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-916.yaml
    - HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 GATEWAY_BASE_URLS=http://localhost E2E_LOGIN=admin E2E_PASSWORD=admin DB_NAME=sc_demo make verify.portal.project_dashboard_primary_entry_browser_smoke.host

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed
  - uncertain_classification

deliverables:
  - console_500_noise_classification
  - next_fix_slice_suggestion

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-916.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-916.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-916.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-915
