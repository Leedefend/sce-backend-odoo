task_id: ITER-2026-04-10-1586
title: backend refactor v2 first meta readonly intent and old-v2 comparison audit
type: governance
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  在 v2 中落地首个 meta 只读意图（meta.registry.catalog），并补齐 old-v2 对照审计脚本，为后续真实意图迁移提供基线。

user_visible_outcome:
  - "v2 已具备首个 meta 只读意图与对照审计能力"

scope:
  allowed_paths:
    - addons/smart_core/v2/**
    - scripts/verify/v2_*audit.py
    - docs/architecture/backend_core_refactor_blueprint_v1.md
    - agent_ops/tasks/ITER-2026-04-10-1586.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1586.md
    - agent_ops/state/task_results/ITER-2026-04-10-1586.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 18
  max_output_lines: 680

change_rules:
  - v2 additive migration only
  - readonly intent only
  - no legacy runtime cutover

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Platform kernel shadow refactor layer
  module: smart_core v2 meta readonly chain
  module_owner: smart_core
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批新增只读元信息链路与对照审计，不触碰业务事实层。
  reason: >
    在大批次重建后立即推进真实意图迁移起点，验证 v2 可承接能力。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1586.yaml"
    - "python3 -m py_compile addons/smart_core/v2/handlers/meta/registry_catalog.py addons/smart_core/v2/services/meta_service.py addons/smart_core/v2/builders/meta_builder.py addons/smart_core/v2/intents/registry.py scripts/verify/v2_intent_comparison_audit.py"
    - "python3 scripts/verify/v2_boundary_audit.py --json"
    - "python3 scripts/verify/v2_intent_comparison_audit.py --json"
    - 'rg -n "intent_name = \"meta.registry.catalog\"|def list_registry_catalog|def build_registry_catalog_contract" addons/smart_core/v2'
    - "rg -n \"v2 meta readonly migration batch\" docs/architecture/backend_core_refactor_blueprint_v1.md"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - v2 meta.registry.catalog readonly intent
  - old-v2 comparison audit script

rollback:
  - git restore addons/smart_core/v2 scripts/verify/v2_intent_comparison_audit.py docs/architecture/backend_core_refactor_blueprint_v1.md

depends_on:
  - ITER-2026-04-10-1585
