task_id: ITER-2026-03-28-024
title: Align load_view access guard with current login contract
type: bugfix_task
status: pending
priority: medium

goal: Update the load_view access contract guard so it reads the current login token envelope and can validate the load_view compatibility path against the running environment.
user_visible_outcome:
  - load_view access guard accepts the current `data.session.token` login contract
  - the load_view compatibility guard can run without auth-shape drift
  - next load_contract/load_view runtime cleanup will have an aligned live verifier

scope:
  allowed_paths:
    - scripts/verify/**
    - agent_ops/tasks/**
    - agent_ops/queue/**
    - docs/ops/iterations/**
    - docs/ops/releases/archive/temp/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: Verification Governance
  module: scripts/verify load_view live guard
  reason: Align the load_view compatibility guard to the active login envelope before opening the next load_contract/load_view runtime cleanup slice.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-024.yaml
    - python3 -m py_compile scripts/verify/load_view_access_contract_guard.py
    - python3 scripts/verify/load_view_access_contract_guard.py

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - verify_failed
  - contract_breaking_change
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore scripts/verify/load_view_access_contract_guard.py agent_ops/tasks/ITER-2026-03-28-024.yaml agent_ops/queue/platform_kernel_refactor_prep_queue.yaml docs/ops/iterations/delivery_context_switch_log_v1.md docs/ops/releases/archive/temp/TEMP_agent_ops_continuous_iteration_status_20260328.md

depends_on:
  - ITER-2026-03-28-023
notes: "Verification alignment before touching load_contract/load_view runtime path."
