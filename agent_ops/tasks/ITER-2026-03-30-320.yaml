task_id: ITER-2026-03-30-320
title: Produce implementation plan for list-page composition contract completion
type: docs
status: in_progress
priority: high

goal: >
  Create the final step implementation plan that turns the previous audit and
  gap analysis into an ordered execution roadmap for completing list-page
  composition contracts and removing frontend-owned orchestration.

user_visible_outcome:
  - repository contains a concrete implementation plan
  - the plan defines contract additions, frontend rollback scope, and execution order
  - the plan can be used directly to start implementation batches

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - docs/tmp/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

change_rules:
  - docs only
  - planning only
  - no product code changes

context:
  branch: codex/next-round
  baseline_sha: 6e26cf1

architecture:
  layer_target: Documentation Layer
  module: list surface completion plan
  module_owner: docs/tmp
  kernel_or_scenario: scenario
  reason: >
    After the frontend-owned composition audit and the native-vs-contract gap
    analysis, we need a single ordered plan that defines how to move page
    composition responsibility back to backend contracts without breaking the
    current list experience.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-30-320.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - task_contract_invalid

deliverables:
  - list_surface_completion_plan

rollback:
  - git restore agent_ops/tasks/ITER-2026-03-30-320.yaml
  - git restore docs/tmp/list_surface_contract_completion_plan_2026-03-30.md

depends_on:
  - ITER-2026-03-30-319

notes: >
  This batch only produces the execution plan. The actual implementation batches
  should follow this plan in subsequent work.
