task_id: ITER-2026-04-03-874
title: Re-verify backend-first create-to-manage closure gates on constrained runtime
type: backend
status: active
priority: medium
mode: verify

goal: >
  Sustain user-facing usability by re-validating backend-first execution closure
  gates and cross-stack contract chain under constrained-runtime baseline.

user_visible_outcome:
  - "创建到管理闭环关键执行链持续可用"
  - "自定义前端通用消费链在后端语义供给下持续稳定"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/models/**/*payment*
    - addons/**/models/**/*settlement*
    - addons/**/models/**/*account*
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify-only
  - no_business_code_change

context:
  branch: codex/next-round
  baseline_sha: 66d5fb8
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-873
  priority_lane: usability_verification_mainline
  usability_battlefield: backend_plus_cross_stack

architecture:
  layer_target: Product Usability Closure
  module: backend-first constrained-runtime closure gates
  module_owner: verify chain
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    Backend semantics own usability closure while frontend remains generic render
    and interaction shell.
  reason: >
    Maintain continuous closure confidence with short-context low-risk verify loops.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-874.yaml
    - make verify.release.second_slice_prepared
    - make verify.product.project_flow.execution_cost
    - make verify.product.project_flow.execution_payment
    - make verify.product.project_flow.execution_settlement
    - make verify.portal.cross_stack_contract_smoke.container

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - backend-first closure re-verification result
  - governance/report artifacts for ITER-2026-04-03-874

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-874.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-874.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-874.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-873
