task_id: ITER-2026-03-28-047
title: Freeze platform kernel boundary baseline
type: governance_task
status: pending
priority: medium

goal: Freeze what currently belongs to the platform kernel, what does not, and what remains deferred so later refactor work does not pull industry semantics inward by drift.
user_visible_outcome:
  - a platform kernel boundary freeze document exists
  - high-risk assets remain explicitly outside the current alignment batch

scope:
  allowed_paths:
    - docs/architecture/**
    - agent_ops/tasks/**
  forbidden_paths:
    - security/**
    - migrations/**
    - deploy/**
    - addons/**/security/**

change_rules:
  - additive_only
  - no_schema_change
  - no_acl_change
  - no_record_rule_change

architecture:
  layer_target: platform kernel governance
  module: boundary freeze baseline
  reason: Freeze kernel ownership categories before any broader code-layer alignment batch is approved.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-03-28-047.yaml
    - rg -n "属于平台内核|不属于平台内核|暂缓判定" docs/architecture/platform_kernel_boundary_freeze_v1.md

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - uncertain_business_decision

deliverables:
  - code
  - report

rollback:
  - git restore docs/architecture/platform_kernel_boundary_freeze_v1.md agent_ops/tasks/ITER-2026-03-28-047.yaml

depends_on:
  - ITER-2026-03-28-046
notes: "Freeze only current ownership categories; do not move business code."
