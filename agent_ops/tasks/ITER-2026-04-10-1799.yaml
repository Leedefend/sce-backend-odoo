task_id: ITER-2026-04-10-1799
title: form fallback richest-view selection fix

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  当 action 未显式绑定 form 视图时，避免默认命中精简 form；按结构丰富度从模型 form 视图中择优。

user_visible_outcome:
  - "views.form.layout 的 notebook/page 能反映业务页签而非精简页签"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1799.yaml
    - addons/smart_core/app_config_engine/models/app_view_config.py
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1799.md
    - agent_ops/state/task_results/ITER-2026-04-10-1799.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - backend contract selection logic only
  - no acl/rule/manifest changes
  - additive compatibility-safe changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend contract semantic layer
  module: app.view.config safe_get_view_data fallback strategy
  module_owner: smart_core
  kernel_or_scenario: scenario
  reason: >
    action=531 未显式绑定 form，默认取到简化视图导致业务 tab 缺失，需要 form fallback 择优。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1799.yaml"
    - 'rg -n "fallback_candidates|form fallback|count_notebook_pages" addons/smart_core/app_config_engine/models/app_view_config.py'
    - "make restart"
    - "make frontend.restart"

risk:
  level: medium-low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - backend fallback selection root-fix patch
  - iteration report and task result

rollback:
  - "git restore addons/smart_core/app_config_engine/models/app_view_config.py"

depends_on:
  - ITER-2026-04-10-1798
