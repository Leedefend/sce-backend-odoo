task_id: ITER-2026-04-08-1407
title: P0-C verify add kanban to native parity coverage
status: active
type: governance
priority: high
mode: verify
batch: 1/1

goal: >
  补齐 tree/form/kanban 三视图对齐验证覆盖，确保自定义前端与原生契约消费在
  三种核心视图类型上的口径一致。

user_visible_outcome:
  - "对齐验证报告包含 tree、form、kanban 三类视图字段覆盖结果"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-08-1407.yaml
    - scripts/verify/native_business_admin_config_center_intent_parity_verify.py
    - docs/ops/business_admin_config_center_intent_parity_v1.md
    - agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1407.md
    - agent_ops/state/task_results/ITER-2026-04-08-1407.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/ir.model.access.csv
    - addons/**/__manifest__.py
    - addons/**/*payment*
    - addons/**/*settlement*
    - addons/**/*account*

limits:
  max_files: 6
  max_output_lines: 220
  forbid_repo_wide_scan: true

change_rules:
  - verify stage executes declared checks only
  - no acl/financial semantics change
  - frontend remains generic consumer

context:
  branch: codex/next-round
  baseline_sha: 9109f99
  explicit_user_authorization: true

architecture:
  layer_target: Governance parity verification coverage
  module: native parity verify script for config center intent
  module_owner: verification gate tooling
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批仅补齐场景契约视图类型验证覆盖，不新增业务事实。
  reason: >
    落地 P0-C：将 kanban 纳入与 tree/form 同口径的对齐验证。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-08-1407.yaml"
    - "python3 scripts/verify/native_business_admin_config_center_intent_parity_verify.py"

stage_gate:
  deferred_commands:
    - "CONTRACT_PREFLIGHT_SKIP_DOCS=1 CONTRACT_PREFLIGHT_SKIP_GROUPED_GOV_BUNDLE=1 CONTRACT_PREFLIGHT_SKIP_SCENE_CAPABILITY_GUARD=1 CONTRACT_PREFLIGHT_STRICT_VIEW_TYPES=0 BASELINE_FREEZE_ENFORCE=0 make verify.contract.preflight DB_NAME=sc_demo E2E_LOGIN=admin E2E_PASSWORD=admin"
  policy:
    - "Run deferred full-chain gate once at P0 stage close"

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - acceptance_command_failed
  - forbidden_path_touched

deliverables:
  - kanban added into parity verification chain
  - updated parity evidence document

depends_on:
  - ITER-2026-04-08-1406

rollback:
  - "git restore scripts/verify/native_business_admin_config_center_intent_parity_verify.py"
  - "git restore docs/ops/business_admin_config_center_intent_parity_v1.md"
  - "git restore agent_ops/tasks/ITER-2026-04-08-1407.yaml"
  - "git restore agent_ops/reports/2026-04-08/report.ITER-2026-04-08-1407.md"
  - "git restore agent_ops/state/task_results/ITER-2026-04-08-1407.json"
  - "git restore docs/ops/iterations/delivery_context_switch_log_v1.md"
