task_id: ITER-2026-04-03-914
title: Screen authorization path for sc_demo project seed materialization
type: governance
status: active
priority: high
mode: screen

goal: >
  Determine whether delivering non-empty project context for sc_demo can proceed
  under current low-risk scope or requires high-risk authorization exception.

user_visible_outcome:
  - "明确下一步是否可直接做 seed 实现，或必须先走高风险授权"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 2
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - screen_only_no_code_change
  - risk_gate_first

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-913
  priority_lane: usability_release_proof
  usability_battlefield: backend_semantics

architecture:
  layer_target: Product Release Usability Proof
  module: customer seed materialization governance gate
  module_owner: architecture_governance
  kernel_or_scenario: scenario
  backend_sub_layer_target: business-fact
  backend_sub_layer_reason: >
    Seeding project records is business-fact supply rather than scene-only orchestration.
  reason: >
    sc_demo has zero project records; need to classify authorization path before implementation.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-914.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed
  - uncertain_authorization_path

deliverables:
  - authorization_path_classification
  - next_batch_gate_decision

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-914.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-914.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-914.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-913
