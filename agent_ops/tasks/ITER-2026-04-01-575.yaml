task_id: ITER-2026-04-01-575
title: Verify sort summary fallback alignment on the native list mainline
type: frontend
status: active
priority: high
mode: verify

goal: >
  Verify that the sort summary fallback fix remains within the trusted native
  list gate and improves ordering clarity without regressing toolbar behavior.

user_visible_outcome:
  - "确认排序摘要语义修复通过可信门禁"
  - "保持列表主链排序上下文清晰"
  - "为下一张 P1 主链筛选提供干净状态"

scope:
  allowed_paths:
    - frontend/apps/web/src/components/page/PageToolbar.vue
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/apps/web/src/api/**
    - frontend/apps/web/src/stores/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - verify stage only
  - do not make new product edits in this batch
  - run only declared validation commands
  - do not reopen repository scan in this batch

context:
  branch: codex/next-round
  baseline_sha: 5af85a3
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-574
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: sort summary fallback verification
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    Sort summary alignment is now part of the native list mainline. Verification
    must confirm that removing misleading fallback state does not break the
    trusted list toolbar path.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-575.yaml
    - pnpm -C frontend/apps/web typecheck:strict
    - make verify.portal.v0_5.container

risk:
  level: medium
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - typecheck_failed
  - trusted_container_verify_failed

deliverables:
  - verification_result_for_sort_summary_fallback_fix
  - governance/report artifacts for ITER-2026-04-01-575

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-575.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-575.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-575.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-574
