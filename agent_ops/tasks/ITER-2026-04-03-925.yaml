task_id: ITER-2026-04-03-925
title: Verify user-facing project dashboard primary entry remains usable
type: backend
status: active
priority: high
mode: verify

goal: >
  Re-anchor on product delivery lane by validating the primary project dashboard
  entry is user-usable end-to-end in the sim runtime.

user_visible_outcome:
  - "主入口项目看板可正常进入，无卡死加载与 5xx 噪音"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - security/**
    - record_rules/**
    - addons/**/security/**
    - addons/**/__manifest__.py
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 1
  max_output_lines: 120
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

change_rules:
  - verify_only_no_business_change

context:
  branch: codex/next-round
  baseline_sha: b2a6165
  explicit_user_authorization: true
  input_from: ITER-2026-04-03-924
  priority_lane: usability_release_proof

architecture:
  layer_target: Product Release Usability Proof
  module: project dashboard primary entry usability gate
  module_owner: scene_orchestration
  kernel_or_scenario: scenario
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    This batch validates runtime orchestration readiness and user entry behavior,
    without changing business facts.
  reason: >
    User requested immediate return to delivery mainline; validate current
    usability status before any further implementation.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-03-925.yaml
    - HOST_RUNTIME_PROBE_MAX_AGE_SEC=7200 HOST_RUNTIME_PROBE_CACHE_ONLY=1 ENV=test ENV_FILE=.env.prod.sim COMPOSE_PROJECT_NAME=sc-backend-odoo-prod-sim PROJECT=sc-backend-odoo-prod-sim BASE_URL=http://localhost DB_NAME=sc_demo E2E_LOGIN=admin E2E_PASSWORD=admin make verify.portal.project_dashboard_primary_entry_browser_smoke.host

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_failed

deliverables:
  - primary_entry_usability_verification_checkpoint

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-03-925.yaml
  - git restore agent_ops/reports/2026-04-03/report.ITER-2026-04-03-925.md
  - git restore agent_ops/state/task_results/ITER-2026-04-03-925.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-03-924
