task_id: ITER-2026-04-01-588
title: Select the next bounded record-view HUD continuity slice after label readability
type: frontend
status: active
priority: high
mode: screen

goal: >
  Select the next bounded slice under record-view HUD continuity after label
  readability, keeping the focus on immediately useful continuity signals rather
  than broad HUD restructuring.

user_visible_outcome:
  - "明确下一条 HUD continuity 任务"
  - "优先提升最有用的连续性信号可见度"
  - "避免直接扩散成 HUD 大改"

scope:
  allowed_paths:
    - agent_ops/tasks/**
    - agent_ops/reports/**
    - agent_ops/state/task_results/**
    - docs/ops/iterations/**
  forbidden_paths:
    - addons/**
    - frontend/**
    - security/**
    - migrations/**
    - deploy/**

limits:
  max_files: 4
  max_candidates: 3
  max_output_lines: 80
  forbid_repo_wide_scan: true
  use_new_session: true
  carry_long_context: false

role_parallel:
  enabled: true
  allowed_roles:
    - executor
    - auditor
  require_disjoint_writes: true
  require_new_session_per_role: true
  stop_on_conflict: true

change_rules:
  - screen stage only
  - consume only existing HUD continuity artifacts
  - do not reopen repository scan
  - do not implement in this batch

context:
  branch: codex/next-round
  baseline_sha: 59536d3
  explicit_user_authorization: true
  input_from: ITER-2026-04-01-587
  use_new_session: true
  carry_long_context: false
  role_parallel_enabled: true
  priority_lane: P1_core_usability

architecture:
  layer_target: Frontend Layer
  module: record-view HUD continuity screen
  module_owner: frontend/apps/web
  kernel_or_scenario: kernel
  reason: >
    HUD title/message and labels are now readable and the dedicated smoke is
    stable again. The next bounded slice should improve which continuity
    signals appear most prominently, without changing HUD schema or field
    coverage.

acceptance:
  commands:
    - python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-01-588.yaml

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - screen_reads_new_product_files
  - screen_reopens_repo_scan
  - no_bounded_hud_candidate_remains

deliverables:
  - selected_next_record_view_hud_slice
  - governance/report artifacts for ITER-2026-04-01-588

rollback:
  - git restore agent_ops/tasks/ITER-2026-04-01-588.yaml
  - git restore agent_ops/reports/2026-04-01/report.ITER-2026-04-01-588.md
  - git restore agent_ops/state/task_results/ITER-2026-04-01-588.json
  - git restore docs/ops/iterations/delivery_context_switch_log_v1.md

depends_on:
  - ITER-2026-04-01-587
