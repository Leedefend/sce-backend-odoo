task_id: ITER-2026-04-10-1803
title: form tab pipeline stage diagnosis

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  后到前定位 tab 丢失断点：增加 layout/tree/shell 三段计数，明确具体失真层。

user_visible_outcome:
  - "结构摘要新增分段计数，明确 tabs 在哪一层丢失"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1803.yaml
    - frontend/apps/web/src/pages/ContractFormPage.vue
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1803.md
    - agent_ops/state/task_results/ITER-2026-04-10-1803.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 1200

change_rules:
  - frontend diagnostics only
  - no behavior mutation
  - additive and reversible

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Frontend contract-consumer runtime
  module: ContractFormPage structure projection summary
  module_owner: frontend/apps/web
  kernel_or_scenario: scenario
  reason: >
    当前 contract page>0 但 projected tabs=0，需要分段计数定位失真层，再做定点修复。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1803.yaml"
    - 'rg -n "pipeline_stage|layout_tree|detail_shell_raw" frontend/apps/web/src/pages/ContractFormPage.vue'
    - "make frontend.restart"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - stage-wise tab loss diagnostics
  - iteration report and state

rollback:
  - "git restore frontend/apps/web/src/pages/ContractFormPage.vue"

depends_on:
  - ITER-2026-04-10-1802
