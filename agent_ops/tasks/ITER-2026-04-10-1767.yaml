task_id: ITER-2026-04-10-1767
title: form x2many subview depth closure

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  落地 FORM-Batch3 / Task 103-2：深化 x2many subviews，补齐 fields 元数据与关系语义，
  让复杂 form 子视图可被前端稳定消费。

user_visible_outcome:
  - "subviews.{field}.fields 不再为空，至少覆盖 tree columns 的字段元数据"
  - "subviews 提供 relation_model 与入口策略语义"

scope:
  allowed_paths:
    - agent_ops/tasks/ITER-2026-04-10-1767.yaml
    - addons/smart_core/v2/services/ui_contract_service.py
    - scripts/verify/form_subview_depth_audit.py
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1767.md
    - agent_ops/state/task_results/ITER-2026-04-10-1767.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 6
  max_output_lines: 1200

change_rules:
  - backend contract semantic layer only
  - additive compatibility-safe changes
  - no ACL or financial semantics changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend contract semantic layer
  module: form subview depth and relation semantics
  module_owner: smart_core v2 service + verify script
  kernel_or_scenario: kernel
  reason: >
    当前 subviews 仅有 columns/policies，fields 为空，无法支撑前端对 x2many 子视图的
    稳定字段渲染与行为消费。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1767.yaml"
    - "python3 scripts/verify/form_subview_depth_audit.py --json --input tmp/json/form.json"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - subview depth closure patch
  - runtime subview depth audit artifact

rollback:
  - "git restore addons/smart_core/v2/services/ui_contract_service.py"
  - "git restore scripts/verify/form_subview_depth_audit.py"

depends_on:
  - ITER-2026-04-10-1766
