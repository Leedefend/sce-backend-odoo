task_id: ITER-2026-04-10-1684
title: fix v2 request_schema symbolic id import failure for ui.contract dispatch

type: implementation
status: active
priority: high
mode: implement
batch: 1/1

goal: >
  修复 v2 调度管道将 request_schema 契约标识符（如 v2.ui.contract.request.v1）错误当作可导入模块路径，
  导致 ui.contract 点击菜单时报 DISPATCH_FAILED: No module named 'v2'。

user_visible_outcome:
  - "点击菜单时 ui.contract 不再报 No module named 'v2'"

scope:
  allowed_paths:
    - addons/smart_core/v2/kernel/pipeline.py
    - agent_ops/tasks/ITER-2026-04-10-1684.yaml
    - agent_ops/reports/2026-04-10/report.ITER-2026-04-10-1684.md
    - agent_ops/state/task_results/ITER-2026-04-10-1684.json
    - docs/ops/iterations/delivery_context_switch_log_v1.md
  forbidden_paths:
    - security/**
    - record_rules/**
    - "**/__manifest__.py"
    - "**/*payment*"
    - "**/*settlement*"
    - "**/*account*"

limits:
  max_files: 5
  max_output_lines: 600

change_rules:
  - dispatch robustness fix only
  - no business semantics changes
  - no acl/schema changes

context:
  branch: codex/next-round
  baseline_sha: 0049675
  explicit_user_authorization: true

architecture:
  layer_target: Backend v2 dispatch pipeline
  module: smart_core v2 kernel request-schema handling
  module_owner: addons/smart_core/v2/kernel
  kernel_or_scenario: kernel
  backend_sub_layer_target: scene_orchestration
  backend_sub_layer_reason: >
    本批次仅修正契约标识与导入路径识别策略，属于语义交付通道健壮性，不涉及业务事实。
  reason: >
    当前 request_schema 使用契约ID命名（...v1）会被当作 class 名导入，导致模块导入失败并阻断 ui.contract。

acceptance:
  commands:
    - "python3 agent_ops/scripts/validate_task.py agent_ops/tasks/ITER-2026-04-10-1684.yaml"
    - "python3 -m py_compile addons/smart_core/v2/kernel/pipeline.py"
    - "rg -n \"request_schema|contract id|module import\" addons/smart_core/v2/kernel/pipeline.py"

risk:
  level: low
  manual_approval_required: false

stop_conditions:
  - forbidden_path_touched
  - acceptance_command_failed
  - contract_uncertainty

deliverables:
  - request_schema symbolic id no longer causes import error

rollback:
  - git restore addons/smart_core/v2/kernel/pipeline.py

depends_on:
  - ITER-2026-04-10-1683
