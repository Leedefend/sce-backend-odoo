# 合同安全候选重算 v1

任务：`ITER-2026-04-13-1841`

## 输入事实

- 合同导出：1694 行
- 已写入/锁定项目骨架范围匹配：146 行
- 未进入已写入项目范围：1548 行
- `DEL=1`：65 行
- 可推断方向：1555 行
- 方向待定：139 行
- partner 唯一匹配：0 行

## 首轮安全候选规则

本轮仍沿用保守安全写入条件，必须同时满足：

1. `XMID` 命中已写入/锁定项目骨架范围；
2. 合同方向可确定为 `out` 或 `in`；
3. 相对方文本可唯一匹配到现有 partner；
4. 合同标题或编号可作为名称来源；
5. `DEL != 1`。

## 阻断项统计

| 阻断项 | 命中行数 |
|---|---:|
| 未进入已写入项目范围 | 1548 |
| partner 未解决 | 1694 |
| 方向待定 | 139 |
| 删除标志 `DEL=1` | 65 |

说明：阻断项可叠加，同一行可能同时命中多个阻断原因。

## 重算结果

| 指标 | 数量 |
|---|---:|
| 安全候选行 | 0 |
| 可进入合同写入 | 0 |
| 写入结论 | NO-GO |

## 结论

合同数据暂不具备 create-only 写入条件。当前首要阻塞不是合同字段本身，而是 partner 主数据未覆盖旧系统合同相对方文本。下一轮应先做 partner 主数据候选清单与人工确认规则，不应直接写入合同。

