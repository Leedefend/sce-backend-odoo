# This file is deprecated and no longer used.
# The devcontainer now attaches directly to the 'odoo' service
# defined in the main docker-compose.yml file, which uses a pre-built image.
# This file can be safely deleted.